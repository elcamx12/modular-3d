# ★★ 운송 탭 정리 (완료, 2026-06-02)
1. **벽 단위중량 입력 제거**: 운송 옵션의 "벽 단위중량" 그룹박스(내부/외부 두 칸)를 제거. 이 두 칸은 프로젝트 설정값을 비춰 잠가두던 미러였음. 단일 출처인 프로젝트 설정을 유지하고, _read_options 가 self._proj_settings 에서 직접 읽음(설정 주입 전이면 기본값 내부30/외부55). apply_project_settings 의 _lock_spin 2개·_wire_auto_recompute 배선 2개·QDoubleSpinBox import 함께 제거.
2. **회차표 탭 제거**: _build_area_subtabs 의 QTabWidget("회차표" 단일 탭) → QGroupBox("회차표") 로 교체해 표만 직접 표시. _sub_tabs 참조는 이 파일 외엔 없어 안전. QTabWidget import 제거.
- 검증: py_compile OK.

# ★★ L6 — 우측 패널 내용맞춤 최소폭 (완료, 2026-06-02)
- **단면설계**: SectionDesignPanel._fit_to_content — 옵션/단면변경 박스 sizeHint + 타입목록 항목폭 max → 부모 pane setFixedWidth. (표/트리 없는 컨트롤 모음)
- **물량**: main_3d 가 _quantity_panel.minimumWidth() 로 _quantity_right_pane setFixedWidth(min만 → fixed 로 교정).
- **구조해석(핵심·난항)**: ① 부재명 col0 Stretch→ResizeToContents(내용폭 고정, 패널 여분 흡수 방지) ② 폭 계산을 sizeHintForColumn(진동/과대)→ *항목 텍스트 직접 측정* ③ **부재력 트리를 _tabs 밖(root)으로 분리** — QTabWidget 이 숨은 페이지(물량/운송) 최소폭까지 반영하던 문제 ④ **진짜 범인: _fit_panel_to_quantity 가 구조해석 F6 때 물량 재산정과 함께 호출돼 같은 par 를 567 로 덮어씀(부재력 477 위에). → _tabs.isVisible() 일 때만 par 고정하도록 조건**. (FORCE/CHAIN 진단으로 확정)
- **접합부**: 범례+구분선을 '변경 내용 저장' 버튼 아래로 이동(사용자 요청).
- **배치/접합 폭**: 320 고정 유지(컨트롤 모음이라 잘림 없음, 사용자 결정).
- **운송 탭 폭**: 스플리터(손잡이 조절)라 별도 — 미착수.

# 탭 기능·데이터흐름 지도 (3단계 파이프라인 통합 사전조사)

> 목적: 데이터 흐름을 하나로 묶기(3단계) 전에, **모든 탭의 기능·입출력·재계산/무효화
> 트리거를 빠짐없이** 파악한다. 이 지도가 정확한 무효화 설계(무엇이 바뀌면 무엇을 다시
> 계산해야 하나)의 근거가 된다. 읽기 전용 조사 — 코드 변경 없음.

## 탭 목록 (main_3d.py:295-304, addTab 순서)
1. 모듈 정의 (_define_tab) — 모듈/부재 *정의*(치수·단면타입 프리셋)
2. 배치 설계 (_tab_design) — 3D 배치(F5 컨트롤러)
3. 접합부 설계 (_tab_joint_edit) — 접합 자동계산 + 사용자 수정
4. 구조해석 (_tab_analysis)
5. 단면 설계 (_tab_section)
6. 물량 (_tab_quantity)
7. 운송 (_tab_transport)
8. 공정표 (_tab_schedule)
9. 종합 (_tab_evaluation)
10. 비교 (_tab_compare)

## 각 탭 기록 형식
- 패널/컨트롤러 파일
- **기능(사용자 동작)**: 버튼·조작 전부
- **읽음(입력)**: scene / AnalysisModel(am) / DesignResult / options / 기타
- **바꿈(출력·변이)**: scene 부재 / joint_overrides / design_section / 옵션 / 룸 등
- **재계산 트리거**: 어떤 동작이 무엇을 다시 계산하나
- **무효화 영향**: 이 탭의 변경이 어떤 하류 결과를 stale 시키나 (파이프라인 무효화 근거)

## 데이터 의존 사슬 (가설 — 조사로 확정)
scene(부재 배치·치수·회전·개구부·실) →[토폴로지]→ AnalysisModel(am) →[해석]→ 내력/변위
→[단면설계]→ design_section(부재별) →[물량]→ 수량·비용 / →[운송]→ 적재·비용 / →[공정]→ 일정
접합: joint_overrides(scene 부속) → am 빌드 시 반영 → 해석부터 하류 전체 영향.

---

# 탭 1. 모듈 정의 (_define_tab)  [잠정 — 루프서 보강]
- 파일: ui/define_tab.py (+ DesignPropertiesPanel, RoomPropsPanel, palette, F5 컨트롤러, 라이브러리)
- 기능(사용자): 모듈/부재 정의(치수·보 단면타입), 팔레트 선택, 상세모드 토글, 실(room) 그리기, 개구부 추가, 내벽 배치, 정의 라이브러리 저장/불러오기/새로/삭제/파일에서 로드
- 읽음: **자체 Scene**(define_tab.py:48 self._scene = Scene() — 배치 탭과 별개일 가능성, 확정 필요)
- 바꿈: 자체 scene 부재/실/개구부, definition_library(저장된 정의 프로토타입)
- 재계산 트리거: (정의 단계 — 해석/물량 등 하류 재계산 없음 추정)
- 무효화 영향: 라이브러리 정의가 배치 설계 탭서 인스턴스화될 때만 하류 영향. 직접 무효화는 배치 탭에서.
- ✅ 확정: 모듈 정의 탭은 **별도 작업공간/Scene**(main_3d:1226-1234 주석 "자체 작업공간(별도 인스턴스)", define_tab.py:48 자체 Scene). 정의→배치 전달은 definition_library 경유(저장→배치 탭서 인스턴스화). 따라서 파이프라인은 "main 의 self._scene" 기준이고 정의 탭은 별도 취급.

---

## ★ 교차 발견 (파이프라인 설계 핵심 — 계속 갱신)
- **① scene 소유**: main_3d.py:111 `self._scene = Scene()` 이 **배치·접합·해석·단면·물량·운송·공정·종합 8탭 공유 단일 scene**. 모듈정의 탭만 별도(위). 비교 탭은 저장파일(.case.json) 로드라 또 별개.
- **⑤ 기존 무효화(ad-hoc, _on_tab_changed)**: (a) 정의·배치 탭 진입 시 `set_section_design_result(None)` — 모델 편집하면 단면설계 결과 폐기(main_3d:1222-1224). (b) 접합 탭→해석/단면/물량 이동 시 ops 뷰 무효화 후 재빌드(1203-1208). → *이미 "모델 바뀌면 하류 폐기"를 손으로 일부 하고 있음. 파이프라인은 이걸 fingerprint 로 일반화·자동화하는 것.*
- **②④ build_analysis_model / design_result 흐름**: 호출처 — main_3d.py:1644(단면설계), 1049(물량 am), main_3d_joint.py:82(접합뷰), analysis_panel(해석). design_result 는 controller 에 저장(set_section_design_result) + 물량/운송에 push. (탭별 상세는 아래 각 탭에서 확정)

# 탭 2. 배치 설계 (_tab_design)  — 모델 주 변이원(최상류)
- 파일: ui/controls/__init__.py(Controller), controls_f5.py(F5 패널 핸들러), design_props_panel.py(우측 속성), room_props_panel.py, main_3d._build_design_tab.
- 기능(사용자): 팔레트 선택→프리뷰→치수확정→배치(instantiate); 이동(자유·축), 복사, 삭제, 회전, 앵커 변경, undo(반쪽); 스냅·갭 자동; 다층 복제(F5 FLOORS); 벽↔바닥패널 합체(WALL MERGE); 종속부재 배치(캔틸레버·중간보·중간기둥); 실(room) 생성/삭제/용도편집; 개구부 추가/삭제(다층 동기); 보 단면타입 shs/h 변경; scene 저장/불러오기; 정의 라이브러리 import 배치; 코어슬래브 생성/재생성.
- 읽음: self._scene(공유), definition_library(인스턴스화 원본), DEFAULT_DIMS, 단면타입 프리셋.
- 바꿈: **self._scene 컴포넌트(추가/이동/삭제/치수/회전/앵커/단면타입)** + generate_sub_components(형상 재생성) + undo_stack + joint_records(F5 JOINT REC) + rooms + openings. → 모델의 주 변이원.
- 재계산 트리거: 직접 해석/물량 재계산은 안 함(배치 자체만). generate_sub_components 로 형상만 즉시 갱신.
- 무효화 영향: **최상류 — 어떤 배치/이동/삭제/치수·회전·단면타입 변경도 토폴로지(am)→해석→단면설계→물량→운송→공정·평가 전부 stale.** 현재는 탭 전환 시 section_design_result 만 폐기(부분, main_3d:1222). compute_scene_fp(이미 존재)가 이 변이를 감지하는 핵심 지문.
- 비고: 보 단면타입(shs/h) 변경도 scene 변이 → 해석 강성·물량 영향(하류 무효화 대상).

# 탭 3. 접합부 설계 (_tab_joint_edit)
- 파일: ui/joint_edit_panel.py(JointEditPanel) + ui/main_3d_joint.py(접합 뷰 빌드) + analysis/joint_rules.py(match_override·_resolve_override_dofs) + model/joint_override.py(JointOverride).
- 기능(사용자): 접합 선택 후 **제거/핀/강접** 설정(_btn_remove/_btn_pin/_btn_rigid); 사용자 접합 추가(add 모드); 전체층 적용 토글; 필터(수직·수평 / 패널·모듈); 다이어프램 마스터·슬레이브 표시 토글; 합성거동 옵션(현재 더미·비활성); 자동계산으로 되돌리기(revert); 저장(save).
- 읽음: self._scene, **scene.joint_overrides**, 접합 표시용 자체 am+ops(main_3d_joint.py:82-83 — build_analysis_model+build_ops_model 만, **solve 없이** spec 표시용).
- 바꿈: **scene.joint_overrides 에 JointOverride 추가/변경/제거**(undo 스택과 분리 — O006). 합성 플래그(spec).
- 재계산 트리거: 접합 뷰는 build_ops_model 만(solve X). override 변경 즉시 재해석은 안 함 — 탭 떠날 때 무효화.
- 무효화 영향: **joint_overrides 변경 → 해석 ops 빌드의 DOF 결합(_resolve_override_dofs)이 바뀜 → 해석부터 하류(단면·물량·운송·공정·평가) 전부 stale.** 현재 처리: 접합 탭→해석/단면/물량 이동 시 ops 뷰 무효화 후 재빌드(main_3d:1203-1208, 수동).
- ★ 파이프라인 확정과제: **compute_scene_fp 가 joint_overrides 를 포함하는가?** (감사 R001 서 scene_fp 가 joint_records 제외 확인. joint_overrides 도 제외면 운송/물량 캐시가 접합 변경에 무효화 안 돼 stale 위험.) → 탭6·7 캐시 조사 때 반드시 확정.
- ③ 답: joint_overrides 는 scene 부속으로 저장되고, ops 빌드의 결합 등록 직전 match_override/_resolve_override_dofs 가 읽어 DOF 를 조정/제거(해석 단계 반영, 토폴로지 am 아님).

# 탭 4. 구조해석 (_tab_analysis)
- 파일: ui/analysis_panel.py + main_3d(_build_analysis_tab·_run_analysis_if_needed:1575·_run_structural_analysis) + ops_solver.solve_all_cases.
- 기능(사용자): 해석 자동 실행(탭 진입 시); 하중조합 케이스 선택(드롭다운, case_changed); 응력비 뷰 토글(ratio_view_changed); 기둥 분절 수 변경(column_segments_changed); 부재 선택(트리/3D, member_selected); 정책 1/2/3종 변경(policy_changed); 시각화 오버레이 토글(슬래브 압력·보 UDL·기둥두부·반력·접합·변형형상·부재 응력비 밴드·다이어프램·강체링크 등).
- 읽음: self._scene, 해석 결과 om/OpsResults(solve_all_cases), 정책.
- 바꿈: 시각화 상태(viewer 오버레이), **column_segments(분절 수)**, **current_policy(1/2/3종)**. 모델(scene)은 안 바꿈(읽기+해석).
- 재계산 트리거: 탭 진입 _run_analysis_if_needed → (ops 뷰 OFF 일 때만) _run_structural_analysis = build_ops_model + solve_all_cases. column_segments 변경→재분절·재해석. policy 변경→단면/물량/평가에 push.
- ★★ **해석 "최신 판단" = is_ops_view_active() (시각화 ON/OFF)뿐, 모델 지문 검사 전무**(main_3d:1591). → 현재 사실상 캐시 키가 "와이어프레임이 켜져 있나"라서, 모델이 바뀌어도 뷰가 켜진 채면 stale. 무효화는 _on_tab_changed 가 뷰를 끄는 것에 의존(브리틀). **3단계가 이걸 fingerprint 기반으로 대체해야 함.**
- 무효화 영향: column_segments·policy 변경 → 단면/물량/운송 캐시(seg_fp·정책) stale.

## ★ 교차 발견 갱신
- **② build_ops_model/solve 호출처(확정)**: controller._run_structural_analysis(해석탭), _run_section_design_if_needed→_run_analysis_if_needed(단면탭, main_3d:1617 재사용), 물량 진입(_run_analysis_if_needed 경유 1288/1314/1360), main_3d_joint(접합뷰 — solve 없이 build 만). build_analysis_model(am)은 그 안 + 물량(1049)·단면(1644)서 각각.
- **⑤ 해석 재계산 기준 = 뷰 활성 상태**(지문 없음, 위). 정의·배치 진입 시 design_result 폐기(1222). 접합→해석/단면/물량 시 뷰 무효화(1203).

# 탭 5. 단면 설계 (_tab_section)
- 파일: ui/section_design_panel.py + analysis/section_converge.py(converge_sections·derive_section_types·expand_locks_to_members) + main_3d(_run_section_design_if_needed:1603·_converge_and_color:1633).
- 기능(사용자): 통일 범위(전체 / 크기동일 타입끼리 / 완전동일 타입끼리), 묶음 입도(전부 한 단면 / 기둥·보 / 기둥·천장보·바닥보), 기둥 층분할 구간 수, **적용 버튼**(재수렴), 타입 목록서 타입별 단면 콤보 수동 선택(잠금 _section_locks), 응력비 5단계 색(빨강=NG) 표시.
- 읽음: self._scene, am(build_analysis_model 1644 — **또 빌드**), options(current_options), _section_locks, _last_ops_analysis.
- 바꿈: self._section_result(ConvergeResult: 부재별 단면·응력비·NG·수렴여부), **controller.section_design_result(=물량·운송 단일출처, set_section_design_result 1652, P5b)**, viewer 응력비 색, 타입 목록. → **scene 의 BeamData 엔 안 씀**(렌더가 단면을 반영 못 하는 L1-1 의 직접 원인).
- 재계산 트리거: 탭 진입 자동 수렴(_run_section_design_if_needed → 와이어프레임 확보 후 기본 '모두 통일' converge); 적용 버튼/옵션·잠금 변경 시 _converge_and_color 재수렴.
- 무효화 영향: design_result 변경 → 물량(수량·비용)·운송(무게)·평가 stale. 모델·접합·분절·정책 변경 → design_result 재수렴 필요(현재: 모델편집 탭 진입 시 폐기 1222 + 탭 재진입 시 재수렴).
- ④ 답: 단면설계 결과는 scene 이 아니라 **controller.section_design_result / _design_results** 에 저장 → 물량 build_quantity_by_component(main_3d:1049 dr 인자)·운송 populate_transport→set_design_result 가 읽음. "단일 출처" 의도는 있으나 controller 속성에 얹는 방식.
- ② 추가확정: build_analysis_model 이 단면설계서 또 호출(1644) — 해석탭·물량과 중복. **첫 조각(공유 am 제공자)이 제거할 대상.**

# 탭 6. 물량 (_tab_quantity)  — 순수 소비/표시(모델 변이 없음)
- 파일: ui/quantity_panel.py + analysis/quantity_by_component(build_quantity_by_component·design_result_without_core)·quantity_takeoff(build_quantity_report·compute_material_cost) + main_3d 물량 빌드(1020~1110).
- 기능(사용자): 타입별 물량 트리(선택 시 3D 강조 type_selected), 하단 전체 본수표(강재 길이·중량·금액), 자재비표(강재·데크·콘크리트 단가×물량, 단가 미입력 경고는 0단계서 추가), 물량 3D 도식(부재 형상).
- 읽음: self._scene, **design_result(controller._design_results 우선, 없으면 self._section_result→converge_result_to_design_result)**, **am(result.am 재사용, main_3d:1040)**, UnitPrices.load(), derive_section_types.
- 바꿈: **없음(표시 전용)**. UnitPrices 는 매번 fresh 로드.
- 재계산 트리거: 탭 진입 시 _run_analysis_if_needed 후 물량 빌드. design_result/_section_result 가 있어야 함(없으면 빈 결과·폴백 실패→경고).
- 무효화 영향: 하류 없음(말단 소비). 단 자신이 stale design_result/am 을 쓰면 잘못된 물량. 정책(1/2/3종)·단면결과·scene 에 의존.
- 비고: 물량 3D 는 **운송 어댑터 build_transport_input 재사용**(main_3d:1068) — 물량·운송이 같은 부재 형상 변환 공유. quantity 가 am 을 새로 빌드하진 않고 단면결과의 am 재사용(그나마 양호). 단 design_result 를 controller 속성서 끌어오는 ad-hoc 패턴은 그대로(L3-1).

# 탭 7. 운송 (_tab_transport)  — 8단계 캐시 보유(유일하게 지문 기반)
- 파일: ui/transport_panel.py + transport/adapter(build_transport_input)·cache(compute_scene_fp·ti_fp·pack_fp·TransportCache)·packer(pack_items) + main_3d transport 빌드.
- 기능(사용자): 트럭 카탈로그 로드·편집·저장(load_all_trucks/save_project_truck); 옵션(캔틸포함·벽분류·단위중량·거리·운임방식·v3눕힘 등); 지역별 단가 override(area overrides) + 초기화; 참고자료 dialog; **수동 재계산 버튼(_run_transport)**(자동 재계산은 제거됨 — 옵션 변경 시 캐시 무효화만); 3D 적재뷰; 회차표(trip table, 더블클릭→3D); 경제성표.
- 읽음: self._scene, **design_result(set_design_result 로 주입, populate_transport 경유)**, am, trucks, options, project_settings(거리 등).
- 바꿈: 트럭 카탈로그(파일 저장), 운송 옵션·area overrides(런타임). **모델(scene)은 안 바꿈.**
- 재계산 트리거: **수동 실행 버튼만**. 옵션 변경 → 해당 단계부터 캐시 무효화(_on_option_changed), 실행은 수동.
- 무효화 영향: 말단(하류 없음). 자신이 stale design/scene 쓰면 잘못된 적재·비용.
- ★★ **(a) 확정 — 핵심 결함**: compute_scene_fp 는 scene.components 만 순회(cache.py:65) → **joint_overrides 미포함**. 또 design_fp = `design_{id(design_result)}`(객체 id, 내용해시 아님, cache.py:290). → 접합을 바꿔도 *단면설계를 다시 안 돌리면* design_result 객체가 그대로라 design_fp 불변 → 운송/물량 캐시가 stale. (접합탭 진입은 design_result 폐기 안 함; 폐기는 정의·배치 탭만 1222.) **파이프라인 통합 시 모델 지문에 joint_overrides·정책·분절수를 반드시 포함하고 design_fp 를 내용 기반으로.**
- (b) 재확인: compute_ti_fp(옵션 10종)·compute_pack_fp(트럭·site·spacing·economics)·trucks_fp 는 옵션/트럭/거리 변경을 잘 잡음(감사 R002 유지). 약점은 (a) 의 scene_fp 측.

## ★ 교차 발견 갱신 — 파이프라인 무효화 키(확정)
통합 ProjectModel 의 "모델 지문"은 최소: **compute_scene_fp(부재) + joint_overrides + 정책(1/2/3종) + 기둥분절수 + 보 단면타입**. design_fp 는 id() 대신 내용 해시로. 그래야 어느 탭의 변경이든 하류가 정확히 무효화됨.

# 탭 8. 공정표 (_tab_schedule)  — 팀원 HTML + 브리지(별도 산출)
- 파일: ui/schedule_panel.py(SchedulePanel + _ScheduleBridge QWebChannel) + schedule/schedule_adapter.py(scene→export dict). HTML(팀원)이 실제 일정 계산.
- 기능(사용자): 공정표 HTML 뷰(간트). resize 시 디바운스 recalc(JS calc()). 프로젝트 설정 적용(apply_project_settings).
- 읽음: self._scene(comps), project_settings. (on_enter(comps, settings, scene) main_3d:1383)
- 바꿈: **모델 안 바꿈.** scene 을 dict{summary, components(AABB+joint_records)}로 export → 팀원 HTML importScene 이 소비. HTML calc() 결과는 schedule_payload_pushed 로 Python 에 push → main._schedule_payload 캐시 → 종합 탭이 소비.
- 재계산 트리거: on_enter(탭 진입) + resize(HTML calc). 
- 무효화 영향: 말단(종합 탭에만 payload 공급). 
- ★ 별도 산출(Q002 확정): 공정 접합 카운트는 **schedule_adapter 가 또 build_analysis_model+build_ops_model 호출**(scene 인자 시)해 spec 접합을 룰별 집계 — 구조해석 결과를 재사용 안 함. 물량도 기하 AABB·자체 규칙으로 재산출(구조 quantity 모듈과 무관) → 같은 부재가 공정/물량서 다른 수량일 수 있음.
- ② 추가 빌드처 확정: schedule_adapter(접합 카운트용). 누적 build_analysis_model 호출처 = 해석탭·단면설계(1644)·물량(result.am 재사용)·접합뷰(main_3d_joint)·**공정표(schedule_adapter)**. 독립 빌드 4~5곳.

# 탭 9. 종합 (_tab_evaluation)  — 말단 소비/보고 + 스냅샷 저장
- 파일: ui/evaluation_panel.py + main_3d(_on_evaluation_save_case·_on_policy_changed_for_evaluation·_on_schedule_payload_pushed).
- 기능(사용자): 단일 스크롤 보고서(A 건물개요/B 부재구성/C 물량·자재비/D 운송/E 공기/F 종합비용) + 케이스 저장 버튼(save_case_requested → .case.json: 결과 + 평면도 png_b64).
- 읽음: 물량(qbc/report/mc), 운송 비용, schedule_payload, current_policy(물량탭 단일 출처 — 자체 토글 없음), 평면도 pixmap.
- 바꿈: **모델 안 바꿈.** .case.json 스냅샷 저장(외부 파일).
- 재계산 트리거: 탭 진입 + policy_changed push(활성 시 즉시 재계산) + schedule_payload_pushed 수신.
- 무효화 영향: 말단(보고). stale 상류 쓰면 보고 틀림.

# 탭 10. 비교 (_tab_compare)  — 파이프라인과 독립(오프라인 비교)
- 파일: ui/compare_panel.py + evaluation/case_io(load_case).
- 기능(사용자): 3 슬롯, 각 슬롯에 .case.json 로드 → 저장된 케이스끼리 비용·공기·평면효율 표·차트 비교.
- 읽음: **.case.json 파일(저장본)만. 현 scene/파이프라인과 완전 독립.**
- 바꿈: 없음.
- 재계산 트리거: 파일 로드 시. 무효화 영향: 없음(파이프라인 무관).

---

# ★★ 총정리 — 10탭 데이터흐름 + 3단계 첫 조각 설계

## ①~⑤ 답 요약
- **① scene 소유**: main 의 단일 self._scene 을 배치·접합·해석·단면·물량·운송·공정·종합 8탭 공유. 모듈정의 탭만 별도 작업공간(자체 Scene+라이브러리), 비교 탭은 .case.json 오프라인(독립).
- **② build_analysis_model 독립 호출처(중복)**: 해석(_run_structural_analysis)·단면설계(_converge_and_color:1644)·접합뷰(main_3d_joint:82)·공정(schedule_adapter, 접합카운트). 물량은 단면결과의 am 재사용(중복 아님). → 같은 토폴로지를 4곳서 따로 빌드.
- **③ joint_overrides**: scene 부속 저장 → ops 빌드 시 _resolve_override_dofs 가 읽어 DOF 결합 조정/제거(토폴로지 am 이 아니라 OPS 단계 반영). 따라서 **am 은 joint_overrides 와 무관**(중요 — 첫 조각 안전성의 근거).
- **④ design_result**: 단면설계가 controller.section_design_result/_design_results 에 보관(scene 미반영 → 렌더 L1-1). 물량·운송·종합이 그걸 읽음. design_fp 는 id() 기반(내용해시 아님).
- **⑤ 재계산/무효화(현행, ad-hoc)**: 해석 최신판단=ops 뷰 활성 여부(지문 없음). 모델편집 탭(정의·배치) 진입 시 design_result 폐기(1222). 접합→해석/단면/물량 시 ops 뷰 무효화(1203). 운송만 8단계 지문 캐시 보유하나 scene_fp 가 joint_overrides 제외(stale 위험).

## 파이프라인 무효화 키(통합 시 필요)
- **am(토폴로지)**: compute_scene_fp(부재 기하)만으로 충분 — joint_overrides/정책/단면과 무관.
- **해석 결과(ops)**: am 지문 + joint_overrides + 분절수.
- **design_result**: 해석 지문 + 단면옵션/잠금 + 정책.
- **물량·운송**: design_result 지문(내용해시) + 단가/트럭/옵션/거리.

## 3단계 첫 조각 — "공유 해석모델(am) 제공자" 구체 설계
- **만들 것**: controller(또는 main)에 get_analysis_model(scene) 1개. compute_scene_fp(scene) 지문 캐시 — 지문 같으면 캐시 am 반환, 다르면 build_analysis_model 1회 후 캐시. (am 은 joint/정책/단면 무관이라 키가 scene_fp 면 충분 — 안전·단순)
- **바꿀 호출처(폴백 유지하며 1곳씩)**: ① 해석 _run_structural_analysis ② 단면설계 _converge_and_color:1644 ③ 접합뷰 main_3d_joint:82 ④ 공정 schedule_adapter. 각자 build_analysis_model 직접호출 → get_analysis_model 경유로.
- **선결 검증(중요)**: build_ops_model 이 받은 am 을 변이(>=10000 split 노드 추가)했다가 청소해 복원하는지 확인(카탈로그 주석상 5케이스 반복서 청소함). 청소가 보장되면 am 공유 안전. 안 되면 제공자가 매 사용 시 방어적 처리.
- **무효화**: scene 편집(compute_scene_fp 변화) 시 자동 폐기 → 다음 get 에서 재빌드. (am 은 그 외엔 불변)
- **폴백 전략**: 제공자 실패/예외 시 기존 build_analysis_model 직접호출로 자동 폴백 — 동작 보존.
- **검증**: 1단계 스타터 테스트(계산 회귀) + 각 호출처 변경마다 사용자 GUI 확인(해석·단면·접합뷰·공정 정상 표시).
- **이 조각의 효과/한계**: 효과 — 토폴로지 4중 빌드 제거(재계산 낭비↓), 단일 진실원천 첫걸음. 한계 — ④ design_result 의 id() 지문, joint_overrides 의 scene_fp 누락(운송 stale) 등은 *다음 조각*에서. 첫 조각은 "동작 불변 + 중복 제거"만.

(지도 완성 — 10/10 탭 + 총정리)

---

## ★ 적용 이력 — 3단계 첫 조각 "공유 해석모델(am) 제공자" (완료)
- **신설**: `analysis/model_provider.py` — get_analysis_model(scene). compute_scene_fp 지문 단일 슬롯 캐시. 지문 같으면 직전 am 재사용, 다르면 build_analysis_model 1회 후 캐시. **반환 직전 항상 분절 잔여물(>=10000 노드·is_split_sub 부재) 청소 + 인덱스 무효화** → build_ops_model 의 지연 청소(다음 빌드 시작 때만 비움) 위험을 제공자에서 선차단.
- **교체 4곳(폴백 유지)**: ① 해석 탭(controls_f6 F6 핸들러) ② 단면설계(main_3d _converge_and_color) ③ 접합뷰(main_3d_joint) ④ 공정(schedule_adapter). 각자 build_analysis_model 직접호출 → get_analysis_model 경유. 제공자 실패 시 기존 빌드로 자동 폴백(이중 try) — 동작 보존.
- **호출처 실측 보정**: 지도 ②의 "해석(_run_structural_analysis)" 실체는 **구조해석 탭 F6 핸들러(controls_f6:151)**였음. ops_solver:1064 / section_converge:447 의 빌드는 prebuilt_am 미전달 시에만 타는 *내부 폴백*이라 교체 대상 아님(호출처가 am 을 넘김).
- **검증**: 스타터 골든 테스트 8/8 통과(계산 회귀 없음) + 4곳 GUI 확인 정상.
- **3단계 종료**: 첫 조각(공유 am 제공자)만 적용하고 3단계 마무리. 나머지 조각 2건은 조사 후 보류(아래).

## ★ 조사 후 보류 — design_result id() 지문 → 내용해시 (불채택)
- **결론**: 보류. 정확성 이득 없음 + 위험 큼.
- **근거 1(이벤트 무효화가 이미 커버)**: set_design_result(transport_panel:465)가 새 결과 수신 시 invalidate_from(3) 호출 → design 이하(분할·분류·입력·패킹·경제성) 전부 clear. 하위 결과가 None 이 되어 무조건 재계산되므로, design_fp 가 id 든 해시든 stale 안 생김. id 재사용/제자리수정 경로도 invalidate_from(3) 이 막음. (단면설계는 매 실행 새 ConvergeResult 반환 — 제자리수정 없음.)
- **근거 2(해시 전환 위험)**: ConvergeResult 가 am(AnalysisModel 전체)을 필드로 보유(section_converge.py:68). 내용해시 시 ① 거대 객체 매번 직렬화(비용) ② am 은 build_ops_model 이 제자리 변이 → 같은 결과인데 해시 흔들림(캐시 무력화) ③ member_sections 값이 커스텀 단면 객체 → 직렬화/해시 미보장. → 전체 해시 금지. 굳이 하려면 "부재→단면 타입·치수"만 식별 해시해야 안전하나 실익 없음.

## ★ 조사 후 보류 — 운송 scene_fp 에 joint_overrides 추가 (불채택)
- **결론**: 보류. 운송은 접합을 입력으로 안 씀 + 첫 조각(공유 am 제공자)과 충돌.
- **근거 1(운송 미사용)**: transport 패키지 전체에서 joint 를 입력으로 받는 곳 0. 운송은 design_result(단면)·기하만 사용. 접합은 운송에 직접 영향 없음 — "접합→해석→단면 크기"의 간접 영향뿐이고, 그 무효화는 단면설계 재실행 → invalidate_from(3)으로 이미 보장. 지문에 넣어도 단면 동일하면 운송 입력 동일 → 재계산 낭비만.
- **근거 2(첫 조각과 충돌)**: 운송이 쓰는 compute_scene_fp 는 공유 am 제공자(model_provider)의 캐시 키와 동일 함수. 여기 joint 추가 시 접합만 바꿔도 am 재빌드(am 은 토폴로지라 joint 무관) → 첫 조각 중복제거 효과 훼손. 운송 전용이 필요했다면 별도 합성 지문이어야 하나, 근거1 때문에 불필요.

## ☆ 발견(미해결) — 접합 변경 후 단면설계 결과 미폐기 (잠재 stale)
- **현상**: 모델편집(배치·정의) 탭 진입 시엔 set_section_design_result(None)으로 단면결과 폐기(main_3d:1222). 그러나 접합 변경(_on_joint_change/_revert/_add_pick, main_3d_joint:195/318/466)에는 단면결과 폐기 경로 없음. 접합탭→해석/단면/물량 이동(main_3d:1203)도 ops 뷰만 숨김(캐시 무효화 아님).
- **영향**: 접합만 바꾸고 단면설계를 재실행하지 않으면 물량·운송이 접합 미반영 옛 단면결과를 계속 사용 가능. (운송 지문이 아니라 접합→단면 사이의 갭.)
- **상태**: 사용자 결정으로 이번엔 보류. 후속 단계에서 "접합 변경 시 단면결과 폐기" 단일 지점 추가로 처리 가능. 선결 — 접합 변경이 실제로 단면을 바꾸는지(부재력 변화) 재현 확인 필요.

---

# ★★ 4단계 — 렌더 통합 (진행 중)

## 적용 이력 — 4① 조각 a: 운송/물량 3D 직렬화 공통화 (L2-3, 완료)
- **신설(loaded_3d_three.py)**: _new_scene_data(공통 헤더 빌더 — trips·gap·n_groups·group_colors, hide_truck 옵션) + _render_three_html(JSON 임베드 + three.js 인라인, 운송·물량 빌더 공유).
- **교체**: serialize_pack_for_three / serialize_quantity_for_three 의 data 헤더 초기화 → _new_scene_data. build_loaded_3d_three_html / build_quantity_3d_html 의 마무리 3줄 → _render_three_html.
- **하지 않음(의도)**: 직렬화 두 함수의 *배치 알고리즘* 통합 — 운송(트럭 적재 그룹핑) vs 물량(타입별 전시)이 본질적으로 달라 부적절. 부재 1개 직렬화(_serialize_placement)는 *이미* 공유 중. 회차별 trip_data 키도 미묘한 차이(quantity_label_html·instance_count 등) 있어 각자 유지.
- **검증**: 컴파일 OK + 스모크(pack/qty 헤더 키가 변경 전과 동일, hide_truck 운송 부재·물량 True, HTML 양쪽 생성·토큰 치환) + 운송탭·물량탭 3D GUI 정상. 동작 불변.
## 적용 이력 — 4① 조각 b: three 부트스트랩 차이 정리 (L2-2 잔여, 완료)
- **조사 결론(부트스트랩 완전 추출은 불채택)**: 3표면(배치 viewer / 2D 정렬 alignment / 운송 loaded) 셋업 비교 결과 정당한 차이(2D=직교카메라+회전잠금+자체입력 vs 3D=원근+OrbitControls+Raycaster, 줌 거리한계=데이터 스케일)가 많아 셋업 코드 단일 모듈화는 부적절. → 우연한 차이만 통일.
- **통일 적용**: ① 배치뷰 배경 0xf5f7fa→0xffffff(3뷰 흰색 통일) ② 배치·정렬 setPixelRatio→Math.min(dpr,2)(운송과 동일, 고DPI 성능) ③ 운송 loaded 의 castShadow/receiveShadow 9곳 제거(재질이 MeshBasicMaterial+조명없음+shadowMap미설정 → 완전 무효과 죽은코드). 좌표계 주석은 이미 충분해 미변경.
- **검증**: 컴파일 OK + grep 그림자 0 + HTML 생성 정상 + 3뷰 GUI 정상. 동작 불변(배치 배경색만 의도된 시각 변화).
- **남은 렌더 조각**: ⓒ 부재 형상 단일화(L2-1) — *탐색 후 보류*(아래).

# ★★ 5단계 — 층간변위비(drift) 검토 + 코어 발산 버그 수정 (L4-1, 완료)

## 적용 — 층간변위비 검토
- **신설(analysis/drift_check.py)**: compute_story_drifts / compute_all_drifts / worst_drift. OpsResults.story_disp(z→δx,δy, 케이스별)에서 (위층δ−아래층δ)/층고 산출. 한계 상수 SEISMIC_DRIFT_LIMIT=0.015·풍 WIND_DRIFT_LIMIT=1/400(18~20층 공동주택 통상, 고정). 케이스 키로 지진(E)/풍(W) 구분, 전도방지(_OT)·중력 제외.
- **표시**: 단면설계 상태 라벨에 한 줄 요약 '변위비 지진 1/N(OK)·풍 1/N(NG)'(main_3d _drift_summary_text → set_status). 구조해석 패널 표 시도→사용자 결정으로 철회(되돌림). 현재 활성 해석 결과(수렴 단면) 추종.
- **데이터 연결**: ConvergeResult.ops_results(신규 필드) ← converge_sections 마지막 solve_all_cases 결과 저장(추가 해석 없음). _drift_summary_text 가 이걸 사용.

## 부수 수정 — 조합 케이스 층변위 미합성
- **문제**: _linear_combo/_pm_envelope(ops_solver)가 부재력·node_disps 는 합성하나 story_disp 누락 → 조합 케이스(Ex/Ey/Wx/Wy) 변위 빈 dict → drift 표 안 뜸.
- **수정**: _linear_combo 에 story_disp 선형합 추가, _pm_envelope 에 env.story_disp=plus.story_disp 추가.

## ★ 중대 버그 수정 — 코어 슬래브 발산 (drift 검토가 발견)
- **증상**: 최상층(코어 슬래브 z=13680) 횡변위가 수십만~수백만 mm 발산(mechanism). 하위층은 1/2500 수준 정상. drift 검토가 이 숨은 결함을 잡아냄.
- **원인(진단 확정)**: 코어 슬래브 frame 노드는 z=슬래브윗면+MODULE_JOINT_GAP(=h+20, R09 모듈천장 정합용)에 생성. 그런데 코어 벽 상단은 z=h. → 코어 슬래브가 코어 벽보다 20mm 위 + 병합/접합 로직이 코어 슬래브 제외 → 코어 슬래브가 벽 위에 안 얹히고 공중 부유 → 발산. (xy 는 정합, z만 20mm 어긋남 — 사용자 육안 확인.)
- **수정(topology.py:585)**: 코어 벽 z_top = h → h + MODULE_JOINT_GAP_MM. ① 코어 슬래브 받침 노드(h+20)와 z 일치 → 자동 통합 ② 층고 H=h+gap 이므로 상단=다음층 코어벽 base → 층간 코어벽 연속까지 동시 충족. 슬래브·모듈천장 결합은 그대로(트레이드오프 없음). 사용자 통찰(코어벽을 올리는 게 슬래브를 내리는 것보다 우월) 채택.
- **검증**: 발산 해소(변위비 정상값) + 코어 슬래브가 벽과 수직 연결 + 스타터 테스트 8/8 + GUI 정상. 임시 진단(DRIFT/DIA/CORE/XY) 전부 제거.

## ★ 조사 후 보류 — ⓒ 부재 형상 단일화 (L2-1, 불채택)
- **결론**: 보류. 진짜 중복은 H형강 I자 단면 하나뿐, 통합 위험>이득.
- **4벌 실측**: ① vispy mesh_builder(Component→numpy 메시, 형상 규칙 보유) ② 정의/배치 three(이미 만든 메시만 받아 그림 — 형상 규칙 없음) ③ 2D 정렬 three(부재를 납작한 사각형으로 — 단면 개념 없음, 용도 다름) ④ 운송/물량 three(저수준 dict 받아 박스/H형강). → 용도가 달라 자연히 다른 코드, "같은 규칙 4중 재구현" 아님.
- **유일 중복**: H형강 I자 단면 12점 압출이 mesh_builder.build_h_section(L148~)과 loaded_3d_three._addBeamHsection(L954~) 두 곳. vispy(numpy)와 three(ExtrudeGeometry)는 렌더 API가 근본적으로 달라 spec 공유 비현실. 색상은 이미 동일값 유지 중.

## ☆ 핵심 사실 — 물량뷰 = 운송뷰 부재 직렬화 *공유*
- 물량뷰(serialize_quantity_for_three)와 운송뷰(serialize_pack_for_three)는 부재 1개를 푸는 **_serialize_placement / _serialize_body_parts / _serialize_attached_part 를 공유**. JS 그리기(_addPyBox/_addBeamHsection)·HTML 템플릿도 공유. **다른 것은 오직 배치**(물량=타입별 전시 좌표 / 운송=실제 트럭 패킹 좌표). → 부재 표현 관련 수정은 한 곳이면 양쪽 동시 반영.

## ☆ 발견(미해결) — 운송/물량 H형강 두께가 8/12 고정 (WYSIWYG 갭)
- **현상**: loaded_3d_three._addBeamHsection 의 웨브 T1=8mm·플랜지 T2=12mm 가 고정. 부재 실제 설계 두께 무시. (배치 3D mesh_builder 는 부재 section_t 사용 + 플랜지=1.5·t1 — 부재별 반영.) 외형 높이·폭은 운송도 부재값 사용(문제 없음).
- **반영하려면(중간 규모)**: ① BodyPart/AttachedPart(models.py:119~,194~)에 웨브 두께 필드 추가(현재 web_dx/dy/dz·section_type 만 있고 두께 없음) ② adapter 가 그 필드에 설계 단면 실제 두께 주입 ③ _serialize_body_parts/_attached_part 가 내보냄 ④ JS 가 고정값 대신 사용. 데이터가 설계→어댑터→직렬화→JS 로 새로 이어져야 함.
- **상태**: 보류(사용자 결정). 두께는 시각적으로 미세해 체감 효과 작고 경로가 길어 위험 대비 이득 낮음.

## ★ 조사 후 보류 — 4③ 면 정합 (L1-2, 불채택)
- **결론**: 보류. stale 아님(기하 생성 규칙 문제 — 순차 진행 무관, 언제 계산해도 동일 어긋남). 진단은 정확하나 수정 위험이 최고.
- **실측**: model/core.py generate_sub_components(286~ 등 클래스별) 가 s=_SECTION_W(200)·half_s(100) 고정 인셋으로 기둥(코너 안쪽 100)·보(단면폭 200 들임)·슬래브(4변 200)·비내력벽(두께 100) 생성. 부재 크기 무관 고정. → 깊이/폭 다른 모듈 인접 시 가장자리 보·슬래브 절대위치 불일치 + 공유면 이중벽.
- **위험**: generate_sub_components 는 모든 부재 좌표의 원천 → 해석(부재력)·단면·물량·운송·충돌·렌더 전부 영향. 인셋은 오타 아닌 단면 기반 물리 기하. 증상은 비표준 치수 모듈 인접 시에만 발현(표준 치수만 쓰면 안 보임). 사용자 사용패턴상 보류 합리적.

# ★★ 4② WYSIWYG — 설계 단면을 3D 렌더에 반영 (L1-1, 완료)
- **문제**: 단면설계가 보를 H400·기둥 250각으로 키워도 BeamData/ColumnData.section_w/h/t 가 200 고정 → 3D 솔리드가 설계 결과 무시(균일 막대).
- **판정(탐색)**: 렌더 전용으로 안전. section_w/h/t 는 mesh_builder 만 실시간 사용 — 해석(ops_builder)·물량은 AnalysisMember.design_section/공칭값 별도 경로라 영향 0. AnalysisMember 의 section_w/h/t 는 이미 _apply_section(section_converge:213)이 갱신 중. 누락된 것은 *렌더가 읽는 컴포넌트 형상*으로의 write-back.
- **신설**: render/section_apply.py — apply_design_sections_to_scene(scene, am). am.comp_to_members(comp.id→[mid]) + members(role·design_section)로, 컴포넌트별 기둥/보 역할 대분류 대표 단면을 그 comp 의 ColumnData/BeamData 전체에 적용(컴포넌트 일괄, 사용자 결정). H 는 design_section 외형치수 사용(웨브두께는 기존 한계).
- **트리거**: main_3d _converge_and_color 의 set_section_design_result 직후, 별도 try 로 격리(렌더 실패가 설계 막지 않음). am 은 공유 제공자(1644)가 준 객체 — 단면설계가 design_section 써넣은 상태.
- **가시 효과**: 단면설계 탭 와이어프레임이 아니라 정의/배치 솔리드 뷰·단면 타입 선택 컴포넌트 3D 에서 굵기 반영.
- **부수효과(정상)**: 굵게 그리면 보류한 면 정합(L1-2) 겹침이 화면에 드러남 — 이전엔 굵기 무시로 가려졌던 실제 상태가 정직하게 보이는 것.
- **검증**: 컴파일 OK + mock write-back(보 400×300·기둥 250각 정확 반영, 역할 비혼동) + GUI 정상. 해석·물량 불변.

