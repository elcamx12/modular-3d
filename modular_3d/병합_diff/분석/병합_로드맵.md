# 2차 병합 로드맵 (작성중)

> 기준: `C:/Users/이건영/Desktop/종설/04_코드/my_project/modular_3d` (내 코드)
> 흡수 대상: `C:/Users/이건영/Desktop/종설/팀원코드/source_repo/modular_3d` (팀원 코드)
> 1차 병합 (커밋 524ca6e) 이후 *팀원이 다시 작업한 분량*. 양쪽 다 수정.

## 분류 (CRLF 무시 후 진짜 변경)
- **A. 팀원만 추가**: 3개 — ui/fonts·home_panel·section_viewer
- **B. 나만 추가**: 8개 — analysis 4 + analysis/tests 4 + render 2 + ui 1
- **C. 작은 변경 (양쪽)**: 24개 — 추가/삭제 합 100줄 미만
- **D. 중간 변경 (양쪽)**: 2개 — ui/analysis_panel, ui/transport_panel
- **E. 큰 변경 (양쪽)**: 6개 — evaluation_adapter, compare_panel, evaluation_panel, main_3d, project_settings, loaded_3d_three

---

## 분석 진행 상태
- [x] A. 팀원만 추가 (3)
- [x] B. 나만 추가 (8)
- [x] C. 작은 변경 (24)
- [x] D. 중간 변경 (2)
- [ ] E. 큰 변경 (6)
  - [x] E-1. evaluation/evaluation_adapter.py
  - [x] E-2. transport/loaded_3d_three.py
  - [x] E-3. ui/compare_panel.py
  - [x] E-4. ui/evaluation_panel.py
  - [ ] E-5. ui/main_3d.py
  - [ ] E-6. ui/project_settings.py
- [ ] 종합 로드맵 + 충돌 정책 + 진행 순서

---

## A. 팀원만 추가 (3개)

### A-1. `ui/fonts.py` (신규, 73줄)
- **내용**: 한국어 산세리프 폰트 등록 — `Freesentation`(본문) + `Paperlogy`(헤드라인) TTF 9 weight 씩 QFontDatabase 로 등록. 패밀리 상수 (`F_BODY`, `F_HEAD` 등) export.
- **외부 자원 의존**: `source_repo/프리젠테이션/`, `source_repo/페이퍼로지/` 두 폴더 (TTF 파일). 내 my_project 루트엔 두 폴더 없음.
- **fonts.py 자체 동작**: `Path(__file__).resolve().parents[2]` → 내 환경에선 `my_project/` 가 됨. 두 폴더 미존재 시 *조용히 skip* → Qt 기본 폰트로 fallback. 크래시 없음.
- **내 코드 참조**: 0건 (의존성 없음).
- **권장 처리**: `ui/fonts.py` 복사 + `프리젠테이션/`, `페이퍼로지/` 폴더 둘도 함께 복사. 폴더 안 가져오면 폰트만 fallback (UI 기능은 정상).
- **위험**: 낮음. fallback 안전.

### A-2. `ui/home_panel.py` (신규, 287줄)
- **내용**: 프로그램 첫 진입 화면 — QStackedWidget 의 page 0 으로 랜딩 카드. 좌측 큰 카드에 *프로젝트 설정 폼 전체* 인라인. [새 프로젝트 시작] 버튼 → ProjectSettings 적용 후 시그널 발화 → MainWindow 가 모듈 정의 탭으로 전환.
- **의존**: `from modular_3d.ui.project_settings import ProjectSettings, ProjectSettingsForm`. 즉 *팀원 project_settings.py 가 ProjectSettingsForm 클래스를 새로 분리* 했을 가능성 (E 카테고리에서 확인 필요 — project_settings 추가 305줄).
- **내 코드 참조**: 0건.
- **권장 처리**: 복사. 단 `ProjectSettingsForm` 이 팀원 project_settings.py 에 존재한다는 전제 → E 카테고리 처리 후 도입.
- **연관**: MainWindow (`ui/main_3d.py`) 가 QStackedWidget + home_panel 사용하는 구조로 바뀌었을 가능성 → E 카테고리에서 확인.
- **위험**: 중간. project_settings 와 main_3d 둘 다 큰 변경이라 *세 파일이 한 세트*.

### A-3. `ui/section_viewer.py` (신규, 999줄)
- **내용**: 거실 단면 정보 다이얼로그 — 배치설계 탭의 "단면 정보" 버튼이 호출. 거실(`room_type='living'`) 폴리곤의 가장 긴 변(외벽 추정) 과 평행하면서 무게중심을 지나가는 단면선을 잡아 1·2층 모듈/벽/패널을 QPainter 로 2D 단면 이미지 생성.
- **의존**: `from modular_3d.ui.fonts import F_BODY, F_HEAD` — try/except 로 방어 (없으면 `Malgun Gothic` fallback). 그 외 `scene`, `Room` 등 model 사용 (내 코드 model 호환).
- **내 코드 참조**: 0건.
- **권장 처리**: 복사. fonts.py 없어도 fallback 동작. 단 배치설계 탭에 "단면 정보" 버튼 추가하는 부분이 main_3d 또는 alignment_view 에 있을 것 → E 카테고리에서 확인.
- **위험**: 낮음.

**A 종합**: 3개 모두 복사 안전. 단 home_panel 은 project_settings(E) 와 main_3d(E) 와 한 세트로 묶여 *동시 도입* 필요.

---

## B. 나만 추가 (8개) — 유지 + 팀원 변경 파일이 *지울 위험* 점검

전부 *Phase 1 (1차 병합 이후) 에 내가 추가* 한 모듈. 팀원 코드엔 없음.

### B-1. `analysis/drift_check.py`
- **역할**: 층간변위비(KDS 41 17) 검토 — read-only. 해석은 안 건드림.
- **외부 참조**: `analysis/ops_solver.py:1026` 주석에 언급 (사용자 노트).
- **권장**: 유지.

### B-2. `analysis/member_roles.py`
- **역할**: 부재 역할 한글 매핑(`ROLE_KO`) + `classify_role_ko`·`role_ko_order_key` 공용 함수.
- **외부 참조 (내 코드 내부)**: `analysis/quantity_by_component.py:39`, `ui/analysis_panel.py:41`.
- **위험**: 팀원 `analysis_panel.py` 가 *member_roles import 가 없는 옛 버전* 일 가능성 큼 → 덮어쓰면 *역할 한글 매핑 깨짐*. D 카테고리(analysis_panel) 에서 *내 import 블록 보존* 필수.
- **권장**: 유지 + analysis_panel 병합 시 import 블록·역할 함수 호출부 모두 보존.

### B-3. `analysis/model_provider.py`
- **역할**: `get_analysis_model(scene)` 공유 빌더 — 4개 소비처 토폴로지 중복 빌드 단일화. scene 지문 캐싱.
- **외부 참조**: `schedule/schedule_adapter.py:247`, `ui/controls/controls_f6.py:154`.
- **위험**: 팀원 `schedule_adapter.py` 변경 1/6 줄밖에 안 됨 — 같은 import 블록 살아 있을 가능성 크나 라인 확인 필요. `controls_f6.py` 도 변경량 작음 (5/13).
- **권장**: 유지. C 카테고리 검토 시 import 라인 살아있는지 확인.

### B-4. `analysis/quantity_by_component.py`
- **역할**: 컴포넌트별·타입별 물량 분해 — 물량탭 개편 Phase 1. `build_quantity_by_component`, `UnitPrices`.
- **외부 참조**: `ui/main_3d.py:1047,1048,1081`, `ui/quantity_panel.py`.
- **위험**: ui/main_3d.py 가 *큰 변경 (E 카테고리)*. 팀원 main_3d 에는 이 import 가 없음 → 덮어쓰면 *물량탭 깨짐*. 라인별 병합 필수.
- **권장**: 유지 + main_3d 병합 시 1047~1089 라인 영역 보존.

### B-5. `analysis/tests/` 신규 4개 (perf_probe / prof_loads / regression_solve / test_golden_starter)
- **역할**: 회귀·골든·프로파일 테스트. 본체 코드 의존 0건 (테스트만).
- **권장**: 유지. 단순 복사 안전.

### B-6. `render/section_apply.py`
- **역할**: 단면설계 결과 → 렌더 형상 (BeamData/ColumnData) write-back. WYSIWYG 복구.
- **외부 참조**: `ui/main_3d.py:2059,2118` (메서드명 `_section_apply_override` 가 동일 단어라 검색에 잡혔으나 실제는 다른 함수). 직접 import 는 별도 확인 필요.
- **권장**: 유지. main_3d 병합 시 import 라인 보존.

### B-7. `render/three_assets.py` + `render/vendor/`
- **역할**: three.js CDN 의존 제거 — vendor/ 폴더 라이브러리 인라인 주입. 오프라인 지원.
- **외부 참조**: `render/viewer_three.py:88`, `transport/loaded_3d_three.py:1410`, `ui/alignment/alignment_view_three.py:126`.
- **위험**: viewer_three / loaded_3d_three / alignment_view_three *셋 다 양쪽 변경*. 팀원 버전에 inline_three_libs 호출 없을 것 → 덮어쓰면 *오프라인 3D 깨짐*. 라인별 병합 필수.
- **권장**: 유지 + 세 파일 병합 시 inline_three_libs 호출 보존.

### B-8. `ui/quantity_panel.py`
- **역할**: 물량탭 우측 비용 패널 — 트리 + 강재본수표 + 자재비표.
- **외부 참조**: `ui/main_3d.py:762, 800-803, 1041, 1073, 1089` (QuantityPanel 생성·연결).
- **위험**: main_3d 큰 변경 — 팀원 버전에 QuantityPanel 코드 없음 → 덮어쓰면 *물량탭 우측 패널 사라짐*. 라인별 병합 필수.
- **권장**: 유지 + main_3d 병합 시 762·800~803·1041·1073·1089 라인 영역 보존.

**B 종합**:
- 8개 전부 유지. 단순 복사 위험 0.
- **🚨 핵심 위험**: 카테고리 E (큰 변경) 의 팀원 파일을 *그대로 덮어쓰기 금지*. 다음 6개 파일에 내 import / 사용 라인이 박혀 있음:
  - `ui/main_3d.py` — quantity_panel·quantity_by_component·model_provider·section_apply 다수 import + 라인 1047~1089·762·800~803 보존
  - `ui/analysis_panel.py` — member_roles import (라인 39·41·59·863)
  - `transport/loaded_3d_three.py` — three_assets import (라인 1410)
  - `ui/alignment/alignment_view_three.py` — three_assets import (라인 126)
  - `render/viewer_three.py` — three_assets import (라인 88)
  - `schedule/schedule_adapter.py`, `ui/controls/controls_f6.py` — model_provider import (작은 변경 D/C 영역)
- 팀원 코드는 1차 병합(`524ca6e`) 이전 베이스(`97a3ccc`) 에서 그대로 분기한 듯 — *내가 1차 병합 이후 추가한 모듈을 모름*. 즉 *팀원 파일 = 옛 import 블록*.

---

## C. 작은 변경 24개 — 전수 분석 결론

**전체 결론**: **24개 전부 *내 변경* 이며 *내 버전 유지***. 팀원은 *공통 베이스(`97a3ccc` 또는 1차 병합 직후) 그대로* — 작은 변경 영역에서 의도적 수정 0건. 즉 이 카테고리는 *팀원 코드 가져올 것 없음*.

(이는 큰 함정 — *팀원 폴더 통째로 덮어쓰면 24개 파일이 회귀* 함.)

### C-1. `_utils/debug.py` (25/157)
- 내 변경: 통합 로거(`_get_logger`) + cp949 안전 핸들러(`_SafeStreamHandler`) + 파일 로그(my_project/logs/modular_3d.log) + tee(`_Tee`, `install_stdout_tee`) + `log_info/warn/error` API + `log_path()`.
- 팀원: 단순 `dprint` 토글 (베이스).
- **권장**: 내 버전 유지. 다수 파일 (C-render·controls·alignment) 가 `log_error` 사용 — 회귀 시 import 깨짐.

### C-2. `analysis/load_calculator.py` (25/53)
- 내 변경 (2026-06-02 성능): `iw_by_parent`·`rooms_by_floor` 색인으로 O(N²)→O(N), 비내력벽 분류 캐시 (`_module_face_cache`·`_struct_wall_r_cache`).
- 팀원: 전체 순회 베이스.
- **권장**: 내 버전 유지.

### C-3. `analysis/ops_builder.py` (15/20)
- 내 변경 (2026-06-02 성능): `build_ops_model(am, scene, verify=True)` — `verify=False` 면 self-validate/verify_against_opensees 생략. 단면 수렴 2~N 반복에서 검증 비용 제거.
- 팀원: 항상 검증.
- **권장**: 내 버전 유지.

### C-4. `analysis/ops_solver.py` (8/62)
- 내 변경 (2026-06-02 성능): `_reset_domain_between_cases()` 신설, `solve_all_cases(...verify=True)` 인자, 6 케이스마다 *모델 재빌드 → 1회 빌드 + 도메인 리셋*. `core_*` 부재 분포 단면력 생략 (양단 2점). 층별 횡변위 `story_disp` (drift_check 용).
- 팀원: 매 케이스 재빌드 베이스, story_disp 없음.
- **권장**: 내 버전 유지. drift_check (B 카테고리) 가 story_disp 의존.

### C-5. `analysis/section_converge.py` (8/34)
- 내 변경: 첫 반복만 `verify=True`, 이후 `verify=(it==1)`. `ConvergeResult.ops_results` 필드 (drift_check 용). `_comp_section_sig(scene=scene)` 종속 부재 단면 시그니처에 포함 → 자체 같고 종속만 다른 케이스 분기.
- 팀원: 베이스 (자체 부재만).
- **권장**: 내 버전 유지.

### C-6. `analysis/topology.py` (8/10)
- 내 변경 (2026-06-01): 코어벽 상단 `z_top = h + MODULE_JOINT_GAP_MM` — 코어 슬래브 받침 노드와 z 일치 → 자동 통합. 옛 `z=h` 시 슬래브 노드 200mm 어긋나 *발산 버그*.
- 팀원: 옛 `z=h` (베이스).
- **권장**: 내 버전 유지. **결정적 — 회귀 시 해석 발산**.

### C-7. `model/core.py` (2/4)
- 내 변경: Vertical3Module 슬래브 끝변(x=w 쪽) 단면폭 s 만큼 안쪽으로 들임 — 끝변 보 안쪽 면(x=w-s)에 맞춤.
- 팀원: x=w (베이스).
- **권장**: 내 버전 유지.

### C-8. `model/type_naming.py` (1/42)
- 내 변경 (2026-06-01): `merged_wall_ids` 종속 구성 시그니처 포함, 흡수 구조벽(`merged_fp_id`) 라벨 부여(`"<부모> 구조벽n"`). + log_error 사용.
- 팀원: 옛 (베이스).
- **권장**: 내 버전 유지.

### C-9. `render/mesh_builder.py` (8/20)
- 내 변경 (2026-06-02): 벽 채움면 표시 전역 토글 (`_SHOW_WALL_FILL` + `set_wall_fill_visible`) — 배치설계에서 채움면 끄면 프레임·슬래브·코어만.
- 팀원: 토글 없음 (베이스).
- **권장**: 내 버전 유지. controls_f5 의 `set_wall_fill_visible` 메서드와 연동.

### C-10. `render/room_mesh.py` (3/5)
- 내 변경: `ROOM_PLANE_Z_MM = 105.0` (슬래브 상면 100 + 5mm). 팀원: `250.0` (근사). 정밀화.
- **권장**: 내 버전 유지.

### C-11. `render/viewer.py` (6/35)
- 내 변경 (2026-06-02): 코어 부재 라벤더 색·width 1.5·노드 size 2.0 (구조해석 시각 정리). 단면설계 화면에서 다이어프램 master 노드 마커 숨김.
- 팀원: 짙은 보라·width 3.0·균등 사이즈 (베이스).
- **권장**: 내 버전 유지.

### C-12. `render/viewer_strangler.py` (1/2)
- 내 변경: `log_error` 사용. 팀원: print.
- **권장**: 내 버전 유지.

### C-13. `render/viewer_three.py` (28/5)
- 내 변경: `inline_three_libs(html)` (오프라인 라이브러리 인라인), `set_section_y` (2026-06-01), `capture_section_front` (2026-06-02 PNG 캡처). log_error 사용.
- 팀원: 셋 다 없음 (베이스).
- **권장**: 내 버전 유지. three_assets (B-7) 와 한 세트.

### C-14. `schedule/schedule_adapter.py` (1/6)
- 내 변경: `get_analysis_model(scene)` 경유 (폴백 `build_analysis_model`).
- 팀원: 직접 `build_analysis_model` (베이스).
- **권장**: 내 버전 유지. model_provider (B-3) 의존.

### C-15. `transport/models.py` (0/6)
- 내 변경: `MODULE_PAIR_MAX_LEN_MM = 6000.0` 상수 신규 (모듈 페어링 6m 정책 — 1차 4.5m → 6m 변경, 패커 4 곳 공유).
- 팀원: 상수 없음 + 4.5m 하드코드 (베이스).
- **권장**: 내 버전 유지. **정책 변경** — 6m 페어링.

### C-16. `transport/packer.py` (1/1)
- 내 변경: 주석 4.5m → 6m.
- **권장**: 내 버전 유지.

### C-17. `transport/packer_core.py` (13/14)
- 내 변경: `MODULE_PAIR_MAX_LEN_MM` 상수 참조로 리팩토링 + 값 6m. 5 군데 (옆자리 검사·_merge_small_modules·V2 후처리).
- 팀원: 하드코드 4.5 (베이스).
- **권장**: 내 버전 유지.

### C-18. `transport/packer_local_search.py` (5/6)
- 내 변경: 위와 동일 — 상수 참조.
- **권장**: 내 버전 유지.

### C-19. `transport/packer_meta.py` (4/6)
- 내 변경: 위와 동일 + 비용 하한 산식 6m 반영.
- **권장**: 내 버전 유지.

### C-20. `ui/alignment/alignment_dock_three.py` (1/2)
- 내 변경: log_error 사용.
- **권장**: 내 버전 유지.

### C-21. `ui/alignment/alignment_view.py` (2/4)
- 내 변경: log_error 사용 (2곳).
- **권장**: 내 버전 유지.

### C-22. `ui/alignment/alignment_view_three.py` (1/5)
- 내 변경: `inline_three_libs(html)` + log_error.
- **권장**: 내 버전 유지. three_assets (B-7) 의존.

### C-23. `ui/controls/controls_f5.py` (3/33)
- 내 변경: log_error + QMessageBox(저장/불러오기 실패 사용자 알림), `_rooms_visible` 토글 (옵트아웃 안 그리기), `set_wall_fill_visible`·`set_rooms_visible` 메서드 신설.
- 팀원: print 만, 토글 없음 (베이스).
- **권장**: 내 버전 유지.

### C-24. `ui/controls/controls_f6.py` (5/13)
- 내 변경: `get_analysis_model` 경유 + `solve_all_cases(prebuilt_am=am)` 전달, log_error 사용.
- 팀원: 직접 빌드 + print.
- **권장**: 내 버전 유지.

### C-25. `ui/joint_edit_panel.py` (4/5)
- 내 변경: 범례 위치 — 패널 *맨 위* → *변경 내용 저장 버튼 아래* (UI 레이아웃 의도적 변경).
- 팀원: 맨 위 (베이스).
- **권장**: 내 버전 유지. (사용자 UX 결정 사항)

### C-26. `ui/main_3d_joint.py` (1/6)
- 내 변경: `get_analysis_model` 경유.
- **권장**: 내 버전 유지.

### C-27. `ui/section_design_panel.py` (6/30)
- 내 변경 (L6): 옵션·단면변경 박스를 `self._option_box`·`self._change_box` 로 보관 → `_fit_to_content` + `_force_pane_width` 메서드로 우측 패널 폭 컨텐츠 폭 맞춤.
- 팀원: 지역 변수 + 폭 자동조정 없음 (베이스).
- **권장**: 내 버전 유지.

**C 종합**: 24개 전부 *내 버전 유지*. 가져올 것 없음. *팀원 폴더 통째 덮어쓰기 절대 금지* 강력 시그널 (또 다시 확인).

---

## D. 중간 변경 (2개)

### D-1. `ui/analysis_panel.py` (95/80)
- **내 변경 (2026-05-31 ~ 06-02)**:
  - `member_roles` 모듈에서 ROLE_KO/_classify_role_ko_fn import (B-2 모듈 분리) — 물량 분해기와 공유.
  - 모든 컬럼 ResizeToContents (col0 도 내용폭 고정) — 우측 흰 공간 해소.
  - **부재력 트리 _tabs 밖 root 직접 배치** — 탭 묶음 최소폭 누설 차단. (2026-06-02 완전 분리)
  - show_tabs(show_quantity, show_transport) — show_member 인자 제거 (member 페이지가 항상 root 에 표시되므로).
  - 물량 탭 폭 고정은 `_tabs.isVisible()` 일 때만 (부재력 폭 덮어쓰기 방지).
  - `_fit_panel_to_tree`: 항목 텍스트 직접 측정 (들여쓰기·헤더 폭 계산) — 컬럼 폭 진동 해결.
- **팀원 변경**: 베이스 그대로 — ROLE_KO 인라인, col0 Stretch, 부재력 = _tabs[0], sizeHintForColumn 폭 산정.
- **충돌**: 없음 — 팀원은 손 안 댐.
- **권장 처리**: 내 버전 유지.
- **위험**: 낮음. (member_roles 의존성은 B-2 보존되므로 무관)

### D-2. `ui/transport_panel.py` (425/131)
- **내 변경 (2026-05-31 운송탭 개편 + Phase 5-K5)**:
  - 좌측 입력 패널 폐지 → *우측 단일 패널* 구조. main_3d 가 우측 pane 만 reparent.
  - ① 상태바·8단계 인디케이터·정책 라벨 폐지.
  - ② 옵션: 비내력벽 자동판별 ON·격자 100mm·FloorPanel 장애물 포함을 *코드 고정값* 화 (UI 입력 제거).
  - ② 옵션: 벽 단위중량 내부/외부 입력 칸 제거 — 프로젝트 설정 단일 출처 (`_proj_settings`) 에서 직접 읽음.
  - 자동 재계산 디바운스 타이머·체크박스 제거 — *수동 [▷ 실행] 만*.
  - 적재율 sub-탭·경제성 sub-탭 제거 → "회차표" 단일 그룹박스.
  - 회차표 컬럼 재구성 — 7컬럼 → 5컬럼 (`차량/아이템/화물중량+최대화물중량/GVW/운임`).
  - ⑧ 진단 누적 패널 폐지 → *문제만 QMessageBox*.
  - 트럭 카탈로그 관리 버튼 폐지.
  - 결과 요약 카드에 *총 운임* 추가 — `_last_eco` 합계.
  - **🪲 디버그 저장 버튼 (Phase 5-K5)** — BB 트레이스 → 바탕화면 bb_trace.txt.
- **팀원 변경**: 베이스 그대로 — 좌·우 분할 splitter / 8단계 인디케이터 / 자동 재계산 디바운스 / 적재율·경제성 sub-탭 / 진단 누적 패널 / 회차표 7컬럼 / 벽 단위중량 입력 칸 / 카탈로그 버튼.
- **충돌**: 없음 — 팀원은 손 안 댐.
- **권장 처리**: 내 버전 유지.
- **위험**: 낮음. 단 *main_3d (E 카테고리) 가 운송탭의 좌·우 pane 을 reparent* 하는 옛 코드를 가지고 있으면 NPE — 팀원 main_3d 가 `_left_pane_scroll` 도 reparent 하는지 E 카테고리에서 확인 필요. 내 transport_panel 은 `_left_pane_scroll` 안 만들므로 *hasattr 가드 없으면 깨짐*.

**D 종합**: 2개 전부 *내 버전 유지*. **그러나 D-2 → E (ui/main_3d) 의존성 함정**: 팀원 main_3d 의 운송탭 reparent 로직이 옛 좌·우 pane 둘 다 만질 수 있음. E 카테고리에서 *main_3d 의 운송탭 진입 분기를 내 코드 기준으로 보존* 필수.

---

## E. 큰 변경 (6개)

### E-1. `evaluation/evaluation_adapter.py` (117/7)
- **내 변경 (2026-06-01)** — 단면설계 탭 → 종합탭 연결 강화:
  - `_build_members_from_section_types()` **신설** — 단면 설계 탭의 *타입 목록* 을 부재 구성으로 그대로 변환. (단면설계가 분류한 타입을 종합탭이 신뢰)
  - `_collect_module_sections_split()` **신설** — 기둥 / 천장보 / 바닥보 단면 라벨을 *별도 컬럼* 으로 분리. (종합탭 부재 컬럼 셀 잘림 해소)
  - `_build_members()` 에 `split_cache` + `**split` 키 합성. 변형 'A-1타입/A-2타입' 부여 시 컬럼 분리 단면도 함께 저장.
  - 패널 카운트·타입 목록에서 *코어슬래브 제외* (코어슬래브는 패널이 아님).
  - `build_evaluation_data()` 시그니처에 `section_types`, `scene_components_by_id` 인자 **추가** — 둘 다 옵셔널, 미전달 시 기존 자동 분류 폴백.
- **팀원 변경**: 7줄 — 베이스 일부 줄 제거 정도. *주요 신규 코드 없음*.
- **충돌 라인**: 없음 — 팀원이 신설 영역을 안 만짐.
- **권장 처리**: 내 버전 유지. 단순 *팀원 파일을 가져올 필요 없음*.
- **호환 영향**: `build_evaluation_data()` 옛 호출은 *그대로 동작* (옵셔널 인자). 단 **단면설계 결과가 종합탭에 정확히 반영되려면** 호출처 (E-5 main_3d 의 평가 저장 경로 + 평가탭 진입 코드) 가 `section_types=`·`scene_components_by_id=` 를 넘겨야 함.
- **위험**: 낮음. 단 E-5 검토 시 *내 main_3d 가 새 인자를 넘기고 있는지 확인 필요* — 안 넘기면 옛 폴백 동작 (외곽 + 강재 시그니처 자동 분류) 으로 동작, 종합탭 라벨이 단면설계 탭과 *살짝 다를 수 있음* (기능 회귀는 아님).

### E-2. `transport/loaded_3d_three.py` (156/250)
- **내 변경 (대규모 — 1차 병합 후 추가)**:
  1. **`_new_scene_data()` 헬퍼 신설** — 운송/물량 두 빌더가 공유하는 scene JSON 헤더 + `hide_truck` 옵션.
  2. **물량탭 전용 3D 도식 통째 신설** (~150줄):
     - `serialize_quantity_for_three(items_by_type)` — 트럭 없이 *단면설계 타입 1개 = 가짜 Trip 1개* 평면 배치. 같은 타입 인스턴스 -Y(폭) 1.5m 누적, 다른 타입 +X(길이) 3m 분리. trip_no = 타입 인덱스.
     - `build_quantity_3d_html(items_by_type)` — 물량탭 HTML.
     - 헬퍼 `_qty_fake_truck`, `_qty_label_html`, 상수 `QTY_INTRA_TYPE_GAP_M`, `QTY_INTER_TYPE_GAP_M`.
     - 라벨에 *타입명 + 부재별 단면 목록* (응력비 제외).
  3. **`_render_three_html()` 헬퍼 신설** — 운송/물량 공유, three.js 라이브러리 인라인 (`inline_three_libs`, B-7 의존).
  4. **색상 팔레트 전면 교체** — 배치설계 vispy mesh_builder 부재 색과 일치 (column=빨강, floor_beam=파랑, ceil_beam=하늘, slab=베이지, wall=회색, wall_nl=청회색). 캔틸/중간 부재도 *같은 종류 색으로 통일* (사용자 결정).
  5. **시각 정책 전환 — 흰 배경 + 평면 단색**:
     - 배경 `#ffffff` (팀원: `#0d1117` 다크).
     - **거리 안개(fog) 제거** + 광원 4종(Ambient/Hemi/Dir×2) + 그림자(`shadowMap`) + 바닥 plane *전부 제거*.
     - 모든 재질 `MeshBasicMaterial` (팀원: `MeshStandardMaterial` + roughness/metalness).
     - 라벨 색 `#1a1a1a` 검은 텍스트, 라벨 배경 흰 (팀원: `#e6edf3` 흰 텍스트, 다크 배경).
  6. **강조 정책 단순화 — 빨간 outline 만**:
     - `_setGroupDimmed`·`DIMMED_OPACITY_*` 함수 *통째 제거* (팀원: 다른 그룹 반투명 처리).
     - 모든 차량 항상 선명, 강조는 outline 으로만.
     - 다른 그룹 라벨 dim 처리도 제거.
  7. **hide_truck 분기**: ① 적재함 외곽선·바닥·캐빈·바퀴 등 트럭/적재공간 렌더 *전체 skip* + ② `renderGroupLabelHtml` 에서 `trip.quantity_label_html` 우선 사용 + ③ 그룹 라벨 좌표·outline 좌표 보정 (-Y 범위 화물).
  8. **`exports`**: `serialize_quantity_for_three`·`build_quantity_3d_html`·`QTY_*` 추가.
- **팀원 변경 (베이스, 옛 다크 사실적 테마)**:
  - `_new_scene_data` 없음 (인라인 dict).
  - 사실적 부재 색 — `steel/concrete/cant_beam/cant_slab/midbeam/midcolumn` 매핑. 모듈=파랑, floor=초록, dep_floor=주황.
  - 검은 배경 + `FogExp2` 안개 + Ambient·Hemi·Directional×2 광원 + 그림자(shadowMap PCFSoft) + 바닥 plane.
  - `MeshStandardMaterial(roughness, metalness)` 전반.
  - 라벨 다크 테마 (배경 `rgba(13,17,23,0.88)`, 텍스트 `#e6edf3`).
  - `_setGroupDimmed` 함수 + DIMMED_OPACITY_CARGO/TRUCK + 강조 시 다른 그룹 dim.
  - 물량탭 3D 도식 없음 (1차 병합 전이라 못 가질 수밖에).
- **충돌 라인**: **대규모** — 같은 라인에서 *정반대 방향* 변경. 색상 팔레트(~10줄), 배경+안개+광원+바닥(`init()` ~20줄), 모든 재질 생성(~10곳 `MeshBasicMaterial` ↔ `MeshStandardMaterial`), 라벨 스타일(CSS ~15줄), `_setGroupDimmed`+highlightTrip 강조 로직.
- **권장 처리**: **내 버전 유지**. 시각 정책이 사용자 결정으로 *명시 전환* (배치설계 일관성 + 흰 배경 + 단색 평면 + 물량탭 3D 도식 추가). 팀원 변경 가져오면 *모든 시각 결정 회귀* + 물량탭 3D 도식 통째 사라짐.
- **의존성**: B-7 `inline_three_libs` 호출 (1410행 추정) 보존 필수. 팀원 파일엔 이 호출 없음 — 덮어쓰면 *오프라인 모드 깨짐*.
- **위험**: 낮음 (병합 결정만 명확하면). 다만 *팀원의 사실적 셰이딩 기능 의도가 있다면* 사용자 결정 재확인 필요. 1차 병합 이후 *사용자가 명시적으로 단색 평면을 선택* 한 정황 (CLAUDE.md 컨텍스트의 vispy 배치설계 일관성) 이라 회귀할 이유 없음.

### E-3. `ui/compare_panel.py` (605/636) — 양쪽 *별개 디자인 두 세대*
- 내 파일 885줄 / 팀원 파일 823줄. 추가/삭제가 거의 동시에 큰 이유는 *클래스 자체가 바뀌어서*.
- **내 변경 (2026-06-01 재설계)**:
  - **클래스 자체 교체**: `_CaseSlot`(헤드라인 + body 본문 통합) → `_CaseBox`(상단 박스 + 단일 큰 카드 토글).
  - **레이아웃 정반대**: 종전 *3슬롯 가로 + 슬롯당 평면도+단면도+2열 표 통합* → *행=지표·열=케이스 A/B/C 카드 표 + 우측 사이드바 가로 막대 차트 3개* (공기/비용/평면효율).
  - **`_SectionTable` 클래스 삭제** — 더 이상 슬롯 내부에 2열 라벨/값 표를 두지 않음.
  - **폰트 등록 도입**: `from modular_3d.ui.fonts import F_BODY, F_HEAD, ensure_fonts_loaded` + `ensure_fonts_loaded()` 모듈 임포트 시 1회 호출. A-1 fonts.py 의존.
  - **`_won` 억원 단위 포매팅** 추가 (1e8 이상이면 "X.XX억원").
  - **케이스 색상 팔레트** `_CASE_COLORS = ["#7BB3F0", "#3A78D6", "#BBD7F8"]` 추가 (A/B/C 막대 색).
  - **카드 세로 520 고정** (`setFixedHeight(520)`) — 비교탭 세로 절반.
  - **`_active_view` 토글** — 채워진 상태에서 "plan" / "section" 뷰 전환 (`_mk_toggle_btn` + `_set_view`).
  - **거실 단면 도식 카드 안 그림** (2026-06-02) — section_viewer 의존 (A-3).
- **팀원 변경 (베이스, 1차 병합 시점)**:
  - `_CaseSlot` 클래스 — 슬롯 헤드라인 (`#HEADLINE_BG`, slot_label 흰색) + body (평면도 + 단면도 자리 + `_SectionTable` 섹션별 2열 표).
  - `_SectionTable` 클래스 — 섹션 제목 막대 + 2열 (라벨/값) 그리드. 줄무늬 배경.
  - 폰트 등록 없음. `_won` 단순 (원 단위만).
  - `_SLOT_MIN_WIDTH = 300` 슬롯 폭 상수.
  - 빈 상태: "파일 불러오기" 56px 버튼 + ".case.json 파일을 선택하세요" 힌트.
- **충돌 라인**: **거의 전부** — 같은 클래스 이름은 `_CaseSlot` 정도지만 내부 구현이 완전 다름. `_render_empty`·`_render_filled`·body 구성·헤더 구성·헬퍼 클래스 (`_SectionTable`) 존재 여부 등 *통째 다른 두 파일*.
- **권장 처리**: **내 버전 유지 (통째)**. 라인별 병합 불가능 — *별개 디자인 두 세대*. 사용자가 2026-06-01 명시적 재설계.
- **의존성**:
  - A-1 `ui/fonts.py` (F_BODY/F_HEAD/ensure_fonts_loaded) — *없으면 ImportError*. 도입 필수.
  - A-3 `ui/section_viewer.py` (거실 단면) — 거실 단면 토글 사용 시 필요. *try/except 폴백* 가능 여부 확인 — 안 되면 A-3 도입 필수.
- **위험**: 중간. *fonts·section_viewer 동시 도입 안 하면 깨짐*. A 카테고리 결정대로 *A-1·A-3 함께 도입* 필요.

---
