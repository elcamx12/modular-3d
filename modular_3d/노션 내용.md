2. 프로그램(UI 및 기능) 수정 필요
    1. 디자인 수정 필요 → 기능을 구현해 두고 디자인md로 디자인하거나 직접 이미지로 디자인을 만들고(ppt만들듯이) 각 버튼 위치에 실제 작동하는 버튼을 숨겨서 구현할 수 있음
    2. 기능 추가 필요(프로그램개요md를 기반으로 내용 추가 예정)
        1. 디자인탭 2개로 분리 → 모듈 패널 정의하는 탭과 배치해서 설계하는 탭 ✅ 완료
            ◆ 완료한 작업
            1. 정의 라이브러리 저장/불러오기 ✅ 완료 (파일 json 영구 저장, 단일 층 1세트로 저장)
                - 건물 파일(scene.json, 접합 변경 포함·같은 양식)을 정의 탭 작업공간으로 직접 불러오기 ✅ (프로그램은 불러오기만; 이후 사용자가 직접 안 쓸 부재를 지우고 남긴 한 부재를 정의로 재저장하는 용도)
            2. 정의 탭에서 부재 편집(치수·배치·이동 등) ✅ 완료
            3. 배치 탭 ✅ 완료
                - 실 구성(폴리곤+용도) ✅
                - 개구부(벽·슬래브 실제 관통, 고스트 배치/이동/복사/삭제/되돌리기) ✅
                - 모듈 4면 비내력 벽(하중·물량 0) ✅
                - 좌측패널에서 정의 가져오기(그룹 고스트+R/V/클릭, 층 자동 복제) ✅
                - 내벽(세대 내부 칸막이) 직접 배치·이동·복사·회전·삭제 ✅ (부모=모듈·바닥패널·캔틸레버슬래브 종속, 두께 100·높이 자동, 다층 자동 복제, 개구부 적용 가능, 반투명 표현, 하중·물량 0; "실 및 벽 배치 모드"의 "벽 배치(내벽)" 버튼, 정의 탭 저장 지원)
            4. 배치 탭에서 하중 정의 ✅ 완료 (KDS 41 12 00)
                - 실 용도별 활하중 + 마감 등 부가 고정하중(SDL) 정의 ✅
                - 슬래브별 실 폴리곤 면적가중 평균(빈 영역 주거 2.0) ✅
                - 옥상 자동검출(최상단 바닥패널=지붕 활하중 1.0·SDL 2.5, 코어슬래브 제외) ✅
                - 벽 자중(모듈 4면벽·구조벽 채움→변 보 선하중, 외부/내부 55·30 자동판별; 내벽 30kg/m²→부모 슬래브 면적평균, 개구부 차감) ✅
                - 실 속성 패널에서 활/부가하중 용도 기본값+사용자 수정 ✅
                - (모두 노드 추가 없이 둘레 보 등가하중으로 처리)
            5. 보를 각형강관·H형강 중 선택 ✅ 완료
                - 부재 단위 보 단면 타입(부재 속성 패널 + 배치 시 치수 패널 드롭다운) ✅
                - 종속(캔틸레버·중간보)·합체 구조벽은 부모/패널 타입을 따라감 ✅
                - 3D는 H형강이면 I자 단면으로 표시(강축 수직) ✅
                - 구조해석: H형강 단면 강성(강축 Iz=Ix)·강축 휨 강도검토 반영 ✅
                - 자동설계: 보 그룹을 강종별로 분리해 각자 카탈로그(SHS/H)에서 단면 자동 선정 ✅
                - 물량·자재비: 강종별 중량·단가 분리 집계(각형강관/H형강 단가 다름) ✅
                - 기둥은 항상 각형강관 ✅

            ◆ 안 한 작업 (향후)
            6. 대표 표준모델 프리셋(LH 평형별) → 미구현 (자료 받으면 내장 예정)
        2. 접합설계 탭 
            1. 사용자가 접합부를 변경하는 로직(기존 접합부 제거 및 변경, 신규 접합부 추가) 및 저장 가능 + 한 층 편집 시 같은 위치의 다른 층에도 자동 적용 ✅ 완료
                - 기존 접합 제거 / 핀↔강접 변경(3D 픽킹 → 제거/핀/강접) ✅
                - 같은 위치에 겹친 다른 종류 수직접합도 종류별 구분 적용(범례 켜고 끄기 연동) ✅
                - 신규 접합 추가: 부재(보) 모서리 위 임의 점 두 개 클릭, 핀/강접 선택 ✅
                  · 점 선택 시 미리보기 점·연결선·스냅 후보 표시, 점 자리에 절점 없으면 보를 분할해 새 절점 생성 ✅
                  · 스냅 종류 일치 — 기존 꼭지점(초록)/보 위 선점(주황) 구분이 실제 연결 방식에 그대로 반영 ✅
                - 직각(ㄴ자) 결합 추가: 평면·높이 모두 다른 두 점을 중간 가교 절점 거쳐 수직+수평 두 단계로 결합(R03 방식) ✅
                - 다층 일괄 / 이 층만 — 같은 평면 위치(xy) 매칭으로 모든 층 자동 적용("위로 쭉"), "이 층만" 옵션으로 그 층만 적용 ✅
                - 추가·직각 접합 개별 편집/제거(직각은 위-중간-아래 묶음으로 선택·제거, 허공 가교 절점도 함께 정리) ✅
                - 자동 생성 직각접합(R03)도 동일하게 선택·변경·제거 가능 ✅
                - "모든 접합 변경 초기화" 버튼(자동 규칙 상태로 전체 리셋) ✅
                - 제거로 구조가 불안정(메커니즘)해질 수 있으면 즉시 경고(차단은 안 함) ✅
                - 저장: 접합부 설계 탭의 "디자인 + 접합 변경 저장" 버튼(기존 건물 저장과 같은 양식, 접합 변경 포함; 불러오기는 배치 설계 탭) ✅
                - 불러오기 정책 3케이스 — ①저장본 사용/새 자동계산 ②디자인 변경 시 자동 리셋 ③디자인 그대로 재진입 시 유지/초기화 ✅
                - 실제 구조해석(5개 하중조합)·물량까지 자동 반영(자동 규칙보다 사용자 변경 우선) ✅
            2. 보 합성거동 옵션 (사용자가 직접 묶기 + 강재 단면 크기를 최대한 같게 유지하도록 프로그램이 자동으로 합성하는 옵션) → 미구현(향후)
        3. 구조해석 
            1. 코어 단순화(코어 계산)
            2. 프로그램의 구조해석 결과와 실제 해석 결과 비교 
            3. 하중조합 추가 (자중 단독 1.4D, 전도방지 0.9D 조합, 설하중 동시 등 KDS 추가 조합)
        4. 물량
            1. 물량 산출 방법 정합성 파악
        5. 운송
            1. 벽체 패널은 세워서도 운송할 수 있음.
            2. 3d렌더링 미구현(이건 2d 이미지를 없애고 3d렌더링을 구현하는게 좋아보임)
            3. 회차 자동 최적화: 사용할 트럭 종류만 정하면 프로그램이 회차를 자동 배분 + 한 줄에 모아 싣는 방식으로 로직 개선
            4. 현재 회차 배분(최적 운송) 로직 점검·보완 필요
            5. 도로 등급 입력창 정리(현재 미사용)
        6. 평가
        7. 비교 → 각 디자인별 결과 비교
        8. 도움말 추가해야함