# 작업 현황 (마스터 체크리스트)

> **세션 압축·재개 시 이 문서부터 읽으면 다음 작업을 바로 파악할 수 있다.**
> 리팩토링·버그수정 진행 상황의 단일 출처. 새 작업 끝낼 때마다 여기 갱신.

## 핵심 참조 문서 (3개)
- **`클로드 통합 분석.md`** — 전체 버그·제품·렌더·UX 피드백 카탈로그(심층 L1~L6 + 코드감사 A~T, 약 196항목). "무엇을 고칠 수 있나"의 원천.
- **`탭 기능·데이터흐름 지도.md`** — 10개 탭 데이터흐름 + 단계별 *적용/보류 상세 이력*(근거 포함). "어떻게·왜"의 기록.
- **`작업 현황.md`(이 문서)** — 완료/보류/남은 일 *요약*. 빠른 현황 파악용.

## 작업 원칙 (사용자 합의)
- 버그 안 나는 순서로(테스트→파이프라인→렌더→도메인). 각 변경 전 **자연어 설명 후 사용자 승인**, 변경 후 **사용자 GUI 확인**. 대화형.
- 적용 전 항상 **"정말 필요한지·위험은 없는지" 탐색** 먼저. (지금까지 통합 분석 진단이 과장된 경우 많아, 실측 후 보류한 항목 다수.)
- venv 파이썬: `my_project/venv/Scripts/python.exe`. 변경마다 컴파일 점검, 가능하면 스타터 테스트(`modular_3d/analysis/tests/test_golden_starter.py`, 8개).
- 안전망: `my_project/_snapshots/2026-06-01_..._백업_클로드수정전` (수정 전 전체 백업).

---

## ✅ 완료
- **0단계** 미사용 파일 정리 · 3D 라이브러리 로컬 동봉(오프라인화) · 숨은 경고 화면 표시
- **1단계** 스타터 골든 테스트 8개 (`analysis/tests/test_golden_starter.py`)
- **2단계** 통합 로깅 + stdout tee + 주요 오류 log_error 이관 (`_utils/debug.py`)
- **3단계** 공유 해석모델(am) 제공자 — 해석·단면·접합뷰·공정 4곳이 토폴로지 1회 빌드 공유 (`analysis/model_provider.py`)
- **4① 렌더 통합** — 운송/물량 3D 직렬화 공통화(`transport/loaded_3d_three.py`) · three.js 부트스트랩 우연한 차이 통일(배경색·픽셀배율·죽은 그림자 제거)
- **4② WYSIWYG** — 단면설계 결과를 3D 솔리드에 반영(`render/section_apply.py`, 렌더 전용·해석 불변)
- **5단계 (L4-1)** 층간변위비(drift) 검토 — `analysis/drift_check.py`, 단면설계 상태 한 줄 요약. 지진 0.015·풍 1/400. **+ 코어 슬래브 발산(mechanism) 중대 버그 수정**(코어 벽 상단을 슬래브 평면 h+gap 까지 연장, `topology.py:585`)
- **L6 우측 패널 폭(내용 맞춤 최소폭)** — 단면설계 fit · 물량 fit(고정폭) · 접합부 범례를 저장 버튼 아래로 이동 · **구조해석 부재력 트리를 탭 묶음(_tabs) 밖으로 완전 분리**(부재명 컬럼 늘어남 제거 + 물량 fit이 구조해석 폭을 덮어쓰던 버그 수정) · 운송 폭(다른 세션에서 처리)
- **운송 탭 정리 (2026-06-02)** — ① 벽 단위중량 입력(내부/외부) 묶음 제거(프로젝트 설정이 단일 출처, _read_options 가 _proj_settings 에서 직접 읽음·기본값 30/55) ② 회차표 탭 묶음(단일 탭) → 그룹박스로 교체해 표 직접 표시. py_compile OK.
- **프로그램 이름 변경 (2026-06-02)** — 창 제목 '모듈러 부재 스터디' → '모듈러 설계 프로그램' (main_3d.py:107).
- **배치설계 벽/실 표시 토글 (2026-06-02)** — 단면 슬라이더 아래에 체크박스 '벽'·'실' 추가. ① '벽'=채움면(벽패널 wall_fill·내벽·모듈 외피 벽) 토글 → mesh_builder 전역 _SHOW_WALL_FILL 스위치(set_wall_fill_visible)로 메인 빌드 3곳(build_module_mesh·build_struct_wall_mesh·build_interior_wall_mesh)이 채움 생략, 컨트롤러가 전체 부재 재빌드. 프레임·런너·슬래브·코어벽은 유지. hover용 build_component_class_meshes 는 미적용(배치 전용). ② '실'=Room 반투명 색면 토글(set_rooms_visible: 켜면 _render_all_rooms_3d, 끄면 _clear_rooms_3d). _render_room_3d 에 _rooms_visible 가드로 OFF 중 신규 실도 숨김. py_compile OK.
- **배치설계 토글 후속 (2026-06-02)** — ① 벽/실 체크박스 2개 → '벽·실 표시' 단일 체크박스로 통합(_on_wall_room_toggled 가 set_wall_fill_visible+set_rooms_visible 동시 호출). ② 실 표기 z 높이 보정: ROOM_PLANE_Z_MM 250→105(슬래브 상면 z=100 위 5mm). 기존엔 150mm 떠 있었음(슬래브 상면=half_s=100, SECTION_W=200). 다층은 floor_index×FLOOR_HEIGHT 가산이라 전 층 일관.
- **구조해석/단면설계 성능 — 3순위 적용 (2026-06-02)** — controls_f6 _run_structural_analysis 가 solve_all_cases(self._scene) 호출 시 prebuilt_am 미전달 → 토폴로지 중복 빌드. get_analysis_model 로 받은 am 을 prebuilt_am=am 으로 전달해 1회 절감. (무위험·즉시)
- **구조해석/단면설계 성능 — 1순위 적용 (2026-06-02)** — solve_all_cases 가 6 성분(D/L/Ex/Ey/Wx/Wy)마다 build_ops_model 재호출(ops.wipe+전체 재등록)하던 것을 **모델 1빌드 + 케이스 사이 _reset_domain_between_cases() 로 하중·해석 상태만 비우기**로 재구조화(ops_solver.py). 빌드 6→1. 리셋=wipeAnalysis + remove loadPattern/timeSeries(tag 1·2) + reset + setTime(0). [함정] 케이스 사이 loadPattern 미제거 시 하중 누적으로 결과 오염. **2순위(부산물) 자동 해결**: 단면 수렴(converge_sections)이 같은 solve_all_cases 를 반복 호출하므로 반복마다 6배가 1배로. **회귀 검증**: analysis/tests/regression_solve.py 신설(save/check 모드). scene.json 기준값(종설/regression_baseline.json) 대조 → 최대오차 0.000e+00 PASS(구 코드 결과와 비트동일). **남은 병목(미적용)**: ③ 수렴 중 spec.validate 첫 빌드만, 코어 격자 단위(CORE_TRUSS_GRID_MM) 확대 — 빌드 1회 자체를 더 가볍게.
- **성능 — 코어 분포 단면력 단순화 (2026-06-02)** — perf_probe.py 측정 결과 구조해석 1회 ≈1.47초 중 솔브+추출 52%·조합합성 25%·하중 11%·빌드 11%(빌드는 이미 1순위로 6→1). 코어 부재(role 'core_*': 기둥·런너·트러스·슬래브보)는 트러스성(축력 일정·휨 미미)이라 분포 단면력 7점 계산이 무의미 → _extract_member_stations 에서 코어는 양단 2점([f_i,f_j], (2,6))만 넣어 직선 그래프로 표시(member_info_popup 이 2점 직선 AFD/SFD/BMD 렌더). 설계에 쓰는 양단 단면력·해석은 불변(회귀 PASS 0.000). 효과: 솔브6 769→652ms, 구조해석 1회 1470→1344ms(~9%↓), 단면설계 수렴 3314→2956ms(~11%↓). **계측 도구**: analysis/tests/perf_probe.py(단계별 ms), regression_solve.py(결과 동일성). **남은 본질 병목**: 솔브(행렬분해, 같은 K 6회) + 조합합성 — 줄이기 어렵거나 위험.
- **뷰 다듬기 (2026-06-02)** — ① 코어 색 OPS_COLOR_CORE 짙은보라(0.45,0.10,0.55,1.0)→연한 라벤더(0.68,0.52,0.82,0.7), 코어 선 두께 3.0→1.5(viewer.py: _ops_core_lines 2곳 + _deformed_core_lines). 구조해석·단면설계·접합부·변형 뷰 공통 반영. ② 단면설계에서만 다이어프램 중심 노드(master) 마커 숨김 — show_member_ratio_bands 가 노드 마커를 master(=각 diaphragm.master_node) 제외하고 재그림(구조해석 _draw_from_spec 은 master size 3.0 유지).
- **18층 스케일 측정 (2026-06-02, scene7.json 부재 339개)** — perf_probe.py 에 접합부(record_joints) 추가. 실측(사용자): 접합부 5초·구조해석 26초·단면설계 79초. 스크립트 측정: 토폴로지빌드 2.1s(3층 0.1→21배 초선형)·ops빌드 3.5s·**하중계산 4.0s(24배 초선형 의심)**·솔브6 10s·구조해석1회 19.5s·단면설계 80s(수렴 4회). **새 병목 후보(큰 모델)**: calculate_loads·build_analysis_model 의 초선형(O(N²) 의심) + 수렴 중 ops 빌드(spec.validate). 단면설계가 전체의 ~80%.
- **성능 — 하중계산 O(N²) 제거 (2026-06-02)** — 프로파일(prof_loads.py)로 진짜 병목 확정: 하중계산 4초의 대부분이 wall_classifier(비내력벽 내/외부 자동판별) — comp·면마다 scene 전체 obstacle 재수집 + get_world_corners 500만 회. ① _comp_slab_pressures/_interior_wall_sdl_on_slab: 실·내벽 색인화(rooms_by_floor, iw_by_parent) — 효과 작음(주범 아님). ② **핵심: 벽 분류는 xy 노출 패턴만 의존·층(z) 무관 → (comp_type,rotation,xy)를 키로 1회 분류 후 다층 복제에 복사**(_apply_wall_loads 캐시, wall_classifier 미변경). 효과: 하중계산 3.9s→0.28s(14배), 구조해석 19→15.5s, 단면설계 79→64s. 3층 회귀 PASS 0.000(결과 동일). **남은 병목**: 솔브6 ~10s(구조해석의 65%, 같은 K 6회·본질), ops빌드 3.3s, 토폴로지빌드 2s.

## ⏸ 조사 후 보류 (근거는 `탭 기능·데이터흐름 지도.md`)
- design_result id() 지문 → 내용해시 (이벤트 무효화가 이미 커버, 위험 큼)
- 운송 scene_fp 에 joint_overrides 추가 (운송은 접합 미사용 + 공유 am 제공자와 충돌)
- **4③ 면 정합 (L1-2)** — 고정 인셋(200)으로 크기 다른 모듈 면 어긋남. 수정 위험 최고(모든 부재 좌표 원천). 표준치수 위주면 증상 안 보임 → 보류
- 운송/물량 H형강 두께 8/12 고정 (시각 미세 + 데이터 경로 김)
- 반강접 접합 (L4-3) — 사용자 제외
- ⓒ 부재 형상 단일화 (L2-1) — vispy/three 렌더 API 근본 차이로 통합 부적절(진짜 중복은 H형강뿐)
- L6 배치/접합 탭 폭 — 컨트롤 모음이라 320 고정 문제없음(사용자 결정)

## ☆ 발견(미해결, 후속 후보)
- **접합 변경 후 단면결과 자동 미폐기** — 접합만 바꾸고 단면설계 탭을 건너뛰면 물량·운송이 옛 단면 사용. 단, 탭 순차 진행(접합→단면설계→물량)하면 단면설계 탭에서 자동 재수렴되어 해소됨. 비정상 경로에서만 stale.

## ⬜ 남은 작업 후보 (다음에 할 수 있는 것)
- **L4-2** 인양·운송 중 구조검토 — 운송 도구인데 인양·운송 중 구조 안전 검토 전무 (도메인 추가)
- **L4-4** 단계시공 해석 — 코어 선시공/모듈 후시공 단계별 응력 미검토 (우선순위 낮음)
- **L6-2/L6-3** 인라인 setStyleSheet 99곳 산재 → 중앙 테마(QSS) / Qt 사이징 수동 흔적 정리
- **7 동시성** — 전역 OpenSees 도메인. 현재 단일 스레드라 미발생, 우선순위 낮음
- **Part II 코드감사 A~T** 개별 항목 — `클로드 통합 분석.md` 참조(대부분 경미/정보, 위 Top 외)
- (사용자 제외) L5-1 대칭/배열 복제·정렬 도구

## ▶ 다음 추천
렌더·데이터·UI 폭 계열은 일단락. **남은 큰 것은 도메인(L4-2 인양/운송 구조검토)**이거나 **UI 일관성(L6-2 테마)**. 둘 다 "정말 필요한지 탐색 → 설명 → 승인" 순으로 진행. 또는 발견해 둔 "접합→단면 미폐기"를 작은 안전 수정으로 마감하는 것도 가능.
