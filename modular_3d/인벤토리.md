# 인벤토리 (자동 생성)

생성일: 2026-05-27  |  대상 파일 수: 135


이 파일은 `dev_tools/refactor_tools/build_inventory.py` 가 자동 생성합니다. 직접 편집 금지.


## 파일 인벤토리

### `modular_3d/__init__.py` (0 줄)

### `modular_3d/__main__.py` (22 줄)
> python -m modular_3d 진입점.


### `modular_3d/_bootstrap.py` (276 줄)
> 실행 환경 부트스트랩 — 모든 진입점이 공유.

**최상위 함수**
- `_log()` (L34) — 진단 메시지 즉시 flush.
- `_ensure_venv()` (L43)
- `_list_subst_mappings()` (L60) — 현재 시스템의 subst 매핑 dict {'Q:': 'C:\...', ...}.
- `_cleanup_zombie_substs()` (L80) — env_root 또는 그 부모를 가리키는 우리 좀비 매핑 해제.
- `_find_free_drive_letter()` (L98)
- `_subst_mount()` (L105) — target_dir → 가상드라이브 매핑. 성공 시 'Q:' 같은 문자열.
- `_subst_unmount()` (L128)
- `_ascii_rewrite_sys_path()` (L142) — sys.path 중 env_root 로 시작하는 항목을 가상드라이브 경로로 교체.
- `_detect_env_root()` (L175) — 현재 파이썬 환경의 루트 (venv 또는 conda env, 또는 _MEIPASS) 반환.
- `_setup_webengine_env()` (L186) — WebEngine 환경변수 설정. drive 가 있으면 가상드라이브 경로 사용.
- `_preimport_webengine()` (L227)
- `bootstrap()` (L238) — 모든 진입점이 가장 먼저 호출. 멱등.


### `modular_3d/_utils/__init__.py` (6 줄)
> 중립 유틸 패키지 — ui/render/analysis 어디에도 의존하지 않는 도구.


### `modular_3d/_utils/debug.py` (117 줄)
> 카테고리별 진단 로그 토글.

**최상위 함수**
- `_load_env_defaults()` (L40) — 환경변수에서 초기 OFF 카테고리 로드.
- `dprint()` (L53) — 카테고리가 활성 상태일 때만 print 호출.
- `disable()` (L64) — 특정 카테고리 비활성화.
- `enable()` (L69) — 특정 카테고리 활성화 (기본 상태로 복원).
- `disable_all()` (L74) — 알려진 모든 카테고리 비활성화. 알려지지 않은 카테고리는 영향 없음.
- `enable_all()` (L83) — 모든 카테고리 활성화 (비활성 집합 비우기).
- `is_enabled()` (L88)
- `silenced()` (L93) — context manager — 지정 카테고리(없으면 모두) 를 블록 안 OFF.


### `modular_3d/_utils/geometry.py` (17 줄)
> 기하 유틸 — 좌표 박스·범위 계산 등 ui/render 공용 함수.

**최상위 함수**
- `xy_bbox()` (L10) — 부재의 XY 바운딩박스 (x0, y0, x1, y1).


### `modular_3d/analysis/__init__.py` (11 줄)
> 구조해석 패키지 — OpenSeesPy 기반 (Phase 5 이후).


### `modular_3d/analysis/_utils.py` (12 줄)
> analysis 패키지 공용 유틸리티.

**최상위 함수**
- `round_key()` (L8) — 좌표를 1 mm 단위 정수 키로 반올림 — 노드 병합/접합부 매칭용.


### `modular_3d/analysis/constants.py` (43 줄)
> 구조해석 상수 — re-export shim (2026-05-12).


### `modular_3d/analysis/joint_rules.py` (2581 줄)
> 케이스별 명시 접합 규칙 (2026-05-13 신설).

**모듈 상수**
- `_Z_TOL` (L39) = `5.0`
- `_PERP_TOL` (L41) = `5.0`
- `_GAP_MAX` (L45) = `225.0`
- `_EDGE_PERP_MAX` (L50) = `_GAP_MAX`
- `_VSTACK_MAX` (L53) = `225.0`
- `RULE_ID_MOD_MOD_H` (L446) = `'R01_mod_mod_h'`
- `RULE_ID_MOD_MOD_V` (L792) = `'R02_mod_mod_v'`
- `RULE_ID_PANEL_MOD` (L817) = `'R03_panel_mod'`
- `_PANEL_LIKE_ROLES` (L821) = `{'floor_edge_beam', 'cantilever_slab_beam'}`
- `RULE_ID_PANEL_PANEL` (L1199) = `'R04_panel_panel'`
- `RULE_ID_WALL_FLOOR_V` (L1350) = `'R05_wall_floor_v'`
- `RULE_ID_WALL_MOD` (L1426) = `'R06_wall_mod'`
- `RULE_ID_WALL_CANT` (L1607) = `'R08_wall_cant'`
- `RULE_ID_WALL_WALL_H` (L1735) = `'R07_wall_wall_h'`
- `RULE_ID_CORE` (L1783) = `'R09_core'`
- `_CORE_NUMERIC_MARGIN_MM` (L1790) = `5.0`
- `_CORE_LATERAL_MAX` (L1791) = `CORE_WALL_DEFAULT_THICKNESS_MM / 2.0 + SECTION_W_MM / 2.0 + MODULE_JOINT_GAP_...`
- `_CORE_ALIGN_TOL` (L1797) = `CORE_WALL_DEFAULT_THICKNESS_MM / 2.0 - SECTION_W_MM / 2.0 + _CORE_NUMERIC_MAR...`
- `_CORE_LINE_ROLES_AXIS` (L1804) = `('core_column', 'core_top_runner', 'core_bottom_runner', 'core_truss_v', 'cor...`
- `RULE_ID_USER_ADD` (L2078) = `'USER_ADD'`
- `RULE_ID_USER_PIN` (L2079) = `'R10_user_pin'`
- `RULE_ID_USER_RIGID` (L2080) = `'R11_user_rigid'`

**최상위 함수**
- `_collect_module_nodes()` (L58) — 모듈 컴포넌트(MODULE, sub_index=0) 별 노드 ID 집합 반환.
- `_module_corners()` (L83) — 모듈의 꼭지점 노드 ID 추출 — z 레벨별 4 코너.
- `_horizontal_edges()` (L123) — 수평 보 member → [(mid, nA, nB), ...].
- `_module_vertical_edges()` (L159) — 수직모듈의 기둥 분절 member → [(mid, nA, nB), ...].
- `_node_xy()` (L210) — 노드의 평면 좌표(x, y). om.node_tags(tag→coord)에서 직접 조회.
- `_resolve_override_dofs()` (L219) — 접합 오버라이드를 반영한 최종 자유도 묶음.
- `_register_pin_link()` (L251) — equalDOF 핀 결합 등록 (OpenSees + spec 동시).
- `_apply_corner_edge_rule()` (L308) — 코너-코너(a) + 코너-모서리(b) 매칭 코어. R01(모듈)·R04(패널) 공유.
- `apply_module_module_horizontal()` (L449) — R01 — 모듈 ↔ 모듈 수평 인접 접합. _apply_corner_edge_rule 코어 사용.
- `_split_edges_and_link_corners()` (L494) — (b) 보조 — 모서리 보를 사영점에서 분할하고 코너 노드와 결합.
- `_apply_vstack_rule()` (L725) — 수직 적층 코너-코너 매칭 코어. R02(모듈)·R04수직(패널) 공유.
- `apply_module_module_vertical()` (L795) — R02 — 모듈 ↔ 모듈 수직 적층 접합. _apply_vstack_rule 코어 사용.
- `_collect_cantilever_beam_data()` (L824) — 캔틸레버 보 → ({cid: [자유단nid]}, {cid: [(mid, n1, n2)]}).
- `_collect_panel_like_corners()` (L876) — 패널류 컴포넌트별 코너 노드 ID → {cid: [nid, ...]}.
- `apply_panel_module()` (L927) — R03 — 바닥패널 ↔ 모듈 천장 접합. 패널이 모듈에 의존하는 단방향.
- `apply_panel_panel_horizontal()` (L1202) — R04 — 패널류 ↔ 패널류 수평 접합. 모듈-모듈(R01) 과 동일 로직.
- `_collect_wall_data()` (L1238) — 벽 컴포넌트별 노드/종속여부 → {cid: {'top':[nid],'bot':[nid],
- `_wall_vertical_edges()` (L1296) — 벽 기둥(wall_column) 분절 → [(mid, nA, nB), ...].
- `_collect_floor_panel_corners()` (L1325) — 바닥패널만의 코너 → {cid: [4 nid]}.
- `apply_wall_floor_vertical()` (L1353) — R05 — 벽패널 코너 ↔ 바닥패널 코너 수직 핀 접합.
- `apply_wall_module()` (L1429) — R06 — 벽 상단 코너 ↔ 모듈(천장보·기둥) + 캔틸 보 자유단. 같은-z 만.
- `apply_wall_cantilever_beam()` (L1610) — R08 — 벽 상단 코너 ↔ 캔틸 보 자유단/모서리 (같은 z 만).
- `apply_wall_wall_horizontal()` (L1738) — R07 — 벽 코너 ↔ 다른 벽 코너. 코너-코너만(노드끼리).
- `_collect_core_data()` (L1811) — 코어 컴포넌트의 노드 + 축평행 선 → (core_nids, core_lines).
- `apply_core_joint()` (L1851) — R09 — 외부 부재 노드 ↔ 코어벽·코어슬래브 (노드 + 축평행 선) 6 DOF 강결합.
- `apply_all_joint_rules()` (L2035) — 모든 케이스별 접합 룰을 순차 적용.
- `_seg_seg_closest()` (L2083) — 두 선분 [p1,p2],[p3,p4]의 최근접점 쌍(Q12, Q34)과 거리.
- `candidate_joint_points()` (L2118) — 첫 점 p 에서 접합 가능한 다른 부재 위 점 목록 — 접합 추가 두번째 점 후보.
- `_user_point_seg()` (L2195) — 점 p 와 선분 a-b: (투영점, 거리, 매개변수 t∈[0,1]) 반환.
- `_split_member_at_point()` (L2206) — point 에 가장 가까운 보 element 를 그 점에서 분할하고 새 노드 tag 반환.
- `_can_anchor()` (L2331) — (x,y,z) 에 접합 끝점을 만들 수 있는가 — 노드 스냅 또는 보 분할 가능 여부만
- `_anchor_node_at()` (L2363) — (x,y,z) 위치의 접합 끝점 노드 — 가까운 기존 노드가 있으면 스냅,
- `_anchor_zs()` (L2382) — 평면 위치 (x,y) 에 존재하는 z 집합 — 그 위치의 노드 z + 그 xy 를 지나는
- `apply_added_joints()` (L2410) — 사용자 신규 접합(kind='add')을 등록 — 모서리 위 점도 지원.
- `remove_dangling_bridge_nodes()` (L2536) — 접합이 모두 제거되어 허공에 남은 가교 중간노드(N1, panel_z_route)를 정리.


### `modular_3d/analysis/load_calculator.py` (906 줄)
> 하중 계산 — 자중 + 슬래브 사하중 + 활하중을 부재별 등분포로 변환.

**모듈 상수**
- `_KN_M2_TO_N_MM2` (L352) = `0.001`
- `_INTERIOR_WALL_UNIT_KG_M2` (L393) = `30.0`
- `_EXTERIOR_WALL_UNIT_KG_M2` (L394) = `55.0`
- `_WALLFILL_FACE_NAME` (L797) = `{0: '전면', 1: '후면', 2: '좌면', 3: '우면'}`

**최상위 함수**
- `_section_area()` (L135) — 부재 단면적 mm².
- `_apply_self_weight()` (L147) — 모든 frame 부재에 자중 적용.
- `_classify_4_beams_by_direction()` (L177) — 4 개의 둘레 보를 길이별로 두 그룹으로 분류.
- `_distribute_slab_pressure()` (L199) — 슬래브 압력(N/mm²) 을 둘레 보에 등가 평균 UDL 로 분배.
- `_build_beam_shares()` (L232) — 분포 프로파일 peak(중앙 최대) 기준 BeamShare 딕셔너리 생성.
- `_collect_rect_corners()` (L269) — 4 개 둘레 보의 노드 집합에서 4 코너(unique 좌표) 추출 후
- `_find_beam_between_nodes()` (L291) — anchor_nids 두 노드를 양 끝으로 갖는 보 부재를 찾는다 (exclude 제외).
- `_accumulate()` (L333)
- `_slab_pressures()` (L342) — (슬래브 자중 압력, 활하중 압력)  N/mm². 수직3층모듈 등 기본 압력용.
- `_slab_top_z()` (L355) — 부재 슬래브 상면 월드 z (mm). 슬래브 없으면 None.
- `_compute_roof_top_z()` (L371) — 옥상(지붕) 레벨 = 최상단 바닥패널 슬래브 상면 z. 없으면 None.
- `_wall_face_openings()` (L397) — comp.openings 중 해당 face 의 개구부만(레거시 face 없으면 'wall' 간주).
- `_wall_panel_net_area_mm2()` (L406) — 벽 채움 판 순면적 mm² = (길이 u × 높이 v) − 개구부 면적 합. 음수면 0.
- `_wall_panel_weight_n()` (L418) — 벽 무게 N = 순면적(m²) × 단위질량(kg/m²) × 중력가속도.
- `_interior_wall_sdl_on_slab()` (L424) — comp(슬래브 부재) 위에 종속된 내벽들의 무게를 면적평균 사하중(N/mm²)으로.
- `_comp_slab_pressures()` (L445) — 부재 슬래브의 (사하중 압력, 활하중 압력) N/mm² — 실 면적가중 + 옥상 + 내벽.
- `_distribute_vertical3_slabs()` (L475) — 수직 3층 모듈의 3 슬래브 (1F·2F·3F 바닥) 를 z 레벨별 4 둘레 보에 분배.
- `_apply_slab_loads()` (L561) — 슬래브 하중을 둘레 보에 분배.
- `_apply_core_slab_self_weight()` (L737) — CoreSlab 자중을 같은 그룹의 코어 column 부재들에 균등 분배.
- `_nearest_beam_to_xy()` (L800) — 주어진 보들 중 중점 xy 가 xy 에 가장 가까운 보 id. 없으면 None.
- `_apply_wall_loads()` (L813) — 모듈 4면 벽 + 구조벽 채움 자중을 변의 보에 선하중으로 추가.
- `calculate_loads()` (L897) — Scene + AnalysisModel → 부재별 LoadCase.

**클래스**
- `class LoadCase` (L39) — 한 부재가 받는 등분포 하중 (모두 N/mm, 양수, 중력 방향).
  - `total_dead()` (L51)
  - `factored()` (L55) — 1.2 D + 1.6 L  (양수, N/mm).
- `class BeamShare` (L61) — 슬래브에서 한 둘레 보로 분배된 하중의 패턴 + peak 값.
- `class SlabDistribution` (L78) — 한 슬래브의 분배 메타 — 하중 흐름 시각화 Stage 1 용.
- `class LoadResult` (L98)
  - `total_factored_n()` (L110)
  - `get()` (L114)
  - `summary()` (L119)


### `modular_3d/analysis/model_spec.py` (516 줄)
> 구조해석 모델 단일 명세 (2026-05-12 명세통합 Phase 2).

**클래스**
- `class Case(Enum)` (L50) — 해석 케이스. 토폴로지는 5 케이스 공유, 하중만 케이스별 갈아끼움.
- `class NodeRec` (L63) — 노드 — OpenSees `node(tag, x, y, z)` 와 1:1.
- `class BeamRec` (L76) — 보-기둥 요소 — OpenSees `element('elasticBeamColumn', ...)` 와 1:1.
- `class ShellRec` (L93) — 면 요소 — OpenSees `element('ShellMITC4', ...)` 와 1:1.
- `class EqualDofRec` (L109) — 자유도 묶음 — OpenSees `equalDOF(master, slave, *dofs)` 와 1:1.
- `class RigidLinkRec` (L127) — 강체 링크 — OpenSees `rigidLink('beam', master, slave)` 와 1:1.
- `class DiaphragmRec` (L139) — 강체 다이어프램 — OpenSees `rigidDiaphragm(3, master, *slaves)` 와 1:1.
- `class FixRec` (L167) — 고정점 — OpenSees `fix(tag, *dofs)` 와 1:1.
- `class ModelSpec` (L180) — 5 케이스 공유 토폴로지·결합·고정점 단일 명세.
  - `refresh_index()` (L212) — 노드 tag → NodeRec 역인덱스 재구축.
  - `node()` (L216) — 노드 tag 로 NodeRec 조회. 인덱스가 stale 하면 자동 재구축 — silent
  - `iter_nodes()` (L227)
  - `iter_beams()` (L230)
  - `iter_shells()` (L233)
  - `iter_equal_dofs()` (L236)
  - `iter_rigid_links()` (L239)
  - `iter_diaphragms()` (L242)
  - `iter_fixes()` (L245)
  - `validate()` (L250) — 명세 자체 점검.
  - `verify_against_opensees()` (L327) — OpenSees 가 실제 받은 노드·요소를 회수해 명세와 비교.
  - `summary()` (L428)
- `class DistributedLoadRec` (L447) — 부재 분포하중 — OpenSees `eleLoad('-ele', tag, '-type', 'beamUniform', wy, wz, wx)` 매핑
- `class NodalLoadRec` (L459) — 노드 집중하중 — OpenSees `load(tag, fx, fy, fz, mx, my, mz)` 매핑.
- `class LoadSpec` (L474) — 케이스별 하중 명세.
  - `summary()` (L491)
- `class ValidationResult` (L503) — ModelSpec.validate() 결과.
  - `__bool__()` (L512)
  - `__len__()` (L515)


### `modular_3d/analysis/ops_builder.py` (1174 줄)
> OpenSeesPy 모델 빌더.

**모듈 상수**
- `RC_WALL_E_MPA` (L40) = `CONCRETE_E_MPA`
- `RC_WALL_G_MPA` (L41) = `CONCRETE_E_MPA / 2.4`
- `_IFACE_VERT_DZ` (L52) = `JOINT_HORIZ_CONNECT_MAX_MM`
- `_IFACE_VERT_DXY` (L53) = `5.0`
- `_IFACE_HORIZ_DZ` (L54) = `5.0`
- `_IFACE_HORIZ_DIST` (L55) = `JOINT_HORIZ_CONNECT_MAX_MM`
- `_SAME_NODE_TOL` (L58) = `NODE_MERGE_TOL_MM`
- `_NOMINAL_H_SECTION` (L232) = `None`

**최상위 함수**
- `_is_cantilever_node()` (L165) — 노드가 캔틸레버 부재(`cantilever_beam`/`cantilever_slab_beam`)에 속하면 True.
- `_section_props()` (L181) — 부재 단면 (A, Iy, Iz, J) 반환.
- `_nominal_h_section()` (L235) — 공칭 H200×200 단면(없으면 카탈로그 최소). 해석 초기 강성용 — 1회 캐시.
- `_geom_transf_tag()` (L249) — OpenSees geomTransf 등록 + 태그 반환.
- `_vecxz_for_member()` (L272) — 부재 축에 수직이면서 'z 축에 가까운' 임의 벡터 반환 → vecxz.
- `_step_init_ops_domain()` (L286) — 1) OpenSees domain 초기화 (3D 6DOF). 모든 이전 ops 상태 삭제.
- `_step_register_nodes()` (L293) — 2) AnalysisNode.id 를 OpenSees nodeTag 로 사용해 노드 등록.
- `_get_shell_section_tag()` (L313) — 셸 두께 + E 배율별 ElasticMembranePlateSection 등록 + 태그 반환 (캐싱).
- `_step_register_members()` (L332) — 3) AnalysisMember 1:1 → OpenSees 요소 등록.
- `_step_fix_base_nodes()` (L493) — 5) 베이스 지점 — z_min 의 모든 기둥 base + 코어 셸 하단 노드 → 6DOF 고정.
- `_step_fix_first_floor_panel_nodes()` (L540) — [2026-05-11] 1층(z=base) 의 FloorPanel 코너 노드를 6DOF 고정.
- `build_ops_model()` (L581) — AnalysisModel → OpenSees 3D 프레임 모델 등록.
- `_snap_dependent_cantilever_to_module()` (L716) — 종속 캔틸레버 보/슬래브의 한 끝 노드 좌표를 부모 모듈의 가까운 노드 좌표로
- `_build_diaphragms_and_core()` (L812) — 컴포넌트별 rigidDiaphragm 등록 (2026-05-17 per-component 전환).
- `self_check()` (L1016) — 모델 빌드 후 자체 점검 — SelfCheckResult 반환.

**클래스**
- `class DiaphragmInfo` (L79) — 한 층의 강체 다이어프램 정보.
- `class OpsModel` (L87) — OpenSees에 등록된 모델의 시각화·검증용 메타데이터.
  - `summary()` (L148)
- `class SelfCheckResult` (L990) — self_check 의 구조화된 결과.
  - `__bool__()` (L1000)
  - `__iter__()` (L1003)
  - `__len__()` (L1006)
  - `__contains__()` (L1009)
  - `__getitem__()` (L1012)


### `modular_3d/analysis/ops_solver.py` (962 줄)
> OpenSeesPy 기반 정적 해석.

**모듈 상수**
- `_PENALTY_ALPHA` (L52) = `100000000000000.0`
- `_STATION_PER_SUB` (L253) = `7`
- `SDS_DEFAULT` (L560) = `0.5`
- `SD1_DEFAULT` (L561) = `0.2`
- `IE_DEFAULT` (L562) = `1.0`
- `R_DEFAULT` (L563) = `8.0`
- `G_M_S2` (L564) = `9.80665`
- `WIND_V0_M_S` (L567) = `35.0`
- `WIND_GF` (L568) = `0.85`
- `WIND_CF` (L569) = `1.3`

**최상위 함수**
- `_apply_penalty_constraints()` (L55) — OpenSees constraints handler 를 Penalty(α, α) 로 설정.
- `_extract_member_forces()` (L144) — 모든 부재 단면력 추출.
- `_compute_station_forces()` (L205) — Phase 5 — 양단 단면력 + 등분포 가정으로 station n 점 분포 계산.
- `_diagnose_zero_members()` (L256) — 사용자 보고: 0 단면력 부재가 많음.
- `_extract_member_stations()` (L287) — Phase 5 — 풀이 직후 모든 부재의 분포 단면력을 캐시.
- `_consolidate_member_loads()` (L333) — 부재별 (D_self, D_slab, L) [N/mm] 반환.
- `_apply_eleload_factored()` (L346) — 1.2D + 1.6L 계수하중을 부재 한 개에 eleLoad 로 적용.
- `solve_vertical()` (L409) — 수직(D+L = 1.2D + 1.6L) 정적 해석.
- `_compute_floor_seismic_weight()` (L572) — 각 층의 지진중량 W (D 만, 활하중 미포함) [N].
- `_seismic_base_shear()` (L614) — KDS 41 17 00 등가정적 베이스 전단 V [N].
- `_seismic_floor_distribution()` (L627) — 층별 횡력 Fx 분배 (KDS 41 17 00).
- `_wind_floor_distribution()` (L645) — KDS 41 12 00 단순화: 층별 풍하중 [N].
- `_apply_horizontal_loads()` (L676) — 층별 횡력을 다이어프램 master 노드에 절점력으로 적용.
- `_extract_lateral_results()` (L727) — 변위 + 베이스 반력 + 부재력 추출 + 층변위 산정.
- `solve_seismic()` (L840) — 등가정적 지진 해석 (Ex 또는 Ey).
- `solve_wind()` (L880) — 풍하중 해석 (Wx 또는 Wy).
- `solve_all_cases()` (L906) — 5 케이스(D+L, Ex, Ey, Wx, Wy) 자동 해석 — 케이스마다 ops 모델 재빌드.

**클래스**
- `class MemberForce` (L64) — 부재 양단 단면력 (OpenSees 로컬 좌표).
  - `N_max_abs()` (L78)
  - `V_max_abs()` (L82)
  - `M_max_abs()` (L87)
- `class OpsResults` (L93)
  - `summary()` (L114)


### `modular_3d/analysis/projection.py` (219 줄)
> 사영 매칭 공용 유틸리티 (2026-05-13 전역 사영 정책).

**최상위 함수**
- `project_to_line()` (L65) — 점을 두 끝점으로 정의된 직선에 직각 사영.
- `project_to_plane()` (L91) — 점을 평면에 직각 사영. 평면은 한 점 + 법선 벡터로 정의.
- `project_to_quad()` (L113) — 점을 4 노드 quad 평면에 사영 + bilinear 좌표 (u, v) 계산.
- `is_orthogonal_pair()` (L161) — 두 노드의 평면 페어가 직각 사영(한 축 정렬) 인지 검사.
- `_self_test()` (L180) — 단순 검증 — 모듈 단독 호출 시 실행.

**클래스**
- `class ProjectionResult` (L39) — 직선·평면 사영 결과.
- `class QuadProjection` (L48) — 4 노드 quad(셸) 사영 결과.


### `modular_3d/analysis/quantity_takeoff.py` (416 줄)
> 물량 집계 — 강재 본수·길이·중량 + 슬래브 부피.

**모듈 상수**
- `DEFAULT_REBAR_RATIO` (L200) = `0.012`
- `LENGTH_ROUND_MM` (L288) = `1.0`

**최상위 함수**
- `compute_material_cost()` (L108) — 물량 보고서 + 단가 → 자재비.
- `_polygon_area_xy()` (L157) — 4-각 폴리곤의 xy 평면 면적 (Shoelace).
- `_collect_slabs()` (L174) — scene 의 모든 SlabData 수집 (모듈 내부 + 바닥패널 + 캔틸레버 슬래브).
- `aggregate_slabs()` (L203) — 슬래브 부피 + 철근 추정 중량 (m³ 기준).
- `aggregate_core()` (L239) — RC 코어벽(솔리드 직육면체) + 코어 슬래브의 콘크리트 + 철근 물량.
- `aggregate_steel()` (L291) — 단면+길이 동일 부재를 한 줄로 묶어 본수표 생성.
- `build_quantity_report()` (L382) — 단일 정책에 대한 QuantityReport 생성.
- `build_all_reports()` (L402) — 1종/2종/3종 모든 정책의 QuantityReport 생성.

**클래스**
- `class SteelLineItem` (L27) — 강재 본수표 한 행.
- `class SlabQuantity` (L40) — 슬래브 부피 + 철근 추정 중량.
- `class CoreQuantity` (L51) — RC 코어 물량 — 코어벽(솔리드 직육면체) + 코어 슬래브 통합.
- `class QuantityReport` (L67) — 1정책에 대한 종합 물량 보고서. 정책별 사전이 메인 진입점.
- `class MaterialCost` (L81) — 자재비(재료비) 산출 결과 — 물량 × 단가.


### `modular_3d/analysis/room_loads.py` (176 줄)
> 실(Room) 용도별 하중 → 슬래브 적용을 위한 순수 기하 유틸 (5단계).

**최상위 함수**
- `polygon_area()` (L27) — 단순 폴리곤 면적(절댓값, shoelace). 점 3개 미만이면 0.
- `clip_polygon_to_rect()` (L40) — 축정렬 사각형 [x0,x1]×[y0,y1] 으로 폴리곤 클리핑 (Sutherland–Hodgman).
- `_lerp_x()` (L87) — 선분 a-b 가 수직선 X=x 와 만나는 점.
- `_lerp_y()` (L97) — 선분 a-b 가 수평선 Y=y 와 만나는 점.
- `slab_room_overlap_area()` (L107) — 슬래브 사각형(x0,y0,x1,y1)과 실 폴리곤의 교집합 면적 mm².
- `area_weighted_loads()` (L117) — 슬래브(rect) 위 같은 층 실들의 면적가중 (활하중, 부가고정하중) kN/m².
- `detect_roof_top_z()` (L153) — 슬래브 상면 z 목록에서 옥상 레벨(최댓값) 반환. 비면 None.
- `is_roof_level()` (L165) — 슬래브 상면 z 가 옥상 레벨(roof_top_z) 과 tol 이내면 옥상으로 판정.


### `modular_3d/analysis/section_catalog.py` (30 줄)
> SHS 단면 카탈로그 — re-export shim (2026-05-12).


### `modular_3d/analysis/section_design.py` (504 줄)
> 부재 분류 + 그룹핑 + 단면 자동 선정.

**모듈 상수**
- `POLICY_1` (L26) = `'1종'`
- `POLICY_2` (L27) = `'2종'`
- `POLICY_3` (L28) = `'3종'`
- `ALL_POLICIES` (L29) = `(POLICY_1, POLICY_2, POLICY_3)`
- `CAT_MODULE_COLUMN` (L33) = `'module_column'`
- `CAT_MODULE_BEAM` (L34) = `'module_beam'`
- `CAT_CANTILEVER_BEAM` (L35) = `'cantilever_beam'`
- `CAT_FLOOR_PANEL_BEAM` (L36) = `'floor_panel_beam'`
- `CAT_WALL_PANEL_COLUMN` (L37) = `'wall_panel_column'`
- `CAT_WALL_PANEL_BEAM` (L38) = `'wall_panel_beam'`
- `CAT_RC_CORE` (L40) = `'rc_core'`

**최상위 함수**
- `classify_member()` (L61) — role 기반 카테고리 분류.
- `group_categories()` (L79) — 정책별 그룹명 → 포함 카테고리 리스트.
- `group_members()` (L111) — 정책에 따라 부재 ID 를 그룹별로 분류.
- `_kind_from_category()` (L140) — 6 카테고리를 strength_check 의 3 kind 로 단순화.
- `extract_demand()` (L149) — OpsResults 의 부재 양단 단면력에서 절댓값 최댓값을 추출.
- `extract_all_demands()` (L183) — 모든 부재의 demand 를 한꺼번에 추출.
- `extract_envelope_demands()` (L190) — (2026-05-19 작업 5) 모든 하중조합의 단면력 절댓값 최대로 envelope demand 생성.
- `design_all_policies_envelope()` (L232) — 모든 정책 산정을 envelope demand 기반으로 수행 (지배조합).
- `select_section_for_group()` (L254) — 카탈로그를 단면적 오름차순 순회 → 그룹 모든 부재 OK 인 첫 단면 채택.
- `design_for_policy()` (L318) — 단일 정책에 대해 그룹별 단면 산정.
- `design_all_policies()` (L353) — 1종/2종/3종 모두 산정 → dict[policy → DesignResult].
- `split_columns_by_floor()` (L363) — 기둥 그룹을 층 범위 K개 구간으로 동적계획법 최적 분할.

**클래스**
- `class GroupDesign` (L244) — 그룹 단면 산정 결과.
- `class DesignResult` (L305) — 한 정책에 대한 전체 산정 결과.
  - `ng_groups()` (L314)


### `modular_3d/analysis/spatial_matcher.py` (121 줄)
> 공간 매칭 헬퍼 — fuzzy 매칭 4곳의 공통 알고리즘 추출.

**최상위 함수**
- `nearest_node()` (L23) — 점 ↔ 점 가장 가까운 후보 찾기.
- `project_point_to_segment()` (L59) — 점을 (xy 평면) 선분에 사영.
- `nearest_segment_projection()` (L84) — 점 ↔ 여러 선분 중 가장 가까운 것 찾기.


### `modular_3d/analysis/strength_check.py` (291 줄)
> KDS 41 31 00 강구조 LRFD 강재 단면 강도 검토.

**모듈 상수**
- `DEFL_LIMIT_BEAM_DENOM` (L28) = `360.0`
- `DEFL_LIMIT_CANTILEVER_DENOM` (L29) = `180.0`

**최상위 함수**
- `check_axial()` (L88) — 축력 검토 (압축이면 좌굴 포함, 인장이면 항복).
- `check_bending()` (L127) — 휨 검토 — 강축+약축 단순 합 (보수적).
- `check_shear()` (L143) — 전단 검토 — 강·약축 중 큰 값.
- `check_combined()` (L156) — KDS 41 31 00 H1.1 P-M 조합 상관식.
- `estimate_deflection()` (L201) — 강축 모멘트로부터 단면 의존 처짐 추정.
- `check_deflection()` (L221) — 처짐 검토 — 단면 의존 추정 처짐을 한계와 비교.
- `_as_shs_view()` (L261) — H형강이면 SHS 컨벤션 뷰로 변환, 각형강관이면 그대로 반환(덕타이핑).
- `check_member()` (L273) — 5개 항목 전부 계산 → CheckResult 반환. 각형강관·H형강 단면 모두 허용.

**클래스**
- `class MemberDemand` (L33) — 부재 요구 내력 (해석 결과로부터 추출).
- `class CheckResult` (L60) — 단면 검토 결과 — 5개 항목 + 최댓값 + 지배 항목.
  - `from_ratios()` (L72)
- `class _SecView` (L242) — check_* 함수가 쓰는 SHS 컨벤션 단면 속성만 추린 경량 뷰.


### `modular_3d/analysis/tests/__init__.py` (0 줄)

### `modular_3d/analysis/tests/test_joint_override.py` (634 줄)
> 접합부 오버라이드 — 직렬화·매칭·빌드 반영(다층 자동) 테스트.

**최상위 함수**
- `test_serialize_roundtrip()` (L28)
- `test_effective_dofs()` (L41)
- `test_match_bidirectional_and_tol()` (L49)
- `test_match_latest_wins()` (L64)
- `test_match_by_rule_id()` (L70) — 같은 평면 위치(수직 겹침)에서 rule_id 로 종류를 구분해 매칭.
- `test_set_override_keeps_different_rule()` (L85) — 같은 위치라도 rule_id 가 다르면 둘 다 유지, 같으면 교체.
- `test_match_override_single_layer_z()` (L101) — single_layer 면 같은 xy 라도 z(층)가 맞아야 매칭('이 층만').
- `test_set_override_single_layer_keeps_other_floor()` (L118) — single_layer 변경은 층(z)별로 별개 — 다른 층 변경을 지우지 않는다.
- `test_same_joint()` (L137)
- `test_scene_set_override_replaces_same_joint()` (L146)
- `test_scene_clear_override_at()` (L158)
- `test_scene_io_roundtrip()` (L169)
- `_make_stacked_module()` (L185) — 수직 n층 모듈 1개 그룹 씬 — R02 수직 적층 접합이 코너마다 생긴다.
- `_vstack_pairs()` (L195) — vstack 결합의 (xy_key, dofs) 목록. 수직이라 master/slave xy 동일.
- `test_build_baseline_has_vstack()` (L209)
- `test_build_remove_applies_all_floors()` (L219)
- `test_build_rigid_applies_all_floors()` (L235)
- `test_build_no_override_unchanged()` (L249) — 오버라이드 없으면 결합 개수·dofs 가 회귀 없이 동일해야 한다.
- `_fake_om()` (L260) — ops 노드만 등록한 최소 om — apply_added_joints 단위 검증용.
- `_user_add_pairs()` (L279)
- `test_add_serialize_roundtrip()` (L284)
- `test_add_vertical_single()` (L293)
- `test_add_horizontal_rigid()` (L305)
- `test_add_single_layer_no_multifloor()` (L316) — single_layer=True 면 다른 층에 복제하지 않고 클릭한 층만 추가한다.
- `test_add_right_angle_makes_intermediate_node()` (L330) — 직각(ㄴ자) add 는 중간 노드(N1)를 거쳐 두 결합(수직+수평)을 만든다.
- `test_right_angle_both_links_when_lower_constrained()` (L347) — 아래 노드가 이미 종속(자동결합 slave)이어도 직각접합 두 결합이 모두 생성.
- `test_add_rule_id_pin_vs_rigid()` (L359) — 핀 추가 → R10, 강접 추가 → R11 로 rule_id 가 구분된다.
- `test_add_replicates_all_floors()` (L378) — 수직 add(0→100)가 모든 인접 층 노드쌍에 복제되어야 한다.
- `test_anchor_zs_includes_split_layers()` (L392) — 분할되어 member_to_ele_tag 에서 빠진 보의 층도 z 가 수집돼야 한다
- `test_can_anchor_layer_gate()` (L412) — _can_anchor 는 그 평면 위치·층에 노드/보가 있을 때만 True.
- `test_on_edge_serialize_roundtrip()` (L430) — 선-분할 플래그(a_on_edge/b_on_edge)가 직렬화 왕복에서 보존된다.
- `test_can_anchor_on_edge_skips_node()` (L439) — on_edge=True 면 노드 스냅을 안 쓰고 보 분할만 본다 —
- `test_add_on_edge_creates_split_node_not_snap()` (L455) — 선 위 점(b_on_edge=True)으로 추가하면 가까운 기존 노드로 끌려가지 않고
- `test_remove_dangling_bridge_node()` (L484) — 직각접합 제거로 링크가 모두 사라진 허공 가교노드(N1)는 정리되고,
- `test_candidate_joint_points()` (L510) — 첫 점에서 축정렬·400mm 이내 다른 부재 위 후보가 검출된다.
- `test_candidate_right_angle()` (L533) — 직각 모드 후보 = 첫 점에서 수직 내린 다른 높이 보 위, 평면으로 떨어진 점.
- `test_add_none_when_no_partner()` (L557) — 짝 노드가 없으면(거리 밖) 등록 0.
- `test_add_dedup_in_scene()` (L565) — 같은 두 점 add 를 두 번 넣으면 Scene 이 중복 제거(1개 유지).
- `test_add_splits_beam_at_midpoint()` (L579) — 모서리(보) 중간 점에 add 하면 그 보가 분할되고 새 결합이 생긴다.
- `test_add_midpoint_multifloor()` (L605) — 모서리 중간 점 add 도 모든 층에 복제된다(보가 층마다 분할).


### `modular_3d/analysis/tests/test_room_loads.py` (88 줄)
> 실 용도별 하중 유틸(room_loads) 순수함수 테스트 (5단계).

**최상위 함수**
- `test_polygon_area_square()` (L12)
- `test_overlap_full_inside()` (L17)
- `test_overlap_partial_clip()` (L24)
- `test_overlap_none()` (L32)
- `test_area_weighted_empty_slab_uses_default()` (L38)
- `test_area_weighted_half_living_half_empty()` (L46)
- `test_area_weighted_corridor_mix()` (L57)
- `test_area_weighted_override()` (L66)
- `test_roof_detection()` (L77)
- `test_roof_detection_empty()` (L86)


### `modular_3d/analysis/topology.py` (1629 줄)
> 해석 토폴로지 빌드 (중심선 모델 + 컴포넌트별 독립 노드).

**모듈 상수**
- `_SECTION_KW` (L175) = `dict(section_w=SECTION_W_MM, section_h=SECTION_H_MM, section_t=SECTION_T_MM)`
- `_EXTRACTORS` (L753) = `{Module: _extract_module, FloorPanel: _extract_floor_panel, StructWall: _extr...`

**최상위 함수**
- `_extract_module()` (L188) — Module → 8 노드 / 12 부재 (4 코너 × 2 z레벨, 기둥 1개씩).
- `_extract_floor_panel()` (L256) — FloorPanel → 4 노드 / 4 요소 (가장자리 보만).
- `_extract_struct_wall()` (L295) — StructWall → 4 노드 / 4 요소 (2 코너 × 2 z레벨, 기둥 1개씩 + 런너 2).
- `_extract_cantilever_beam()` (L348) — CantileverBeam → 2 노드 / 1 요소.
- `_extract_cantilever_slab()` (L376) — CantileverSlab → 4 노드 / 3 요소 (U 자 보 3개).
- `_extract_mid_beam()` (L411)
- `_extract_mid_column()` (L432)
- `_extract_vertical_module()` (L453) — Vertical3Module → 16 노드 / 28 부재.
- `_extract_core()` (L534) — Core (RC 코어벽) → 외곽 boundary 프레임 + 내부 등가 트러스 격자.
- `_extract_core_slab()` (L685) — CoreSlab → 외곽 보 격자 (4 변).
- `_extract_one()` (L767)
- `_merge_duplicate_members()` (L777) — 동일 양단 노드를 가진 중복 부재 통합 (노드 병합 후 발생).
- `_segments_colinear()` (L808)
- `_project()` (L825)
- `_round1()` (L829) — 좌표값을 1mm 단위 정수로 반올림.
- `_merge_panel_overlaps_and_check()` (L840) — 완전 겹친 부재 통합 (패널↔패널, 패널↔모듈) + 부분 겹침 검사.
- `build_analysis_model()` (L990) — Scene → AnalysisModel.
- `_build_diaphragms()` (L1077) — 컴포넌트 단위 바닥 다이어프램 그룹 생성.
- `_consolidate_dependent_nodes()` (L1198) — 종속 관계로 얽힌 부재의 인접 노드를 같은 nid 로 통합.
- `_split_hosts_for_mid_endpoints()` (L1495) — mid_beam/mid_column 끝점이 다른 부재(주로 모듈 보) 직선 위에 떨어지면
- `summarize()` (L1617)

**클래스**
- `class AnalysisNode` (L39) — 해석용 절점 (월드 좌표 mm).
  - `__repr__()` (L45)
- `class AnalysisMember` (L51) — 해석용 부재 (1차원 보-기둥 요소).
  - `__repr__()` (L84)
- `class Diaphragm` (L90) — 단일 컴포넌트(또는 종속 캔틸 포함)의 바닥 다이어프램 그룹.
- `class AnalysisModel` (L109)
  - `invalidate_indices()` (L125) — members 변경 후 캐시 무효화 — split sub 추가/제거 직후 호출.
  - `_build_role_indices()` (L130) — role / role_prefix 인덱스 일괄 빌드.
  - `members_by_role()` (L144) — role 정확 매칭 mid 리스트 (lazy 캐시). 빈 결과 = 빈 리스트.
  - `members_by_role_prefix()` (L150) — role.startswith(prefix) 매칭 mid 리스트 (lazy 캐시).
  - `get_member_length()` (L156)
  - `base_node_ids()` (L165)
- `class _LocalExtract` (L179) — 단일 컴포넌트의 중심선 추출 결과 (글로벌 ID 할당 전).


### `modular_3d/analysis/물량산출_설계.py` (322 줄)
> 물량산출 + 단면 자동 산정 기능 — 설계 (CoT 한글 각주 only)


### `modular_3d/evaluation/__init__.py` (4 줄)
> 평가 탭 데이터 수집/직렬화 패키지.


### `modular_3d/evaluation/case_io.py` (82 줄)
> 평가 케이스 저장·로드 (.case.json).

**모듈 상수**
- `_CASE_KIND` (L18) = `'modular_case_v1'`

**최상위 함수**
- `_safe_asdict()` (L21) — ProjectSettings 같은 dataclass 를 dict 로. 실패 시 빈 dict.
- `save_case()` (L35) — 케이스 파일 저장.
- `load_case()` (L69) — 케이스 파일 로드. kind 검증.


### `modular_3d/evaluation/evaluation_adapter.py` (379 줄)
> 평가 데이터 수집 어댑터.

**모듈 상수**
- `_MODULE_FLOOR_HEIGHT_MM` (L61) = `3000.0`

**최상위 함수**
- `_aabb()` (L43)
- `_module_floor_span()` (L64)
- `_build_headline()` (L73)
- `_build_members()` (L124)
- `_compute_material_cost_safe()` (L161) — analysis_panel 과 동일 로직으로 자재비 산출. 실패 시 None.
- `_build_materials()` (L178) — C. 물량·자재비 — current_policy 기준만 표시(물량 탭과 일치).
- `_build_transport()` (L258)
- `_build_schedule()` (L300)
- `_build_cost()` (L316)
- `build_evaluation_data()` (L334) — 평가 화면이 받을 dict.


### `modular_3d/evaluation/tests/__init__.py` (0 줄)

### `modular_3d/evaluation/tests/test_evaluation_adapter.py` (277 줄)
> 평가 어댑터·케이스 저장 회귀 테스트 (Phase O + P).

**최상위 함수**
- `_mk_module()` (L19)
- `_mk_fake_quantity_report()` (L29) — compute_material_cost 가 받을 수 있는 가짜 QuantityReport.
- `test_empty_inputs_all_status_false()` (L66)
- `test_headline_basic_counts()` (L75)
- `test_total_floor_area_vertical_module()` (L91)
- `test_members_breakdown()` (L103)
- `test_transport_avg_load_and_distance_and_cost()` (L140)
- `test_materials_uses_compute_material_cost()` (L166)
- `test_materials_policy_fallback()` (L187)
- `test_cost_total_sum()` (L198)
- `test_partial_results_transport_missing()` (L228)
- `test_case_save_load_roundtrip()` (L241)
- `test_case_load_wrong_kind()` (L266)


### `modular_3d/io/__init__.py` (8 줄)
> I/O 패키지 — scene JSON 직렬화 + 마이그레이션 진입점.


### `modular_3d/io/scene_io.py` (291 줄)
> Scene 저장/불러오기 — JSON 라운드트립 + 구버전 마이그레이션 통합 (단계 3 단순화).

**모듈 상수**
- `_WALL_FP_AUTO_MATCH_MAX_XY_MM` (L29) = `500.0`

**최상위 함수**
- `snap_n_floors_to_three()` (L34) — 층수를 가장 가까운 3 의 배수로 스냅 (동률은 내림). 최소 3.
- `scene_to_state_dict()` (L48) — Scene + 층수 → 직렬화 dict (파일 저장 없이 메모리 표현).
- `save_scene()` (L89) — Scene 의 모든 Component + 층수를 JSON 으로 저장.
- `load_scene()` (L99) — JSON 파일을 읽어 (Scene, n_floors) 반환. 역직렬화는 state_dict_to_scene 위임.
- `state_dict_to_scene()` (L106) — 직렬화 dict → (Scene, n_floors).
- `_auto_match_wall_to_fp()` (L230) — 구버전 씬: wall.merge_with_panel=True 인데 merged_fp_id 가 비어 있는 경우.
- `_auto_estimate_parent_id()` (L265) — 구버전 씬: parent_id 가 비어 있는 종속 부재의 부모를 추정.


### `modular_3d/model/__init__.py` (25 줄)
> modular_3d.model 패키지 — 데이터 모델.


### `modular_3d/model/core.py` (1138 줄)
> 데이터 모델 — 부재, 씬, 액션 정의.

**최상위 함수**
- `_rotate_point()` (L93) — 로컬 좌표 (x, y)를 rotation 각도만큼 원점 기준 회전.
- `_rotate_local_to_world()` (L106) — 로컬 좌표를 회전 + 평행이동하여 월드 좌표로 변환.
- `instantiate()` (L1098) — Component 서브클래스 생성 + 하위 부재 자동 생성.
- `effective_beam_section_type()` (L1120) — 부재의 유효 보 단면 타입 — 종속/합체는 부모/패널을 따라간다.

**클래스**
- `class AppState(Enum)` (L19)
- `class ComponentType(Enum)` (L27)
- `class BeamData` (L63)
- `class ColumnData` (L77)
- `class SlabData` (L86)
- `class Component` (L117) — 모든 부재의 기본 클래스.
  - `_apply_beam_section_type()` (L187) — 본 부재의 모든 보(BeamData)에 beam_section_type 을 전파.
  - `get_bounding_box()` (L197) — AABB 바운딩박스 (min_xyz, max_xyz) 반환.
  - `get_world_corners()` (L202) — 바닥면 4 꼭지점 + 상단 4 꼭지점 = 8개 월드 좌표.
  - `_get_all_beams()` (L223) — 하위 부재 중 모든 보 목록 반환 (서브클래스에서 오버라이드).
  - `get_snap_points()` (L227) — 스냅 가능한 모든 점 반환 (바닥 꼭지점 + 보 중심/1/4).
  - `_anchor_offset()` (L244) — 앵커 인덱스에 따른 로컬 오프셋.
- `class Module(Component)` (L258) — 모듈: 기둥 4 + 하부보 4 + 상부보 4 + 슬래브 1 + 비내력 벽 4면(채움).
  - `generate_sub_components()` (L272) — dimensions + position + rotation으로 하위 부재 자동 생성.
  - `_get_all_beams()` (L369)
- `class FloorPanel(Component)` (L374) — 바닥패널: 보 4개 + 슬래브 (기둥 없음).
  - `generate_sub_components()` (L380)
  - `_get_all_beams()` (L411)
- `class WallPanelData` (L416) — 벽체 채움 (솔리드 판).
- `class StructWall(Component)` (L423) — 구조벽: 기둥 2 + 런너 2 + 벽체 채움.
  - `generate_sub_components()` (L431)
  - `_get_all_beams()` (L471)
- `class InteriorWall(Component)` (L481) — 내벽(비내력 칸막이벽) — 채움 판 1장. 기둥·보·런너 없음.
  - `generate_sub_components()` (L495)
  - `_get_all_beams()` (L521)
- `class Vertical3Module(Component)` (L526) — 수직 3층 모듈: 한 칸이 3개 층을 차지하는 모듈.
  - `generate_sub_components()` (L549)
  - `_get_all_beams()` (L623)
- `class Core(Component)` (L632) — RC 코어벽 — 한 장 단위. wide-column 모델용 단일 frame element 표현.
  - `generate_sub_components()` (L654)
  - `_get_all_beams()` (L681)
- `class CoreSlab(Component)` (L686) — RC 코어 슬래브 — 보 없는 순수 RC 판. 코어벽 그룹의 종속 부재.
  - `generate_sub_components()` (L705)
  - `_get_all_beams()` (L728)
- `class CantileverBeam(Component)` (L733) — 캔틸레버보: 단일 보.
  - `generate_sub_components()` (L737)
  - `_get_all_beams()` (L752)
- `class CantileverSlab(Component)` (L757) — 캔틸레버 슬래브: 3변 보 + 슬래브.
  - `generate_sub_components()` (L762)
  - `_get_all_beams()` (L798)
- `class MidBeam(Component)` (L803) — 중간보: 단일 보 (모듈 내부 보강용).
  - `generate_sub_components()` (L807)
  - `_get_all_beams()` (L822)
- `class MidColumn(Component)` (L827) — 중간기둥: 단일 기둥.
  - `generate_sub_components()` (L831)
- `class Action` (L850) — Undo 가능한 행동.
- `class Scene` (L858) — 부재 컬렉션 + Undo 스택.
  - `__init__()` (L861)
  - `add_room()` (L877) — 실 추가, ID 반환(undo 스택에는 넣지 않음 — 부재와 별개 흐름).
  - `remove_room()` (L884) — 실 삭제, 삭제된 실 반환(없으면 None).
  - `set_joint_override()` (L893) — 접합 오버라이드 추가. 같은 접합을 가리키는 동종 오버라이드는 교체.
  - `clear_joint_override_at()` (L931) — 노드쌍에 매칭되는 기존-접합 오버라이드 제거(자동 규칙 복귀).
  - `clear_joint_overrides()` (L943) — 모든 접합 오버라이드 제거(불러오기 시 '새로 자동계산' 선택용).
  - `add_component()` (L947) — 부재 추가, ID 반환.
  - `remove_component()` (L958) — 부재 삭제, 삭제된 부재 반환.
  - `undo()` (L963) — 마지막 액션 취소. 취소된 액션 반환 (실제 복원은 Controller에서).
  - `begin_group_action()` (L978) — 그룹 액션 시작 — undo_stack 의 현재 길이를 마크.
  - `end_group_action()` (L982) — 그룹 액션 종료 — 시작 이후 push 된 모든 액션을 _inner_actions 로 묶고
  - `merge_wall_to_fp()` (L1002) — 벽패널을 플로어패널에 합체. 성공 시 True.
  - `unmerge_wall_from_fp()` (L1019) — 벽패널을 플로어패널에서 분리. 성공 시 True.
  - `find_nearest_fp_for_wall()` (L1036) — 벽패널 하부 런너와 가장 가까운 FP 가장자리 보를 가진 FP ID 반환.
  - `component_count()` (L1061)


### `modular_3d/model/definition_library.py` (165 줄)
> 모듈/패널 정의 라이브러리 (DB).

**최상위 함수**
- `_detect_root_type()` (L40) — state dict 의 루트(부모 없는) 부재를 검사해 루트 타입을 반환.
- `_summarize()` (L60) — state dict 에서 (부재 수, 타입 요약 문자열) 계산.

**클래스**
- `class ComponentDefinition` (L23) — 정의 1건 — 이름 + 직렬화 상태(state dict) + 요약 메타.
  - `to_scene()` (L35) — 정의를 (Scene, n_floors) 로 복원.
- `class DefinitionLibrary` (L72) — 정의 라이브러리 — {이름 → ComponentDefinition}.
  - `__post_init__()` (L83)
  - `add_from_scene()` (L87) — 현재 작업공간(Scene)을 이름 붙여 정의로 저장(추가/갱신).
  - `get()` (L102)
  - `list_names()` (L105)
  - `remove()` (L108)
  - `__len__()` (L115)
  - `save()` (L119) — path 가 있으면 전체 정의를 json 파일로 저장(디렉토리 자동 생성).
  - `_load()` (L141) — path 의 json 파일을 읽어 정의를 채운다. 없거나 손상 시 무시.


### `modular_3d/model/definition_place.py` (34 줄)
> 정의 가져오기 배치용 그룹 변환 헬퍼.

**최상위 함수**
- `rotate_xy_deg()` (L14) — 원점 기준 90° 단위 회전(반시계). deg 는 0/90/180/270 로 정규화.
- `transform_point()` (L26) — 점(px,py)을 pivot 중심 rot_deg 회전 후, pivot→target 평행이동.


### `modular_3d/model/joint_override.py` (172 줄)
> 접합부 오버라이드 — 사용자가 컴포넌트 간 접합을 직접 제거/변경/추가한 기록.

**모듈 상수**
- `MATCH_TOL_MM` (L23) = `50.0`

**최상위 함수**
- `_xy_close()` (L125) — 두 평면 좌표가 tol(mm) 이내인가.
- `match_override()` (L131) — 노드쌍(평면 좌표 ma_xy, mb_xy)에 매칭되는 기존-접합 오버라이드 반환.
- `same_joint()` (L168) — 오버라이드가 가리키는 접합이 노드쌍(a_xy, b_xy)과 같은 접합인가(양방향).

**클래스**
- `class JointOverride` (L31) — 접합 하나에 대한 사용자 변경 기록.
  - `to_dict()` (L75)
  - `from_dict()` (L93)
  - `effective_dofs()` (L112) — 이 오버라이드가 만들 자유도 묶음. remove 면 None(등록 안 함).


### `modular_3d/model/joint_recorder.py` (227 줄)
> UI 측 조인트 기록기 (2026-05-12 신규, 옵션 (나)).

**모듈 상수**
- `JOINT_TOL_XY` (L40) = `350.0`
- `JOINT_TOL_Z` (L41) = `500.0`

**최상위 함수**
- `_column_xy_list()` (L44) — 본 컴포넌트의 기둥들 (cx, cy, z_top, z_bot) 목록.
- `_cantilever_endpoints()` (L72) — 캔틸레버 부재의 자유단 끝점만 후보로 — (x, y, z) 목록.
- `_fp_bottom_corners()` (L107) — FloorPanel/CantileverSlab 의 4 바닥 코너 (월드) — shape=(4,3).
- `_best_column_for_corner()` (L116) — 단일 코너에 대해 가장 가까운 기둥 단을 찾아 record dict 반환.
- `record_joints()` (L197) — 씬 전체를 스캔해 FloorPanel·CantileverSlab 의 joint_records 채움.


### `modular_3d/model/multi_floor.py` (466 줄)
> 다층 그룹 생성 헬퍼 — F5 배치 모드용.

**최상위 함수**
- `_floor_z_list()` (L38) — 부재 타입과 종속 옵션에 따라 각 floor_index 별 z 좌표 리스트 반환.
- `_instantiate()` (L130) — Component 서브클래스 생성 — model.core.instantiate 위임 (단계 2 통합).
- `next_group_id()` (L141) — 현재 Scene 안에서 가장 큰 group_id + 1 반환. 모두 0이면 1 반환.
- `create_multi_floor_group()` (L153) — 다층 그룹을 생성하고 (group_id, [comp_id_list]) 반환.
- `_next_sub_index()` (L261) — 같은 group_id 안에서 사용된 가장 큰 sub_index + 1 반환.
- `_compute_core_slab_bbox_xy()` (L274) — 그룹의 그 층 모든 코어벽들의 월드 xy bbox 반환.
- `regenerate_core_slabs()` (L298) — 그룹의 모든 층(0..n_floors) 코어 슬래브를 재계산 / 갱신.
- `delete_group()` (L421) — 그룹 통째로 삭제. 삭제된 comp_id 리스트 반환.
- `translate_components()` (L434) — 주어진 comp_id 들만 (dx, dy, dz) 이동. 영향받은 comp_id 리스트 반환.
- `translate_group()` (L451) — 그룹 통째로 (dx, dy, dz) 이동. 영향받은 comp_id 리스트 반환.


### `modular_3d/model/room.py` (111 줄)
> 실(Room) 데이터 모델 — 2D 폴리곤으로 정의되는 공간.

**클래스**
- `class Room` (L18) — 실 1건 — 폴리곤(월드 xy mm) + 용도 + 소속 층.
  - `effective_live_load()` (L35) — 유효 활하중(kN/m²) — override 우선, 없으면 용도 기본값.
  - `effective_sdl()` (L43) — 유효 부가고정하중(kN/m²) — override 우선, 없으면 용도 기본값.
  - `centroid()` (L51) — 폴리곤 무게중심(라벨 표시용). 점이 없으면 (0, 0).
  - `to_dict()` (L82) — 직렬화 dict (json 가능). scene_io 가 사용.
  - `from_dict()` (L96) — 직렬화 dict → Room. scene_io 가 사용. (옛 'name' 키는 무시.)


### `modular_3d/render/__init__.py` (0 줄)

### `modular_3d/render/auto_snap.py` (194 줄)
> 자동 갭 스냅 모듈 (2026-05-11 신규).

**모듈 상수**
- `_MODULE_LIKE` (L46) = `frozenset({ComponentType.MODULE, ComponentType.FLOOR_PANEL, ComponentType.VER...`
- `_DEPENDENT_ZERO_GAP` (L55) = `frozenset({ComponentType.CANTILEVER_BEAM, ComponentType.CANTILEVER_SLAB, Comp...`

**최상위 함수**
- `gap_between()` (L64) — 두 부재 타입 쌍의 갭(mm) 결정.
- `_new_bbox()` (L88) — 1층 본체 XY bbox 반환.
- `apply_auto_gap()` (L127) — 현재 마우스 월드 좌표(wx, wy) 를 자동 갭으로 보정한 새 좌표를 반환.


### `modular_3d/render/dep_snap.py` (274 줄)
> 종속 부재 자동 스냅 (Phase 2 — 2026-05-11, v2 사용자 피드백 반영).

**모듈 상수**
- `SLAB_MAX_WIDTH_MM` (L30) = `3400.0`

**최상위 함수**
- `_nearest_edge()` (L33) — bbox 4 변 중 가장 가까운 변 라벨 ('L','R','B','T').
- `_nearest_vertex_on_edge()` (L46) — edge 위 양 끝 꼭지점 중 마우스 가까운 1 개의 (x, y) 반환.
- `_is_inside()` (L62)
- `snap_cantilever_beam()` (L78)
- `snap_cantilever_slab()` (L127)
- `snap_mid_column()` (L168)
- `snap_mid_beam()` (L192)


### `modular_3d/render/mesh_builder.py` (459 줄)
> 부재 → 3D 메쉬 변환.

**모듈 상수**
- `COLOR_STEEL` (L9) = `np.array([0.5, 0.5, 0.5, 1.0], dtype=np.float32)`
- `COLOR_STEEL_INNER` (L10) = `np.array([0.375, 0.375, 0.375, 1.0], dtype=np.float32)`
- `COLOR_CONCRETE` (L11) = `np.array([0.75, 0.75, 0.75, 1.0], dtype=np.float32)`
- `COLOR_WALL` (L238) = `np.array([0.815, 0.815, 0.815, 1.0], dtype=np.float32)`
- `COLOR_WALL_PARTITION` (L239) = `np.array([0.815, 0.815, 0.815, 0.35], dtype=np.float32)`
- `COLOR_CORE_WALL` (L388) = `np.array([0.75, 0.75, 0.75, 0.7], dtype=np.float32)`
- `_MESH_BUILDERS` (L433) = `{_CT.MODULE: build_module_mesh, _CT.FLOOR_PANEL: build_floor_panel_mesh, _CT....`

**최상위 함수**
- `_local_frame()` (L16) — 보/기둥 방향 벡터로 로컬 좌표계 (forward, right, up) 계산.
- `build_hollow_box_section()` (L37) — 중공 각형강관 메쉬 생성.
- `build_h_section()` (L118) — H형강(I자 단면) 압출 메시. 강축 휨 — 웨브가 수직(up), 플랜지 수평(right).
- `build_solid_box()` (L176) — 6면 솔리드 박스 (슬래브, 벽체 등).
- `_merge_meshes()` (L215) — 여러 메쉬를 하나로 합치기.
- `_build_slab_mesh()` (L242) — SlabData → 솔리드 박스 메쉬. openings 있으면 z 관통 사각 구멍.
- `_build_wall_fill_mesh()` (L261) — WallPanelData → 솔리드 판 메쉬. openings 있으면 두께 관통 사각 구멍.
- `_add_columns()` (L285)
- `_add_beams()` (L291)
- `_openings_for_face()` (L302) — comp.openings 중 face 에 해당하는 것만(레거시는 default_face 로 간주).
- `build_module_mesh()` (L308)
- `build_floor_panel_mesh()` (L324)
- `build_struct_wall_mesh()` (L333)
- `build_interior_wall_mesh()` (L347) — 내벽 — 채움 판 1장만 (반투명, 개구부 face='wall').
- `build_cantilever_beam_mesh()` (L357)
- `build_cantilever_slab_mesh()` (L364)
- `build_mid_beam_mesh()` (L373)
- `build_mid_column_mesh()` (L380)
- `build_core_mesh()` (L391) — RC 코어벽 — 단순 솔리드 박스 (회전·앵커 반영된 월드 8 코너의 AABB).
- `build_core_slab_mesh()` (L409) — RC 코어 슬래브 — 보 없는 순수 RC 판. SlabData 1 개만.
- `build_vertical3_module_mesh()` (L417) — 수직 3층 모듈: 4 통기둥 + 층 슬래브 받침보 12 + 옥상 프레임 보 4 + 슬래브 3.
- `build_component_mesh()` (L448) — 부재 타입에 따라 적절한 메쉬 빌더 호출. 미지의 타입은 module 폴백.
- `build_ghost_component_mesh()` (L454) — 부재 메쉬와 동일하지만 alpha=0.3.


### `modular_3d/render/opening_mesh.py` (372 줄)
> 개구부(사각 구멍) 관통 박스 메시.

**최상위 함수**
- `openings_to_world_rects()` (L17) — 면 로컬 개구부(u,v,w,h) → 월드 평면 사각형 (a0,a1,b0,b1) 목록.
- `build_box_with_holes()` (L45) — 축정렬 박스(mn,mx)에 관통축 방향 사각 구멍들을 뚫은 메시.
- `_face_corners_for_opening()` (L162) — 부재의 개구부 기준 면 코너(4,3) + 종류 + 두께 반환. 없으면 (None,'',0).
- `_point_seg_dist_xy()` (L201) — 점(px,py)과 선분 a-b(둘 다 (x,y,..)) 의 xy 거리.
- `resolve_opening_face()` (L214) — 클릭 위치에서 개구부 대상 면 결정. 'slab'|'wall'|'wall_N' 또는 None.
- `opening_xy_poly()` (L245) — 개구부 1건(op dict)을 2D 평면(xy) 사각형 [(x,y)x4] 로 변환. 없으면 None.
- `opening_xy_polygons()` (L285) — 부재의 각 개구부를 2D 평면 사각형으로 변환. [(index, pts, kind), ...].
- `opening_world_box()` (L301) — 개구부 1건의 3D 볼륨(관통 박스) AABB (mn, mx) 반환. 없으면 None.
- `opening_facelocal_from_click()` (L335) — 클릭 월드(wx,wy)+크기(ew,eh)+앵커 → (u0, v0, kind, face). 대상 아니면 None.


### `modular_3d/render/room_mesh.py` (123 줄)
> 실(Room) 폴리곤 → 3D 반투명 색면 메시 빌더.

**모듈 상수**
- `ROOM_PLANE_Z_MM` (L17) = `250.0`

**최상위 함수**
- `_signed_area()` (L20) — 폴리곤 부호 면적(>0 = CCW).
- `_point_in_triangle()` (L31) — 점(px,py)이 삼각형 abc 내부(경계 포함)인지 — 부호 일관성 검사.
- `triangulate_simple_polygon()` (L41) — 단순 다각형(구멍 없음)을 ear-clipping 으로 삼각분할.
- `build_room_mesh()` (L97) — 실 폴리곤 → (vertices Nx3, faces Mx3, face_colors Mx4).


### `modular_3d/render/snap.py` (162 줄)
> 스냅 시스템 — 배치된 부재 꼭지점에 스냅 + 20mm 갭 적용.

**클래스**
- `class SnapManager` (L9) — 부재 꼭지점 스냅 관리. 부재 추가/삭제 시 인덱스 갱신.
  - `__init__()` (L12)
  - `add_component()` (L19) — 부재의 스냅 포인트를 캐시에 추가 (꼭지점 + 보 중심/1/4).
  - `remove_component()` (L28) — 부재 꼭지점을 캐시에서 제거.
  - `clear()` (L32) — 전체 캐시 초기화.
  - `find_nearest()` (L38) — vispy 씬 변환으로 각 스냅 포인트를 화면에 투영하고,
  - `apply_gap()` (L94) — 스냅 위치에 배치할 때 갭 자동 적용 (기본 MODULE_JOINT_GAP_MM).


### `modular_3d/render/viewer.py` (2707 줄)
> 3D 렌더링 — Vispy SceneCanvas + 커스텀 카메라.

**모듈 상수**
- `OPS_COLOR_BEAM` (L27) = `(0.05, 0.05, 0.05, 1.0)`
- `OPS_COLOR_SHELL` (L28) = `(0.45, 0.1, 0.55, 1.0)`
- `OPS_COLOR_CORE` (L29) = `(0.45, 0.1, 0.55, 1.0)`
- `OPS_COLOR_PIN` (L30) = `(0.95, 0.85, 0.1, 1.0)`
- `OPS_COLOR_RIGID6` (L31) = `(0.7, 0.05, 0.5, 1.0)`
- `OPS_COLOR_NODE` (L32) = `(0.3, 0.3, 0.3, 1.0)`
- `OPS_COLOR_NODE_EDGE` (L33) = `(0.15, 0.15, 0.15, 1.0)`
- `OPS_COLOR_BASE` (L34) = `(0.0, 0.0, 0.0, 1.0)`
- `OPS_LINK_KIND_COLOR` (L39) = `{'panel_module': OPS_COLOR_PIN, 'wall_module': OPS_COLOR_PIN, 'vertical_stack...`
- `OPS_LINK_SKIP_KINDS` (L49) = `{'wall_fp_merge'}`
- `OPS_COLOR_DIAPHRAGM` (L87) = `(0.55, 0.55, 0.55, 0.55)`
- `OPS_COLOR_RIGIDLINK` (L88) = `(0.35, 0.35, 0.35, 0.85)`
- `OPS_COLOR_DOF_1` (L92) = `(0.2, 0.8, 0.85, 1.0)`
- `OPS_COLOR_DOF_2` (L93) = `(0.5, 0.85, 0.2, 1.0)`
- `OPS_COLOR_DOF_3` (L94) = `(1.0, 0.55, 0.1, 1.0)`
- `OPS_COLOR_DOF_6` (L95) = `(0.7, 0.05, 0.5, 1.0)`

**최상위 함수**
- `_cam_axes()` (L98) — 카메라의 world-space 위치, forward, right, up 벡터 계산.
- `_raycast_z0_from_cam()` (L133) — NDC 좌표에서 Z=0 평면 교차점 반환. None이면 교차 없음.

**클래스**
- `class CustomCamera(scene.TurntableCamera)` (L156) — 가운데 버튼 = Orbit, Shift+가운데 = Pan, 휠 = 커서방향 줌.
  - `__init__()` (L162)
  - `viewbox_mouse_event()` (L170)
  - `_zoom_toward_cursor()` (L229) — 마우스 커서 방향으로 확대/축소. 매 휠 이벤트마다 현재 카메라로 재계산.
- `class Viewer3D` (L256) — Vispy 3D 뷰어 관리.
  - `__init__()` (L259)
  - `_create_axis_arrows()` (L433) — 원점에 XYZ 축 화살표.
  - `add_component_visual()` (L464)
  - `remove_component_visual()` (L507)
  - `add_room_visual()` (L519) — 실 폴리곤 반투명 메시 추가(같은 id 가 있으면 먼저 제거).
  - `remove_room_visual()` (L533)
  - `update_opening_ghost()` (L545) — 개구부 고스트 박스 갱신(없으면 생성).
  - `clear_opening_ghost()` (L559)
  - `update_room_ghost()` (L565)
  - `clear_room_ghost()` (L578)
  - `set_ghost_enabled()` (L590) — F5 PREVIEW 진입/이탈 시 controller가 호출.
  - `update_ghost()` (L598)
  - `clear_ghost()` (L611)
  - `show_snap_markers()` (L617) — 스냅 대상 부재의 모든 꼭지점을 노란 점으로, 스냅된 점은 크게 표시.
  - `hide_snap_marker()` (L646)
  - `show_selection_box()` (L652) — [Phase 0 — 부재 호버 시스템] 컴포넌트 빨간 테두리 강조 비활성.
  - `hide_selection_box()` (L664) — 선택 하이라이트 숨기기 — 비주얼 자체가 항상 invisible 이라 캐시만 갱신.
  - `highlight_member()` (L671) — 해석 부재(mid)를 두꺼운 노란 선으로 하이라이트.
  - `clear_member_highlight()` (L690) — 해석 부재 하이라이트 숨김.
  - `_pattern_factor()` (L697) — 프로파일 높이 비율 (peak 기준 0~1). s ∈ [0, 1] 은 보 축 파라미터.
  - `show_slab_overlay()` (L719) — Stage 1: 슬래브 면 + 판정 라벨 + 분배 패턴 프로파일 + peak 수치.
  - `hide_slab_overlay()` (L868)
  - `is_slab_overlay_visible()` (L875)
  - `show_beam_udl_overlay()` (L879) — 각 보(beam) 부재 아래에 factored UDL 프로파일(직사각형) + 수치 표시.
  - `hide_beam_udl_overlay()` (L944)
  - `is_beam_udl_overlay_visible()` (L949)
  - `show_column_head_overlay()` (L953) — 각 기둥(column) 부재의 상단 노드에 해당 기둥이 받는 축력 N 을 텍스트로 표시.
  - `hide_column_head_overlay()` (L1001)
  - `is_column_head_overlay_visible()` (L1005)
  - `show_reaction_overlay()` (L1009) — 각 베이스 노드 위에 수직 반력 Rz 화살표 + 수치 표시.
  - `hide_reaction_overlay()` (L1067)
  - `is_reaction_overlay_visible()` (L1072)
  - `show_joint_overlay()` (L1077) — 해석에서 실제 사용하는 beam_line 데이터로 접합부 시각화.
  - `hide_joint_overlay()` (L1197)
  - `is_joint_overlay_visible()` (L1204)
  - `screen_to_world_ray()` (L1209) — Qt 화면 좌표 (Y=0이 위, 아래로 증가) → (ray_origin, ray_direction).
  - `get_scene_transform()` (L1238) — vispy 씬→캔버스 변환 객체 반환 (3D world → 2D canvas pixels).
  - `pick_ops_member_at()` (L1244) — F6 ops 뷰에서 Qt 마우스 좌표에 가장 가까운 부재 mid 를 반환.
  - `pick_ops_joint_at()` (L1312) — F6 ops 뷰에서 마우스에 가장 가까운 컴포넌트 간 접합을 선택.
  - `highlight_ops_joint_chain()` (L1432) — 여러 노드를 잇는 접합 체인 강조(직각접합 위-N1-아래 폴리라인).
  - `_ensure_ops_visuals()` (L1457) — OpenSees 시각화 visual들 lazy 생성.
  - `set_show_diaphragms()` (L1534) — 강체 다이어프램 마스터↔슬레이브 연결선 표시 토글.
  - `set_show_rigid_links()` (L1540) — 강체 링크 표시 토글.
  - `set_dof_color_mode()` (L1546) — 자유도 묶음 색을 DOF 수별로 분리해 표시할지 토글.
  - `set_rule_visibility()` (L1552) — 규칙 ID 별 결합선 시각화 토글 (단순 시각 — 실제 결합은 유지).
  - `_draw_from_spec()` (L1567) — ModelSpec 단일 순회기로 좌측 뷰를 그린다 (Phase 6 — 단일화 핵심).
  - `show_ops_view()` (L1808) — OpenSees 모델을 좌측 뷰에 표시 — ModelSpec 단일 순회기 사용 (Phase 6).
  - `hide_ops_view()` (L1949) — OpenSees 뷰 비활성 → 원래 메쉬 색상 복원.
  - `is_ops_view_active()` (L1991)
  - `update_deformed_shape()` (L1996) — ops 모델 위에 변형 형상을 덧그린다.
  - `hide_deformed_shape()` (L2077)
  - `_ensure_deformed_visuals()` (L2085)
  - `is_deformed_shape_active()` (L2111)
  - `_ratio_to_jet()` (L2117) — 비율 r ∈ [0, 1] → jet 컬러맵 (blue→cyan→green→yellow→red).
  - `_ratio_to_5band()` (L2136) — 응력비 → 5단계 색상 (물량산출 탭 시각화).
  - `show_member_ratio_bands()` (L2151) — 5단계 색상으로 부재 색칠 (물량산출 탭 진입 시 호출).
  - `show_member_contour()` (L2184) — 일반 부재선을 부재별 ratio (0..1+) 에 따라 jet 컬러맵으로 색칠.
  - `show_unstable_warning()` (L2218) — 불안정 구조 경고 — 문제 노드 + 그 노드를 포함한 부재를 **빨간색** 강조.
  - `hide_unstable_warning()` (L2274)
  - `_ensure_unstable_visuals()` (L2281)
  - `hide_member_contour()` (L2297) — 컨투어 해제 → 일반 부재 검정 단색으로 복귀.
  - `highlight_ops_member()` (L2318) — ops 모델에서 (n1, n2) 부재를 파란색 강조.
  - `update_pinned_members()` (L2333) — Phase 9 — 슬롯별 클릭 고정 부재 강조 갱신.
  - `highlight_ops_member_hover()` (L2357) — Phase 2 — 부재 호버 회색 강조. 기본 회색, Phase 13 에서 다음
  - `clear_ops_member_hover()` (L2375) — 부재 호버 강조 제거.
  - `_ensure_joint_highlight_visuals()` (L2382) — 접합 선택 강조용 Line/Markers lazy 생성.
  - `highlight_ops_joint()` (L2393) — 선택 접합 강조 — 두 노드를 잇는 굵은 선 + 끝점 마커(노란색).
  - `clear_ops_joint_highlight()` (L2414) — 접합 선택 강조 제거.
  - `highlight_ops_node()` (L2426) — 단일 노드 강조 — 접합 추가 모드의 첫 점 표시(초록 마커).
  - `pick_ops_node_at()` (L2440) — 마우스에 가장 가까운 ops 노드 반환 — 접합 추가 시 두 점 선택용.
  - `pick_ops_edge_point_at()` (L2482) — 모서리(보) 위에서 마우스에 가장 가까운 점을 반환 — 접합 추가용.
  - `_ensure_joint_ghost()` (L2573)
  - `show_joint_ghost()` (L2580) — 접합 추가 시 클릭 전 미리보기 마커. 스냅(꼭지점)이면 초록, 아니면 주황.
  - `clear_joint_ghost()` (L2589)
  - `_ensure_cand_visuals()` (L2596)
  - `show_joint_candidates()` (L2606) — 접합 가능한 후보점들을 하늘색 마커로 표시(첫 점 선택 후).
  - `clear_joint_candidates()` (L2618)
  - `show_joint_line_ghost()` (L2624) — 첫 점→현재 점을 잇는 접합 미리보기 선.
  - `clear_joint_line_ghost()` (L2634)
  - `pick_nearest_candidate()` (L2640) — 후보점들을 화면 투영해 마우스에 가장 가까운 것 반환 (point, idx)|None.
  - `highlight_ops_members()` (L2663) — ops 모델에서 여러 부재를 한 번에 파란색 강조.
  - `highlight_ops_node()` (L2687) — ops 모델에서 노드 nid 를 파란색 점으로 강조.
  - `get_native_widget()` (L2706)


### `modular_3d/schedule/__init__.py` (4 줄)
> 공정표 모듈 패키지.


### `modular_3d/schedule/schedule_adapter.py` (350 줄)
> 공정표 어댑터 — 우리 모델(scene + ProjectSettings) → 팀원 HTML 입력 직렬화.

**모듈 상수**
- `_MODULE_FLOOR_HEIGHT_MM` (L114) = `3000.0`
- `_ADJ_TOL_MM` (L132) = `400.0`
- `_OVERLAP_REJECT_MM` (L133) = `50`

**최상위 함수**
- `_aabb_xy_extent()` (L42) — 월드 AABB (xmin, ymin, zmin, xmax, ymax, zmax) — 회전·앵커 모두 반영.
- `_to_jsonable()` (L60) — numpy / 비표준 자료형을 JSON 안전형으로 변환.
- `_normalize_joint_records()` (L75) — joint_records 의 numpy 값들을 모두 native 형으로 변환.
- `_component_to_export_dict()` (L85) — 컴포넌트를 팀원 HTML 이 기대하는 component dict 로 직렬화.
- `_module_floor_span()` (L117) — 이 모듈이 점유하는 [시작층, 끝층] (inclusive).
- `_adjacent_pairs()` (L136) — 팀원 countHorizJointsFloor0 와 동일 산식: 인접쌍 카운트.
- `_is_module_kind()` (L156)
- `_build_summary()` (L160)
- `_module_types_from_components()` (L244) — 모듈 폭(m)·길이(m) 같은 타입을 묶어 카운트.
- `_region_key_for_html()` (L287) — ProjectSettings.region_city("천안 충남") → HTML REGION_GADONG 키("천안") 추출.
- `build_scene_data()` (L300) — 씬 + 프로젝트 설정 → 팀원 HTML 이 받을 dict.


### `modular_3d/schedule/tests/__init__.py` (0 줄)

### `modular_3d/schedule/tests/test_schedule_adapter.py` (164 줄)
> schedule_adapter 회귀 테스트 (Phase F).

**최상위 함수**
- `_mk_module()` (L27)
- `test_empty_scene_returns_zero_counts()` (L38)
- `test_two_floor_two_module_per_floor_counts()` (L52)
- `test_vertical_module_no_self_vertical_joints()` (L74)
- `test_vertical_module_horizontal_joints_per_floor_with_neighbor()` (L93)
- `test_json_serializable_with_numpy_and_rotation()` (L115)
- `test_core_slab_area_extracted()` (L138)
- `test_module_types_multi_type_extraction()` (L153)


### `modular_3d/transport/__init__.py` (27 줄)
> 운송 시뮬레이션 서브패키지.


### `modular_3d/transport/_snapshots/2026-05-26_운송_FFD/packer.py` (859 줄)
> 모듈·패널 → 운송 회차 산정 (FFD 빈 패킹).

**최상위 함수**
- `_module_compatible_trucks()` (L134)
- `_floor_panel_compatible_trucks()` (L138)
- `_wall_panel_compatible_trucks()` (L142)
- `_dependent_compatible_trucks()` (L147) — A-1: lshape/ㄷ자/3면/4면/부분벽 종속 floor 모두 동일 트럭.
- `_dependent_max_thickness()` (L153) — 종속 패널 1매 점유 두께 = floor 바닥(thickness) + 가장 높은 wall segment.
- `_dependent_free_inner_dims()` (L163) — wall_segments 가 점유한 변(과 두께)을 뺀 적층 가능 내공 (width, length).
- `_can_stack_on_dependent()` (L189) — cand 패널을 base(종속 floor) 위에 적층 가능한가?
- `_eff_truck_width()` (L225) — 화물이 트럭 폭보다 양쪽으로 각 side_overhang_mm 까지 튀어나와 결박
- `_effective_cargo_limit()` (L232) — 이 트럭에 실을 수 있는 화물 합산 무게 상한.
- `_diagnose_blocked()` (L254) — item 이 어떤 트럭으로도 못 실릴 때, "가장 적합한" 트럭의 위반 사유로
- `_max_modules_per_truck()` (L276)
- `_max_floor_panels_per_truck()` (L289)
- `_max_dependent_per_truck()` (L309) — 단일사양 종속 floor 1트럭 기저 배치 최대 매수 (적층 미포함).
- `_closest_fit_truck()` (L326) — 정규화 여유 합이 최소인 트럭 (큰 트럭 낭비 방지).
- `_pack_modules()` (L336)
- `_recompute_ppr()` (L376) — B-5 정정: 혼적 시 sample 첫 패널이 아닌 가장 긴 패널 기준으로 ppr 재계산.
- `_pack_horizontal_stacked()` (L384) — 플로어/벽 패널 공용 FFD — 무게 내림차순, 눕혀서 적층, 혼적 허용.
- `_pack_floor_panels()` (L473)
- `_pack_wall_panels()` (L480)
- `_pack_dependent_panels()` (L488) — A-1 일반화: wall_segments 가 있는 모든 floor 패널 + 원본 lshape 통합.
- `_trip_cargo_weight_with_extra()` (L629) — B-3 정정: stacked_items 무게를 포함한 화물 합.
- `_panel_overcount_reason()` (L634) — 패널 적재 불가 원인을 사람이 읽기 쉬운 문자열로 진단 (원본 이식 — D2).
- `recheck_trip_with_truck()` (L683) — 주어진 trip 의 화물을 new_truck 에 그대로 실을 수 있나 검사.
- `pack_items()` (L793) — 모듈·패널 → 회차 산정 (현장 제한 site + 트럭 한도로 검사).

**클래스**
- `class Trip` (L46)
  - `cargo_weight()` (L65) — 화물 무게 합산 (base + stacked). B-25 정정 — 적층 무게 누락 없음.
  - `gross_weight()` (L74) — 차체 + 화물 (GVW). B-25 신규 — 도로 한도 비교용.
  - `total_weight()` (L79) — [deprecated] 원본 호환 — cargo_weight 와 동일. 신규 코드는 gross_weight 사용.
  - `weight_utilization()` (L84)
  - `length_utilization()` (L90)
  - `utilization()` (L96)
  - `kind()` (L100)
- `class PackResult` (L110)
  - `total_trips()` (L115)
  - `module_trips()` (L119)
  - `panel_trips()` (L123)
  - `avg_utilization()` (L127)


### `modular_3d/transport/adapter.py` (1482 줄)
> 우리 씬 → 운송 입력 변환 단일 진입점 (분석 ④).

**최상위 함수**
- `to_transport_section()` (L121) — 디자인 단면 → 운송 Section. 각형강관(SHSSection)·H형강(HSection) 모두 처리.
- `lookup_section_for_member()` (L154) — 부재 mid → 채택 SHSSection (그룹 경유).
- `lookup_sections_for_component()` (L167) — 컴포넌트의 모든 부재 mid → (mid, SHSSection) 리스트.
- `_pick_heaviest()` (L185) — 단면 일관성 깨질 때 가장 무거운 단면 채택 (보수적).
- `_alpha_label()` (L193) — 0=a, 1=b, ... 25=z, 26=aa, ...
- `build_comp_meta()` (L204) — analysis_panel._fill_member_tree 와 동일 알고리즘으로 라벨 재구성.
- `_format_label()` (L234) — 'a모듈-#3' 식 라벨. xy_label + tname + floor 정보.
- `_classify_module_member()` (L241) — 부재 mid 가 기둥/보/캔틸 등 어느 카테고리인지 — group_categories 와 동일.
- `_sum_4_face_nonbearing_weight()` (L254) — 4 면 비내력벽 자중 합산 — 면별 한 면 면적 합산 (A-3, 양면 X).
- `_sum_v3_face_nonbearing_weight()` (L283) — 수직 3 모듈 12 면 합산 (사용자 결정 ⑥-4).
- `_slab_self_weight_kg()` (L312) — 슬래브 자중 (kg) = 면적(m²) × 두께(m) × 콘크리트 밀도(kg/m³).
- `_sum_mid_member_weight_in_module()` (L318) — 모듈 내부 MidBeam/MidColumn 강재 자중 (parent_id 기준).
- `_sum_cantilever_weight_attached_to()` (L349) — 부모 cid 에 매달린 캔틸 부재(보 + 슬래브) 자중.
- `_section_name_for_comp()` (L395) — 부재 cid 의 *대표* 단면명 (시각화 라벨용). 없으면 빈 문자열.
- `_world_to_parent_nominal()` (L404) — 월드 좌표 → 부모 컴포넌트의 *회전·앵커 풀린* nominal 좌표 (mm).
- `_nominal_aabb_of_points()` (L428) — 월드 좌표 점들을 모두 parent nominal 로 변환 + AABB.
- `_nominal_aabb_beam()` (L437) — 보의 nominal AABB — 시작/끝 + 단면 두께 반영.
- `_nominal_aabb_column()` (L458) — 기둥의 nominal AABB — base/top + 단면 폭/깊이.
- `_nominal_aabb_slab()` (L471) — 슬래브의 nominal AABB — 4 코너 (z = 하면) + thickness (z 방향).
- `_nominal_aabb_wallpanel()` (L481) — 벽 채움의 nominal AABB — 평면 4 코너 + thickness 방향 확장.
- `_nominal_to_transport_box()` (L507) — *nominal AABB → 운송 좌표계 박스* (x, y, z, L, W, H).
- `_make_body_part()` (L522) — nominal AABB → BodyPart (운송 좌표계 변환 후 생성). 영부피면 None.
- `_make_attached_part()` (L536) — nominal AABB → AttachedPart (운송 좌표계 변환 후 생성). 영부피면 None.
- `_collect_module_body_parts()` (L552) — 단층 모듈 — 기둥 4 + 하부보 4 + 상부보 4 + 슬래브 1 + 비내력벽 4.
- `_collect_floor_body_parts()` (L577) — 바닥패널 — 둘레 보 4 + 슬래브 1.
- `_collect_wall_body_parts()` (L591) — 구조벽 — 양쪽 기둥 2 + 상·하 런너 2 + 채움 1.
- `_collect_v3_body_parts()` (L613) — 수직3모듈 — 4 통기둥 + 층별 슬래브 받침보 4×3 + 옥상 보 4 + 슬래브 3.
- `_attached_part_kind()` (L639)
- `_collect_child_parts()` (L650)
- `_attached_part_nominal_aabb_OLD()` (L693) — 종속 부재의 nominal AABB (부모 좌표계 기준).
- `_remap_part_v3_lying()` (L736) — V3 lying 추가 회전 — 운송 좌표 재매핑.
- `_remap_attached_v3_lying()` (L760)
- `_remap_part_wall_lying()` (L769) — 독립 wall 패널의 *눕히기* 매핑 재적용.
- `_collect_attached_parts()` (L793) — 부모에 매달린 종속 부재(중간보·중간기둥·캔틸 보·캔틸 슬래브) 수집.
- `_z_normalize_parts()` (L826) — 모든 부재의 x/y/z 최저점이 0 이 되도록 *3 축* 평행이동.
- `_outer_xy_dims()` (L867) — 모든 부재의 외곽 길이/폭 (x 방향 최대, y 방향 최대).
- `convert_module()` (L889) — 씬 Module → 운송 Module.
- `convert_vertical3_lying()` (L960) — 수직 3 모듈을 옆으로 눕혀 운송 (사용자 정정).
- `_wall_to_segment()` (L1049) — StructWall → WallSegment (FloorPanel 변과 매칭).
- `_inject_wall_sections()` (L1080) — StructWall 에 단면을 임시 속성으로 주입 (wall_segment 변환용).
- `convert_floor_panel_pure()` (L1100) — 순수 floor (merged_wall_ids 빈 경우) → Panel(kind='floor', wall_segments=()).
- `convert_floor_panel_dependent()` (L1142) — 종속 floor (L자/ㄷ자/3면/4면) — wall_segments 채워서 Panel(kind='floor').
- `convert_independent_wall_panel()` (L1222) — 독립 wall → Panel(kind='wall').
- `build_transport_input()` (L1287) — 우리 씬 → 운송 입력 단일 진입점.
- `_normalize_dims_for_transport()` (L1406) — 모듈/패널의 length 가 width 보다 작은 경우 swap (긴 쪽 = length 정규화).

**클래스**
- `class TransportOptions` (L67) — 운송탭 UI 옵션. 분석 ⑥ 결정 반영.
- `class ExcludedItem` (L94)
- `class Diagnostics` (L101)
- `class TransportInput` (L107)
- `class TransportError(Exception)` (L116) — 어댑터 변환 실패 (선행 조건 미충족 등).


### `modular_3d/transport/benchmarks/__init__.py` (0 줄)

### `modular_3d/transport/benchmarks/run_validation.py` (283 줄)
> Phase 9 — 검증 보고서 자동 생성 (`run_validation`).

**최상위 함수**
- `_measure()` (L58) — 한 packer 실행 측정 — 시간 / 비용 / 적재율.
- `_run_fixture_mode()` (L77) — 한 픽스처 × 한 비용 모드 → (old, new_inf, new_500) 3 결과 반환.
- `_format_section()` (L123) — 한 픽스처 × 비용 모드 의 보고서 섹션 (markdown 표).
- `_format_summary()` (L166) — 전체 픽스처 × 모드 요약 — 합격 기준 비교.
- `run_validation()` (L201) — 모든 픽스처 × 비용 모드 × {기존, 신규} 돌리고 MD 보고서 생성.

**클래스**
- `class _OneRun` (L44) — 한 픽스처 × 비용 모드 × packer 의 실행 결과.


### `modular_3d/transport/cache.py` (336 줄)
> 운송 파이프라인 8단계 캐시 (분석 ⑧).

**최상위 함수**
- `_sha256()` (L42)
- `_coerce()` (L46) — JSON 직렬화용 변환 — numpy/타입 안전.
- `compute_scene_fp()` (L55) — 씬의 컴포넌트 구성으로 fingerprint 산출.
- `compute_classify_fp()` (L81)
- `compute_seg_fp()` (L91)
- `compute_ti_fp()` (L95)
- `compute_trucks_fp()` (L111)
- `compute_pack_fp()` (L125) — 패킹 캐시 키 — 입력 변경 시 자동 무효화.

**클래스**
- `class TransportCache` (L158) — 운송 파이프라인 8단계 lazy memoization (분석 ⑧).
  - `invalidate_from()` (L190) — step 이상의 모든 캐시 무효 (1~8).
  - `cache_status_flags()` (L214) — 8단계 상태 (UI 상태바용).
  - `set_analysis_result()` (L230) — [2] 구조해석 결과 주입 (UI '구조해석 실행' 버튼 → 호출).
  - `get_or_compute_pack()` (L241) — 파이프라인 [5]→[7] 진입점.
  - `get_or_compute_economics()` (L321) — [8] 운임 계산. pack_result 가 있어야 호출 가능.


### `modular_3d/transport/catalog_io.py` (155 줄)
> 트럭 카탈로그 로드/저장 (2 계층).

**모듈 상수**
- `_BUILTIN_DATA_DIR` (L37) = `Path(__file__).resolve().parent / 'data'`

**최상위 함수**
- `_project_data_dir()` (L40) — 프로젝트 오버라이드 디렉토리 경로 계산.
- `_load_trucks_from_file()` (L51) — 단일 JSON 파일에서 Truck 목록 로드. 검증 실패 시 위치 메시지 포함.
- `load_builtin_trucks()` (L73) — 패키지 내장 트럭 카탈로그 로드.
- `load_project_trucks()` (L78) — 프로젝트 오버라이드 트럭 카탈로그 로드. 파일 없으면 빈 리스트.
- `load_all_trucks()` (L86) — 내장 + 프로젝트 머지. project 가 같은 name 의 builtin 항목을 덮어쓴다.
- `save_project_truck()` (L100) — 프로젝트 트럭 카탈로그에 1 종 저장(추가 또는 갱신).
- `_truck_to_dict()` (L128)
- `validate_truck()` (L143) — dict 인풋 검증. 에러 메시지 리스트 반환 (빈 리스트 = OK).


### `modular_3d/transport/collision.py` (191 줄)
> Phase 4-B — 100mm 셀 3D 그리드 인덱스 (충돌 후보 가속 조회).

**최상위 함수**
- `_floor_div()` (L44) — 음수에서도 안전한 floor 나눗셈.
- `_cell_range_for_box()` (L49) — 박스가 점유하는 셀 인덱스 범위 (ix, iy, iz).
- `boxes_overlap()` (L150) — 두 박스가 겹치는가 (경계 접촉은 안 겹침으로 봄 = strict).
- `boxes_min_distance()` (L158) — 두 박스 사이 최소 3D 거리 (mm). 겹치면 0.
- `boxes_gap_ok()` (L173) — 두 박스가 *모든 3D 방향* 으로 min_gap_mm 이상 떨어졌는가.

**클래스**
- `class CollisionGrid` (L62) — 100mm 셀 3D 그리드 인덱스.
  - `__init__()` (L71)
  - `insert()` (L77) — boxes 를 owner_id 명의로 그리드에 등록.
  - `remove()` (L99) — owner_id 의 모든 박스 제거. 셀에서 owner_id 도 빼고, 빈 셀은 폐기.
  - `query_near()` (L117) — box (+ margin 확장) 와 셀이 겹치는 모든 owner_id set.
  - `boxes_of()` (L137) — 등록된 박스 리스트 (없으면 빈 리스트).
  - `all_owners()` (L141)
  - `clear()` (L144)


### `modular_3d/transport/economics.py` (280 줄)
> 운임 계산 — 거리(km) 기반 두 방식.

**모듈 상수**
- `FREIGHT_RATE_TABLE` (L29) = `{5: {'11톤': 11, '18톤': 16, '25톤': 18, '추레라': 20}, 10: {'11톤': 12, '18톤': 17, ...`

**최상위 함수**
- `freight_col()` (L53) — 톤수 기준 요금표 열 자동 매핑 (트럭 max_weight kg).
- `lookup_freight_rate()` (L65) — 왕복 거리 기준 요금(원). 계단식 — 해당 거리 이상인 첫 구간 요금.
- `_per_km_rate()` (L202) — 트럭 종류 → km단가. 저상/초저상은 lowbed 단가, 광폭은 extendable,
- `_fixed_per_trip_rate()` (L212) — 트럭 종류 → 1회 고정 운송비 (거리 무관).
- `compute_trip_cost()` (L222) — 회차 1 건의 운임 산정 (거리 기반 두 방식).
- `compute_economics()` (L263) — 패킹 결과 전체에 대한 운임 산정 (회차별 + 총합).

**클래스**
- `class EconomicsOptions` (L79) — 운임 계산 옵션 — *단일 진실원*.
  - `round_trip_km()` (L107) — 왕복 거리 (편도 × 2).
  - `fixed_per_trip_rate()` (L112) — fixed_per_trip 모드의 *트럭 종류 평균* 비용 (구 EcoOptions 호환용).
  - `fixed_rate_for_truck_type()` (L121) — 트럭 종류별 1회 고정비 (fixed_per_trip / freight_table 의 *예측치*).
  - `km_rate_for_truck()` (L136) — 트럭별 km단가 (per_km 모드).
  - `lowbed_km_rate()` (L147)
  - `extendable_km_rate()` (L151)
  - `aframe_km_rate()` (L155)
- `class TripCost` (L160) — 회차 1 건의 운임 정보.
  - `cost_label()` (L171)
- `class EconomicsResult` (L176) — 패킹 결과 전체에 대한 운임 합산.
  - `total_cost_krw()` (L182)
  - `freight_total_krw()` (L186)
  - `per_km_total_krw()` (L191)
  - `average_per_trip_krw()` (L195)


### `modular_3d/transport/legacy_packer.py` (859 줄)
> 모듈·패널 → 운송 회차 산정 (FFD 빈 패킹).

**최상위 함수**
- `_module_compatible_trucks()` (L134)
- `_floor_panel_compatible_trucks()` (L138)
- `_wall_panel_compatible_trucks()` (L142)
- `_dependent_compatible_trucks()` (L147) — A-1: lshape/ㄷ자/3면/4면/부분벽 종속 floor 모두 동일 트럭.
- `_dependent_max_thickness()` (L153) — 종속 패널 1매 점유 두께 = floor 바닥(thickness) + 가장 높은 wall segment.
- `_dependent_free_inner_dims()` (L163) — wall_segments 가 점유한 변(과 두께)을 뺀 적층 가능 내공 (width, length).
- `_can_stack_on_dependent()` (L189) — cand 패널을 base(종속 floor) 위에 적층 가능한가?
- `_eff_truck_width()` (L225) — 화물이 트럭 폭보다 양쪽으로 각 side_overhang_mm 까지 튀어나와 결박
- `_effective_cargo_limit()` (L232) — 이 트럭에 실을 수 있는 화물 합산 무게 상한.
- `_diagnose_blocked()` (L254) — item 이 어떤 트럭으로도 못 실릴 때, "가장 적합한" 트럭의 위반 사유로
- `_max_modules_per_truck()` (L276)
- `_max_floor_panels_per_truck()` (L289)
- `_max_dependent_per_truck()` (L309) — 단일사양 종속 floor 1트럭 기저 배치 최대 매수 (적층 미포함).
- `_closest_fit_truck()` (L326) — 정규화 여유 합이 최소인 트럭 (큰 트럭 낭비 방지).
- `_pack_modules()` (L336)
- `_recompute_ppr()` (L376) — B-5 정정: 혼적 시 sample 첫 패널이 아닌 가장 긴 패널 기준으로 ppr 재계산.
- `_pack_horizontal_stacked()` (L384) — 플로어/벽 패널 공용 FFD — 무게 내림차순, 눕혀서 적층, 혼적 허용.
- `_pack_floor_panels()` (L473)
- `_pack_wall_panels()` (L480)
- `_pack_dependent_panels()` (L488) — A-1 일반화: wall_segments 가 있는 모든 floor 패널 + 원본 lshape 통합.
- `_trip_cargo_weight_with_extra()` (L629) — B-3 정정: stacked_items 무게를 포함한 화물 합.
- `_panel_overcount_reason()` (L634) — 패널 적재 불가 원인을 사람이 읽기 쉬운 문자열로 진단 (원본 이식 — D2).
- `recheck_trip_with_truck()` (L683) — 주어진 trip 의 화물을 new_truck 에 그대로 실을 수 있나 검사.
- `pack_items()` (L793) — 모듈·패널 → 회차 산정 (현장 제한 site + 트럭 한도로 검사).

**클래스**
- `class Trip` (L46)
  - `cargo_weight()` (L65) — 화물 무게 합산 (base + stacked). B-25 정정 — 적층 무게 누락 없음.
  - `gross_weight()` (L74) — 차체 + 화물 (GVW). B-25 신규 — 도로 한도 비교용.
  - `total_weight()` (L79) — [deprecated] 원본 호환 — cargo_weight 와 동일. 신규 코드는 gross_weight 사용.
  - `weight_utilization()` (L84)
  - `length_utilization()` (L90)
  - `utilization()` (L96)
  - `kind()` (L100)
- `class PackResult` (L110)
  - `total_trips()` (L115)
  - `module_trips()` (L119)
  - `panel_trips()` (L123)
  - `avg_utilization()` (L127)


### `modular_3d/transport/limits.py` (121 줄)
> 현장 제한 + 트럭 + 적재 아이템 운송 가능성 검사기.

**최상위 함수**
- `_item_dims()` (L39) — (length, width, height, weight) 통일 추출.
- `can_carry()` (L58) — 단일 아이템 1개를 단일 트럭으로 현장 제한 아래 운송 가능한가?

**클래스**
- `class CarryResult` (L29) — can_carry 결과. ok=False 여도 reasons 에 모든 위반 사유 누적.
  - `__bool__()` (L35)


### `modular_3d/transport/loaded_3d_three.py` (1512 줄)
> 운송 적재 3D 도식 — Three.js + OrbitControls + CSS2DRenderer.

**모듈 상수**
- `_GROUP_COLORS` (L486) = `['#58a6ff', '#3fb950', '#d29922', '#bc8cff', '#ff7b72', '#79c0ff', '#56d364',...`
- `_HTML_TEMPLATE` (L599) = `'<!DOCTYPE html>\n<html>\n<head>\n<meta charset="utf-8">\n<title>운송 적재 3D 도식<...`

**최상위 함수**
- `_trip_signature()` (L40) — trip 의 *동일 결과* 판정용 시그니처.
- `_classify_item()` (L84) — 화물 종류 식별 — 색상 매핑용.
- `_serialize_wall_segment()` (L97) — wall_segment 위치 (m 단위) 산출 — floor 좌하단 기준 + 적재면 위.
- `_nominal_dims_for_visual()` (L147) — *시각화 박스* 의 nominal 차원 (mm) — 부재 외곽이 아닌 부모 본체 치수.
- `_serialize_attached_part()` (L162) — AttachedPart → JSON. 부모 화물의 좌하단(월드) 기준 위치 (m).
- `_serialize_body_parts()` (L180) — item.body_parts (운송 좌표계 BodyPart) → 화물 좌하단 기준 m 단위 dict 리스트.
- `_serialize_module_parts_LEGACY()` (L203) — [폐기 — Phase 3-fix] 추정 분해. body_parts 우선 사용. 호환용 보존.
- `_serialize_floor_panel_parts()` (L289) — floor 패널 = 둘레 보 4 + 슬래브 1 분해.
- `_serialize_wall_panel_parts()` (L322) — 벽 패널(kind='wall') = 양쪽 기둥 2 + 위·아래 보 2 + 채움 분해.
- `_serialize_placement()` (L373) — Placement → JSON dict (m 단위). 트럭 좌측 끝 = x=0 기준.
- `_serialize_legacy_item()` (L436) — 역호환 — Trip.placements 비어있을 때 items 만으로 위치 추정.
- `serialize_pack_for_three()` (L492) — PackResult.trips → Three.js scene 구축용 JSON dict.
- `build_loaded_3d_three_html()` (L1496) — Three.js HTML 문서 생성 — PackResult.trips 데이터를 인라인 임베드.


### `modular_3d/transport/models.py` (475 줄)
> 운송 시뮬레이션 데이터모델.

**클래스**
- `class Section` (L66) — KS 표준 강재 단면 (원본 그대로).
  - `__post_init__()` (L80)
- `class BodyPart` (L114) — 부모 화물(모듈·바닥패널·벽패널·수직3모듈) *자체* 부재 한 개의 박스.
  - `__post_init__()` (L135)
- `class AttachedPart` (L153) — 부모 화물에 종속된 *자식 컴포넌트의 부재 한 개* 박스.
  - `__post_init__()` (L179)
- `class WallSegment` (L201) — 플로어 패널의 한 변(또는 변의 일부)에 매달린 벽 세그먼트.
  - `__post_init__()` (L217)
- `class Module` (L229) — 수평/수직 모듈. 무게는 프레임 + extra_weight_kg 자동 합산.
  - `__post_init__()` (L250)
  - `weight()` (L257)
- `class Panel` (L270) — 플로어/벽체 패널 (다면 종속 일반화 모델).
  - `__post_init__()` (L301)
  - `_side_length()` (L328) — 변 인덱스 → 변의 길이 (mm). 0/2 변 → 길이(length), 1/3 변 → 폭(width).
  - `weight()` (L333) — [주의] Phase 1 은 운송프로그램 원본 식 그대로 보존.
- `class Truck` (L374) — 모듈러 운송 차량.
  - `__post_init__()` (L401)
- `class SiteLimit` (L420) — 현장 운송 제한 — 공장→현장 경로(진입로 포함)가 허용하는 한도.
  - `__post_init__()` (L439)
- `class SpacingParams` (L449) — 패널 적재 간격 (mm). 2026-05-26 이후 사용자 입력이 아닌 내장 고정값.
  - `__post_init__()` (L464)


### `modular_3d/transport/packer.py` (864 줄)
> 모듈·패널 → 운송 회차 산정.

**최상위 함수**
- `_module_compatible_trucks()` (L143)
- `_floor_panel_compatible_trucks()` (L147)
- `_wall_panel_compatible_trucks()` (L151)
- `_dependent_compatible_trucks()` (L156) — A-1: lshape/ㄷ자/3면/4면/부분벽 종속 floor 모두 동일 트럭.
- `_dependent_max_thickness()` (L162) — 종속 패널 1매 점유 두께 = floor 바닥(thickness) + 가장 높은 wall segment.
- `_dependent_free_inner_dims()` (L172) — wall_segments 가 점유한 변(과 두께)을 뺀 적층 가능 내공 (width, length).
- `_can_stack_on_dependent()` (L198) — cand 패널을 base(종속 floor) 위에 적층 가능한가?
- `_eff_truck_width()` (L234) — 화물이 트럭 폭보다 양쪽으로 각 side_overhang_mm 까지 튀어나와 결박
- `_effective_cargo_limit()` (L241) — 이 트럭에 실을 수 있는 화물 합산 무게 상한.
- `_diagnose_blocked()` (L263) — item 이 어떤 트럭으로도 못 실릴 때, "가장 적합한" 트럭의 위반 사유로
- `_max_modules_per_truck()` (L285)
- `_max_floor_panels_per_truck()` (L298)
- `_max_dependent_per_truck()` (L318) — 단일사양 종속 floor 1트럭 기저 배치 최대 매수 (적층 미포함).
- `_closest_fit_truck()` (L335) — 정규화 여유 합이 최소인 트럭 (큰 트럭 낭비 방지).
- `_pack_modules()` (L345)
- `_recompute_ppr()` (L385) — B-5 정정: 혼적 시 sample 첫 패널이 아닌 가장 긴 패널 기준으로 ppr 재계산.
- `_pack_horizontal_stacked()` (L393) — 플로어/벽 패널 공용 FFD — 무게 내림차순, 눕혀서 적층, 혼적 허용.
- `_pack_floor_panels()` (L482)
- `_pack_wall_panels()` (L489)
- `_pack_dependent_panels()` (L497) — A-1 일반화: wall_segments 가 있는 모든 floor 패널 + 원본 lshape 통합.
- `_trip_cargo_weight_with_extra()` (L638) — B-3 정정: stacked_items 무게를 포함한 화물 합.
- `_panel_overcount_reason()` (L643) — 패널 적재 불가 원인을 사람이 읽기 쉬운 문자열로 진단 (원본 이식 — D2).
- `recheck_trip_with_truck()` (L692) — 주어진 trip 의 화물을 new_truck 에 그대로 실을 수 있나 검사.
- `pack_items()` (L802) — 모듈·패널 → 회차 산정 — Best-Fit + 다중 시드 + VND + 무게중심 보정.

**클래스**
- `class Trip` (L55)
  - `cargo_weight()` (L74) — 화물 무게 합산 (base + stacked). B-25 정정 — 적층 무게 누락 없음.
  - `gross_weight()` (L83) — 차체 + 화물 (GVW). B-25 신규 — 도로 한도 비교용.
  - `total_weight()` (L88) — [deprecated] 원본 호환 — cargo_weight 와 동일. 신규 코드는 gross_weight 사용.
  - `weight_utilization()` (L93)
  - `length_utilization()` (L99)
  - `utilization()` (L105)
  - `kind()` (L109)
- `class PackResult` (L119)
  - `total_trips()` (L124)
  - `module_trips()` (L128)
  - `panel_trips()` (L132)
  - `avg_utilization()` (L136)


### `modular_3d/transport/packer_balance.py` (266 줄)
> Phase 7 — 무게중심 최종 시각 보정 (`balance_trips`).

**최상위 함수**
- `_floor_frame_weight()` (L31) — 둘레 보 4 변의 자중 (kg).
- `_seg_beam_weight()` (L37)
- `_seg_column_weight()` (L41)
- `_panel_local_cog()` (L45) — 패널 *좌하단 원점* 기준 무게중심 (x, y) (mm).
- `_placement_cog_xy()` (L99) — 한 placement 의 *트럭 좌표계* 무게중심 (x, y).
- `_compute_trip_cog()` (L121) — 회차 무게중심 (xC, yC) — 트럭 적재함 중심 원점.
- `_within_truck_bounds()` (L139) — new_x, new_y 위치에 패널이 트럭 적재함 안에 들어가는가.
- `_no_overlap_with_others()` (L157) — idx 의 placement 를 (new_x, new_y) 로 옮길 때 같은 단의 다른 placement
- `_balance_one_trip()` (L187) — 같은 단·자리 내에서 10 mm 격자 슬라이드 — 회차 COG 거리 최소화.
- `balance_trips()` (L240) — 모든 회차 완성 후 회차별 1 회 무게중심 보정.


### `modular_3d/transport/packer_core.py` (1454 줄)
> Phase 4 — Best-Fit 자유 배치 단일 패킹 (`pack_one_seed`).

**모듈 상수**
- `_DEFAULT_STACK_BONUS` (L373) = `1.0`
- `_DEFAULT_DEP_BONUS` (L374) = `1.5`
- `SHUFFLED_STRATEGY` (L751) = `GreedyStrategy(name='shuffled', sort_items_fn=lambda items: list(items), truc...`

**최상위 함수**
- `default_eco_options()` (L58) — 기본 비용 옵션 — EconomicsOptions() 인스턴스.
- `_is_simple_wall()` (L66) — 단순 wall 패널(kind=wall, wall_segments 미사용)인가.
- `_is_dependent_floor()` (L75) — 종속 floor (wall_segments 가 있거나 호환용 kind=lshape)인가.
- `_postures_for()` (L82) — 이 화물에 시도할 자세 후보.
- `_truck_posture_compatible()` (L93) — 트럭 — 자세 호환성.
- `_allowed_slots_for()` (L105) — 자세·화물 종류별 허용 자리.
- `_volume_of()` (L119) — 볼륨 정렬용 (mm³).
- `_find_stack_bottom_idx()` (L130) — STACK 자리: 새 단 아래에 깔릴 placement 인덱스.
- `_find_dep_inner_parent_idx()` (L158) — DEP_INNER 자리: 이 item 을 종속 패널의 빈 슬롯에 넣을 수 있는 부모 인덱스.
- `_compute_xyz()` (L192) — 트럭 적재함 중심 원점 좌표 (mm).
- `evaluate_slot()` (L242) — 자리 평가 — 미통과면 (-inf, None). 통과면 (점수, meta dict) 반환.
- `evaluate_slot_v2()` (L377) — V2 자리 평가 — 4-tuple weights (α, β, γ, δ).
- `_commit_placement()` (L527) — TruckState 에 Placement 추가 + 누적 자원 갱신.
- `_simulate_packing_weight()` (L571) — 후보 트럭 1 대에 items 를 무게 내림차순 simple-FFD 로 적재해 실리는 무게 합.
- `default_truck_score()` (L621) — *그 트럭 1회 비용*. 낮을수록 우선.
- `make_truck_pref_score()` (L646) — *특정 트럭 우선* 점수 함수 팩토리.
- `cost_efficiency_truck_score()` (L662) — 트럭 자체 효율 — 1회 고정비 / 트럭 실효 적재능력 (원/kg).
- `_dependent_first_sorter()` (L705) — 종속 패널 묶음을 정렬 키 앞에, 그 안에서 무게 내림차순.
- `_is_in_lookahead()` (L769)
- `_filter_compat_trucks()` (L773) — 호환 트럭 후보 + 거부 사유 분리.
- `_select_best_new_truck()` (L807) — 새 트럭 선정 — *진짜 lookahead* 로 결과 기반 결정.
- `_quick_simulate_pack()` (L940) — lookahead 용 *빠른* 그리디 패킹 (V1 단순). 내부 select 는 재귀 fallback.
- `_merge_small_modules()` (L957) — 모듈만 든 회차 중 *모듈 1 개 + 길이 ≤ 4500mm* 인 회차들을 2 개씩 합쳐 회차 절감.
- `_truck_state_to_trip()` (L1028) — TruckState 의 placements 를 기존 Trip 데이터 구조로 변환.
- `pack_one_seed()` (L1098) — 한 그리디 전략 + 한 가중치 조합으로 1 회 패킹 → 1 후보 해 반환.
- `_sync_grid_for_trucks()` (L1205) — 각 트럭 상태에 대한 CollisionGrid 동기화 (commit 직후 호출).
- `_verify_v2_collision()` (L1232) — commit 직전 V2 페어와이즈 충돌 검사.
- `pack_one_seed_v2()` (L1266) — V2 그리디 패커 — 비용 인식 자리 점수 + 부재 페어와이즈 충돌 검사.

**클래스**
- `class GreedyStrategy` (L698) — 그리디 전략 — 정렬 + 트럭선정 정책의 결합.


### `modular_3d/transport/packer_local_search.py` (510 줄)
> Phase 6 — VND (Variable Neighborhood Descent) 5 종 수술.

**최상위 함수**
- `_trip_to_truck_state()` (L42) — Trip.placements 로부터 TruckState 재구성.
- `_pack_to_truck_states()` (L60)
- `_truck_states_to_pack()` (L66)
- `_clone_truck_state()` (L81) — TruckState 깊은 복사 — placements 의 누적 자원도 다시 누적.
- `_build_truck_state_without()` (L92) — ts 에서 p_idx 의 placement 를 제거한 새 TruckState.
- `_try_add_to_truck()` (L118) — item 을 ts 에 Best-Fit 으로 추가 시도. 성공 시 *새* TruckState 반환.
- `_check_module_merge_constraint()` (L153) — target_state 에 new_item 을 추가했을 때 모듈 합산 규칙을 만족하는가.
- `op_transfer()` (L174) — 회차 A의 화물 한 장을 B로 이동. 비용 감소 발견 시 즉시 채택.
- `op_swap()` (L225) — 회차 A의 한 장 ↔ B의 한 장 교환. 비용 감소 시 채택.
- `op_posture_toggle()` (L278) — 단순 wall 패널 한 장의 자세 토글 + 재배치. 비용 감소 시 채택.
- `op_merge()` (L325) — 두 회차 A·B의 모든 화물을 한 트럭에 다시 패킹. 성공 시 회차 1 절감.
- `op_truck_downgrade()` (L393) — 회차의 트럭을 더 싼 트럭으로 교체 시도. 성공 시 비용 감소.
- `vnd_improve()` (L447) — VND — 작은 수술 → 큰 수술 순. 큰 수술 개선 시 작은 수술로 복귀.


### `modular_3d/transport/packer_meta.py` (518 줄)
> Phase 5 — 다중 시드 + Lower Bound 가지치기 (`pack_all_seeds`).

**최상위 함수**
- `trip_cost()` (L46) — 단일 회차의 비용 (원).
- `compute_cost()` (L57) — PackResult 전체 비용 합산.
- `_dummy_truck()` (L78) — 비용 계산 헬퍼용 — 단가 조회만 사용. 실패 처리는 무시.
- `lower_bound_cost()` (L89) — 비용 하한 — 모듈 합산(4.5m) 반영한 회차수 하한 × 모드별 최저 단가.
- `pack_all_seeds()` (L163) — 54 시드 (결정론 24 + 무작위 30) 돌리고 비용 최저 채택.
- `_item_full_signature()` (L299) — 완전 동일 화물 판별 시그너처 — 모든 형상·무게·종속 부재까지 비교.
- `_group_identical_items()` (L350) — 완전 동일 화물 그룹화 — 그룹당 *대표 1개* 리스트.
- `_adaptive_threshold()` (L369) — LB 가지치기 임계 동적 전환 — 85% → 75%.
- `pack_all_seeds_v2()` (L392) — V2 메타 패커 — 비용 인식 자리 점수 + 부재 페어와이즈 충돌 + LB 가지치기.


### `modular_3d/transport/packer_safety.py` (497 줄)
> Phase 2 — 새 패커의 안전 검사 일원화.

**최상위 함수**
- `_nominal_dims_for_posture()` (L45) — 컴포넌트 자체의 nominal 차원 (부재 포함 외곽 X — 단순 본체 박스).
- `boxes_of_component()` (L59) — 컴포넌트의 모든 부재 박스 리스트 (트럭 좌표 mm).
- `component_outer_aabb()` (L115) — 컴포넌트의 *모든 부재* 외곽 AABB. 그리드 인덱스 후보 조회용.
- `_expand_with_attachments()` (L135) — 부착 부재(attached_parts) 의 박스를 부모 화물 박스와 합쳐 외곽 차원 산출.
- `_item_dims_for_posture()` (L160) — 화물의 점유 차원 → (length, width, height_occupied, weight).
- `can_place()` (L207) — 이 트럭의 현재 상태에 *이 화물을 이 자세로 이 자리에* 놓을 수 있는가?
- `_trip_xyz_from_truck_center()` (L349) — packer 가 쓰는 *적재함 중심 원점* 좌표 → 컴포넌트 *좌하단* mm 변환.
- `can_place_v2()` (L372) — V2 안전 검사 — 부재 페어와이즈 + 갭 100 + 트럭 한도 + 폭 ±200 돌출.


### `modular_3d/transport/packer_types.py` (113 줄)
> Phase 2 — 새 패커의 데이터 구조 정의.

**클래스**
- `class Posture(str, Enum)` (L20) — 화물 적재 자세.
- `class PlacementSlot(str, Enum)` (L27) — 트럭 안에서 화물이 놓이는 자리 종류.
- `class Placement` (L39) — 한 화물의 트럭 내 배치 메타데이터.
- `class TruckState` (L55) — 패킹 도중 한 트럭(=한 회차)의 잔여 자원 추적.
  - `remaining_cargo_limit()` (L70) — 잔여 화물 적재 가능 무게 (kg). 0 미만이면 0 으로 보정.
  - `remaining_floor_length()` (L74) — 잔여 트럭 바닥 길이 (mm). 의사코드 § 2.1 식.
  - `used_inner_height()` (L78) — 현재까지 쌓인 단의 총 점유 높이 (mm).
  - `n_layers()` (L90) — 현재 단 수 (시각화·UI 용).
- `class CheckResult` (L97) — 안전 검사 통과 여부 + 위반 사유들.
  - `__bool__()` (L102)


### `modular_3d/transport/tests/__init__.py` (0 줄)

### `modular_3d/transport/tests/test_adapter.py` (380 줄)
> Phase 3 단위 테스트 — adapter.

**모듈 상수**
- `SHS_NAME` (L155) = `'SHS 200x200x8'`
- `SHS_SMALL` (L156) = `'SHS 150x150x6'`

**최상위 함수**
- `_attach_module_to_model()` (L68) — 씬 모듈 cid 에 대해 stub 모델 + design_result 채우기.
- `_attach_floor_panel()` (L103)
- `_attach_struct_wall()` (L124)
- `_make_scene_module()` (L159)
- `test_empty_scene()` (L168)
- `test_none_design_result_raises()` (L178)
- `test_none_scene_raises()` (L185)
- `test_single_module_converts()` (L193)
- `test_floor_panel_with_one_merged_wall()` (L213)
- `test_floor_panel_with_two_merged_walls()` (L242)
- `test_independent_struct_wall()` (L268)
- `test_core_excluded()` (L282)
- `test_to_transport_section_roundtrip()` (L298)
- `test_label_format()` (L309)
- `test_vertical3_lying_mapping()` (L325)
- `test_build_comp_meta_alpha_label()` (L344)
- `test_to_transport_section_shs()` (L358) — 각형강관 단면 변환 — type=SHS, w/h/t 매핑.
- `test_to_transport_section_h()` (L370) — H형강 단면 변환 — type=H, B/H/t1/t2 매핑 + 단위중량 일치 (버그 수정).

**클래스**
- `class _StubMember` (L32)
  - `__init__()` (L33)
- `class _StubNode` (L41)
  - `__init__()` (L42)
- `class _StubAM` (L47)
  - `__init__()` (L48)
- `class _StubGroup` (L54)
  - `__init__()` (L55)
- `class _StubDR` (L61)
  - `__init__()` (L62)


### `modular_3d/transport/tests/test_adapter_normalize.py` (239 줄)
> Phase A 단위 테스트 — `_normalize_dims_for_transport` (어댑터 정규화).

**모듈 상수**
- `_SHS` (L22) = `Section(name='SHS200x8', section_type='SHS', width=200, height=200, thickness...`

**최상위 함수**
- `_make_module()` (L29)
- `_make_floor()` (L36)
- `_make_wall()` (L43) — 단순 wall: length=벽 길이, width=벽 높이.
- `_seg()` (L53)
- `_make_lshape()` (L62) — L 자 패널 — 한 변에 wall_segment.
- `_make_4face()` (L74)
- `test_module_swap_when_length_less_than_width()` (L89) — 길이 3400, 폭 4500 → swap 후 (length=4500, width=3400).
- `test_module_idempotent_when_already_normalized()` (L98) — 이미 length ≥ width → 변화 없음.
- `test_module_unchanged_when_square()` (L106) — 정사각형 (length == width) → 변화 없음.
- `test_module_preserves_sections_and_weight()` (L114) — swap 후에도 단면·extra_weight_kg 보존.
- `test_pure_floor_swap_when_length_less_than_width()` (L126)
- `test_pure_floor_idempotent()` (L135)
- `test_simple_wall_not_swapped_even_when_length_less_than_width()` (L145) — 단순 wall (벽 길이 < 벽 높이) 도 swap 안 함 — 물리 의미 보존.
- `test_simple_wall_normal_case_unchanged()` (L153) — 정상 케이스 (벽 길이 > 벽 높이) 도 변화 없음.
- `test_dependent_floor_side_rotated_plus_1_mod_4()` (L170) — swap 발생 시 wall_segments.side 가 (side+1) % 4 로 회전.
- `test_dependent_floor_segment_dimensions_preserved()` (L180) — 세그먼트 자체 치수 (length_mm, start_offset_mm, height_mm, thickness_mm) 보존.
- `test_dependent_floor_idempotent_when_already_normalized()` (L199) — 이미 length ≥ width 인 종속 floor 는 side 도 그대로.
- `test_dependent_floor_4face_all_sides_rotated()` (L208) — 4 면 종속 — 4 개 side 모두 +1 mod 4 회전.
- `test_normalize_does_not_break_packer()` (L220) — 정규화된 모듈·패널이 신규 패커로 정상 패킹.


### `modular_3d/transport/tests/test_cache.py` (233 줄)
> Phase 5 단위 테스트 — cache.py.

**최상위 함수**
- `_make_scene_and_design()` (L35)
- `_make_inputs()` (L45)
- `test_first_call_computes_all()` (L53)
- `test_second_call_same_input_returns_cached()` (L72)
- `test_unit_weight_change_invalidates_from_step_6()` (L83)
- `test_invalidate_from_step_3_clears_design_and_below()` (L99)
- `test_invalidate_from_step_7_only_clears_pack()` (L126)
- `test_cache_status_flags_initial_all_stale()` (L140)
- `test_cache_status_flags_after_pack()` (L145)
- `test_scene_fp_deterministic()` (L157)
- `test_scene_fp_changes_on_mutation()` (L164)
- `test_classify_fp_changes_on_option_change()` (L175)
- `test_trucks_fp_changes_on_truck_edit()` (L184)
- `test_set_analysis_result_invalidates_below()` (L196)
- `test_economics_after_pack()` (L208)
- `test_economics_requires_pack_first()` (L219)
- `test_current_policy_tracking()` (L226)


### `modular_3d/transport/tests/test_catalog_io.py` (128 줄)
> Phase 1 단위 테스트 — catalog_io.py.

**최상위 함수**
- `test_load_builtin_trucks_count()` (L23)
- `test_aframe_is_inactive_in_builtin()` (L32)
- `test_load_all_trucks_active_filter()` (L39)
- `test_load_project_trucks_empty_when_no_dir()` (L48)
- `test_save_then_load_project_truck_roundtrip()` (L52)
- `test_project_overrides_builtin()` (L68) — 프로젝트가 동일 name 의 내장 트럭을 덮어쓰는지.
- `test_save_project_truck_replaces_existing()` (L89) — 같은 name 두번 저장 시 갱신만 되는지.
- `test_validate_truck_ok()` (L103)
- `test_validate_truck_bad_type()` (L109)
- `test_truck_json_with_invalid_item_reports_index()` (L117)


### `modular_3d/transport/tests/test_collision.py` (162 줄)
> Phase 4-B 단위 테스트 — CollisionGrid + 박스 헬퍼.

**최상위 함수**
- `test_floor_div_positive()` (L21)
- `test_floor_div_negative()` (L28) — 음수에서 표준 // 처럼 floor 동작.
- `test_cell_range_basic()` (L36) — 0~100 박스는 셀 (0,) 한 칸만 점유 (max 면이 셀 경계).
- `test_cell_range_spanning()` (L45) — 0~250 박스는 셀 0, 1, 2 = 3 칸 점유.
- `test_boxes_overlap_yes()` (L53)
- `test_boxes_overlap_touch()` (L59) — 경계 접촉은 겹침 X.
- `test_boxes_min_distance_apart()` (L66)
- `test_boxes_min_distance_diagonal()` (L72) — 대각 분리 — 유클리드 거리.
- `test_boxes_gap_ok_axis_separation()` (L80) — 축 분리 정책 — 한 축이라도 100 이상이면 OK.
- `test_boxes_gap_ok_insufficient()` (L88)
- `test_grid_insert_and_query_basic()` (L96)
- `test_grid_query_with_margin()` (L105) — margin=100 으로 갭 검사.
- `test_grid_remove()` (L119)
- `test_grid_multi_boxes_per_owner()` (L132) — 한 owner 가 여러 박스 (예: 컴포넌트의 부재 여러 개).
- `test_grid_boxes_of()` (L146)
- `test_grid_negative_coords()` (L157) — 음수 좌표 (캔틸이 부모 -x 방향 돌출 등) 도 정상.


### `modular_3d/transport/tests/test_economics.py` (164 줄)
> 단위 테스트 — economics.py (거리 km 기반 두 방식).

**모듈 상수**
- `SHS` (L20) = `Section(name='SHS200x8', section_type='SHS', width=200, height=200, thickness...`
- `TR_LOWBED` (L23) = `Truck(name='저상24t', truck_type='lowbed', max_length=12000, max_width=3000, ma...`
- `TR_EXTEND` (L27) = `Truck(name='광폭28t', truck_type='extendable', max_length=18000, max_width=3400...`

**최상위 함수**
- `_trip()` (L33)
- `test_freight_table_default_mode()` (L42)
- `test_per_km_lowbed()` (L55)
- `test_per_km_extendable()` (L65)
- `test_per_km_aframe()` (L74)
- `test_compute_economics_aggregate()` (L89)
- `test_compute_economics_empty()` (L106)
- `test_cost_label_format()` (L113)
- `test_freight_table_step_function()` (L123)
- `test_freight_col_mapping()` (L133)
- `test_fixed_per_trip_distance_independent()` (L145)
- `test_fixed_per_trip_by_truck_type()` (L158)


### `modular_3d/transport/tests/test_integration.py` (120 줄)
> Phase 9 통합(E2E) 테스트 — 씬 → 어댑터 → 패커 → 운임 / 캐시.

**최상위 함수**
- `_trucks()` (L31)
- `_site()` (L42)
- `_build_two_module_scene()` (L47)
- `test_full_pipeline_scene_to_economics()` (L59)
- `test_cache_hit_and_invalidate()` (L81)
- `test_cache_invalidate_from_levels()` (L102)


### `modular_3d/transport/tests/test_limits.py` (173 줄)
> 단위 테스트 — limits.py (현장 제한 SiteLimit + 트럭 한도, GVW 기본).

**모듈 상수**
- `SHS` (L20) = `Section(name='SHS200x8', section_type='SHS', width=200, height=200, thickness...`
- `TR_24` (L23) = `Truck(name='저상24t', truck_type='lowbed', max_length=12000, max_width=3000, ma...`
- `TR_WIDE` (L28) = `Truck(name='광폭28t', truck_type='extendable', max_length=18000, max_width=3400...`
- `SITE_GO` (L35) = `SiteLimit(max_gvw_kg=40000, max_width_mm=3500, max_height_mm=4500)`
- `SITE_NORMAL` (L36) = `SiteLimit(max_gvw_kg=25000, max_width_mm=3300, max_height_mm=4200)`
- `SITE_NARROW` (L37) = `SiteLimit(max_gvw_kg=15000, max_width_mm=3200, max_height_mm=4000)`
- `SITE_NONE` (L38) = `SiteLimit()`

**최상위 함수**
- `_frame_kg()` (L41) — SHS 단면으로 6×3×3.4 모듈 프레임 무게.
- `_mod()` (L46)
- `test_module_basic_pass()` (L54)
- `test_length_truck_only()` (L62)
- `test_width_truck_limit()` (L72)
- `test_width_overhang_allows_400()` (L79)
- `test_width_site_limit_binds_on_wide_truck()` (L86) — 광폭 트럭(3400)엔 들어가지만 현장 폭(3300)에 막히는 화물.
- `test_height_truck_limit()` (L96)
- `test_height_site_limit_binds()` (L103) — 외측 높이가 트럭 4500엔 OK지만 현장 4000엔 막힘.
- `test_weight_truck_capacity()` (L113) — 화물이 트럭 적재능력(24t) 초과 → 막힘.
- `test_weight_site_gvw_blocks()` (L120) — 화물은 트럭 24t 이내지만 차체+화물(GVW)이 현장 25t 초과 → 막힘.
- `test_site_none_skips_all_site_checks()` (L133) — 현장 제한 전부 None → 트럭 한도만으로 판정.
- `test_panel_pure_floor_pass()` (L144)
- `test_panel_with_wall_segments_height_uses_max_seg()` (L150) — B-14: wall_segments 있으면 thickness + max(seg.height) 가 화물 높이.
- `test_width_site_tighter_than_overhang()` (L165)


### `modular_3d/transport/tests/test_loaded_3d_three.py` (251 줄)
> Three.js 운송 3D 도식 단위 테스트.

**모듈 상수**
- `_SHS` (L34) = `Section(name='SHS200x8', section_type='SHS', width=200, height=200, thickness...`

**최상위 함수**
- `_lowbed()` (L38)
- `_module()` (L45)
- `test_serialize_empty_trips()` (L54)
- `test_serialize_module_trip_unit_conversion()` (L62) — mm → m 단위 환산 확인.
- `test_serialize_same_group_trips_stack_in_y_direction()` (L84) — *같은 트럭 + 같은 화물 (이름 무관)* 회차 2 개 → 같은 그룹 →
- `test_serialize_different_groups_separate_in_x_direction()` (L110) — 다른 트럭 종류 → 다른 그룹 → *X 방향(진행 방향)* 으로 분리.
- `test_serialize_dependent_floor_wall_segments()` (L138) — L 자 → wall_segments 가 직렬화 결과에 포함, 위치 정확.
- `test_serialize_highlight_trip_no_and_costs()` (L162) — 강조 trip_no + 비용 dict 가 그대로 전달.
- `test_serialize_legacy_trip_without_placements()` (L175) — placements 비어있는 Trip 도 items 로 직렬화.
- `test_build_html_contains_threejs_cdn()` (L187) — HTML 안에 Three.js CDN + OrbitControls + CSS2DRenderer 스크립트 태그.
- `test_build_html_inlines_pack_data_json()` (L205) — HTML 안에 PACK_DATA JSON 이 인라인 임베드.
- `test_build_html_empty_trips_still_returns_html()` (L218) — 빈 trips 도 정상 HTML 반환 (안내 메시지 표시).
- `test_build_html_with_real_pack_result()` (L226) — 실제 픽스처 패킹 결과로 HTML 생성 — 예외 없이.
- `test_serialized_data_is_valid_json()` (L242) — 직렬화 결과가 표준 JSON 으로 dumps 가능.


### `modular_3d/transport/tests/test_models.py` (228 줄)
> Phase 1 단위 테스트 — models.py.

**모듈 상수**
- `SHS_200` (L22) = `Section(name='SHS 200x200x8', section_type='SHS', width=200.0, height=200.0, ...`
- `SHS_150` (L28) = `Section(name='SHS 150x150x6', section_type='SHS', width=150.0, height=150.0, ...`

**최상위 함수**
- `test_section_invalid_type_raises()` (L37)
- `test_section_negative_width_raises()` (L42)
- `test_module_weight_b11_correct_formula()` (L48) — Module.weight 가 4(w+l) 정답 식과 일치하는지 검증 (B-11).
- `test_module_weight_not_double_of_original_buggy()` (L62) — 원본의 2배 오버 식 결과와 정답이 다르다는 것을 명시 검증.
- `test_module_extra_weight_negative_raises()` (L76)
- `test_panel_pure_floor_weight()` (L84) — 순수 floor (wall_segments 없음) — 둘레 보 4개 기준.
- `test_panel_floor_with_one_wall_segment_lshape()` (L92) — floor + wall_segment 1개 = L자 일반화. weight 가 둘레보 + 세그먼트 합산.
- `test_panel_floor_with_three_segments_3face()` (L106) — 3면 종속 floor. 4면 종속도 가능하지만 3면 검증.
- `test_panel_wall_independent()` (L125)
- `test_panel_wall_with_segments_raises()` (L133)
- `test_panel_invalid_kind_raises()` (L141)
- `test_panel_wall_without_column_section_raises()` (L147)
- `test_panel_segment_exceeds_side_length_raises()` (L153)
- `test_wall_segment_invalid_side_raises()` (L163)
- `test_wall_segment_negative_offset_raises()` (L169)
- `test_truck_basic_construction_with_new_fields()` (L176)
- `test_truck_invalid_type_raises()` (L184)
- `test_truck_negative_curb_weight_raises()` (L190)
- `test_truck_default_active_true()` (L197)
- `test_site_limit_defaults_all_none()` (L205)
- `test_site_limit_negative_raises()` (L212)
- `test_spacing_defaults()` (L219)
- `test_spacing_negative_raises()` (L226)


### `modular_3d/transport/tests/test_packer.py` (325 줄)
> Phase 2 단위 테스트 — packer.py.

**모듈 상수**
- `SHS` (L27) = `Section(name='SHS200x8', section_type='SHS', width=200, height=200, thickness...`
- `TR_24` (L30) = `Truck(name='저상24t', truck_type='lowbed', max_length=12000, max_width=3000, ma...`
- `TR_28W` (L33) = `Truck(name='광폭28t', truck_type='extendable', max_length=18000, max_width=3400...`
- `SITE` (L37) = `SiteLimit(max_gvw_kg=None, max_width_mm=3500, max_height_mm=4500)`
- `SP` (L38) = `SpacingParams()`

**최상위 함수**
- `_seg()` (L41)
- `test_single_dependent_lshape_packs_one_trip()` (L48)
- `test_2face_dependent_packs()` (L59)
- `test_3face_dependent_packs()` (L69)
- `test_partial_wall_segment_packs()` (L79) — 변의 일부만 채운 부분 벽.
- `test_stack_floor_on_dependent_base()` (L92) — L자 베이스 위에 small floor 패널 적층 가능 — 한 회차 안에 두 화물 모두 진입.
- `test_stack_blocked_if_too_wide()` (L113) — 적층 후보가 베이스 내공 폭보다 넓어도 — 신규 패커는 FLOOR 옆자리 사용으로 1 회차 가능.
- `test_recheck_includes_stacked_weight()` (L134) — B-3 — recheck 시 stacked_items 무게가 합산되어 한도 초과 시 거부.
- `test_recheck_panel_override_to_small_truck_rejected()` (L153) — [A1] 패널 회차를 더 작은 트럭으로 override 하면 적재 매수 초과를 거부.
- `test_mixed_panel_ppr_recomputed()` (L183) — 길이 다른 floor 패널 혼적 시 ppr 이 가장 긴 패널 기준으로 재계산.
- `test_cargo_vs_gross_weight()` (L199)
- `test_module_pack_one_trip_per_module()` (L211)
- `test_module_blocked_when_too_wide()` (L221)
- `test_module_wide_packs_on_extendable()` (L230)
- `test_independent_wall_panel_packs()` (L239)
- `test_empty_inputs()` (L248)
- `test_dependent_blocked_if_too_tall()` (L255) — 베이스 + 가장 큰 wall_segment 높이 + 차체 가 트럭 높이 초과 → blocked.
- `test_cumulative_gvw_splits_into_two_trips()` (L269) — 단순 패널 2장을 한 트럭에 넣으면 트럭 적재한도(max_weight)는 OK 지만
- `test_recheck_rejects_truck_when_cumulative_gvw_exceeded()` (L290) — recheck_trip_with_truck 가 현장 GVW(누적) 위반을 거절해야 한다.
- `test_effective_cargo_limit_helper()` (L312) — _effective_cargo_limit = min(트럭 적재능력, 현장 GVW − 차체).


### `modular_3d/transport/tests/test_packer_balance.py` (233 줄)
> Phase 7 단위 테스트 — packer_balance (무게중심 최종 시각 보정).

**모듈 상수**
- `_SHS` (L37) = `Section(name='SHS200x8', section_type='SHS', width=200, height=200, thickness...`

**최상위 함수**
- `_floor()` (L41)
- `_lshape()` (L48)
- `_4face()` (L60)
- `_lowbed()` (L73)
- `_sp()` (L80)
- `test_panel_local_cog_pure_floor_is_center()` (L87) — 순수 floor 패널 → 중심 (length/2, width/2). (local x = length 방향, y = width 방향).
- `test_panel_local_cog_lshape_side0_shifts_toward_bottom()` (L95) — 1면 종속 (하변 = side 0, x 방향 변) → COG y(=width 방향)가 작은 쪽으로 치우침.
- `test_panel_local_cog_lshape_side2_shifts_toward_top()` (L102) — 상변 (side 2) → COG y(=width 방향)가 큰 쪽으로 치우침.
- `test_panel_local_cog_lshape_side3_shifts_toward_left()` (L109) — 좌변 (side 3, y 방향 변) → COG x(=length 방향)가 작은 쪽으로 치우침.
- `test_panel_local_cog_4face_back_to_center()` (L116) — 4 면 모두 종속 → 대칭 → 중심 근처.
- `test_compute_trip_cog_empty_returns_origin()` (L127)
- `test_compute_trip_cog_single_centered_at_xy()` (L131) — 1 placement → 그 패널의 COG 가 회차 COG.
- `test_balance_trips_does_not_change_cost()` (L148) — 보정 전·후 compute_cost 완전 동일 (회차 수·트럭·화물 보존).
- `test_balance_trips_preserves_trip_count()` (L161)
- `test_balance_trips_preserves_items_per_trip()` (L172)
- `test_balance_one_trip_single_placement_is_noop()` (L188) — 1 placement 만 든 회차 → noop.
- `test_balance_one_trip_reduces_cog_distance_on_asymmetric_load()` (L202) — 비대칭 적재 (한쪽으로 치우침) → 보정 후 COG 거리 감소.
- `test_grid_step_is_10mm()` (L231) — 사용자 결정 — 10 mm 격자.


### `modular_3d/transport/tests/test_packer_core.py` (692 줄)
> Phase 4 단위 테스트 — packer_core (Best-Fit 자유 배치 단일 패킹).

**모듈 상수**
- `_SHS` (L57) = `Section(name='SHS200x8', section_type='SHS', width=200, height=200, thickness...`

**최상위 함수**
- `_make_module()` (L63)
- `_make_floor()` (L70)
- `_make_wall()` (L77) — 단순 wall: length=벽 길이, width=벽 높이, thickness=두께.
- `_make_lshape()` (L86)
- `_sp()` (L98)
- `_site()` (L102)
- `_lowbed()` (L106)
- `_aframe()` (L113)
- `test_postures_for_module_lying_only()` (L123)
- `test_postures_for_simple_wall_both()` (L128)
- `test_postures_for_dependent_floor_lying_only()` (L133)
- `test_postures_for_simple_floor_lying_only()` (L138)
- `test_is_simple_wall_distinguishes_kinds()` (L143)
- `test_is_dependent_floor()` (L150)
- `test_truck_posture_compat_lowbed_lying_only()` (L157)
- `test_truck_posture_compat_aframe_standing_simple_wall_only()` (L163)
- `test_allowed_slots_module_floor_only()` (L171)
- `test_allowed_slots_standing_floor_only()` (L175)
- `test_allowed_slots_panel_lying_all_three()` (L179)
- `test_evaluate_slot_returns_meta_dict_on_pass()` (L187)
- `test_evaluate_slot_returns_minus_inf_on_safety_violation()` (L203) — 초중량 패널 → 무게 검사 위반 → -inf.
- `test_evaluate_slot_stack_blocks_when_upper_larger_than_lower()` (L216) — STACK 기하: 위 단 길이가 아래 단보다 크면 -inf.
- `test_evaluate_slot_stack_passes_when_upper_smaller_or_equal()` (L235) — STACK 기하: 위 단이 아래보다 작거나 같으면 통과.
- `test_evaluate_slot_dep_inner_finds_parent()` (L255) — DEP_INNER 자리: 종속 floor 부모 발견 + 빈 슬롯에 작은 패널 진입.
- `test_evaluate_slot_posture_dimension_lying_vs_standing()` (L277) — 단순 wall 의 LYING / STANDING 점수가 *서로 다른 차원*으로 평가됨.
- `test_commit_placement_updates_used_floor_length_and_weight()` (L298)
- `test_commit_placement_accumulates_gap_after_first()` (L313)
- `test_commit_placement_stack_adds_layer()` (L331) — STACK commit 후 layer_max_thickness 새 단 추가.
- `test_find_stack_bottom_idx_returns_none_when_empty()` (L354)
- `test_find_stack_bottom_idx_returns_floor_placement_first()` (L360) — LYING FLOOR 가 깔려있으면 그 인덱스 반환.
- `test_find_dep_inner_parent_idx_returns_none_when_no_dependent()` (L372)
- `test_find_dep_inner_parent_idx_finds_parent()` (L385)
- `test_compute_xyz_first_floor_is_left_edge_plus_half_length()` (L400) — 첫 FLOOR 화물은 트럭 좌측 끝 + 절반 위치.
- `test_simulate_packing_weight_does_not_mutate_items()` (L417)
- `test_simulate_packing_weight_zero_when_aframe_only_floor()` (L427) — A-frame 트럭 + floor 만 → 아무것도 못 실음 → 0.
- `test_simulate_packing_weight_aframe_only_simple_wall()` (L435) — A-frame 트럭 + 단순 wall → 적재 가능 (STANDING).
- `test_default_truck_score_finite_for_capable_truck()` (L446)
- `test_default_truck_score_inf_when_zero_loadable()` (L454) — A-frame 트럭에 floor 만 → 못 실음 → inf.
- `test_cost_efficiency_score_is_fixed_over_capacity()` (L463)
- `test_default_truck_score_per_km_uses_round_trip()` (L472) — 모드 3 (per_km) 의 1회 비용 = km_rate × round_trip_km.
- `test_pack_one_seed_min_fixture_no_blocked()` (L484) — min 픽스처 — 1 모듈 + 1 패널 모두 진입.
- `test_pack_one_seed_small_fixture_no_blocked()` (L497)
- `test_pack_one_seed_medium_fixture_no_blocked()` (L508)
- `test_pack_one_seed_reproducible()` (L519) — 같은 입력 두 번 → 같은 회차 수 + 같은 화물 분배.
- `test_pack_one_seed_shuffled_no_blocked()` (L534) — random.shuffle 후에도 SHUFFLED_STRATEGY 로 패킹 → blocked=0.
- `test_pack_one_seed_all_strategies_no_blocked()` (L546) — 4 그리디 전략 × 6 가중치 — 모든 조합에서 small 픽스처 blocked=0.
- `test_merge_small_modules_pairs_two_short_modules()` (L563) — 두 짧은 모듈(≤4.5m)이 한 회차에 묶임.
- `test_merge_small_modules_skips_long_modules()` (L579) — 5m 초과 모듈은 합산 X — 각각 별도 회차.
- `test_merge_small_modules_one_short_one_long_no_merge()` (L593) — 한 모듈은 짧고(≤4.5m), 다른 모듈은 긴 경우 — 합산 X.
- `test_truck_state_to_trip_preserves_placements()` (L610)
- `test_truck_state_to_trip_marks_mixed_posture()` (L625) — LYING + STANDING 둘 다 든 trip 은 has_mixed_posture=True.
- `test_select_best_new_truck_returns_compatible()` (L647) — 단순 wall + lowbed/extendable 트럭만 → LYING 호환 트럭 반환.
- `test_select_best_new_truck_none_when_too_big()` (L659) — 트럭보다 큰 화물 → None.
- `test_dependent_first_sorter_puts_dependent_panels_first()` (L673)
- `test_existing_pack_items_still_works()` (L686) — Phase 4 코드 도입이 기존 packer.pack_items 의 회귀를 깨지 않음.


### `modular_3d/transport/tests/test_packer_local_search.py` (353 줄)
> Phase 6 단위 테스트 — packer_local_search (VND 5 종 수술).

**모듈 상수**
- `_SHS` (L44) = `Section(name='SHS200x8', section_type='SHS', width=200, height=200, thickness...`

**최상위 함수**
- `_module()` (L48)
- `_floor()` (L54)
- `_lowbed()` (L61)
- `_site()` (L68)
- `_sp()` (L72)
- `test_trip_to_truck_state_round_trip_preserves_items()` (L79)
- `test_truck_states_to_pack_preserves_truck_count()` (L92)
- `test_clone_truck_state_is_deep_copy()` (L106)
- `test_build_truck_state_without_removes_placement()` (L119)
- `test_try_add_to_truck_succeeds_when_room()` (L139)
- `test_try_add_to_truck_none_when_no_room()` (L147) — 매우 큰 화물 → 빈 트럭에 못 들어감.
- `_build_inefficient_pack()` (L158) — 의도적 비효율 — 각 화물을 별도 회차에 넣은 초기 해.
- `test_op_transfer_reduces_cost_on_inefficient_pack()` (L187) — 5 개 floor 패널이 각각 한 회차 → transfer 가 합치기.
- `test_op_merge_reduces_cost_on_two_sparse_trips()` (L202) — 두 회차에 각각 작은 패널 1 장씩 → merge 로 한 회차로.
- `test_op_swap_returns_false_when_no_improvement()` (L222) — 잘 패킹된 해는 swap 으로 더 줄이기 어려움 → 보통 False.
- `test_op_posture_toggle_returns_bool()` (L237) — 자세 토글 — 단순 wall 이 없으면 즉시 False.
- `test_op_truck_downgrade_returns_bool()` (L252) — freight_table 모드: 큰 트럭으로 작은 화물 든 회차 → 다운그레이드 가능.
- `test_vnd_improve_max_iter_zero_returns_input_unchanged()` (L280) — max_iter=0 → 수술 X → 입력과 동일.
- `test_vnd_improve_reduces_cost_on_inefficient_pack()` (L296) — 비효율 초기 해 → VND 적용 후 비용 ≤ 적용 전.
- `test_vnd_improve_idempotent_after_convergence()` (L310) — 수렴 후 한 번 더 돌려도 결과 변동 없음.
- `test_pack_all_seeds_with_vnd_does_not_increase_cost()` (L328) — VND 적용 결과가 미적용 결과보다 *최소 같거나 더 싸야* (회귀 방지).
- `test_pack_all_seeds_with_vnd_no_blocked()` (L339)
- `test_pack_all_seeds_vnd_max_iter_limits_work()` (L347) — vnd_max_iter 작은 값 → 정상 동작 (예외 없이).


### `modular_3d/transport/tests/test_packer_meta.py` (402 줄)
> Phase 5 단위 테스트 — packer_meta (다중 시드 + Lower Bound).

**모듈 상수**
- `_SHS` (L38) = `Section(name='SHS200x8', section_type='SHS', width=200, height=200, thickness...`

**최상위 함수**
- `_module()` (L44)
- `_floor()` (L50)
- `_lowbed()` (L56)
- `_aframe()` (L63)
- `_sp()` (L70)
- `_site()` (L74)
- `test_trip_cost_fixed_per_trip()` (L81)
- `test_trip_cost_freight_table_lowbed()` (L87)
- `test_trip_cost_freight_table_aframe()` (L93)
- `test_trip_cost_per_km()` (L99)
- `test_trip_cost_unknown_mode_raises()` (L106)
- `test_compute_cost_zero_trips_is_zero()` (L116)
- `test_compute_cost_sums_all_trips()` (L122)
- `test_compute_cost_blocked_adds_penalty()` (L134) — blocked 가 있으면 패널티 추가 — 시드 비교 시 정상 해보다 불리해짐.
- `test_lower_bound_empty_returns_zero()` (L152)
- `test_lower_bound_one_module_one_trip_minimum()` (L158) — 모듈 1 개 → 최소 1 회차 → 최소 1 회 단가.
- `test_lower_bound_two_short_modules_pair_into_one_trip()` (L167) — 4.5m 이하 모듈 2 개 → ⌈2/2⌉ = 1 회차 하한.
- `test_lower_bound_three_short_modules_two_trips()` (L178) — 4.5m 이하 모듈 3 개 → ⌈3/2⌉ = 2 회차 하한.
- `test_lower_bound_long_modules_no_merge()` (L187) — 긴 모듈 2 개 → 합산 X → 2 회차 하한.
- `test_lower_bound_freight_table_uses_min_rate()` (L196) — 모드 2 — 트럭 종류별 최저 단가 적용.
- `test_lower_bound_per_km_uses_min_rate()` (L206) — 모드 3 — 트럭 종류별 최저 km단가 × 왕복거리.
- `test_lower_bound_unknown_mode_raises()` (L217)
- `test_lower_bound_inactive_trucks_excluded()` (L224) — active=False 트럭은 하한 계산에서 제외 — 모두 inactive → 0.
- `test_pack_all_seeds_returns_best_and_meta()` (L238) — min 픽스처 — 최저 비용 해 채택 + 메타 dict 구조.
- `test_pack_all_seeds_small_no_blocked()` (L258)
- `test_pack_all_seeds_runs_24_deterministic_when_no_early_exit()` (L266) — early_exit 안 되면 24 결정론 + 30 무작위 = 54 후보.
- `test_pack_all_seeds_best_label_in_all_costs()` (L281) — 채택된 best_label 이 all_costs 안에 존재.
- `test_pack_all_seeds_best_cost_is_minimum()` (L291) — 채택된 best_cost 는 all_costs 의 최저값.
- `test_pack_all_seeds_fixed_seed_reproducible()` (L304) — 같은 seed_base 두 번 → 같은 결과.
- `test_pack_all_seeds_different_seed_can_differ()` (L317) — 다른 seed_base — 무작위 시드 결과가 *다를 수 있어야* (셔플 효과 확인).
- `test_pack_all_seeds_early_exit_triggers_when_lb_reached()` (L339) — 모듈 1 개 만 — 1 회차 = LB. 첫 시드부터 LB 도달 → 조기 종료.
- `test_pack_all_seeds_no_early_exit_returns_full_candidates()` (L352) — 비효율 케이스 — early_exit 미발동 시 24+30=54 후보 모두 평가.
- `test_pack_all_seeds_three_modes_all_succeed()` (L366) — 3 가지 비용 모드 모두 정상 동작 (blocked=0).
- `test_pack_all_seeds_unknown_mode_raises()` (L375)
- `test_pack_all_seeds_random_count_zero_skips_random()` (L386) — random_count=0 → 24 결정론 시드만.
- `test_default_random_seed_base_is_42()` (L396) — 기본 seed_base = 42 (사용자 결정 — 고정 시드).
- `test_default_random_seed_count_is_30()` (L401)


### `modular_3d/transport/tests/test_packer_types_safety.py` (268 줄)
> Phase 2 단위 테스트 — packer_types + packer_safety.

**모듈 상수**
- `SHS` (L25) = `Section(name='SHS200x8', section_type='SHS', width=200, height=200, thickness...`
- `TR_LOWBED` (L28) = `Truck(name='저상25t', truck_type='lowbed', max_length=12000, max_width=3000, ma...`
- `TR_AFRAME` (L33) = `Truck(name='A프레임', truck_type='aframe', max_length=12000, max_width=3000, max...`
- `SITE` (L38) = `SiteLimit(max_gvw_kg=None, max_width_mm=3500, max_height_mm=4500)`
- `SP` (L39) = `SpacingParams()`

**최상위 함수**
- `test_posture_enum_values()` (L43)
- `test_placement_slot_enum_values()` (L48)
- `test_placement_freezes_state()` (L55)
- `test_check_result_bool()` (L65)
- `test_truck_state_remaining_cargo_limit()` (L72)
- `test_truck_state_remaining_cargo_limit_negative_clamped()` (L78) — used_cargo_weight 가 effective_cargo_limit 를 넘으면 0 으로 clamp.
- `test_truck_state_used_inner_height_empty()` (L85)
- `test_truck_state_used_inner_height_two_layers()` (L90) — 2 단 = 두께 합 + (단 수 − 1) × gap
- `test_truck_state_n_layers()` (L100)
- `test_truck_state_remaining_floor_length()` (L106)
- `test_dims_module_always_lying()` (L114)
- `test_dims_simple_floor_lying()` (L121)
- `test_dims_wall_panel_standing()` (L128) — 세움 자세: 점유 (length, thickness, width) — width 가 높이방향으로.
- `test_dims_dependent_panel_lshape_lying()` (L136) — 종속 패널 (L자) 점유 두께 = thickness + max(seg.height).
- `_empty_ts()` (L149)
- `test_can_place_ok_simple_floor_lying_on_empty_truck()` (L153)
- `test_can_place_length_violation()` (L160) — 패널 길이가 트럭 유효 길이 초과.
- `test_can_place_width_violation_truck()` (L169) — 패널 폭이 트럭 폭 + 여유 초과 → 트럭 측 위반 메시지.
- `test_can_place_width_violation_site()` (L178) — 현장 폭 제한이 트럭 + 여유보다 더 빡빡한 경우.
- `test_can_place_height_violation_truck()` (L189) — 벽 점유 높이가 트럭 내공 초과 (LYING).
- `test_can_place_weight_violation_truck()` (L201) — 누적 화물이 트럭 적재능력 초과.
- `test_can_place_weight_violation_site_gvw()` (L212) — 현장 GVW 가 트럭 적재능력보다 빡빡한 경우 — 누적 GVW 메시지.
- `test_can_place_stack_uses_layer_height()` (L226) — STACK 자리는 단 위에 쌓아 단별 잔여 높이 검사.
- `test_can_place_stack_overflow_height()` (L238) — STACK 적층 시 내공 높이 초과.
- `test_trip_placements_default_empty()` (L252) — 기존 코드가 placements 안 채워도 동작 — 기본값 빈 리스트.
- `test_trip_with_placements_field()` (L260) — 새 패커가 placements 채워도 OK.


### `modular_3d/transport/tests/test_run_validation.py` (69 줄)
> Phase 9 단위 테스트 — run_validation 스크립트.

**최상위 함수**
- `test_pass_criteria_constants()` (L21) — Phase 1 결정 — 합격 기준 5% 평균 + 0% 회귀.
- `test_run_validation_small_creates_report()` (L27) — min + small 픽스처만 빠르게 측정 → 보고서 생성 + 필수 섹션 존재.
- `test_run_validation_three_modes()` (L44) — 3 비용 모드 모두 측정 — 보고서 안에 세 모드 모두 등장.
- `test_run_validation_overwrites_existing()` (L58) — 매 실행 덮어쓰기 — 기존 내용은 사라짐.


### `modular_3d/transport/tests/test_transport_panel.py` (152 줄)
> Phase 6 단위 테스트 — TransportTab UI 위젯.

**최상위 함수**
- `test_transport_panel_module_imports()` (L24)
- `test_analysis_panel_module_imports()` (L29)
- `test_transport_tab_has_signals()` (L35)
- `test_transport_tab_has_core_methods()` (L42)
- `test_transport_tab_init_signature()` (L62)
- `test_transport_tab_phase7_9_methods()` (L71)
- `test_apply_overrides_signature()` (L89) — _apply_overrides 는 PackResult 받아 PackResult 반환 (1 인자).
- `test_on_option_changed_takes_step()` (L97) — [Phase 9 결정 3] _on_option_changed 가 step 인자 수신 (정밀 무효화).
- `test_dialog_modules_import()` (L105)
- `test_analysis_panel_has_policy_changed_signal()` (L114)
- `test_analysis_panel_has_build_transport_tab_method()` (L120)
- `test_pipeline_labels_constant()` (L129)
- `test_gui_instance_smoke()` (L140) — 사용자가 실제 데스크탑에서 직접 modular_3d 메인 윈도우 띄워 확인.


### `modular_3d/transport/tests/test_transport_v2_fixtures.py` (224 줄)
> Phase 3 단위 테스트 — transport_v2_fixtures 자동 생성기.

**최상위 함수**
- `test_generate_all_fixtures_returns_5()` (L37)
- `test_generate_fixture_by_name()` (L44)
- `test_generate_fixture_unknown_raises()` (L51)
- `test_fixture_counts()` (L58)
- `test_modules_diversity_in_large()` (L65) — large(30개) 모듈에 5 사이즈 모두 포함되어야.
- `test_small_module_4_5m_size_exists_for_module_merge_test()` (L75) — small4_5m 모듈은 Phase 4 모듈 합산 검증의 핵심 — 생성에 반드시 포함.
- `_classify_panel()` (L83) — 패널 종류 식별 — 다양성 검증용.
- `test_panels_diversity_in_medium()` (L111) — medium 이상에서 8 종류 패널이 모두 등장 (4face 는 비율 작아 medium 에 없을 수 있음).
- `test_panels_diversity_in_large()` (L119) — large(80개) 에서는 8 종류 모두 등장해야 (4face 포함).
- `test_fixture_packs_with_existing_packer()` (L132) — 자동 생성된 픽스처가 기존 packer.py 로 *오류 없이* 패킹되어야.
- `test_fixture_packs_large_no_blocked()` (L145) — large 사이즈도 기존 packer 가 모두 패킹 가능해야 한다.
- `test_fixed_seed_reproducible()` (L153) — 같은 시드 두 번 → 같은 픽스처.
- `test_different_seed_different_fixture()` (L163) — 다른 시드 → 다른 픽스처 (적어도 무게가 달라야).
- `test_gen_modules_count()` (L174)
- `test_gen_simple_floor_panels_count()` (L179)
- `test_gen_lshape_panels_have_one_segment()` (L184)
- `test_gen_4face_panels_have_four_segments()` (L191)
- `test_gen_partial_wall_has_partial_length()` (L200)
- `test_gen_long_short_combo_panels_mixed_sides()` (L213)


### `modular_3d/transport/tests/test_visualizer.py` (162 줄)
> Phase 4 단위 테스트 — visualizer.

**모듈 상수**
- `SHS` (L21) = `Section(name='SHS200x8', section_type='SHS', width=200, height=200, thickness...`
- `TR` (L23) = `Truck(name='저상24t', truck_type='lowbed', max_length=12000, max_width=3000, ma...`
- `SP` (L26) = `SpacingParams()`

**최상위 함수**
- `_module_trip()` (L29)
- `_floor_trip()` (L37)
- `_wall_trip()` (L45)
- `_dependent_trip()` (L53)
- `test_top_view_module()` (L71)
- `test_rear_view_module()` (L77)
- `test_top_view_floor()` (L84)
- `test_rear_view_floor()` (L89)
- `test_top_view_wall()` (L94)
- `test_rear_view_wall()` (L99)
- `test_top_view_dependent_n_faces()` (L106)
- `test_rear_view_dependent_with_stack()` (L115)
- `test_overload_marks_outline_red()` (L133) — 화물 무게가 트럭 한도 초과 시 경고 텍스트 + 외곽 빨강.
- `test_empty_trip_returns_figure()` (L149)
- `test_omitted_truck_and_sp()` (L160)


### `modular_3d/transport/tests/test_visualizer_3d.py` (407 줄)
> Phase B 단위 테스트 — `draw_loaded_3d_view` (3D 적재 시각화).

**모듈 상수**
- `_SHS` (L38) = `Section(name='SHS200x8', section_type='SHS', width=200, height=200, thickness...`

**최상위 함수**
- `_lowbed()` (L42)
- `_module()` (L49)
- `_floor()` (L55)
- `_lshape()` (L61)
- `test_cube_mesh_has_8_vertices_12_triangles()` (L77)
- `test_wireframe_box_has_12_edges()` (L87)
- `test_draw_empty_trips_returns_figure_with_annotation()` (L97)
- `test_draw_single_module_trip_has_traces()` (L107) — 모듈 1 회차 → 적재함 외곽선 + 차체(캐빈·바퀴) + 모듈 박스 + 라벨.
- `test_draw_dependent_floor_includes_wall_segment_traces()` (L128) — L 자 종속 floor → wall_segment 직육면체도 trace 로 들어감.
- `test_draw_4face_dependent_includes_4_walls()` (L143) — 4 면 종속 → 4 개 wall_segment 직육면체.
- `test_multiple_trips_have_x_offset_accumulated()` (L172) — 회차 2 개 → 두 번째 트럭의 x 좌표가 첫 번째보다 max_length + 2000 이상 큼.
- `test_standing_wall_panel_correct_dims()` (L203) — 단순 wall 의 STANDING 자세 → width 가 위로 솟음. mesh 차원이 맞는지.
- `test_legacy_trip_without_placements()` (L231) — placements 가 비어있는 (구 패커 결과) Trip 도 items 로 그릴 수 있음.
- `test_draw_real_pack_result_no_exception()` (L248) — 실제 픽스처로 신규 패커 → 결과를 3D 시각화 — 예외 없이.
- `test_truck_bed_includes_cabin_wheels_label()` (L264) — 1 회차 모듈 1 + 트럭 — 적재함 외곽선 + 캐빈 mesh + 바퀴 6 개 + 차종 라벨 포함.
- `test_truck_type_specific_color()` (L288) — 차종별 캐빈 색상 — 저상 파랑 / 광폭 초록 / A-frame 주황.
- `test_camera_preset_buttons_present()` (L329) — updatemenus 에 Iso/Top/Side 버튼 3 개.
- `test_highlight_trip_no_adds_red_outline()` (L346) — highlight_trip_no=1 → 빨간 굵은 외곽선 trace 추가.
- `test_highlight_nonexistent_trip_no_no_change()` (L363) — 존재하지 않는 trip_no → 강조 추가 없음.
- `test_trip_costs_in_overlay()` (L375) — trip_costs 전달 → 오버레이 텍스트에 비용 표시.
- `test_aframe_truck_has_extra_frame()` (L390) — A-frame 트럭 → 양옆 A 프레임 박스 2 개 추가.


### `modular_3d/transport/tests/test_wall_classifier.py` (138 줄)
> Phase 3 단위 테스트 — wall_classifier.

**최상위 함수**
- `_make_module()` (L27)
- `_make_floor_panel()` (L35)
- `test_single_module_all_faces_exterior()` (L44)
- `test_two_modules_adjacent_one_face_interior()` (L53)
- `test_partial_adjacency_ratio_mid()` (L72)
- `test_wide_gap_all_exterior()` (L84)
- `test_floor_panel_obstacle_toggle()` (L93)
- `test_vertical3_module_12_faces()` (L107)
- `test_disabled_returns_zero_ratio()` (L123)
- `test_face_unit_weight_weighted_average()` (L131)


### `modular_3d/transport/tests/transport_v2_fixtures.py` (367 줄)
> Phase 3 — 새 패커 검증용 픽스처 자동 생성기.

**모듈 상수**
- `_SHS` (L30) = `Section(name='SHS200x8', section_type='SHS', width=200, height=200, thickness...`

**최상위 함수**
- `default_trucks()` (L50) — 검증세트 전용 트럭 카탈로그. 3 종 × 한 대씩.
- `default_site()` (L65) — 현장 제한 — 폭·높이 광로급(트럭보다 넓음), GVW 해당없음.
- `_make_module()` (L71)
- `gen_modules()` (L79) — count 개 모듈을 다양 사이즈로 생성.
- `gen_simple_floor_panels()` (L104) — 순수 floor 패널을 두께/길이/폭 다양 조합으로 생성.
- `gen_simple_wall_panels()` (L124) — 독립 wall 패널 — 높이·길이·두께 다양.
- `_seg()` (L147) — 변 인덱스에 따른 wall_segment 생성. seg_length=-1 이면 변 전체 길이.
- `gen_lshape_panels()` (L163) — L자 — 변 0/1/2/3 중 무작위 한 변에 wall_segment 1 개.
- `gen_2face_panels()` (L178) — 2면 종속 — 마주보는 변 (0,2) 또는 직각 변 (0,1) (1,3).
- `gen_long_short_combo_panels()` (L194) — 장변(0 또는 2) + 단변(1 또는 3) 결합 — 사용자가 명시 요청.
- `gen_3face_panels()` (L210) — 3면 종속 — 4 가지 변 조합.
- `gen_4face_panels()` (L226) — 4면 종속 — 네 변 모두.
- `gen_partial_wall_panels()` (L240) — 부분 벽 — start_offset > 0, length_mm < 변 전체 길이.
- `gen_mixed_panels()` (L259) — panel_count 개 패널을 8 종류로 골고루 분배.
- `generate_all_fixtures()` (L304) — 5 단계 사이즈의 픽스처 5 개를 고정 시드로 생성.
- `generate_fixture()` (L326) — SIZE_PRESETS 중 한 이름의 픽스처만 생성.

**클래스**
- `class Fixture` (L36) — 한 검증 픽스처 = (이름, 모듈 리스트, 패널 리스트, 트럭 리스트, 현장, 간격).


### `modular_3d/transport/visualizer.py` (1030 줄)
> 운송 회차 시각화 (Plotly 기반).

**모듈 상수**
- `SEAT_PALETTE` (L34) = `['#f4d35e', '#a8dadc', '#bde0fe', '#cdb4db', '#ffafcc', '#fdc4b6']`
- `LAYER_PALETTE` (L35) = `['#1d3557', '#e63946', '#2a9d8f', '#9d0208', '#003049', '#7209b7']`
- `MODULE_COLOR` (L36) = `'#1f77b4'`
- `FLOOR_COLOR` (L37) = `'#DEB887'`
- `WALL_SEG_COLOR` (L38) = `'#A0522D'`
- `STACK_COLOR_FILL` (L39) = `'rgba(46,139,87,0.45)'`
- `STACK_COLOR_EDGE` (L40) = `'#1a6b3c'`
- `OVERLOAD_LINE` (L41) = `'#cc0000'`
- `_COLOR_MODULE` (L535) = `'#2a7ade'`
- `_COLOR_FLOOR` (L536) = `'#4CAF50'`
- `_COLOR_DEP_FLOOR` (L537) = `'#FF9800'`
- `_COLOR_WALL_PANEL` (L538) = `'#9C27B0'`
- `_COLOR_WALL_SEG` (L539) = `'#8B4513'`
- `_COLOR_TRUCK_LINE` (L540) = `'#333'`

**최상위 함수**
- `_overload_diagnose()` (L45) — 트럭 한도 대비 화물이 초과한 항목 메시지 (없으면 None).
- `_truck_outline_top()` (L72)
- `_truck_outline_rear()` (L84)
- `_edge_zones_top()` (L110)
- `_draw_wall_segments_top()` (L123) — Top view 에서 종속 패널의 벽 세그먼트를 위에서 본 두께 박스로 표시.
- `draw_top_view()` (L165) — 위에서 본 적재 평면 (회차 1 개).
- `draw_rear_view()` (L333) — 뒤에서 본 적재 단면 (회차 1 개).
- `_cube_mesh()` (L543) — 축 정렬 직육면체 Mesh3d — 8 정점 + 12 삼각형.
- `_wireframe_box_3d()` (L563) — 직육면체 외곽선 — 12 edge 를 None 으로 구분한 단일 Scatter3d trace.
- `_item_dims_3d()` (L594) — 화물의 3D 차원 (length, width, height) — 자세 반영.
- `_add_truck_bed_3d()` (L607) — 트럭 적재함 외곽선 + 적재함 바닥 판 + 차체(캐빈·바퀴) + 차종 라벨.
- `_placement_color()` (L729) — 화물 종류별 색상 + 분류 라벨.
- `_add_wall_segment_3d()` (L742) — wall_segment 직육면체 — floor 좌하단 + 상단 위에서 벽 높이만큼 솟음.
- `_add_placement_3d()` (L789) — 단일 placement 3D 박스 + 라벨 + (종속 floor 면) wall_segments.
- `_add_legacy_items_3d()` (L836) — 역호환 — Trip.placements 가 비어있는 구 패커 결과에서 items 직접 그리기.
- `_camera_presets()` (L883) — 카메라 프리셋 — Top / Side / Iso. updatemenus 버튼으로 노출.
- `_trip_overlay_text()` (L898) — 회차 오버레이 라벨 — 트럭 위 표시. 트럭명·총중량·적재율·비용.
- `draw_loaded_3d_view()` (L909) — 모든 회차를 한 Plotly 3D Figure 에 *옆으로 나란히* 배치.


### `modular_3d/transport/wall_classifier.py` (311 줄)
> 비내력벽 외부/내부 자동 판별 (분석 ⑥).

**최상위 함수**
- `_component_aabb_xy()` (L68) — 컴포넌트의 XY 평면 AABB (min_xy, max_xy).
- `_component_z_range()` (L74) — 컴포넌트의 z 범위.
- `_box_intersect()` (L80) — 축 정렬 박스 2D 교차.
- `_z_overlap()` (L87) — z 범위 겹침 (tol mm 허용).
- `_module_face_segment_xy()` (L94) — 모듈/v3module 한 면의 외측 평면 좌표 (XY) + outward normal.
- `_classify_face_xy()` (L133) — 면의 외측 슬랩을 segment 단위로 잘라 외부 비율 산출.
- `_gather_obstacles()` (L193) — 씬 안에서 인접 검사 대상이 되는 부재들의 AABB 목록.
- `classify_module_face()` (L223) — 모듈의 한 면 → FaceClass.
- `classify_module()` (L235) — 모듈 4 면 일괄 분류.
- `classify_vertical3_module()` (L245) — 수직 3 모듈을 층별로 12 면 분류 (사용자 결정 ⑥-4).
- `classify_independent_wall()` (L272) — 독립 벽 패널의 양면 분류.
- `face_unit_weight()` (L292) — 가중평균 단위중량 (kg/m²) — 사용자 결정 ⑥-2.

**클래스**
- `class FaceClass` (L47) — 한 면의 외부 노출 비율 + 인접 부재 ID (디버그용).
- `class ClassifierOptions` (L58) — wall_classifier 호출 시 주는 옵션. 어댑터의 TransportOptions 일부만 추림.


### `modular_3d/ui/__init__.py` (0 줄)

### `modular_3d/ui/alignment_helpers.py` (264 줄)
> alignment_view 의 순수 기하·레이어 헬퍼 분리.

**모듈 상수**
- `LAYER_BOTTOM` (L23) = `0`
- `LAYER_TOP` (L24) = `1`
- `TYPE_COLORS` (L28) = `{_CT.MODULE: (80, 80, 200), _CT.FLOOR_PANEL: (80, 180, 80), _CT.STRUCT_WALL: ...`
- `STATE_IDLE` (L43) = `'idle'`
- `STATE_SELECTED` (L44) = `'selected'`
- `STATE_DIRECTION` (L45) = `'direction'`
- `STATE_REFERENCE` (L46) = `'reference'`
- `STATE_DEPENDENCY_PICK` (L47) = `'dependency_pick'`
- `ALIGN_TOL` (L54) = `100.0`
- `GAP_20` (L55) = `float(_GAP_MM)`
- `SNAP_20` (L56) = `float(_GAP_MM)`
- `SNAP_220` (L57) = `float(_SECTION_W + _GAP_MM)`
- `EPS` (L58) = `0.5`
- `EDGE_PICK_PX` (L59) = `8`

**최상위 함수**
- `slab_xy()` (L68) — SlabData → XY 사각형.
- `column_xy()` (L75) — ColumnData → 200×200 XY 사각형 (기둥 중심 기준).
- `beam_xy()` (L83) — BeamData → 축 정렬 XY 사각형 (단면 폭만큼 수직 확장).
- `allowed_parent_types()` (L102) — 종속 부재 타입에 따라 부모로 가능한 ComponentType 집합.
- `nearest_edge_id()` (L121) — 클릭한 월드 좌표에서 가장 가까운 부모 모서리 ID(0~3) 반환.
- `detect_mid_beam_level()` (L136) — 모듈 내 클릭 y 위치에 따라 'bottom' / 'top' 결정 (DD1).
- `iter_component_rects()` (L145) — 부재의 내부 구성 요소를 (rect, role) 튜플로 순회.
- `component_layers()` (L210) — 부재가 속한 레이어 집합.
- `visible_ids()` (L249) — 현재 레이어에 표시되는 부재 ID 리스트 (페어/sibling 중복 제외).


### `modular_3d/ui/alignment_paint.py` (653 줄)
> AlignmentCanvas 의 paintEvent + 보조 그리기 Mixin.

**클래스**
- `class AlignmentCanvasPaintMixin` (L30) — paintEvent + 직접 그리기 헬퍼.
  - `_draw_vline()` (L33)
  - `_draw_hline()` (L38)
  - `_draw_bbox_edges()` (L43)
  - `paintEvent()` (L54)
  - `_draw_f5_ghost()` (L306) — F5 PREVIEW 시 마우스 위치에 반투명 고스트 부재 + 스냅 마커 그림.
  - `_draw_rooms()` (L342) — 완성된 실들을 반투명 색면 + 외곽선 + 라벨로 그림.
  - `_draw_openings()` (L375) — 부재 개구부를 평면(xy) 사각형 외곽으로 표시. 선택분은 강조.
  - `_draw_import_preview()` (L397) — 정의 가져오기 그룹 고스트(부재 footprint + 실 폴리곤, 점선)를 2D 표시.
  - `_draw_room_preview()` (L429) — 실 이동/복사 미리보기 폴리곤(녹색 점선) + 기준 꼭짓점 마커.
  - `_draw_opening_preview()` (L448) — 개구부 고스트 미리보기를 2D 평면 사각형(주황 점선)으로 표시.
  - `_draw_room_in_progress()` (L469) — 실 그리기 중 — 확정점·연결선·고무줄선·점 마커.
  - `_f5_world_snap()` (L511) — 마우스 월드 좌표에서 가장 가까운 1층 본체 꼭지점에 스냅.
  - `_apply_auto_gap()` (L553)
  - `_apply_dep_snap()` (L579)
  - `_f5_compute_ghost_corners()` (L636) — 현재 PREVIEW 부재의 1층 본체 4꼭지점(월드 XY) 반환. 없으면 None.


### `modular_3d/ui/alignment_pick.py` (102 줄)
> AlignmentCanvas 의 hit-test / edge picking Mixin.

**클래스**
- `class AlignmentCanvasPickMixin` (L17) — 클릭 좌표 ↔ 부재·모서리 매칭.
  - `_hit_test_component()` (L20) — XY-bbox 안에 마우스가 있으면 반환 (가장 작은 면적 우선).
  - `_pick_direction_edge()` (L36) — DIRECTION 상태에서 노란 모서리 클릭 → (axis, coord) 반환 or None.
  - `_pick_target_edge()` (L59) — 대상 부재의 두 모서리 중 마우스 커서에 가까운 쪽 좌표 반환.
  - `_misaligned_set()` (L69) — 선택 부재 대비 오차가 있는 부재 → {oid: (dx_or_None, dy_or_None)}.


### `modular_3d/ui/alignment_view.py` (1429 줄)
> 정렬 검증 뷰 (F5) — 2D 탑뷰 평면도 기반.

**최상위 함수**
- `_point_in_polygon()` (L61) — 점(px,py)이 단순 다각형 poly 내부인지 — ray casting.

**클래스**
- `class AlignmentCanvas(QLabel, AlignmentCanvasPaintMixin, AlignmentCanvasPickMixin)` (L77) — 2D 평면뷰 캔버스.
  - **시그널** `move_requested = pyqtSignal(int, int, float)` (L80)
  - **시그널** `selection_changed = pyqtSignal(int)` (L82)
  - **시그널** `room_selection_changed = pyqtSignal(int)` (L84)
  - `__init__()` (L86)
  - `set_placement_callback()` (L183) — Controller 가 F5 배치/조작 콜백을 주입.
  - `handle_palette_select()` (L246)
  - `enter_dependency_pick()` (L269) — controls.py 가 dim 확정 후 호출 — DEPENDENCY_PICK 상태로 진입.
  - `set_edit_mode()` (L280) — 'component' | 'room' | 'opening' 편집 모드 전환(토글 버튼이 호출).
  - `edit_mode()` (L305)
  - `start_opening_add()` (L309) — '개구부 추가' 버튼 → 크기 입력 후 고스트 미리보기 시작(컨트롤러 위임).
  - `begin_opening_preview()` (L317) — 컨트롤러가 크기 확정 후 호출 — 고스트 미리보기 상태 설정.
  - `_op_effective_wh()` (L333) — rot(0/90) 반영 유효 폭·높이.
  - `_op_update_at()` (L342) — 마우스 위치에서 대상 부재 + 면 로컬(u,v) 갱신 + 3D 고스트.
  - `_op_commit_at()` (L376) — 클릭 — 현재 미리보기 개구부 확정(추가/이동/복사).
  - `_cancel_opening_preview()` (L391) — 미리보기 종료 + 3D 고스트 제거.
  - `begin_import_preview()` (L400) — 컨트롤러가 정의 복원 후 호출 — 그룹 고스트 미리보기 시작.
  - `_import_pivot()` (L413)
  - `_import_update_at()` (L418)
  - `_import_commit_at()` (L428)
  - `_cancel_import_preview()` (L438)
  - `begin_room_preview()` (L446) — 선택된 실에 대해 이동/복사 미리보기 시작(M/C 키).
  - `_room_preview_polygon()` (L464) — 현재 미리보기 상태의 폴리곤(월드) 계산.
  - `_room_update_at()` (L492) — 마우스 따라 실 미리보기 폴리곤 + 3D 고스트 갱신.
  - `_room_commit_at()` (L503) — 클릭 — 실 이동/복사 확정.
  - `_cancel_room_preview()` (L514)
  - `_rotate_selected_room()` (L521) — 선택된 실을 중심 기준 90° 제자리 회전(되돌리기 기록은 commit 측).
  - `_hit_test_opening()` (L536) — 월드 점을 품는 개구부 (comp_id, index) 반환. 없으면 None.
  - `start_room_draw()` (L549) — 좌측 팔레트 '실 그리기' 버튼 → 실 모드로 전환 + 새 폴리곤 시작.
  - `clear_room_selection()` (L564) — 실 선택 해제(패널 삭제 등 외부 트리거용) — 시그널 발생 없음.
  - `_hit_test_room()` (L570) — 월드 점(wx,wy)을 품는 실 id 반환(겹치면 나중 생성 우선). 없으면 -1.
  - `_cancel_room_draw()` (L582) — 실 그리기 취소 — 점 버리고 모드 종료.
  - `_finish_room_draw()` (L589) — 실 그리기 완료 — 점 3개 이상이면 콜백으로 폴리곤 전달.
  - `_room_snap()` (L601) — 실 점 스냅: ① 부재 꼭짓점 → ② 부재 모서리(축별)/직교 → ③ 격자(100mm).
  - `reset_state()` (L651) — 선택/방향/기준을 모두 초기화.
  - `set_layer()` (L662)
  - `_world_to_screen()` (L667)
  - `_screen_to_world()` (L673)
  - `resizeEvent()` (L679)
  - `_auto_fit()` (L686)
  - `_current_visible_ids()` (L707)
  - `mousePressEvent()` (L714)
  - `mouseMoveEvent()` (L844)
  - `mouseReleaseEvent()` (L904)
  - `wheelEvent()` (L909)
  - `keyPressEvent()` (L933)
  - `_apply_snap()` (L1192) — 1/2/3/4/5 키에 해당하는 스냅으로 이동 요청.
- `class AlignmentDockPanel(QWidget)` (L1232) — F5 모드 — 2D 탑뷰 배치 + 정렬 도킹 패널.
  - **시그널** `floors_changed = pyqtSignal(int)` (L1241)
  - **시그널** `save_requested = pyqtSignal()` (L1243)
  - **시그널** `load_requested = pyqtSignal()` (L1244)
  - `__init__()` (L1246)
  - `canvas()` (L1312)
  - `floors()` (L1316)
  - `set_floors()` (L1319) — 외부에서 층수 변경. emit=True 면 floors_changed 시그널 발화.
  - `set_floors_control_visible()` (L1330) — 층수 입력 컨트롤(라벨+스핀박스) 표시/숨김.
  - `_snap_to_three()` (L1339) — 가장 가까운 3 의 배수로 스냅 (동률은 내림). 최소 3.
  - `_on_floors_changed()` (L1348)
  - `request_focus()` (L1362) — 캔버스에 포커스 — 키 라우팅 필요 시 사용.
  - `_apply_move()` (L1367) — 선택 부재가 속한 다층 그룹 전체를 (axis, delta)만큼 이동(g5).


### `modular_3d/ui/analysis_panel.py` (1502 줄)
> 메인 윈도우 오른쪽에 도킹되는 구조해석 결과 패널.

**모듈 상수**
- `_ROLE_KO` (L40) = `{'module_column': '기둥', 'module_bottom_beam': '바닥보', 'module_top_beam': '천장보'...`
- `_ROLE_KO_ORDER` (L62) = `['기둥', '벽 기둥', '코어 기둥', '중간기둥', '천장보', '바닥보', '벽 상부보', '벽 하부보', '코어 상부보', '코어...`
- `_COMP_TYPE_ORDER` (L73) = `['모듈', '수직 3층 모듈', '바닥패널', '구조벽', '캔틸레버보', '캔틸레버슬래브', '중간보', '중간기둥', 'RC 코어',...`
- `_CRITICAL_KO` (L106) = `{'axial': '축력', 'bend': '휨', 'shear': '전단', 'combined': '복합', 'defl': '처짐', '...`
- `_KIND_KO` (L116) = `{'beam': '보', 'column': '기둥', 'shell': '쉘'}`
- `_GROUP_KO` (L125) = `{'columns': '기둥', 'beams': '보', 'cantilevers': '캔틸레버', 'all': '전체'}`
- `CASE_DISPLAY_MAP` (L154) = `{'ENVELOPE': '지배조합 (5케이스 envelope)', 'D+L': '1.2D + 1.6L (중력)', 'Ex': '1.2D +...`
- `CASE_DISPLAY_TO_KEY` (L163) = `{v: k for k, v in CASE_DISPLAY_MAP.items()}`
- `CASE_DISABLED_ITEMS` (L166) = `['1.4D (자중 단독) — 추후 구현', '0.9D + 1.0Ex (X 지진 전도 방지) — 추후 구현', '0.9D + 1.0Ey (...`
- `_RATIO_COLOR_BANDS` (L1481) = `[(0.3, (51, 102, 255)), (0.6, (102, 204, 255))]`
- `_RATIO_COLOR_BANDS_LE` (L1487) = `[(0.85, (77, 230, 77)), (1.0, (255, 153, 0))]`
- `_RATIO_COLOR_NG` (L1491) = `(255, 51, 51)`

**최상위 함수**
- `_alpha_label()` (L80) — 0→a, 1→b, … 25→z, 26→aa, 27→ab …  컴포넌트 위치 인덱스 라벨.
- `_role_ko_order_key()` (L91)
- `_comp_type_order_key()` (L98)
- `_group_label_ko()` (L133) — 그룹 키 → 한글 표시명. 'columns_F{a}_{b}' / 'columns_F{a}' 동적 처리.
- `_ratio_to_qcolor()` (L1494) — 응력비 → 5단계 색상 (3D 뷰 `_ratio_to_5band` 와 동일 경계/색).

**클래스**
- `class AnalysisPanel(QWidget)` (L177) — F6 구조해석 결과를 표시하는 도킹 패널.
  - **시그널** `member_selected = pyqtSignal(int)` (L181)
  - **시그널** `members_selected = pyqtSignal(list)` (L183)
  - **시그널** `case_changed = pyqtSignal(str)` (L185)
  - **시그널** `contour_changed = pyqtSignal(str)` (L187)
  - **시그널** `ratio_view_changed = pyqtSignal(str)` (L190)
  - **시그널** `dof_color_toggle = pyqtSignal(bool)` (L193)
  - **시그널** `column_segments_changed = pyqtSignal(int)` (L195)
  - **시그널** `policy_changed = pyqtSignal(str)` (L197)
  - `__init__()` (L199)
  - `select_envelope_case()` (L305) — (2026-05-19 작업 5) 콤보박스를 '지배조합' 으로 자동 전환.
  - `select_dl_case()` (L316) — (2026-05-19 작업 5) 콤보박스를 D+L 로 되돌림.
  - `set_visible_subtabs()` (L326) — 메인 윈도우의 [구조해석]/[물량]/[운송] 탭에서 서브탭만 선택적으로 노출.
  - `set_visualization_controls_visible()` (L359) — [2026-05-27] 상단 *하중조합 / 컨투어 / 자유도 색* 위젯 + 탭바 일괄 hide.
  - `_build_quantity_tab()` (L375) — 물량산출 탭 위젯 구성.
  - `_build_transport_tab()` (L524) — 운송탭 — TransportTab 위젯 인스턴스를 4번째 탭으로 추가.
  - `populate_transport()` (L558) — 외부에서 design_result 와 정책을 운송탭에 주입.
  - `_on_transport_member_highlight()` (L569) — 운송 회차표 더블클릭 → cid 리스트. Controller 가 구독 시 forward.
  - `_on_transport_blocked()` (L574) — 운송 불가 항목 알림 — Phase 9 진단 UI 에서 확장.
  - `populate_ops()` (L581) — OpenSees 결과로 패널을 채운다.
  - `_populate_case_combo_initial()` (L618) — 콤보박스 초기 항목 — 활성 5 종 + 비활성 추가 조합 (KDS 향후 구현용).
  - `_on_case_index_changed()` (L640) — 콤보박스 변경 → 부재 트리 갱신 + 시그널 emit (좌측 변형 갱신용).
  - `_on_case_changed()` (L664) — 레거시 — currentTextChanged 호환 시그니처. 현재 호출 자리 없음.
  - `_on_contour_changed()` (L675)
  - `_fill_member_tree_ops()` (L678) — ops 모델용 부재 트리 — 가독성 개선 (2026-05-18).
  - `_classify_role_ko()` (L842) — 부재 role 영어 → 한글 표시명. floor_edge_beam 은 길이로 장/단변 분기.
  - `_fill_member_tree_flat()` (L862) — scene 미주입 폴백 — 옛 단일컬럼식 평탄 트리.
  - `get_member_full_label()` (L903) — 부재 mid 의 트리 위계 풀이름. 캐시 미스 시 폴백.
  - `get_current_policy()` (L916) — 현재 물량산출 라디오 정책. 초기 상태 폴백은 '1종'.
  - `get_member_group_name()` (L920) — 현 정책 기준 부재의 단면 그룹명. 없으면 ''.
  - `get_member_section_name()` (L926) — 현 정책 기준 부재의 채택 단면 명칭. 없으면 ''.
  - `get_member_ratio()` (L937) — 현 정책 기준 부재 응력비. 없으면 None.
  - `_fit_panel_to_quantity()` (L944) — 물량산출 탭의 표·트리 컨텐츠 폭에 맞춰 패널 minimumWidth 갱신.
  - `_fit_panel_to_tree()` (L1010) — 트리 컬럼 폭 합계에 맞춰 패널의 minimumWidth 를 갱신.
  - `clear()` (L1038) — 패널 내용을 비운다.
  - `_on_tree_item_changed()` (L1047) — 트리 선택 변경 → 단일 부재 / 그룹 모두 처리.
  - `_collect_leaf_mids()` (L1065) — 주어진 트리 아이템의 모든 자손 중 mid 가 달려 있는 leaf 의 mid 리스트.
  - `get_column_segments()` (L1084) — (2026-05-19) 사용자가 입력한 기둥 층구간 수. 1=분할 없음.
  - `_on_col_seg_apply()` (L1088) — (2026-05-19) '적용' 버튼 → controls 가 받아 물량 재산정.
  - `populate_quantity()` (L1092) — 물량산출 자료를 받아 탭을 채운다.
  - `_on_policy_changed()` (L1102) — 라디오버튼 변경.
  - `_on_tab_changed()` (L1115) — 탭 전환 — 물량산출 탭이면 ratio:정책 emit, 아니면 off.
  - `_on_ratio_tree_changed()` (L1122) — 응력비 트리 선택 → 잎이면 단일 부재 emit, 그룹이면 하위 mid 묶음 emit.
  - `_refresh_quantity_views()` (L1134) — 현재 정책 기준으로 모든 표를 다시 채운다.
  - `_fill_material_cost_table()` (L1234) — 자재비 총액표 — 강재/데크슬래브/콘크리트/합계 (2026-05-24).
  - `_fill_ratio_table()` (L1260) — 부재별 응력비 트리 — 구조해석탭(_fill_member_tree_ops) 과
  - `_iter_member_to_group()` (L1410) — rep.member_ratios 키 + sections_by_group 의 한정으로 (mid, gname) 생성.
  - `_lookup_member_group()` (L1425)
  - `_safe_group_max_ratio()` (L1429) — rep 에 group max_ratio 보관이 없어 ratio 사전에서 그룹 부재만 추려 max.
  - `_sum_group_steel()` (L1438) — 그룹별 길이·중량 합 — member_to_group 캐시 + members 길이 사용.
  - `populate_quantity_full()` (L1465) — quantity_reports + 정책별 member_to_group 캐시 함께 주입.


### `modular_3d/ui/controls.py` (1542 줄)
> FSM 컨트롤러 — 상태 전환, 이벤트 처리, 레이캐스팅.

**모듈 상수**
- `KEY_TO_TYPE` (L23) = `{'1': ComponentType.MODULE, '2': ComponentType.FLOOR_PANEL, '3': ComponentTyp...`
- `DEFAULT_DIMS` (L42) = `{ComponentType.MODULE: {'width': 3400, 'depth': 6820, 'height': 3400}, Compon...`

**클래스**
- `class Controller(F5Mixin, F6Mixin)` (L79) — 앱 상태 관리 + Qt 이벤트 → 모델/뷰 연결.
  - `__init__()` (L82)
  - `_set_state()` (L167)
  - `_current_defaults()` (L173)
  - `_update_preview()` (L179) — 고스트를 world_pos에 갱신 (타입별 z 시프트 포함).
  - `_preview_z_for_type()` (L191) — 부재 타입별 배치 z 레벨.
  - `on_qt_key_press()` (L210)
  - `on_qt_mouse_move()` (L289)
  - `refresh_after_camera()` (L296) — 카메라 이동/회전 후 고스트 위치 재계산.
  - `_snap_find()` (L301) — 스냅 탐색 헬퍼: vispy 씬 변환으로 find_nearest 호출.
  - `_update_from_mouse()` (L311)
  - `on_qt_mouse_press()` (L417)
  - `_on_dimension_confirmed()` (L461)
  - `_on_dimension_cancelled()` (L644) — 치수 입력 Esc — 정책 (b) 2026-05-12: IDLE 로 완전 취소.
  - `_do_undo()` (L660)
  - `_pick_component()` (L951) — 화면 좌표에서 레이를 쏴서 가장 가까운 부재 ID 반환.
  - `_select_component()` (L964) — 부재 선택 + 하이라이트 표시. 복사본이면 마스터로 리다이렉트.
  - `_deselect()` (L1005) — 선택 해제.
  - `_delete_selected()` (L1014) — 선택된 부재 삭제. 페어 모듈도 같이 삭제.
  - `_copy_selected()` (L1074) — 선택된 부재와 같은 타입/치수로 배치 모드 진입.
  - `_refresh_all_meshes()` (L1101) — 모든 컴포넌트 메쉬 재생성 (합체/분리 후 시각화 업데이트).
  - `_start_moving()` (L1111) — 선택된 부재의 이동 모드 시작.
  - `_update_moving_preview()` (L1129) — 이동 중 고스트로 미리보기.
  - `_confirm_moving()` (L1142) — 이동 확정.
  - `_move_merge_group()` (L1256) — 합체(merge) 관계로 연결된 컴포넌트를 XY delta만큼 이동.
  - `_cancel_moving()` (L1310) — 이동 취소 — 원래 위치 보존 + IDLE 로 완전 취소 (정책 b 2026-05-12).
  - `_start_axis_move()` (L1320) — MOVING 상태에서 X/Y 축 선택 → 거리 입력 모드.
  - `_confirm_axis_move()` (L1329) — 축 이동 거리 확정.
  - `_calc_mid_beam_placement()` (L1443) — 스냅 지점에서 중간보 자동 배치 (controls_geom 위임).
  - `_calc_mid_column_placement()` (L1447) — 스냅 지점에 중간기둥 자동 배치 (controls_geom 위임).
  - `_get_all_children()` (L1453) — comp_id와 그 페어의 모든 children ID를 반환.
  - `_move_child()` (L1461) — child를 delta만큼 이동 + 비주얼/스냅 갱신.
  - `_delete_children()` (L1474) — comp_id와 그 페어의 모든 children 삭제. {child_id: Component} 반환.
  - `_restore_children()` (L1497) — 삭제된 children 복원. parent_map: {child_id: parent_id}.
  - `_cancel_placement()` (L1535)


### `modular_3d/ui/controls_f5.py` (1851 줄)
> controls.py 의 F5 (도크 배치 모드) 관련 메소드 분리.

**클래스**
- `class F5Mixin` (L26) — F5 배치 모드 메소드 모음 — Controller 가 inherit.
  - `_resolve_move_targets()` (L29) — 선택된 부재 ID 로부터 이동/회전 대상 ID 리스트 결정 (정책 b 단계, 멀티층 의식).
  - `_refresh_2d()` (L119) — 부재 상태가 바뀐 직후 2D 캔버스 즉시 갱신.
  - `set_f5_dock()` (L127) — main_3d 가 만든 F5 도킹 패널을 컨트롤러에 등록.
  - `_f5_save_scene()` (L170) — F5 패널 '저장' 버튼 → Scene + 층수를 JSON 파일로 저장.
  - `_f5_load_scene()` (L186) — F5 패널 '불러오기' 버튼 → JSON 파일 로드 후 Scene 재구성.
  - `regenerate_all_core_slabs()` (L249) — 좌측 팔레트의 '코어 슬래브 생성/재생성' 버튼 핸들러.
  - `_on_floors_changed()` (L325) — 층수 입력창 변경 콜백 (단계 3d + 종속 부재 보존 패치).
  - `_f5_on_type_key()` (L522) — F5 캔버스에서 1~7 키 입력 → 배치 시작.
  - `_f5_on_canvas_click()` (L549) — F5 캔버스에서 PREVIEW 상태일 때 마우스 클릭 → 부재 생성 확정.
  - `_collapse_to_single_floor()` (L905) — [정의 탭 단일층] floor_index>0 부재를 모두 제거.
  - `_f5_on_room_complete()` (L923) — 2D 캔버스에서 폴리곤을 닫으면 호출 — 이름·용도 입력 후 실 생성.
  - `_render_room_3d()` (L955) — 실 1건을 3D 반투명 색면으로 렌더(이미 있으면 갱신).
  - `_render_all_rooms_3d()` (L968) — 씬의 모든 실을 3D 로 렌더(불러오기·작업공간 교체 후 호출).
  - `_clear_rooms_3d()` (L973) — 3D 실 색면 전부 제거(작업공간 비우기 시).
  - `_delete_room()` (L979) — 실 1건 삭제 — 씬·3D 색면 제거 + 되돌리기 기록 + 2D 갱신.
  - `_room_ghost()` (L993) — 실 이동/복사 미리보기 3D 고스트(반투명 색면) 갱신.
  - `_room_ghost_clear()` (L1006)
  - `_room_commit()` (L1010) — 실 이동/복사 확정 — 되돌리기 기록 + 3D/2D 갱신.
  - `_opening_target_kind()` (L1038) — 부재가 개구부 대상이면 'wall'|'slab', 아니면 ''.
  - `_opening_add_start()` (L1047) — '개구부 추가' → 크기 다이얼로그 후 캔버스 고스트 미리보기 시작.
  - `_opening_ghost()` (L1059) — 개구부 고스트(반투명 박스) 3D 갱신 — 미리보기 중 마우스 따라.
  - `_opening_ghost_clear()` (L1076)
  - `_opening_commit()` (L1080) — 개구부 확정 — add/move/copy + 되돌리기 기록 + 3D/2D 갱신.
  - `_delete_opening()` (L1107) — 부재의 index 번째 개구부 삭제 + 되돌리기 기록 + 3D/2D 갱신.
  - `_rebuild_component_visual()` (L1123) — 부재 1개의 3D 메시를 다시 생성(개구부 변경 반영).
  - `_import_definition_start()` (L1134) — '가져오기' 선택 → 정의 복원 + 캔버스 그룹 고스트 미리보기 시작.
  - `_f5_on_import_ghost()` (L1169) — 그룹 고스트(1층 합본 메시) 갱신 — 마우스 따라.
  - `_f5_on_import_cancel()` (L1193) — 가져오기 취소 — 고스트/템플릿 정리.
  - `_f5_on_import_commit()` (L1201) — 클릭 — 정의를 N층 복제 + 실 각층 복제로 씬에 삽입(되돌리기 기록).
  - `_update_room()` (L1281) — 실 용도·하중 수정 — 데이터 갱신 + 3D 색면 재렌더 + 2D 갱신.
  - `_sync_dependent_beam_sections()` (L1299) — 종속·합체 부재의 beam_section_type 을 부모/패널 유효 타입에 맞춰 동기화.
  - `_set_beam_section_type()` (L1315) — 선택 부재의 보 단면 타입 변경(각형강관/H형강) — 다층 복제본 + 종속 동기화.
  - `_f5_on_escape()` (L1359) — F5 캔버스 Esc — 배치/이동/복사 취소.
  - `_f5_update_ghost_at()` (L1379) — 현재 pending dims/comp_type/rotation/anchor 로 좌측 3D 고스트 갱신.
  - `_f5_on_canvas_move()` (L1398) — F5 캔버스 마우스 이동 (PREVIEW) → 좌측 3D 고스트 갱신.
  - `_f5_on_rotate()` (L1402) — R 키 — PREVIEW 고스트 90° 회전. 즉시 2D 화면 갱신.
  - `_f5_on_anchor()` (L1416) — V 키 — PREVIEW 고스트 앵커 변경. 즉시 2D 화면 갱신.
  - `_f5_on_move_key()` (L1434) — SELECTED + M 키 — 자유 이동 모드 진입.
  - `_interior_wall_height_for_parent()` (L1492) — 내벽 높이 = 부모 기준 자동 검출.
  - `_f5_on_dependency_pick()` (L1517) — DEPENDENCY_PICK 단계에서 부모 부재 클릭 시 호출.
  - `_f5_on_undo()` (L1601) — Z 키 — 직전 사용자 액션 한 번 되돌리기 (그룹 단위).
  - `_f5_on_copy()` (L1607) — C 키 — 선택된 부재의 복사 모드 진입.
  - `_f5_on_delete()` (L1779) — SELECTED + Delete — 다층 그룹 통째로 삭제 + 좌측 3D visual 제거.


### `modular_3d/ui/controls_f6.py` (977 줄)
> controls.py 의 F6 (구조해석 도크) 관련 메소드 분리.

**모듈 상수**
- `_PIN_PALETTE` (L24) = `[(0.0, 0.78, 0.78, 1.0), (0.82, 0.0, 0.75, 1.0), (0.66, 0.82, 0.0, 1.0), (0.1...`
- `_PIN_MAX_SLOTS` (L31) = `5`

**클래스**
- `class F6Mixin` (L34) — F6 해석 모드 메소드 모음 — Controller 가 inherit.
  - `_open_alignment_view()` (L37) — F5 도킹 패널 토글. F6 ↔ F5 배타 관리(2-2).
  - `_run_structural_analysis()` (L104) — F6: OpenSeesPy 기반 구조해석 (Phase 1 = 모델 빌드 + 좌측 OpenSees 뷰).
  - `_populate_ops_panel()` (L239) — 우측 도킹 패널에 ops 모델 요약 + 부재력 표시 + 케이스 토글.
  - `_run_quantity_takeoff()` (L274) — 초기 진입 — D+L 결과로 단면 산정 + 물량 집계 → 패널·뷰어 주입.
  - `_run_quantity_takeoff_for_case()` (L315) — (2026-05-19 작업 5) 선택 케이스에 대해 단면 산정 + 물량 집계 재실행.
  - `_on_column_segments_changed()` (L378) — (2026-05-19) 기둥 층구간 분할 '적용' 버튼 → 현재 케이스 재산정.
  - `attach_deformed_controls()` (L409) — main_3d 가 만든 변형 형상 컨트롤 위젯들을 컨트롤러에 연결 (Phase 4-B).
  - `_active_case_results()` (L418) — 현재 콤보박스에서 선택된 케이스의 OpsResults 반환 (없으면 None).
  - `_on_panel_case_changed()` (L427) — 패널 케이스 콤보박스 변경 → 좌측 변형 + 컨투어 + 물량 재산정.
  - `_refresh_all_pinned_popups()` (L463) — Phase 15 — 떠 있는 클릭 고정 정보창의 본문을 현재 케이스로 갱신.
  - `_on_deformed_toggled()` (L474)
  - `_on_deformed_scale_changed()` (L480)
  - `_refresh_deformed_view()` (L487) — 현재 케이스 + 슬라이더 배율로 좌측 변형 형상 그리기.
  - `_on_panel_contour_changed()` (L512)
  - `_refresh_contour()` (L516)
  - `_update_ops_hover()` (L563) — Phase 2~3 (설계서_부재호버.md) — F6 ops 뷰에서 마우스 호버 부재 강조 + 정보창.
  - `_ensure_hover_popup()` (L597) — 첫 호버 시 정보창 위젯 lazy 생성. 부모는 캔버스 native widget.
  - `_show_hover_popup_for()` (L612) — 호버 정보창 표시 — 마우스 옆 이동 + Phase 4 본문 갱신.
  - `_refresh_popup_content()` (L628) — Phase 4 — 부재 mid 정보로 정보창 본문 갱신.
  - `_hide_hover_popup()` (L686) — 호버 정보창 숨김. 위젯이 없으면 무시.
  - `_pinned_slots()` (L695) — 슬롯 리스트 lazy init. 각 항목: dict(mid, popup, color) 또는 None.
  - `_find_pinned_slot_for_mid()` (L706) — 같은 부재가 이미 고정돼 있으면 그 슬롯 인덱스 반환, 없으면 None.
  - `_allocate_pinned_slot()` (L714) — 빈 슬롯 인덱스 반환. 다 차면 가장 오래된 슬롯(인덱스 0) 자동 해제.
  - `_free_pinned_slot()` (L728) — 슬롯 닫기 — 정보창 destroy + 슬롯 None. 강조선은 호출자가 갱신.
  - `_refresh_pinned_highlight()` (L739) — 모든 슬롯의 부재 강조선을 한 번에 viewer 에 보냄.
  - `pin_member()` (L760) — 부재 클릭 시 호출 — 슬롯 할당 + 정보창 생성 + 강조선 갱신.
  - `close_all_pinned()` (L795) — Phase 14 — ESC 단축키 → 떠 있는 모든 정보창 + 강조선 동시 제거.
  - `_close_pinned_by_mid()` (L802) — Phase 11 — × 버튼 클릭으로 mid 슬롯 해제 + 강조선 갱신.
  - `_next_pin_color_preview()` (L810) — Phase 13 — 다음 클릭 시 배정될 슬롯 색.
  - `_cascade_if_overlapped()` (L822) — Phase 12 — 새 정보창이 기존 슬롯 창과 거의 겹치면 비킴.
  - `_on_panel_members_selected()` (L868) — 그룹 노드(모듈/층/역할) 클릭 → 하위 모든 부재를 한 번에 파란색 강조.
  - `_pin_member_at_canvas_corner()` (L891) — Phase 16 — 트리 잎 클릭 시 정보창을 캔버스 우하단에 등장.
  - `_on_panel_member_selected()` (L913) — 도킹 패널에서 부재 클릭 → 좌측 ops 뷰에 파란색 강조.
  - `_on_ratio_view_changed()` (L939) — analysis_panel.ratio_view_changed 시그널 핸들러.
  - `set_quantity_reports()` (L968) — Phase 6 통합 진입점에서 호출 — 정책별 QuantityReport 주입.


### `modular_3d/ui/controls_geom.py` (161 줄)
> controls.py 의 순수 기하 헬퍼 분리.

**최상위 함수**
- `ray_aabb_intersect()` (L24) — Ray-AABB 교차 판정. 교차 시 t 값 반환, 아니면 None.
- `raycast_z0()` (L47) — Ray 와 평면 z=z_target 의 교점 반환. 평행/뒤쪽이면 None.
- `create_component()` (L60) — Component 인스턴스 생성 — model.core.instantiate 위임 (단계 2 통합).
- `calc_mid_beam_placement()` (L69) — 스냅 지점에서 중간보 자동 배치.
- `calc_mid_column_placement()` (L143) — 스냅 지점에 중간기둥 자동 배치.


### `modular_3d/ui/define_tab.py` (430 줄)
> 모듈 정의 탭 — 컴포넌트(모듈/패널/수직모듈 + 종속부재)를 만들어 라이브러리에 저장.

**클래스**
- `class DefineTab(QWidget)` (L32) — 모듈 정의 탭 — 독립 작업공간 + 정의 라이브러리.
  - `__init__()` (L35)
  - `_build_ui()` (L106)
  - `_build_library_panel()` (L141)
  - `_on_room_mode_toggle()` (L181)
  - `_on_room_draw()` (L188)
  - `_on_opening_mode_toggle()` (L194)
  - `_on_opening_add()` (L199)
  - `_on_wall_place()` (L205) — '벽 배치(내벽)' → 부재 배치 머신으로 내벽 종속 배치 시작.
  - `_on_room_selected()` (L221)
  - `_show_room_props()` (L225) — 실 선택 시 실 패널 표시(부재 패널 숨김), 해제 시 반대.
  - `_on_room_edited()` (L236)
  - `_on_room_delete()` (L241)
  - `_on_palette_select()` (L250)
  - `_refresh_lib_list()` (L260)
  - `_selected_lib_name()` (L271)
  - `_on_lib_save()` (L278)
  - `_on_lib_load()` (L296)
  - `_on_lib_load_from_file()` (L313) — 건물 파일(scene.json)을 작업공간으로 불러온다 — 배치/접합부 설계 탭의
  - `_on_lib_new()` (L332)
  - `_on_lib_delete()` (L337)
  - `_clear_workspace()` (L345)
  - `_load_scene_into_workspace()` (L360)
  - `on_enter()` (L396) — main_3d 가 정의 탭 진입 시 호출 — 캔버스에 포커스.
  - `eventFilter()` (L401)


### `modular_3d/ui/design_props_panel.py` (180 줄)
> 우측 디자인 속성 패널 — 활성/선택 부재 정보.

**모듈 상수**
- `_DIRECT_BEAM_TYPES` (L32) = `{ComponentType.MODULE, ComponentType.FLOOR_PANEL, ComponentType.VERTICAL_MODULE}`
- `_DEP_BEAM_TYPES` (L35) = `{ComponentType.CANTILEVER_BEAM, ComponentType.CANTILEVER_SLAB, ComponentType....`
- `_NO_BEAM_TYPES` (L38) = `{ComponentType.MID_COLUMN, ComponentType.CORE, ComponentType.CORE_SLAB, Compo...`

**클래스**
- `class DesignPropertiesPanel(QWidget)` (L44) — 디자인 탭 우측 패널 — 활성/선택 부재 정보 + 보 단면 타입 선택.
  - **시그널** `beam_section_changed = pyqtSignal(int, str)` (L48)
  - `__init__()` (L50)
  - `_setup_ui()` (L59)
  - `refresh_active()` (L127) — 활성 부재 변경 시 호출 — 라벨 갱신.
  - `refresh_selected()` (L135) — 선택 부재 변경 시 호출 — 라벨 + 보 단면 타입 콤보 갱신.
  - `_refresh_section_combo()` (L153) — 보 단면 타입 콤보 — 직접선택 부재는 활성, 종속/합체는 부모 따라감(비활성).
  - `_on_sec_changed()` (L174)


### `modular_3d/ui/evaluation_panel.py` (470 줄)
> 평가 탭 패널 — 한 케이스의 종합 보고서.

**모듈 상수**
- `_CARD_BG` (L26) = `'#FFFFFF'`
- `_CARD_BORDER` (L27) = `'#DDE4ED'`
- `_HEADLINE_BG` (L28) = `'#F5F8FF'`
- `_HEADLINE_BORDER` (L29) = `'#1F4E79'`
- `_SECTION_TITLE_COLOR` (L30) = `'#1F4E79'`
- `_PAGE_BG` (L31) = `'#EDF2F7'`
- `_EMPTY_COLOR` (L32) = `'#a0a8b5'`

**최상위 함수**
- `_won()` (L35)
- `_section_title()` (L39)
- `_card()` (L48)
- `_empty_label()` (L58)

**클래스**
- `class EvaluationPanel(QWidget)` (L66)
  - **시그널** `save_case_requested = pyqtSignal()` (L67)
  - `__init__()` (L69)
  - `apply_data()` (L208)
  - `_render_headline()` (L220)
  - `_render_members()` (L235)
  - `_render_materials()` (L265)
  - `_render_transport()` (L337)
  - `_render_schedule()` (L356)
  - `_render_cost()` (L391)
  - `_table()` (L403)
  - `_mk_head_cell()` (L433)
  - `_mk_cost_cell()` (L448)
  - `_clear_layout()` (L464)


### `modular_3d/ui/joint_edit_panel.py` (363 줄)
> 접합부 조정 탭 우측 위젯 — 결합 토글·옵션·범례.

**클래스**
- `class JointEditPanel(QWidget)` (L25) — 접합부 조정 탭 우측 패널.
  - **시그널** `diaphragm_toggle = pyqtSignal(bool)` (L38)
  - **시그널** `rule_visibility_changed = pyqtSignal(str, bool)` (L41)
  - **시그널** `edit_mode_toggled = pyqtSignal(bool)` (L44)
  - **시그널** `joint_change_requested = pyqtSignal(str)` (L46)
  - **시그널** `joint_revert_requested = pyqtSignal()` (L48)
  - **시그널** `add_mode_toggled = pyqtSignal(bool)` (L51)
  - **시그널** `save_requested = pyqtSignal()` (L55)
  - `__init__()` (L57)
  - `_setup_ui()` (L65)
  - `_on_edit_toggle()` (L230) — 편집 모드 토글 — 켜면 추가 모드 해제.
  - `_on_add_toggle()` (L236) — 추가 모드 토글 — 켜면 편집 모드 해제.
  - `is_add_rigid()` (L246) — 접합 추가 시 강접으로 만들지(체크) 여부.
  - `is_add_single_layer()` (L250) — 접합 추가 시 이 층만(다른 층 복제 안 함) 여부.
  - `is_add_right_angle()` (L254) — 접합 추가 시 직각(ㄴ자) 결합 여부.
  - `is_edit_single_layer()` (L258) — 접합 제거·편집 시 이 층만 적용 여부(체크) — 끄면 모든 층 일괄.
  - `set_add_hint()` (L262) — 접합 추가 안내 문구 갱신.
  - `show_selected_joint()` (L267) — 픽킹된 접합 정보를 표시하고 변경 버튼을 활성화한다.
  - `clear_selection()` (L291) — 선택 해제 — 변경 버튼 비활성(전체 초기화 버튼은 항상 활성).
  - `refresh_legend()` (L301) — spec 에서 추출한 rule_id 집합으로 범례를 새로 그림.


### `modular_3d/ui/main_3d.py` (1864 줄)
> 진입점 — QMainWindow + QTabWidget(7 탭) + Vispy + Controller 연결.

**모듈 상수**
- `TAB_DEFINE` (L79) = `0`
- `TAB_DESIGN` (L80) = `1`
- `TAB_JOINT_EDIT` (L81) = `2`
- `TAB_ANALYSIS` (L82) = `3`
- `TAB_QUANTITY` (L83) = `4`
- `TAB_TRANSPORT` (L84) = `5`
- `TAB_SCHEDULE` (L87) = `6`
- `TAB_JOINT_AIR` (L88) = `TAB_SCHEDULE`
- `TAB_EVALUATION` (L90) = `7`
- `TAB_FINAL` (L91) = `TAB_EVALUATION`

**최상위 함수**
- `_apply_app_font()` (L1839) — 전역 폰트를 Pretendard 로 설정.
- `main()` (L1855)

**클래스**
- `class _TransportBridge(QObject)` (L48) — Three.js JS 가 트럭 클릭 시 on_truck_clicked(trip_no) 호출.
  - **시그널** `truck_clicked_in_3d = pyqtSignal(int)` (L53)
  - `on_truck_clicked()` (L56)
- `class MainWindow(QMainWindow)` (L94)
  - `__init__()` (L95)
  - `_build_menu_bar()` (L306) — 상단 메뉴 줄 — 사업 공통 설정 진입점(프로젝트 설정) 1 개.
  - `_open_project_settings()` (L321) — 프로젝트 설정 모달 — 공통 설정 입력. 확인 시 세션 메모리에 보관.
  - `_open_transport_catalog()` (L340) — 파일 메뉴 진입점 — AnalysisPanel 의 _transport_tab 에 위임.
  - `_on_schedule_payload_pushed()` (L348) — 공정표 HTML 의 calc() 가 보낸 결과를 캐시. 평가 탭이 진입 시 어댑터로 전달.
  - `_on_evaluation_save_case()` (L354) — 저장 버튼 → QFileDialog → .case.json 완전 스냅샷 저장.
  - `_build_deformed_widget()` (L395)
  - `_build_design_tab()` (L416) — 디자인 탭: 좌 팔레트 + 중앙(3D | 2D) + 우 속성 + 하단 치수.
  - `_build_joint_edit_tab()` (L471) — 접합부 조정 탭: 중앙(3D 와이어프레임) + 우 접합부 UI placeholder.
  - `_build_analysis_tab()` (L501) — 구조해석 탭: 중앙(3D + 변형슬라이더) + 우 AnalysisPanel(요약/내부력).
  - `_build_quantity_tab()` (L531) — 물량 탭: 중앙(3D + 변형슬라이더) + 우 AnalysisPanel(물량산출).
  - `_build_transport_tab_layout()` (L558) — 운송 탭 — *3 단 분할* (좌 입력 / 중 3D 도식 / 우 결과).
  - `_on_transport_left_toggle()` (L655) — [2026-05-27] 운송 탭 좌측 입력 패널 접기 / 펼치기 토글.
  - `_on_transport_pack_updated()` (L679) — [Phase C] 운송 패널 계산 완료 → center pane 3D 도식 갱신.
  - `_on_transport_trip_clicked()` (L694) — [Phase E + Three.js 마이그레이션] 회차표 행 클릭 → JS 함수 호출로
  - `_on_3d_truck_clicked()` (L708) — [양방향 동기화 — 2026-05-27] 3D 트럭 클릭 → 회차표·경제성표 행 선택.
  - `_render_transport_3d()` (L721) — [Three.js 마이그레이션 — 2026-05-26] 운송 3D 도식 렌더.
  - `_write_transport_3d_temp_html()` (L790) — ASCII 경로 임시 파일에 HTML 저장 — file:// URL 로 로드 가능.
  - `_build_placeholder_tab()` (L817) — 빈 탭 — 운송 / 공기 및 접합부 / 최종평가 용. 내부 내용은 추후 추가.
  - `_on_tab_changed()` (L838)
  - `_restore_design_view()` (L1039) — 구조해석/물량 탭 → 디자인 탭으로 돌아왔을 때 일반 메쉬 뷰 복원.
  - `_rebuild_all_component_visuals()` (L1076) — Scene 의 모든 부재 메쉬를 viewer 에서 완전히 제거 후 재빌드.
  - `_run_joint_edit_preview()` (L1104) — 접합부 조정 탭 진입 시 spec 만 빌드해 와이어프레임 표시.
  - `_on_joint_edit_mode()` (L1166) — 접합 편집 모드 토글. 끄면 선택·강조 정리.
  - `_on_joint_pick()` (L1174) — 3D 좌클릭 → 가장 가까운 컴포넌트 간 접합 선택·강조.
  - `_highlight_selected_joint()` (L1190) — 선택 접합 강조 — 직각접합이면 위-N1-아래 체인, 아니면 한 결합.
  - `_right_angle_chain()` (L1199) — 중간 노드 n1_tag 를 공유하는 직각접합 결합들의 노드 체인 [위, N1, 아래].
  - `_find_n1_tag_by_xy()` (L1213) — 평면 위치로 중간 노드(N1, panel_z_route) tag 찾기(다층 중 첫 매칭).
  - `_find_add_override_by_n1()` (L1225) — N1 평면 위치로 직각 add 오버라이드 역추적. N1.xy = 위(z 큰) 점 xy.
  - `_comp_group()` (L1241) — 컴포넌트 id → group_id (보조 식별). 없으면 0.
  - `_on_joint_change()` (L1246) — 선택 접합에 변경(remove/pin/rigid) 적용 → 같은 xy 모든 층 → 재빌드.
  - `_warn_if_unstable()` (L1342) — 현재 접합 프리뷰 모델이 mechanism(불안정) 위험이면 경고(차단 없음).
  - `_on_joint_revert()` (L1369) — 모든 접합 변경 초기화 → 자동 규칙 상태로(확인 후).
  - `_design_signature()` (L1386) — 현재 디자인(부재 구성)의 시그니처 — 부재 id·위치·치수·회전 기반.
  - `_maybe_prompt_joint_choice()` (L1397) — 접합 탭 진입 시 접합 오버라이드 처리 정책(3 케이스).
  - `_on_joint_add_mode()` (L1444) — 접합 추가 모드 토글. 끄면 진행 중 첫 점·후보·고스트 정리.
  - `_reset_add_progress()` (L1451) — 접합 추가 진행 상태(첫 점·후보·고스트·선) 모두 정리.
  - `_validate_add()` (L1462) — 신규 접합 두 점 검증(모서리 위 점). 반환 (가능여부, 안내문).
  - `_update_joint_ghost()` (L1487) — 접합 추가 모드 — 마우스 위치 미리보기.
  - `_on_joint_add_pick()` (L1519) — 접합 추가 모드 좌클릭 — 모서리 위 점 두 개로 신규 접합 생성.
  - `_run_analysis_if_needed()` (L1587) — 구조해석/물량 탭 진입 시 OpenSeesPy 해석을 실행.
  - `_on_room_mode_toggle()` (L1617) — 팔레트 '실 모드' 토글 → 배치 탭 2D 캔버스 편집 모드 전환(2단계).
  - `_on_room_draw()` (L1625) — 팔레트 '실 그리기' → 배치 탭 2D 캔버스 실 그리기 모드 진입(2단계).
  - `_on_opening_mode_toggle()` (L1631) — 팔레트 '개구부 모드' 토글 → 배치 탭 캔버스 편집 모드 전환(3단계).
  - `_on_opening_add()` (L1637) — 팔레트 '개구부 추가' → 다음 부재 클릭으로 개구부 배치(3단계).
  - `_on_wall_place()` (L1643) — 팔레트 '벽 배치(내벽)' → 부재 배치 머신으로 내벽 종속 배치 시작.
  - `_on_room_selected()` (L1660)
  - `_show_room_props()` (L1664) — 실 선택 시 실 패널 표시(부재 패널 숨김), 해제 시 반대.
  - `_on_room_edited()` (L1675)
  - `_on_room_delete()` (L1680)
  - `_on_palette_select()` (L1688) — 팔레트 버튼 클릭 → 2D 캔버스의 키 1~8 입력과 100% 동일 경로.
  - `_on_design_type_select()` (L1708) — 배치 탭 타입 선택 인터셉트(버튼·1~9키 공용).
  - `_on_scenes_floors_changed()` (L1737) — 시나리오 탭의 층수 SpinBox 변경 → F5 패널과 동기화 + 시그널 발화.
  - `_on_scene_save()` (L1742) — 시나리오 탭 '저장' 버튼 → F5Mixin._f5_save_scene 재사용.
  - `_on_scene_load()` (L1747) — 시나리오 탭 '불러오기' 버튼 → F5Mixin._f5_load_scene 재사용 후
  - `_is_design_tab()` (L1756)
  - `eventFilter()` (L1761)
  - `keyPressEvent()` (L1823) — 메인 윈도우 키 이벤트.


### `modular_3d/ui/member_info_popup.py` (436 줄)
> 부재 단위 호버/클릭 정보창 위젯 (설계서_부재호버.md).

**모듈 상수**
- `_KIND_KO` (L27) = `{'beam': '보', 'column': '기둥', 'truss': '트러스'}`
- `_DEFAULT_W` (L151) = `240`
- `_DEFAULT_H` (L152) = `70`
- `HOVER_OFFSET` (L155) = `(16, 16)`

**최상위 함수**
- `_fmt_kn()` (L17) — N(뉴턴) → kN 문자열, 소수 1 자리.
- `_fmt_knm()` (L22) — N·mm → kN·m 문자열, 소수 1 자리.

**클래스**
- `class ForceDiagramWidget(QWidget)` (L31) — AFD/SFD/BMD 공통 미니 그래프 — QPainter 직접 그림 (경량, matplotlib X).
  - `__init__()` (L42)
  - `sizeHint()` (L60)
  - `set_values()` (L63) — station 값 배열 갱신 후 다시 그림. None 이면 빈 상태.
  - `set_title()` (L71) — 제목 갱신 (예: 'SFD (Vz)' — dominant 성분 표시).
  - `paintEvent()` (L77)
- `class MemberInfoPopup(QWidget)` (L158) — 부재 정보 떠다니는 패널 — 캔버스 위 absolute 위젯.
  - **시그널** `closed = pyqtSignal()` (L169)
  - `__init__()` (L171)
  - `_on_close_clicked()` (L248) — × 버튼 클릭 → closed 시그널 emit. 슬롯 매니저가 해제 처리.
  - `set_pinned_mode()` (L252) — 호버 임시창 / 클릭 고정창 모드 전환 (자체점검 #5).
  - `resizeEvent()` (L263) — × 버튼을 항상 우측 상단 모서리에 위치 유지.
  - `set_border_color()` (L272) — 외곽선 색 갱신. paintEvent 가 즉시 다시 그림.
  - `paintEvent()` (L285) — 배경 + 둥근 모서리 + 외곽선을 직접 그림.
  - `set_info()` (L297) — Phase 4 — 부재 본문 텍스트 갱신.
  - `move_to_mouse()` (L385) — 마우스 Qt 좌표 옆 (offset 적용) 으로 이동. 부모 캔버스 영역 안에
  - `mousePressEvent()` (L401) — 클릭 시작 — 드래그 anchor 기록.
  - `mouseMoveEvent()` (L409) — 드래그 — 부모 영역 안으로 클램프하며 이동.
  - `mouseReleaseEvent()` (L431)


### `modular_3d/ui/opening_dialog.py` (68 줄)
> 개구부 크기 입력 다이얼로그.

**최상위 함수**
- `ask_opening_size()` (L60) — 모달 — {'w','h','sill'} 또는 취소 시 None.

**클래스**
- `class OpeningSizeDialog(QDialog)` (L17) — 개구부 크기 입력 — 가로·세로 + 바닥높이(벽 전용).
  - `__init__()` (L20)
  - `result_size()` (L55)


### `modular_3d/ui/palette_panel.py` (259 줄)
> 좌측 부재 팔레트 패널 — 1~9 단축키 버튼화.

**모듈 상수**
- `PALETTE_ITEMS` (L34) = `[('1', ComponentType.MODULE, '모듈', '3400×6820×3400', True), ('2', ComponentTy...`

**클래스**
- `class PalettePanel(QWidget)` (L47) — 좌측 부재 팔레트 — 디자인 탭 전용.
  - `__init__()` (L50)
  - `_setup_ui()` (L75)
  - `_set_component_buttons_enabled()` (L216)
  - `_on_room_mode_toggled()` (L225) — 실 모드 ON/OFF — 개구부 모드와 상호배타, 부재 버튼 비활성/활성.
  - `_on_opening_mode_toggled()` (L235) — 개구부 모드 ON/OFF — 실 모드와 상호배타, 부재 버튼 비활성/활성.
  - `set_room_mode()` (L246) — 외부에서 실 모드 토글 상태 동기화(시그널 발생).
  - `clear_special_modes()` (L251) — 실·벽/개구부 토글을 모두 OFF — 컴포넌트 모드로 복귀(시그널 발생).


### `modular_3d/ui/project_settings.py` (358 줄)
> 프로젝트 설정 — 사업 전체에 공통으로 쓰이는 입력을 한곳에서 받는 모달 창.

**모듈 상수**
- `CITY_HOLIDAYS` (L34) = `[('수도권', '서울', 170, 158), ('수도권', '인천', 170, 156), ('수도권', '수원 경기남부', 170, 16...`
- `_DEFAULT_CITY` (L56) = `'천안 충남'`

**클래스**
- `class ProjectSettings` (L60) — 사업 공통 설정 — 세션 메모리에만 보관되는 값들.
- `class ProjectSettingsDialog(QDialog)` (L92) — 프로젝트 설정 모달 — ProjectSettings 값을 읽어 폼을 채우고, 확인 시 되씀.
  - `__init__()` (L100)
  - `_build_region_combo()` (L251) — 지역그룹 헤더(선택 불가) + 도시 항목으로 콤보를 채운다.
  - `_on_region_changed()` (L273) — 지역 도시 선택 시 별표1/별표2 자동 채움(헤더 선택은 무시).
  - `_select_city()` (L282) — 도시 라벨로 콤보 항목을 선택한다(없으면 무시).
  - `_load_from_settings()` (L291)
  - `_apply_fixed_bulk()` (L326) — 1회 고정비 일괄 적용 — 저상 입력값을 광폭·A-frame 에도 복사.
  - `_on_accept()` (L332) — 위젯 값을 ProjectSettings 에 되쓰고 창을 닫는다.


### `modular_3d/ui/room_dialog.py` (80 줄)
> 실(Room) 용도 선택 다이얼로그.

**최상위 함수**
- `ask_room_type()` (L71) — 모달 다이얼로그 — 용도 key 또는 취소 시 None.

**클래스**
- `class RoomInfoDialog(QDialog)` (L21) — 실 용도 선택.
  - `__init__()` (L24)
  - `_update_hint()` (L59)
  - `result_value()` (L66) — 선택한 용도 key 반환.


### `modular_3d/ui/room_props_panel.py` (174 줄)
> 우측 실(Room) 속성 패널 — 선택한 실의 용도·하중 편집 + 삭제.

**클래스**
- `class RoomPropertiesPanel(QWidget)` (L26) — 선택 실 편집 — 용도 + 활하중/부가하중 + 적용/삭제.
  - **시그널** `room_edited = pyqtSignal(int, str, float, float)` (L30)
  - **시그널** `room_delete_requested = pyqtSignal(int)` (L32)
  - `__init__()` (L34)
  - `_build_ui()` (L41)
  - `_on_type_changed()` (L99) — 용도 변경 → 그 용도 기본 활하중/부가하중으로 칸 자동 채움 + 안내 갱신.
  - `set_room()` (L113) — 선택 실 정보를 채운다.
  - `clear()` (L137) — 선택 없음 상태.
  - `_set_editing()` (L142)
  - `_parse()` (L148)
  - `_on_apply()` (L154)
  - `_on_delete()` (L169)


### `modular_3d/ui/schedule_panel.py` (180 줄)
> 공정표 탭 패널 — 팀원 HTML(모듈러주택_공정표.html) 임베드 + 자동주입.

**모듈 상수**
- `_HERE` (L49) = `Path(__file__).resolve().parent`
- `_SCHEDULE_DIR` (L50) = `_HERE.parent / 'schedule'`
- `_SCHEDULE_HTML` (L51) = `_SCHEDULE_DIR / '모듈러주택_공정표.html'`

**클래스**
- `class _ScheduleBridge(QObject)` (L29) — 공정표 HTML 의 calc() 가 결과를 푸시할 통로.
  - **시그널** `schedule_payload_pushed = pyqtSignal(dict)` (L36)
  - `on_schedule_calculated()` (L39)
- `class SchedulePanel(QWidget)` (L54) — 공정표 탭의 루트 위젯.
  - `__init__()` (L64)
  - `web_view()` (L98)
  - `bridge()` (L101) — 외부(main)가 schedule_payload_pushed 시그널을 받기 위한 핸들.
  - `apply_scene_data()` (L106) — schedule_adapter.build_scene_data() 결과를 페이지에 주입.
  - `_on_load_finished()` (L120)
  - `_apply_now()` (L127) — JS 측에 importScene + moduleTypes + project(지역·착공일) 자동주입.


### `modular_3d/ui/transport_catalog_dialog.py` (440 줄)
> 트럭 카탈로그 관리 다이얼로그 (Phase 7).

**모듈 상수**
- `_HEADERS` (L50) = `['출처', '이름', '종류', '최대길이(mm)', '최대폭(mm)', '최대높이(mm)', '최대중량(kg)', '차체중량(kg)',...`

**클래스**
- `class _TruckEditDialog(QDialog)` (L58) — 단일 트럭 추가/편집 모달.
  - `__init__()` (L65)
  - `_build_ui()` (L85)
  - `_fill_from_truck()` (L137)
  - `_to_dict()` (L153)
  - `_on_accept()` (L167)
  - `result_truck()` (L197)
- `class TransportCatalogDialog(QDialog)` (L201) — 트럭 카탈로그 관리 다이얼로그 (메인).
  - **시그널** `catalog_changed = pyqtSignal()` (L209)
  - `__init__()` (L211)
  - `_build_ui()` (L223)
  - `_reload_table()` (L266)
  - `_selected_row()` (L296)
  - `_selected()` (L300)
  - `_on_selection_changed()` (L306)
  - `_update_buttons_enabled()` (L309)
  - `_existing_names()` (L318)
  - `_on_add()` (L321)
  - `_on_edit()` (L338)
  - `_on_clone()` (L377)
  - `_on_delete()` (L398)
  - `_delete_project_truck()` (L420) — 프로젝트 trucks.json 에서 단일 트럭 삭제 (catalog_io 에 없어 인라인).
  - `_info_saved()` (L433)


### `modular_3d/ui/transport_panel.py` (1339 줄)
> 운송탭 UI — 분석 ⑤ 영역 ①~④ 구현 (Phase 6).

**모듈 상수**
- `_PIPELINE_LABELS` (L73) = `[(2, '해석'), (3, '단면'), (4, '분할'), (5, '분류'), (6, '변환'), (7, '패킹'), (8, '운임'),...`

**최상위 함수**
- `_strip_focus_visible_css()` (L1335)

**클래스**
- `class TransportTab(QWidget)` (L80) — 운송탭 본체 — AnalysisPanel 의 4번째 탭으로 삽입.
  - **시그널** `transport_member_highlight = pyqtSignal(list)` (L83)
  - **시그널** `transport_blocked = pyqtSignal(int)` (L84)
  - **시그널** `transport_pack_updated = pyqtSignal(object, object)` (L87)
  - **시그널** `transport_trip_clicked = pyqtSignal(int)` (L90)
  - `__init__()` (L92)
  - `apply_project_settings()` (L142) — 프로젝트 설정 공통값을 운송 옵션 위젯에 주입하고 읽기전용으로 만든다.
  - `_build_ui()` (L200)
  - `_build_area_toolbar()` (L251) — [Phase 7] 운송탭 최상단 — 카탈로그 관리 버튼.
  - `_build_area_overrides()` (L269) — [Phase 7] 영역 ⑤ — 회차별 트럭 override 표.
  - `_build_area_references()` (L307) — 영역 ⑦ — [📖 참고자료] 버튼.
  - `_build_area_status()` (L319) — 영역 ① — 정책 라벨 + 8단계 인디케이터.
  - `_build_area_options()` (L344) — 영역 ② — 운송 옵션 패널.
  - `_wire_auto_recompute()` (L450) — 옵션 위젯 변경을 무효화 단계와 함께 연결.
  - `_on_option_changed()` (L478) — 옵션 변경 → 해당 단계부터 캐시 무효 + 자동 재계산 ON 이면 디바운스 실행.
  - `_build_area_metrics()` (L491) — 영역 ③ — 5카드 메트릭.
  - `_build_area_subtabs()` (L521) — 영역 ④ — 회차표 / 적재율 / 경제성. (2026-05-26 Phase C: 도식 sub-탭 제거 —
  - `_build_subtab_trip_table()` (L536)
  - `_build_subtab_util_chart()` (L559)
  - `_build_subtab_views()` (L571)
  - `_build_subtab_economics()` (L614)
  - `_build_area_diagnostics()` (L638) — 영역 ⑧ — 진단 (누적).
  - `_diag_append()` (L660) — [Phase 9 C-5] 진단 누적 추가 — 타임스탬프 prefix.
  - `set_scene_and_model()` (L671)
  - `set_design_result()` (L677) — design_result 주입.
  - `set_state()` (L707) — state ∈ {NoDesign, Ready, Computing, ResultShown, Error}.
  - `_read_options()` (L722)
  - `_read_spacing()` (L732)
  - `_read_economics_options()` (L736)
  - `_run_transport()` (L750)
  - `_render_metrics()` (L817)
  - `_render_trip_table()` (L827)
  - `_render_util_chart()` (L844)
  - `_render_view_combo()` (L866)
  - `_on_view_combo_changed()` (L892)
  - `_render_views()` (L899)
  - `_render_trip_pair()` (L912) — 단일 trip 을 Top/Rear 두 WebEngineView 에 렌더 (공유 헬퍼).
  - `_write_temp_html()` (L964) — HTML 을 ASCII 경로 임시 파일로 저장. 반환 경로는 ASCII 보장.
  - `_render_economics()` (L996)
  - `_render_diagnostics()` (L1022) — [2026-05-26] 진단 표시 — 사용자에게 의미 있는 항목만, 일관된 형식으로.
  - `_refresh_stage_indicators()` (L1064)
  - `_update_policy_label()` (L1077)
  - `on_policy_sync()` (L1084) — 물량탭에서 정책 변경 → 정책 라벨 갱신 + 캐시 무효.
  - `on_scene_modified()` (L1100) — 씬 편집 → 해석부터 다 무효 표시. 자동 트리거 X.
  - `_on_trip_row_double_clicked()` (L1107)
  - `_on_trip_row_clicked()` (L1121) — 회차표 행 단일 클릭 → 통합 진입점 select_trip.
  - `_on_eco_row_clicked()` (L1131) — 경제성 sub-탭 행 단일 클릭 → 동일 진입점.
  - `select_trip()` (L1147) — 공통 회차 선택 진입점 — 회차표·경제성표 둘 다 *프로그래밍 선택* 후 신호 emit.
  - `_apply_overrides()` (L1169) — 패킹 결과에 사용자 override 를 일괄 적용.
  - `_render_override_table()` (L1214) — 영역 ⑤ override 표 갱신 — 매 _run_transport 후 호출.
  - `_on_override_changed()` (L1253) — override 콤보 변경 → 즉시 검증 + 재실행 트리거.
  - `_clear_overrides()` (L1269)
  - `open_catalog_dialog()` (L1279) — 외부(파일 메뉴 등) 에서도 호출 가능한 공용 진입점.
  - `_open_catalog_dialog()` (L1283)
  - `_reload_catalog()` (L1292) — 카탈로그 JSON 재로드 + 도로 콤보·override 콤보 갱신.
  - `set_project_root()` (L1311) — 외부(MainWindow) 에서 프로젝트 루트 주입.
  - `_open_references_dialog()` (L1320)
  - `_show_error()` (L1325)


### `modular_3d/ui/transport_references_dialog.py` (115 줄)
> 참고자료 다이얼로그 (Phase 8 영역 ⑦).

**모듈 상수**
- `_REF_DIR` (L25) = `Path(__file__).resolve().parent.parent / 'transport' / 'references'`

**클래스**
- `class TransportReferencesDialog(QDialog)` (L28) — 운송 참고자료 모달 — references/*.md 자동 탭.
  - `__init__()` (L31)
  - `_build_ui()` (L42)
  - `_discover_md_files()` (L63)
  - `_tab_title()` (L68) — 파일명에서 숫자 접두사·확장자 제거한 보기용 제목.
  - `_build_md_view()` (L76)
  - `_build_empty_state()` (L90)
  - `_reload()` (L106) — references 폴더를 다시 읽어 다이얼로그 재구성.


### `modular_3d/ui/ui_panel.py` (334 줄)
> Qt UI — 치수 입력 패널, 상태바 관리.

**클래스**
- `class DimensionInputPanel(QWidget)` (L13) — 하단 치수 입력 패널. Tab으로 필드 이동, Enter 확정, Esc 취소.
  - **시그널** `confirmed = pyqtSignal(dict)` (L16)
  - **시그널** `cancelled = pyqtSignal()` (L17)
  - `__init__()` (L19)
  - `_setup_ui()` (L28)
  - `set_mode_text()` (L81) — 하단 dim 패널 좌측에 현재 모드 표시 (c4).
  - `configure_for_type()` (L85) — 부재 타입별 필드 표시/숨김.
  - `set_defaults()` (L182) — 기본값 설정.
  - `get_values()` (L195) — 현재 입력값 반환. 구조벽일 때 'merge' 키 포함 (X1).
  - `activate()` (L226) — 패널 표시 + 첫 필드에 포커스.
  - `activate_move()` (L239) — 이동 거리 입력 모드. axis_name = 'X' or 'Y'.
  - `get_move_distance()` (L255) — 이동/확장 거리 반환.
  - `deactivate()` (L262) — 패널 숨기기.
  - `eventFilter()` (L269) — QLineEdit의 Enter/Esc/Tab을 가로채서 패널 동작으로 처리.
  - `_focus_next()` (L284) — 다음 활성 필드로 포커스 이동.
- `class StatusBarManager` (L303) — 상태바 관리.
  - `__init__()` (L306)
  - `update_mode()` (L320)
  - `update_coords()` (L330)
  - `update_count()` (L333)


### `modular_3d/카탈로그/__init__.py` (29 줄)
> 매직넘버·재료·단면 카탈로그 — 본부 (2026-05-12 신규).


### `modular_3d/카탈로그/geometry.py` (69 줄)
> 부재 치수 상수 — 모듈 높이·단면 폭·갭·슬래브 두께 등.

**모듈 상수**
- `MODULE_HEIGHT_MM` (L14) = `3400`
- `MODULE_JOINT_GAP_MM` (L16) = `20`
- `FLOOR_HEIGHT` (L19) = `MODULE_HEIGHT_MM + MODULE_JOINT_GAP_MM`
- `VERTICAL_MODULE_FLOORS` (L22) = `3`
- `SECTION_W_MM` (L25) = `200.0`
- `SECTION_H_MM` (L26) = `200.0`
- `SECTION_T_MM` (L27) = `8.0`
- `SLAB_THICKNESS_MODEL_MM` (L33) = `150.0`
- `SLAB_THICKNESS_MM` (L34) = `150.0`
- `NONBEARING_WALL_THICKNESS_MM` (L37) = `100.0`
- `CORE_SLAB_DEFAULT_THICKNESS_MM` (L39) = `180.0`
- `CORE_WALL_DEFAULT_THICKNESS_MM` (L41) = `300.0`
- `CORE_TRUSS_GRID_MM` (L43) = `800.0`
- `SPLIT_NODE_BASE_OFFSET` (L53) = `10000`
- `RULE_NODE_OFFSET_R03` (L54) = `20000`
- `RULE_NODE_OFFSET_R06` (L55) = `30000`
- `RULE_NODE_OFFSET_R09` (L56) = `40000`


### `modular_3d/카탈로그/h_sections.py` (167 줄)
> KS D 3502 H형강(열간압연 H형강) 단면 카탈로그.

**모듈 상수**
- `_H_SPECS` (L117) = `[(100, 100, 6, 8, 10), (125, 125, 6.5, 9, 10), (150, 150, 7, 10, 11), (175, 1...`

**최상위 함수**
- `_h_section_props()` (L32) — H형강 단면 특성 계산.
- `build_h_catalog()` (L145) — H형강 카탈로그를 단면적 오름차순으로 반환.
- `find_h_section_by_name()` (L156) — 이름으로 카탈로그 검색.

**클래스**
- `class HSection` (L73) — H형강 단면.
  - `from_dimensions()` (L93)
  - `weight_per_m_kg()` (L107) — 단위 길이당 중량 (kg/m).


### `modular_3d/카탈로그/loads.py` (22 줄)
> 하중·하중조합·압력 상수.

**모듈 상수**
- `LIVE_LOAD_KN_M2` (L7) = `2.0`
- `LIVE_LOAD_N_MM2` (L8) = `LIVE_LOAD_KN_M2 * 0.001`
- `LOAD_COMBO_DL` (L11) = `1.2`
- `LOAD_COMBO_LL` (L12) = `1.6`
- `SLAB_ONE_WAY_RATIO` (L15) = `2.0`


### `modular_3d/카탈로그/material_prices.py` (50 줄)
> 자재 단가 DB 로더.

**모듈 상수**
- `_JSON_PATH` (L20) = `Path(__file__).with_name('material_prices.json')`

**최상위 함수**
- `load_material_prices()` (L23) — material_prices.json 전체를 dict 로 로드. 실패 시 빈 dict.
- `get_unit_price()` (L32) — 단가 키 → 단가(숫자). 없거나 미입력(null)이면 None.


### `modular_3d/카탈로그/materials.py` (52 줄)
> 재료 상수 — 강재(SS275), 콘크리트(보통 강도).

**모듈 상수**
- `G_M_PER_S2` (L8) = `9.80665`
- `STEEL_E_MPA` (L11) = `205000.0`
- `STEEL_NU` (L12) = `0.3`
- `STEEL_G_MPA` (L13) = `STEEL_E_MPA / (2.0 * (1.0 + STEEL_NU))`
- `STEEL_DENSITY_KG_M3` (L14) = `7850.0`
- `STEEL_UNIT_WEIGHT_N_M3` (L15) = `STEEL_DENSITY_KG_M3 * G_M_PER_S2`
- `STEEL_UNIT_WEIGHT_N_MM3` (L16) = `STEEL_UNIT_WEIGHT_N_M3 * 1e-09`
- `STEEL_FY_MPA` (L19) = `275.0`
- `STEEL_FU_MPA` (L20) = `410.0`
- `PHI_T` (L23) = `0.9`
- `PHI_C` (L24) = `0.9`
- `PHI_B` (L25) = `0.9`
- `PHI_V` (L26) = `0.9`
- `CONCRETE_E_MPA` (L29) = `27000.0`
- `CONCRETE_NU` (L30) = `0.2`
- `CONCRETE_G_MPA` (L31) = `CONCRETE_E_MPA / (2.0 * (1.0 + CONCRETE_NU))`
- `CONCRETE_DENSITY_KG_M3` (L32) = `2400.0`
- `CONCRETE_UNIT_WEIGHT_N_M3` (L33) = `CONCRETE_DENSITY_KG_M3 * G_M_PER_S2`
- `CONCRETE_UNIT_WEIGHT_N_MM3` (L34) = `CONCRETE_UNIT_WEIGHT_N_M3 * 1e-09`
- `RC_WALL_E_MPA` (L37) = `CONCRETE_E_MPA`
- `RC_WALL_G_MPA` (L38) = `CONCRETE_E_MPA / 2.4`


### `modular_3d/카탈로그/rigid.py` (19 줄)
> OpenSees 가상 강체 강성 상수 (rigid arm 의 elasticBeamColumn 단면).

**모듈 상수**
- `RIGID_E_MPA` (L13) = `1000000000.0`
- `RIGID_A_MM2` (L14) = `1000000.0`
- `RIGID_I_MM4` (L15) = `1000000000000.0`
- `RIGID_J_MM4` (L16) = `1000000000000.0`


### `modular_3d/카탈로그/room_types.py` (98 줄)
> 실(공간) 용도 카탈로그 — 아파트 실용도 + KDS 41 12 00 활하중 연계.

**모듈 상수**
- `ROOF_LIVE_LOAD_KN_M2` (L69) = `1.0`
- `ROOF_SDL_KN_M2` (L70) = `2.5`
- `DEFAULT_LIVE_LOAD_KN_M2` (L73) = `2.0`
- `DEFAULT_SDL_KN_M2` (L74) = `1.5`
- `ROOM_TYPE_BY_KEY` (L77) = `{rt.key: rt for rt in ROOM_TYPES}`
- `DEFAULT_ROOM_TYPE_KEY` (L80) = `'living'`

**최상위 함수**
- `get_room_type()` (L83) — key 로 RoomType 조회. 없으면 None.
- `room_type_names()` (L88) — 드롭다운용 (key, 표시명) 목록 — 표시 순서 유지.

**클래스**
- `class RoomType` (L24) — 실 용도 1종.


### `modular_3d/카탈로그/steel_sections.py` (142 줄)
> KS D 3568 각형강관(SHS) 단면 카탈로그.

**모듈 상수**
- `SHS_200x200x8` (L44) = `_shs_section_props(SECTION_W_MM, SECTION_H_MM, SECTION_T_MM)`
- `_SHS_SPECS` (L92) = `[(75, 3.2), (75, 4.5), (100, 3.2), (100, 4.5), (100, 6.0), (125, 4.5), (125, ...`
- `SECTION_PN_N` (L132) = `_FY * SHS_200x200x8['A']`
- `SECTION_MN_N_MM` (L133) = `_FY * (SHS_200x200x8['Iy'] / (SECTION_H_MM / 2.0))`

**최상위 함수**
- `_shs_section_props()` (L18) — 중공 각형강관 단면 특성 계산.
- `build_shs_catalog()` (L106) — SHS 카탈로그를 단면적 오름차순으로 반환.
- `find_section_by_name()` (L117) — 이름으로 카탈로그 검색.

**클래스**
- `class SHSSection` (L48) — 각형강관 단면 (정사각 SHS).
  - `from_dimensions()` (L66) — 치수로부터 모든 단면 특성 계산.
  - `weight_per_m_kg()` (L85) — 단위 길이당 중량 (kg/m).


### `modular_3d/카탈로그/tolerances.py` (51 줄)
> 거리·임계 허용오차 — 노드 병합·스냅·인터페이스 등.

**모듈 상수**
- `NODE_MERGE_TOL_MM` (L12) = `1.0`
- `LINE_COLINEAR_TOL` (L14) = `0.001`
- `OVERLAP_TOL_MM` (L16) = `1.0`
- `JOINT_BRIDGE_TOL_MM` (L21) = `25.0`
- `JOINT_HORIZ_CONNECT_MAX_MM` (L23) = `225.0`
- `FP_SUPPORT_TOL_XY` (L28) = `350.0`
- `FP_SUPPORT_TOL_Z` (L30) = `500.0`
- `SNAP_TRIGGER_MM` (L34) = `100.0`
- `HOVER_SNAP_RADIUS_MM` (L36) = `300.0`
- `GRID_SNAP_MM` (L38) = `100.0`
- `EQUILIBRIUM_TOL` (L41) = `0.001`
- `BENCHMARK_TOL` (L42) = `0.01`

