# 리팩토링 인벤토리 (자동 생성)

생성일: 2026-05-19  |  대상 파일 수: 58


이 파일은 `modular_3d/_refactor_tools/build_inventory.py` 가 자동 생성합니다. 직접 편집 금지.


## 파일 인벤토리

### `modular_3d/__init__.py` (0 줄)

### `modular_3d/__main__.py` (22 줄)
> python -m modular_3d 로 실행 가능하게 하는 엔트리.


### `modular_3d/_utils/__init__.py` (6 줄)
> 중립 유틸 패키지 — ui/render/analysis 어디에도 의존하지 않는 도구.


### `modular_3d/_utils/debug.py` (117 줄)
> 카테고리별 진단 로그 토글.

**최상위 함수**
- `_load_env_defaults()` (L40) — 환경변수에서 초기 OFF 카테고리 로드.
- `dprint()` (L53) — 카테고리가 활성 상태일 때만 print 호출.
- `disable()` (L64) — 특정 카테고리 비활성화.
- `enable()` (L69) — 특정 카테고리 활성화 (기본 상태로 복원).
- `disable_all()` (L74) — 알려진 모든 카테고리 비활성화. 알려지지 않은 카테고리는 영향 없음.
- `enable_all()` (L83) — 모든 카테고리 활성화 (비활성 집합 비우기).
- `is_enabled()` (L88)
- `silenced()` (L93) — context manager — 지정 카테고리(없으면 모두) 를 블록 안 OFF.


### `modular_3d/_utils/geometry.py` (17 줄)
> 기하 유틸 — 좌표 박스·범위 계산 등 ui/render 공용 함수.

**최상위 함수**
- `xy_bbox()` (L10) — 부재의 XY 바운딩박스 (x0, y0, x1, y1).


### `modular_3d/analysis/__init__.py` (11 줄)
> 구조해석 패키지 — OpenSeesPy 기반 (Phase 5 이후).


### `modular_3d/analysis/_utils.py` (12 줄)
> analysis 패키지 공용 유틸리티.

**최상위 함수**
- `round_key()` (L8) — 좌표를 1 mm 단위 정수 키로 반올림 — 노드 병합/접합부 매칭용.


### `modular_3d/analysis/constants.py` (43 줄)
> 구조해석 상수 — re-export shim (2026-05-12).


### `modular_3d/analysis/joint_rules.py` (1999 줄)
> 케이스별 명시 접합 규칙 (2026-05-13 신설).

**모듈 상수**
- `_Z_TOL` (L39) = `5.0`
- `_PERP_TOL` (L41) = `5.0`
- `_GAP_MAX` (L45) = `225.0`
- `_EDGE_PERP_MAX` (L50) = `_GAP_MAX`
- `_VSTACK_MAX` (L53) = `225.0`
- `RULE_ID_MOD_MOD_H` (L396) = `'R01_mod_mod_h'`
- `RULE_ID_MOD_MOD_V` (L735) = `'R02_mod_mod_v'`
- `RULE_ID_PANEL_MOD` (L760) = `'R03_panel_mod'`
- `_PANEL_LIKE_ROLES` (L764) = `{'floor_edge_beam', 'cantilever_slab_beam'}`
- `RULE_ID_PANEL_PANEL` (L1139) = `'R04_panel_panel'`
- `RULE_ID_WALL_FLOOR_V` (L1290) = `'R05_wall_floor_v'`
- `RULE_ID_WALL_MOD` (L1362) = `'R06_wall_mod'`
- `RULE_ID_WALL_CANT` (L1540) = `'R08_wall_cant'`
- `RULE_ID_WALL_WALL_H` (L1665) = `'R07_wall_wall_h'`
- `RULE_ID_CORE` (L1713) = `'R09_core'`
- `_CORE_NUMERIC_MARGIN_MM` (L1720) = `5.0`
- `_CORE_LATERAL_MAX` (L1721) = `CORE_WALL_DEFAULT_THICKNESS_MM / 2.0 + SECTION_W_MM / 2.0 + MODULE_JOINT_GAP_...`
- `_CORE_ALIGN_TOL` (L1727) = `CORE_WALL_DEFAULT_THICKNESS_MM / 2.0 - SECTION_W_MM / 2.0 + _CORE_NUMERIC_MAR...`
- `_CORE_LINE_ROLES_AXIS` (L1734) = `('core_column', 'core_top_runner', 'core_bottom_runner', 'core_truss_v', 'cor...`

**최상위 함수**
- `_collect_module_nodes()` (L58) — 모듈 컴포넌트(MODULE, sub_index=0) 별 노드 ID 집합 반환.
- `_module_corners()` (L83) — 모듈의 꼭지점 노드 ID 추출 — z 레벨별 4 코너.
- `_horizontal_edges()` (L123) — 수평 보 member → [(mid, nA, nB), ...].
- `_module_vertical_edges()` (L159) — 수직모듈의 기둥 분절 member → [(mid, nA, nB), ...].
- `_register_pin_link()` (L204) — equalDOF 핀 결합 등록 (OpenSees + spec 동시).
- `_apply_corner_edge_rule()` (L258) — 코너-코너(a) + 코너-모서리(b) 매칭 코어. R01(모듈)·R04(패널) 공유.
- `apply_module_module_horizontal()` (L399) — R01 — 모듈 ↔ 모듈 수평 인접 접합. _apply_corner_edge_rule 코어 사용.
- `_split_edges_and_link_corners()` (L444) — (b) 보조 — 모서리 보를 사영점에서 분할하고 코너 노드와 결합.
- `_apply_vstack_rule()` (L672) — 수직 적층 코너-코너 매칭 코어. R02(모듈)·R04수직(패널) 공유.
- `apply_module_module_vertical()` (L738) — R02 — 모듈 ↔ 모듈 수직 적층 접합. _apply_vstack_rule 코어 사용.
- `_collect_cantilever_beam_data()` (L767) — 캔틸레버 보 → ({cid: [자유단nid]}, {cid: [(mid, n1, n2)]}).
- `_collect_panel_like_corners()` (L819) — 패널류 컴포넌트별 코너 노드 ID → {cid: [nid, ...]}.
- `apply_panel_module()` (L870) — R03 — 바닥패널 ↔ 모듈 천장 접합. 패널이 모듈에 의존하는 단방향.
- `apply_panel_panel_horizontal()` (L1142) — R04 — 패널류 ↔ 패널류 수평 접합. 모듈-모듈(R01) 과 동일 로직.
- `_collect_wall_data()` (L1178) — 벽 컴포넌트별 노드/종속여부 → {cid: {'top':[nid],'bot':[nid],
- `_wall_vertical_edges()` (L1236) — 벽 기둥(wall_column) 분절 → [(mid, nA, nB), ...].
- `_collect_floor_panel_corners()` (L1265) — 바닥패널만의 코너 → {cid: [4 nid]}.
- `apply_wall_floor_vertical()` (L1293) — R05 — 벽패널 코너 ↔ 바닥패널 코너 수직 핀 접합.
- `apply_wall_module()` (L1365) — R06 — 벽 상단 코너 ↔ 모듈(천장보·기둥) + 캔틸 보 자유단. 같은-z 만.
- `apply_wall_cantilever_beam()` (L1543) — R08 — 벽 상단 코너 ↔ 캔틸 보 자유단/모서리 (같은 z 만).
- `apply_wall_wall_horizontal()` (L1668) — R07 — 벽 코너 ↔ 다른 벽 코너. 코너-코너만(노드끼리).
- `_collect_core_data()` (L1741) — 코어 컴포넌트의 노드 + 축평행 선 → (core_nids, core_lines).
- `apply_core_joint()` (L1781) — R09 — 외부 부재 노드 ↔ 코어벽·코어슬래브 (노드 + 축평행 선) 6 DOF 강결합.
- `apply_all_joint_rules()` (L1962) — 모든 케이스별 접합 룰을 순차 적용.


### `modular_3d/analysis/load_calculator.py` (664 줄)
> 하중 계산 — 자중 + 슬래브 사하중 + 활하중을 부재별 등분포로 변환.

**최상위 함수**
- `_section_area()` (L135) — 부재 단면적 mm².
- `_apply_self_weight()` (L147) — 모든 frame 부재에 자중 적용.
- `_classify_4_beams_by_direction()` (L177) — 4 개의 둘레 보를 길이별로 두 그룹으로 분류.
- `_distribute_slab_pressure()` (L199) — 슬래브 압력(N/mm²) 을 둘레 보에 등가 평균 UDL 로 분배.
- `_build_beam_shares()` (L232) — 분포 프로파일 peak(중앙 최대) 기준 BeamShare 딕셔너리 생성.
- `_collect_rect_corners()` (L269) — 4 개 둘레 보의 노드 집합에서 4 코너(unique 좌표) 추출 후
- `_find_beam_between_nodes()` (L291) — anchor_nids 두 노드를 양 끝으로 갖는 보 부재를 찾는다 (exclude 제외).
- `_accumulate()` (L333)
- `_slab_pressures()` (L342) — (슬래브 자중 압력, 활하중 압력)  N/mm².
- `_distribute_vertical3_slabs()` (L349) — 수직 3층 모듈의 3 슬래브 (1F·2F·3F 바닥) 를 z 레벨별 4 둘레 보에 분배.
- `_apply_slab_loads()` (L435) — 슬래브 하중을 둘레 보에 분배.
- `_apply_core_slab_self_weight()` (L600) — CoreSlab 자중을 같은 그룹의 코어 column 부재들에 균등 분배.
- `calculate_loads()` (L657) — Scene + AnalysisModel → 부재별 LoadCase.

**클래스**
- `class LoadCase` (L39) — 한 부재가 받는 등분포 하중 (모두 N/mm, 양수, 중력 방향).
  - `total_dead()` (L51)
  - `factored()` (L55) — 1.2 D + 1.6 L  (양수, N/mm).
- `class BeamShare` (L61) — 슬래브에서 한 둘레 보로 분배된 하중의 패턴 + peak 값.
- `class SlabDistribution` (L78) — 한 슬래브의 분배 메타 — 하중 흐름 시각화 Stage 1 용.
- `class LoadResult` (L98)
  - `total_factored_n()` (L110)
  - `get()` (L114)
  - `summary()` (L119)


### `modular_3d/analysis/model_spec.py` (516 줄)
> 구조해석 모델 단일 명세 (2026-05-12 명세통합 Phase 2).

**클래스**
- `class Case(Enum)` (L50) — 해석 케이스. 토폴로지는 5 케이스 공유, 하중만 케이스별 갈아끼움.
- `class NodeRec` (L63) — 노드 — OpenSees `node(tag, x, y, z)` 와 1:1.
- `class BeamRec` (L76) — 보-기둥 요소 — OpenSees `element('elasticBeamColumn', ...)` 와 1:1.
- `class ShellRec` (L93) — 면 요소 — OpenSees `element('ShellMITC4', ...)` 와 1:1.
- `class EqualDofRec` (L109) — 자유도 묶음 — OpenSees `equalDOF(master, slave, *dofs)` 와 1:1.
- `class RigidLinkRec` (L127) — 강체 링크 — OpenSees `rigidLink('beam', master, slave)` 와 1:1.
- `class DiaphragmRec` (L139) — 강체 다이어프램 — OpenSees `rigidDiaphragm(3, master, *slaves)` 와 1:1.
- `class FixRec` (L167) — 고정점 — OpenSees `fix(tag, *dofs)` 와 1:1.
- `class ModelSpec` (L180) — 5 케이스 공유 토폴로지·결합·고정점 단일 명세.
  - `refresh_index()` (L212) — 노드 tag → NodeRec 역인덱스 재구축.
  - `node()` (L216) — 노드 tag 로 NodeRec 조회. 인덱스가 stale 하면 자동 재구축 — silent
  - `iter_nodes()` (L227)
  - `iter_beams()` (L230)
  - `iter_shells()` (L233)
  - `iter_equal_dofs()` (L236)
  - `iter_rigid_links()` (L239)
  - `iter_diaphragms()` (L242)
  - `iter_fixes()` (L245)
  - `validate()` (L250) — 명세 자체 점검.
  - `verify_against_opensees()` (L327) — OpenSees 가 실제 받은 노드·요소를 회수해 명세와 비교.
  - `summary()` (L428)
- `class DistributedLoadRec` (L447) — 부재 분포하중 — OpenSees `eleLoad('-ele', tag, '-type', 'beamUniform', wy, wz, wx)` 매핑
- `class NodalLoadRec` (L459) — 노드 집중하중 — OpenSees `load(tag, fx, fy, fz, mx, my, mz)` 매핑.
- `class LoadSpec` (L474) — 케이스별 하중 명세.
  - `summary()` (L491)
- `class ValidationResult` (L503) — ModelSpec.validate() 결과.
  - `__bool__()` (L512)
  - `__len__()` (L515)


### `modular_3d/analysis/ops_builder.py` (1137 줄)
> OpenSeesPy 모델 빌더.

**모듈 상수**
- `RC_WALL_E_MPA` (L40) = `CONCRETE_E_MPA`
- `RC_WALL_G_MPA` (L41) = `CONCRETE_E_MPA / 2.4`
- `_IFACE_VERT_DZ` (L52) = `JOINT_HORIZ_CONNECT_MAX_MM`
- `_IFACE_VERT_DXY` (L53) = `5.0`
- `_IFACE_HORIZ_DZ` (L54) = `5.0`
- `_IFACE_HORIZ_DIST` (L55) = `JOINT_HORIZ_CONNECT_MAX_MM`
- `_SAME_NODE_TOL` (L58) = `NODE_MERGE_TOL_MM`

**최상위 함수**
- `_is_cantilever_node()` (L161) — 노드가 캔틸레버 부재(`cantilever_beam`/`cantilever_slab_beam`)에 속하면 True.
- `_section_props()` (L177) — 부재 단면 (A, Iy, Iz, J) 반환.
- `_geom_transf_tag()` (L223) — OpenSees geomTransf 등록 + 태그 반환.
- `_vecxz_for_member()` (L246) — 부재 축에 수직이면서 'z 축에 가까운' 임의 벡터 반환 → vecxz.
- `_step_init_ops_domain()` (L260) — 1) OpenSees domain 초기화 (3D 6DOF). 모든 이전 ops 상태 삭제.
- `_step_register_nodes()` (L267) — 2) AnalysisNode.id 를 OpenSees nodeTag 로 사용해 노드 등록.
- `_get_shell_section_tag()` (L287) — 셸 두께 + E 배율별 ElasticMembranePlateSection 등록 + 태그 반환 (캐싱).
- `_step_register_members()` (L306) — 3) AnalysisMember 1:1 → OpenSees 요소 등록.
- `_step_fix_base_nodes()` (L467) — 5) 베이스 지점 — z_min 의 모든 기둥 base + 코어 셸 하단 노드 → 6DOF 고정.
- `_step_fix_first_floor_panel_nodes()` (L514) — [2026-05-11] 1층(z=base) 의 FloorPanel 코너 노드를 6DOF 고정.
- `build_ops_model()` (L555) — AnalysisModel → OpenSees 3D 프레임 모델 등록.
- `_snap_dependent_cantilever_to_module()` (L679) — 종속 캔틸레버 보/슬래브의 한 끝 노드 좌표를 부모 모듈의 가까운 노드 좌표로
- `_build_diaphragms_and_core()` (L775) — 컴포넌트별 rigidDiaphragm 등록 (2026-05-17 per-component 전환).
- `self_check()` (L979) — 모델 빌드 후 자체 점검 — SelfCheckResult 반환.

**클래스**
- `class DiaphragmInfo` (L79) — 한 층의 강체 다이어프램 정보.
- `class OpsModel` (L87) — OpenSees에 등록된 모델의 시각화·검증용 메타데이터.
  - `summary()` (L144)
- `class SelfCheckResult` (L953) — self_check 의 구조화된 결과.
  - `__bool__()` (L963)
  - `__iter__()` (L966)
  - `__len__()` (L969)
  - `__contains__()` (L972)
  - `__getitem__()` (L975)


### `modular_3d/analysis/ops_solver.py` (962 줄)
> OpenSeesPy 기반 정적 해석.

**모듈 상수**
- `_PENALTY_ALPHA` (L52) = `100000000000000.0`
- `_STATION_PER_SUB` (L253) = `7`
- `SDS_DEFAULT` (L560) = `0.5`
- `SD1_DEFAULT` (L561) = `0.2`
- `IE_DEFAULT` (L562) = `1.0`
- `R_DEFAULT` (L563) = `8.0`
- `G_M_S2` (L564) = `9.80665`
- `WIND_V0_M_S` (L567) = `35.0`
- `WIND_GF` (L568) = `0.85`
- `WIND_CF` (L569) = `1.3`

**최상위 함수**
- `_apply_penalty_constraints()` (L55) — OpenSees constraints handler 를 Penalty(α, α) 로 설정.
- `_extract_member_forces()` (L144) — 모든 부재 단면력 추출.
- `_compute_station_forces()` (L205) — Phase 5 — 양단 단면력 + 등분포 가정으로 station n 점 분포 계산.
- `_diagnose_zero_members()` (L256) — 사용자 보고: 0 단면력 부재가 많음.
- `_extract_member_stations()` (L287) — Phase 5 — 풀이 직후 모든 부재의 분포 단면력을 캐시.
- `_consolidate_member_loads()` (L333) — 부재별 (D_self, D_slab, L) [N/mm] 반환.
- `_apply_eleload_factored()` (L346) — 1.2D + 1.6L 계수하중을 부재 한 개에 eleLoad 로 적용.
- `solve_vertical()` (L409) — 수직(D+L = 1.2D + 1.6L) 정적 해석.
- `_compute_floor_seismic_weight()` (L572) — 각 층의 지진중량 W (D 만, 활하중 미포함) [N].
- `_seismic_base_shear()` (L614) — KBC 2022 §0306.4 등가정적 베이스 전단 V [N].
- `_seismic_floor_distribution()` (L627) — 층별 횡력 Fx 분배 (KBC §0306.4.5).
- `_wind_floor_distribution()` (L645) — KDS 41 12 00 단순화: 층별 풍하중 [N].
- `_apply_horizontal_loads()` (L676) — 층별 횡력을 다이어프램 master 노드에 절점력으로 적용.
- `_extract_lateral_results()` (L727) — 변위 + 베이스 반력 + 부재력 추출 + 층변위 산정.
- `solve_seismic()` (L840) — 등가정적 지진 해석 (Ex 또는 Ey).
- `solve_wind()` (L880) — 풍하중 해석 (Wx 또는 Wy).
- `solve_all_cases()` (L906) — 5 케이스(D+L, Ex, Ey, Wx, Wy) 자동 해석 — 케이스마다 ops 모델 재빌드.

**클래스**
- `class MemberForce` (L64) — 부재 양단 단면력 (OpenSees 로컬 좌표).
  - `N_max_abs()` (L78)
  - `V_max_abs()` (L82)
  - `M_max_abs()` (L87)
- `class OpsResults` (L93)
  - `summary()` (L114)


### `modular_3d/analysis/projection.py` (219 줄)
> 사영 매칭 공용 유틸리티 (2026-05-13 전역 사영 정책).

**최상위 함수**
- `project_to_line()` (L65) — 점을 두 끝점으로 정의된 직선에 직각 사영.
- `project_to_plane()` (L91) — 점을 평면에 직각 사영. 평면은 한 점 + 법선 벡터로 정의.
- `project_to_quad()` (L113) — 점을 4 노드 quad 평면에 사영 + bilinear 좌표 (u, v) 계산.
- `is_orthogonal_pair()` (L161) — 두 노드의 평면 페어가 직각 사영(한 축 정렬) 인지 검사.
- `_self_test()` (L180) — 단순 검증 — 모듈 단독 호출 시 실행.

**클래스**
- `class ProjectionResult` (L39) — 직선·평면 사영 결과.
- `class QuadProjection` (L48) — 4 노드 quad(셸) 사영 결과.


### `modular_3d/analysis/quantity_takeoff.py` (335 줄)
> 물량 집계 — 강재 본수·길이·중량 + 슬래브 부피.

**모듈 상수**
- `DEFAULT_REBAR_RATIO` (L123) = `0.012`
- `LENGTH_ROUND_MM` (L211) = `1.0`

**최상위 함수**
- `_polygon_area_xy()` (L80) — 4-각 폴리곤의 xy 평면 면적 (Shoelace).
- `_collect_slabs()` (L97) — scene 의 모든 SlabData 수집 (모듈 내부 + 바닥패널 + 캔틸레버 슬래브).
- `aggregate_slabs()` (L126) — 슬래브 부피 + 철근 추정 중량 (m³ 기준).
- `aggregate_core()` (L162) — RC 코어벽(솔리드 직육면체) + 코어 슬래브의 콘크리트 + 철근 물량.
- `aggregate_steel()` (L214) — 단면+길이 동일 부재를 한 줄로 묶어 본수표 생성.
- `build_quantity_report()` (L302) — 단일 정책에 대한 QuantityReport 생성.
- `build_all_reports()` (L322) — 1종/2종/3종 모든 정책의 QuantityReport 생성.

**클래스**
- `class SteelLineItem` (L27) — 강재 본수표 한 행.
- `class SlabQuantity` (L39) — 슬래브 부피 + 철근 추정 중량.
- `class CoreQuantity` (L50) — RC 코어 물량 — 코어벽(솔리드 직육면체) + 코어 슬래브 통합.
- `class QuantityReport` (L66) — 1정책에 대한 종합 물량 보고서. 정책별 사전이 메인 진입점.


### `modular_3d/analysis/section_catalog.py` (30 줄)
> SHS 단면 카탈로그 — re-export shim (2026-05-12).


### `modular_3d/analysis/section_design.py` (495 줄)
> 부재 분류 + 그룹핑 + 단면 자동 선정.

**모듈 상수**
- `POLICY_1` (L26) = `'1종'`
- `POLICY_2` (L27) = `'2종'`
- `POLICY_3` (L28) = `'3종'`
- `ALL_POLICIES` (L29) = `(POLICY_1, POLICY_2, POLICY_3)`
- `CAT_MODULE_COLUMN` (L33) = `'module_column'`
- `CAT_MODULE_BEAM` (L34) = `'module_beam'`
- `CAT_CANTILEVER_BEAM` (L35) = `'cantilever_beam'`
- `CAT_FLOOR_PANEL_BEAM` (L36) = `'floor_panel_beam'`
- `CAT_WALL_PANEL_COLUMN` (L37) = `'wall_panel_column'`
- `CAT_WALL_PANEL_BEAM` (L38) = `'wall_panel_beam'`
- `CAT_RC_CORE` (L40) = `'rc_core'`

**최상위 함수**
- `classify_member()` (L61) — role 기반 카테고리 분류.
- `group_categories()` (L79) — 정책별 그룹명 → 포함 카테고리 리스트.
- `group_members()` (L111) — 정책에 따라 부재 ID 를 그룹별로 분류.
- `_kind_from_category()` (L136) — 6 카테고리를 strength_check 의 3 kind 로 단순화.
- `extract_demand()` (L145) — OpsResults 의 부재 양단 단면력에서 절댓값 최댓값을 추출.
- `extract_all_demands()` (L179) — 모든 부재의 demand 를 한꺼번에 추출.
- `extract_envelope_demands()` (L186) — (2026-05-19 작업 5) 모든 하중조합의 단면력 절댓값 최대로 envelope demand 생성.
- `design_all_policies_envelope()` (L228) — 모든 정책 산정을 envelope demand 기반으로 수행 (지배조합).
- `select_section_for_group()` (L250) — 카탈로그를 단면적 오름차순 순회 → 그룹 모든 부재 OK 인 첫 단면 채택.
- `design_for_policy()` (L314) — 단일 정책에 대해 그룹별 단면 산정.
- `design_all_policies()` (L344) — 1종/2종/3종 모두 산정 → dict[policy → DesignResult].
- `split_columns_by_floor()` (L354) — 기둥 그룹을 층 범위 K개 구간으로 동적계획법 최적 분할.

**클래스**
- `class GroupDesign` (L240) — 그룹 단면 산정 결과.
- `class DesignResult` (L301) — 한 정책에 대한 전체 산정 결과.
  - `ng_groups()` (L310)


### `modular_3d/analysis/spatial_matcher.py` (121 줄)
> 공간 매칭 헬퍼 — fuzzy 매칭 4곳의 공통 알고리즘 추출.

**최상위 함수**
- `nearest_node()` (L23) — 점 ↔ 점 가장 가까운 후보 찾기.
- `project_point_to_segment()` (L59) — 점을 (xy 평면) 선분에 사영.
- `nearest_segment_projection()` (L84) — 점 ↔ 여러 선분 중 가장 가까운 것 찾기.


### `modular_3d/analysis/strength_check.py` (258 줄)
> KBC 2022 LRFD 강재 단면 강도 검토.

**모듈 상수**
- `DEFL_LIMIT_BEAM_DENOM` (L28) = `360.0`
- `DEFL_LIMIT_CANTILEVER_DENOM` (L29) = `180.0`

**최상위 함수**
- `check_axial()` (L88) — 축력 검토 (압축이면 좌굴 포함, 인장이면 항복).
- `check_bending()` (L127) — 휨 검토 — 강축+약축 단순 합 (보수적).
- `check_shear()` (L143) — 전단 검토 — 강·약축 중 큰 값.
- `check_combined()` (L156) — KBC H1.1 P-M 조합 상관식.
- `estimate_deflection()` (L201) — 강축 모멘트로부터 단면 의존 처짐 추정.
- `check_deflection()` (L221) — 처짐 검토 — 단면 의존 추정 처짐을 한계와 비교.
- `check_member()` (L241) — 5개 항목 전부 계산 → CheckResult 반환.

**클래스**
- `class MemberDemand` (L33) — 부재 요구 내력 (해석 결과로부터 추출).
- `class CheckResult` (L60) — 단면 검토 결과 — 5개 항목 + 최댓값 + 지배 항목.
  - `from_ratios()` (L72)


### `modular_3d/analysis/tests/__init__.py` (0 줄)

### `modular_3d/analysis/tests/_smoke_test_ops_solver.py` (434 줄)
> ops_solver 단위 스모크 테스트 — 평형 검증 중심.

**최상위 함수**
- `_add_module()` (L15)
- `case_handcalc()` (L27) — 손계산 vs ops 결과 — 단일 모듈 D+L 총 하중.
- `case_1()` (L53) — 단일 모듈 — 평형 1% 이내 통과 기대.
- `case_2()` (L70) — 수평 인접 2 모듈 — 평형 1% 이내.
- `case_3()` (L84) — 수직 적층 2 모듈 — 평형 1% 이내.
- `case_4_2x2_grid_2story()` (L98) — 2×2 평면 × 2층 (총 8 모듈) — 다층 + 다이어프램 작동 확인.
- `case_with_floor_panel()` (L117) — 모듈 + FloorPanel — fp 가 모듈 위에 얹힌 케이스. 핀 연결 누락 시 mechanism.
- `case_with_struct_wall()` (L146) — 모듈 + StructWall — 벽패널 추가 케이스.
- `case_rotated_module()` (L173) — rotation=90 모듈 — 좌표 변환 + ops 빌드 + 평형 검증.
- `case_with_cantilever()` (L196) — 모듈 + CantileverBeam 1개 — 자유단 노드가 mechanism 만들지 않는지.
- `case_large_3x3_3story()` (L220) — 3×3 평면 × 3층 (총 27 모듈) — 큰 모델 평형 + 성능 + 다이어프램 다중 층.
- `case_rotated_stack()` (L240) — 회전 모듈 + 위에 회전 안 한 모듈 적층 — 좌표 변환 + 인터페이스 검출 검증.
- `case_different_size_modules()` (L267) — 다른 크기 두 모듈 인접 (4×6 + 6×6) — 인접 코너 매칭 검증.
- `case_partial_floor_panel()` (L284) — 모듈 위에 더 작은 FloorPanel — fp 코너 4개 중 일부만 모듈 코너와 일치.
- `case_offset_stack()` (L321) — 1층과 2층의 위치가 어긋난 적층 — 일부 column 만 핀 연결.
- `case_seismic_handcalc()` (L360) — 등가정적 지진 손계산 vs ops 검증.
- `case_5_seismic_wind()` (L388) — 횡력 5 케이스 일괄 해석 — 평형 1% 이내, 층변위 양수, 베이스 전단 적용 일치.
- `main()` (L413)


### `modular_3d/analysis/topology.py` (1617 줄)
> 해석 토폴로지 빌드 (중심선 모델 + 컴포넌트별 독립 노드).

**모듈 상수**
- `_SECTION_KW` (L172) = `dict(section_w=SECTION_W_MM, section_h=SECTION_H_MM, section_t=SECTION_T_MM)`
- `_EXTRACTORS` (L750) = `{Module: _extract_module, FloorPanel: _extract_floor_panel, StructWall: _extr...`

**최상위 함수**
- `_extract_module()` (L185) — Module → 8 노드 / 12 부재 (4 코너 × 2 z레벨, 기둥 1개씩).
- `_extract_floor_panel()` (L253) — FloorPanel → 4 노드 / 4 요소 (가장자리 보만).
- `_extract_struct_wall()` (L292) — StructWall → 4 노드 / 4 요소 (2 코너 × 2 z레벨, 기둥 1개씩 + 런너 2).
- `_extract_cantilever_beam()` (L345) — CantileverBeam → 2 노드 / 1 요소.
- `_extract_cantilever_slab()` (L373) — CantileverSlab → 4 노드 / 3 요소 (U 자 보 3개).
- `_extract_mid_beam()` (L408)
- `_extract_mid_column()` (L429)
- `_extract_vertical_module()` (L450) — Vertical3Module → 16 노드 / 28 부재.
- `_extract_core()` (L531) — Core (RC 코어벽) → 외곽 boundary 프레임 + 내부 등가 트러스 격자.
- `_extract_core_slab()` (L682) — CoreSlab → 외곽 보 격자 (4 변).
- `_extract_one()` (L764)
- `_merge_duplicate_members()` (L774) — 동일 양단 노드를 가진 중복 부재 통합 (노드 병합 후 발생).
- `_segments_colinear()` (L805)
- `_project()` (L822)
- `_round1()` (L826) — 좌표값을 1mm 단위 정수로 반올림.
- `_merge_panel_overlaps_and_check()` (L837) — 완전 겹친 부재 통합 (패널↔패널, 패널↔모듈) + 부분 겹침 검사.
- `build_analysis_model()` (L987) — Scene → AnalysisModel.
- `_build_diaphragms()` (L1065) — 컴포넌트 단위 바닥 다이어프램 그룹 생성.
- `_consolidate_dependent_nodes()` (L1186) — 종속 관계로 얽힌 부재의 인접 노드를 같은 nid 로 통합.
- `_split_hosts_for_mid_endpoints()` (L1483) — mid_beam/mid_column 끝점이 다른 부재(주로 모듈 보) 직선 위에 떨어지면
- `summarize()` (L1605)

**클래스**
- `class AnalysisNode` (L39) — 해석용 절점 (월드 좌표 mm).
  - `__repr__()` (L45)
- `class AnalysisMember` (L51) — 해석용 부재 (1차원 보-기둥 요소).
  - `__repr__()` (L81)
- `class Diaphragm` (L87) — 단일 컴포넌트(또는 종속 캔틸 포함)의 바닥 다이어프램 그룹.
- `class AnalysisModel` (L106)
  - `invalidate_indices()` (L122) — members 변경 후 캐시 무효화 — split sub 추가/제거 직후 호출.
  - `_build_role_indices()` (L127) — role / role_prefix 인덱스 일괄 빌드.
  - `members_by_role()` (L141) — role 정확 매칭 mid 리스트 (lazy 캐시). 빈 결과 = 빈 리스트.
  - `members_by_role_prefix()` (L147) — role.startswith(prefix) 매칭 mid 리스트 (lazy 캐시).
  - `get_member_length()` (L153)
  - `base_node_ids()` (L162)
- `class _LocalExtract` (L176) — 단일 컴포넌트의 중심선 추출 결과 (글로벌 ID 할당 전).


### `modular_3d/analysis/물량산출_설계.py` (322 줄)
> 물량산출 + 단면 자동 산정 기능 — 설계 (CoT 한글 각주 only)


### `modular_3d/io/__init__.py` (8 줄)
> I/O 패키지 — scene JSON 직렬화 + 마이그레이션 진입점.


### `modular_3d/io/scene_io.py` (249 줄)
> Scene 저장/불러오기 — JSON 라운드트립 + 구버전 마이그레이션 통합 (단계 3 단순화).

**모듈 상수**
- `_WALL_FP_AUTO_MATCH_MAX_XY_MM` (L29) = `500.0`

**최상위 함수**
- `snap_n_floors_to_three()` (L34) — 층수를 가장 가까운 3 의 배수로 스냅 (동률은 내림). 최소 3.
- `save_scene()` (L48) — Scene 의 모든 Component + 층수를 JSON 으로 저장.
- `load_scene()` (L82) — JSON 파일을 읽어 (Scene, n_floors) 반환.
- `_auto_match_wall_to_fp()` (L188) — 구버전 씬: wall.merge_with_panel=True 인데 merged_fp_id 가 비어 있는 경우.
- `_auto_estimate_parent_id()` (L223) — 구버전 씬: parent_id 가 비어 있는 종속 부재의 부모를 추정.


### `modular_3d/model/__init__.py` (24 줄)
> modular_3d.model 패키지 — 데이터 모델.


### `modular_3d/model/core.py` (902 줄)
> 데이터 모델 — 부재, 씬, 액션 정의.

**최상위 함수**
- `_rotate_point()` (L79) — 로컬 좌표 (x, y)를 rotation 각도만큼 원점 기준 회전.
- `_rotate_local_to_world()` (L92) — 로컬 좌표를 회전 + 평행이동하여 월드 좌표로 변환.
- `instantiate()` (L887) — Component 서브클래스 생성 + 하위 부재 자동 생성.

**클래스**
- `class AppState(Enum)` (L16)
- `class ComponentType(Enum)` (L24)
- `class BeamData` (L54)
- `class ColumnData` (L63)
- `class SlabData` (L72)
- `class Component` (L103) — 모든 부재의 기본 클래스.
  - `get_bounding_box()` (L158) — AABB 바운딩박스 (min_xyz, max_xyz) 반환.
  - `get_world_corners()` (L163) — 바닥면 4 꼭지점 + 상단 4 꼭지점 = 8개 월드 좌표.
  - `_get_all_beams()` (L184) — 하위 부재 중 모든 보 목록 반환 (서브클래스에서 오버라이드).
  - `get_snap_points()` (L188) — 스냅 가능한 모든 점 반환 (바닥 꼭지점 + 보 중심/1/4).
  - `_anchor_offset()` (L205) — 앵커 인덱스에 따른 로컬 오프셋.
- `class Module(Component)` (L219) — 모듈: 기둥 4 + 하부보 4 + 상부보 4 + 슬래브 1.
  - `generate_sub_components()` (L226) — dimensions + position + rotation으로 하위 부재 자동 생성.
  - `_get_all_beams()` (L294)
- `class FloorPanel(Component)` (L299) — 바닥패널: 보 4개 + 슬래브 (기둥 없음).
  - `generate_sub_components()` (L305)
  - `_get_all_beams()` (L335)
- `class WallPanelData` (L340) — 벽체 채움 (솔리드 판).
- `class StructWall(Component)` (L347) — 구조벽: 기둥 2 + 런너 2 + 벽체 채움.
  - `generate_sub_components()` (L355)
  - `_get_all_beams()` (L392)
- `class Vertical3Module(Component)` (L402) — 수직 3층 모듈: 한 칸이 3개 층을 차지하는 모듈.
  - `generate_sub_components()` (L425)
  - `_get_all_beams()` (L498)
- `class Core(Component)` (L507) — RC 코어벽 — 한 장 단위. wide-column 모델용 단일 frame element 표현.
  - `generate_sub_components()` (L529)
  - `_get_all_beams()` (L556)
- `class CoreSlab(Component)` (L561) — RC 코어 슬래브 — 보 없는 순수 RC 판. 코어벽 그룹의 종속 부재.
  - `generate_sub_components()` (L580)
  - `_get_all_beams()` (L603)
- `class CantileverBeam(Component)` (L608) — 캔틸레버보: 단일 보.
  - `generate_sub_components()` (L612)
  - `_get_all_beams()` (L626)
- `class CantileverSlab(Component)` (L631) — 캔틸레버 슬래브: 3변 보 + 슬래브.
  - `generate_sub_components()` (L636)
  - `_get_all_beams()` (L671)
- `class MidBeam(Component)` (L676) — 중간보: 단일 보 (모듈 내부 보강용).
  - `generate_sub_components()` (L680)
  - `_get_all_beams()` (L694)
- `class MidColumn(Component)` (L699) — 중간기둥: 단일 기둥.
  - `generate_sub_components()` (L703)
- `class Action` (L722) — Undo 가능한 행동.
- `class Scene` (L730) — 부재 컬렉션 + Undo 스택.
  - `__init__()` (L733)
  - `add_component()` (L738) — 부재 추가, ID 반환.
  - `remove_component()` (L749) — 부재 삭제, 삭제된 부재 반환.
  - `undo()` (L754) — 마지막 액션 취소. 취소된 액션 반환 (실제 복원은 Controller에서).
  - `begin_group_action()` (L769) — 그룹 액션 시작 — undo_stack 의 현재 길이를 마크.
  - `end_group_action()` (L773) — 그룹 액션 종료 — 시작 이후 push 된 모든 액션을 _inner_actions 로 묶고
  - `merge_wall_to_fp()` (L793) — 벽패널을 플로어패널에 합체. 성공 시 True.
  - `unmerge_wall_from_fp()` (L810) — 벽패널을 플로어패널에서 분리. 성공 시 True.
  - `find_nearest_fp_for_wall()` (L827) — 벽패널 하부 런너와 가장 가까운 FP 가장자리 보를 가진 FP ID 반환.
  - `component_count()` (L852)


### `modular_3d/model/joint_recorder.py` (227 줄)
> UI 측 조인트 기록기 (2026-05-12 신규, 옵션 (나)).

**모듈 상수**
- `JOINT_TOL_XY` (L40) = `350.0`
- `JOINT_TOL_Z` (L41) = `500.0`

**최상위 함수**
- `_column_xy_list()` (L44) — 본 컴포넌트의 기둥들 (cx, cy, z_top, z_bot) 목록.
- `_cantilever_endpoints()` (L72) — 캔틸레버 부재의 자유단 끝점만 후보로 — (x, y, z) 목록.
- `_fp_bottom_corners()` (L107) — FloorPanel/CantileverSlab 의 4 바닥 코너 (월드) — shape=(4,3).
- `_best_column_for_corner()` (L116) — 단일 코너에 대해 가장 가까운 기둥 단을 찾아 record dict 반환.
- `record_joints()` (L197) — 씬 전체를 스캔해 FloorPanel·CantileverSlab 의 joint_records 채움.


### `modular_3d/model/multi_floor.py` (451 줄)
> 다층 그룹 생성 헬퍼 — F5 배치 모드용.

**최상위 함수**
- `_floor_z_list()` (L38) — 부재 타입과 종속 옵션에 따라 각 floor_index 별 z 좌표 리스트 반환.
- `_instantiate()` (L121) — Component 서브클래스 생성 — model.core.instantiate 위임 (단계 2 통합).
- `next_group_id()` (L130) — 현재 Scene 안에서 가장 큰 group_id + 1 반환. 모두 0이면 1 반환.
- `create_multi_floor_group()` (L142) — 다층 그룹을 생성하고 (group_id, [comp_id_list]) 반환.
- `_next_sub_index()` (L246) — 같은 group_id 안에서 사용된 가장 큰 sub_index + 1 반환.
- `_compute_core_slab_bbox_xy()` (L259) — 그룹의 그 층 모든 코어벽들의 월드 xy bbox 반환.
- `regenerate_core_slabs()` (L283) — 그룹의 모든 층(0..n_floors) 코어 슬래브를 재계산 / 갱신.
- `delete_group()` (L406) — 그룹 통째로 삭제. 삭제된 comp_id 리스트 반환.
- `translate_components()` (L419) — 주어진 comp_id 들만 (dx, dy, dz) 이동. 영향받은 comp_id 리스트 반환.
- `translate_group()` (L436) — 그룹 통째로 (dx, dy, dz) 이동. 영향받은 comp_id 리스트 반환.


### `modular_3d/render/__init__.py` (0 줄)

### `modular_3d/render/auto_snap.py` (194 줄)
> 자동 갭 스냅 모듈 (2026-05-11 신규).

**모듈 상수**
- `_MODULE_LIKE` (L46) = `frozenset({ComponentType.MODULE, ComponentType.FLOOR_PANEL, ComponentType.VER...`
- `_DEPENDENT_ZERO_GAP` (L55) = `frozenset({ComponentType.CANTILEVER_BEAM, ComponentType.CANTILEVER_SLAB, Comp...`

**최상위 함수**
- `gap_between()` (L64) — 두 부재 타입 쌍의 갭(mm) 결정.
- `_new_bbox()` (L88) — 1층 본체 XY bbox 반환.
- `apply_auto_gap()` (L127) — 현재 마우스 월드 좌표(wx, wy) 를 자동 갭으로 보정한 새 좌표를 반환.


### `modular_3d/render/dep_snap.py` (274 줄)
> 종속 부재 자동 스냅 (Phase 2 — 2026-05-11, v2 사용자 피드백 반영).

**모듈 상수**
- `SLAB_MAX_WIDTH_MM` (L30) = `3400.0`

**최상위 함수**
- `_nearest_edge()` (L33) — bbox 4 변 중 가장 가까운 변 라벨 ('L','R','B','T').
- `_nearest_vertex_on_edge()` (L46) — edge 위 양 끝 꼭지점 중 마우스 가까운 1 개의 (x, y) 반환.
- `_is_inside()` (L62)
- `snap_cantilever_beam()` (L78)
- `snap_cantilever_slab()` (L127)
- `snap_mid_column()` (L168)
- `snap_mid_beam()` (L192)


### `modular_3d/render/mesh_builder.py` (349 줄)
> 부재 → 3D 메쉬 변환.

**모듈 상수**
- `COLOR_STEEL` (L9) = `np.array([0.5, 0.5, 0.5, 1.0], dtype=np.float32)`
- `COLOR_STEEL_INNER` (L10) = `np.array([0.375, 0.375, 0.375, 1.0], dtype=np.float32)`
- `COLOR_CONCRETE` (L11) = `np.array([0.75, 0.75, 0.75, 1.0], dtype=np.float32)`
- `COLOR_WALL` (L180) = `np.array([0.815, 0.815, 0.815, 1.0], dtype=np.float32)`
- `COLOR_WALL_PARTITION` (L181) = `np.array([0.815, 0.815, 0.815, 0.35], dtype=np.float32)`
- `COLOR_CORE_WALL` (L279) = `np.array([0.75, 0.75, 0.75, 0.7], dtype=np.float32)`
- `_MESH_BUILDERS` (L324) = `{_CT.MODULE: build_module_mesh, _CT.FLOOR_PANEL: build_floor_panel_mesh, _CT....`

**최상위 함수**
- `_local_frame()` (L16) — 보/기둥 방향 벡터로 로컬 좌표계 (forward, right, up) 계산.
- `build_hollow_box_section()` (L37) — 중공 각형강관 메쉬 생성.
- `build_solid_box()` (L118) — 6면 솔리드 박스 (슬래브, 벽체 등).
- `_merge_meshes()` (L157) — 여러 메쉬를 하나로 합치기.
- `_build_slab_mesh()` (L184) — SlabData → 솔리드 박스 메쉬.
- `_build_wall_fill_mesh()` (L193) — WallPanelData → 솔리드 판 메쉬.
- `_add_columns()` (L207)
- `_add_beams()` (L213)
- `build_module_mesh()` (L219)
- `build_floor_panel_mesh()` (L229)
- `build_struct_wall_mesh()` (L237)
- `build_cantilever_beam_mesh()` (L249)
- `build_cantilever_slab_mesh()` (L256)
- `build_mid_beam_mesh()` (L264)
- `build_mid_column_mesh()` (L271)
- `build_core_mesh()` (L282) — RC 코어벽 — 단순 솔리드 박스 (회전·앵커 반영된 월드 8 코너의 AABB).
- `build_core_slab_mesh()` (L300) — RC 코어 슬래브 — 보 없는 순수 RC 판. SlabData 1 개만.
- `build_vertical3_module_mesh()` (L308) — 수직 3층 모듈: 4 통기둥 + 층 슬래브 받침보 12 + 옥상 프레임 보 4 + 슬래브 3.
- `build_component_mesh()` (L338) — 부재 타입에 따라 적절한 메쉬 빌더 호출. 미지의 타입은 module 폴백.
- `build_ghost_component_mesh()` (L344) — 부재 메쉬와 동일하지만 alpha=0.3.


### `modular_3d/render/snap.py` (162 줄)
> 스냅 시스템 — 배치된 부재 꼭지점에 스냅 + 20mm 갭 적용.

**클래스**
- `class SnapManager` (L9) — 부재 꼭지점 스냅 관리. 부재 추가/삭제 시 인덱스 갱신.
  - `__init__()` (L12)
  - `add_component()` (L19) — 부재의 스냅 포인트를 캐시에 추가 (꼭지점 + 보 중심/1/4).
  - `remove_component()` (L28) — 부재 꼭지점을 캐시에서 제거.
  - `clear()` (L32) — 전체 캐시 초기화.
  - `find_nearest()` (L38) — vispy 씬 변환으로 각 스냅 포인트를 화면에 투영하고,
  - `apply_gap()` (L94) — 스냅 위치에 배치할 때 갭 자동 적용 (기본 MODULE_JOINT_GAP_MM).


### `modular_3d/render/viewer.py` (2198 줄)
> 3D 렌더링 — Vispy SceneCanvas + 커스텀 카메라.

**모듈 상수**
- `OPS_COLOR_BEAM` (L27) = `(0.05, 0.05, 0.05, 1.0)`
- `OPS_COLOR_SHELL` (L28) = `(0.45, 0.1, 0.55, 1.0)`
- `OPS_COLOR_CORE` (L29) = `(0.45, 0.1, 0.55, 1.0)`
- `OPS_COLOR_PIN` (L30) = `(0.95, 0.85, 0.1, 1.0)`
- `OPS_COLOR_RIGID6` (L31) = `(0.7, 0.05, 0.5, 1.0)`
- `OPS_COLOR_NODE` (L32) = `(0.3, 0.3, 0.3, 1.0)`
- `OPS_COLOR_NODE_EDGE` (L33) = `(0.15, 0.15, 0.15, 1.0)`
- `OPS_COLOR_BASE` (L34) = `(0.0, 0.0, 0.0, 1.0)`
- `OPS_LINK_KIND_COLOR` (L39) = `{'panel_module': OPS_COLOR_PIN, 'wall_module': OPS_COLOR_PIN, 'vertical_stack...`
- `OPS_LINK_SKIP_KINDS` (L49) = `{'wall_fp_merge'}`
- `OPS_COLOR_DIAPHRAGM` (L82) = `(0.55, 0.55, 0.55, 0.55)`
- `OPS_COLOR_RIGIDLINK` (L83) = `(0.35, 0.35, 0.35, 0.85)`
- `OPS_COLOR_DOF_1` (L87) = `(0.2, 0.8, 0.85, 1.0)`
- `OPS_COLOR_DOF_2` (L88) = `(0.5, 0.85, 0.2, 1.0)`
- `OPS_COLOR_DOF_3` (L89) = `(1.0, 0.55, 0.1, 1.0)`
- `OPS_COLOR_DOF_6` (L90) = `(0.7, 0.05, 0.5, 1.0)`

**최상위 함수**
- `_cam_axes()` (L93) — 카메라의 world-space 위치, forward, right, up 벡터 계산.
- `_raycast_z0_from_cam()` (L128) — NDC 좌표에서 Z=0 평면 교차점 반환. None이면 교차 없음.

**클래스**
- `class CustomCamera(scene.TurntableCamera)` (L151) — 가운데 버튼 = Orbit, Shift+가운데 = Pan, 휠 = 커서방향 줌.
  - `__init__()` (L157)
  - `viewbox_mouse_event()` (L165)
  - `_zoom_toward_cursor()` (L224) — 마우스 커서 방향으로 확대/축소. 매 휠 이벤트마다 현재 카메라로 재계산.
- `class Viewer3D` (L251) — Vispy 3D 뷰어 관리.
  - `__init__()` (L254)
  - `_create_axis_arrows()` (L428) — 원점에 XYZ 축 화살표.
  - `add_component_visual()` (L459)
  - `remove_component_visual()` (L488)
  - `set_ghost_enabled()` (L503) — F5 PREVIEW 진입/이탈 시 controller가 호출.
  - `update_ghost()` (L511)
  - `clear_ghost()` (L524)
  - `show_snap_markers()` (L530) — 스냅 대상 부재의 모든 꼭지점을 노란 점으로, 스냅된 점은 크게 표시.
  - `hide_snap_marker()` (L559)
  - `show_selection_box()` (L565) — [Phase 0 — 부재 호버 시스템] 컴포넌트 빨간 테두리 강조 비활성.
  - `hide_selection_box()` (L577) — 선택 하이라이트 숨기기 — 비주얼 자체가 항상 invisible 이라 캐시만 갱신.
  - `highlight_member()` (L584) — 해석 부재(mid)를 두꺼운 노란 선으로 하이라이트.
  - `clear_member_highlight()` (L603) — 해석 부재 하이라이트 숨김.
  - `_pattern_factor()` (L610) — 프로파일 높이 비율 (peak 기준 0~1). s ∈ [0, 1] 은 보 축 파라미터.
  - `show_slab_overlay()` (L632) — Stage 1: 슬래브 면 + 판정 라벨 + 분배 패턴 프로파일 + peak 수치.
  - `hide_slab_overlay()` (L781)
  - `is_slab_overlay_visible()` (L788)
  - `show_beam_udl_overlay()` (L792) — 각 보(beam) 부재 아래에 factored UDL 프로파일(직사각형) + 수치 표시.
  - `hide_beam_udl_overlay()` (L857)
  - `is_beam_udl_overlay_visible()` (L862)
  - `show_column_head_overlay()` (L866) — 각 기둥(column) 부재의 상단 노드에 해당 기둥이 받는 축력 N 을 텍스트로 표시.
  - `hide_column_head_overlay()` (L914)
  - `is_column_head_overlay_visible()` (L918)
  - `show_reaction_overlay()` (L922) — 각 베이스 노드 위에 수직 반력 Rz 화살표 + 수치 표시.
  - `hide_reaction_overlay()` (L980)
  - `is_reaction_overlay_visible()` (L985)
  - `show_joint_overlay()` (L990) — 해석에서 실제 사용하는 beam_line 데이터로 접합부 시각화.
  - `hide_joint_overlay()` (L1110)
  - `is_joint_overlay_visible()` (L1117)
  - `screen_to_world_ray()` (L1122) — Qt 화면 좌표 (Y=0이 위, 아래로 증가) → (ray_origin, ray_direction).
  - `get_scene_transform()` (L1151) — vispy 씬→캔버스 변환 객체 반환 (3D world → 2D canvas pixels).
  - `pick_ops_member_at()` (L1157) — F6 ops 뷰에서 Qt 마우스 좌표에 가장 가까운 부재 mid 를 반환.
  - `_ensure_ops_visuals()` (L1230) — OpenSees 시각화 visual들 lazy 생성.
  - `set_show_diaphragms()` (L1307) — 강체 다이어프램 마스터↔슬레이브 연결선 표시 토글.
  - `set_show_rigid_links()` (L1313) — 강체 링크 표시 토글.
  - `set_dof_color_mode()` (L1319) — 자유도 묶음 색을 DOF 수별로 분리해 표시할지 토글.
  - `set_rule_visibility()` (L1325) — 규칙 ID 별 결합선 시각화 토글 (단순 시각 — 실제 결합은 유지).
  - `_draw_from_spec()` (L1340) — ModelSpec 단일 순회기로 좌측 뷰를 그린다 (Phase 6 — 단일화 핵심).
  - `show_ops_view()` (L1581) — OpenSees 모델을 좌측 뷰에 표시 — ModelSpec 단일 순회기 사용 (Phase 6).
  - `hide_ops_view()` (L1722) — OpenSees 뷰 비활성 → 원래 메쉬 색상 복원.
  - `is_ops_view_active()` (L1764)
  - `update_deformed_shape()` (L1769) — ops 모델 위에 변형 형상을 덧그린다.
  - `hide_deformed_shape()` (L1850)
  - `_ensure_deformed_visuals()` (L1858)
  - `is_deformed_shape_active()` (L1884)
  - `_ratio_to_jet()` (L1890) — 비율 r ∈ [0, 1] → jet 컬러맵 (blue→cyan→green→yellow→red).
  - `_ratio_to_5band()` (L1909) — 응력비 → 5단계 색상 (물량산출 탭 시각화).
  - `show_member_ratio_bands()` (L1924) — 5단계 색상으로 부재 색칠 (물량산출 탭 진입 시 호출).
  - `show_member_contour()` (L1957) — 일반 부재선을 부재별 ratio (0..1+) 에 따라 jet 컬러맵으로 색칠.
  - `show_unstable_warning()` (L1991) — 불안정 구조 경고 — 문제 노드 + 그 노드를 포함한 부재를 **빨간색** 강조.
  - `hide_unstable_warning()` (L2047)
  - `_ensure_unstable_visuals()` (L2054)
  - `hide_member_contour()` (L2070) — 컨투어 해제 → 일반 부재 검정 단색으로 복귀.
  - `highlight_ops_member()` (L2091) — ops 모델에서 (n1, n2) 부재를 파란색 강조.
  - `update_pinned_members()` (L2106) — Phase 9 — 슬롯별 클릭 고정 부재 강조 갱신.
  - `highlight_ops_member_hover()` (L2130) — Phase 2 — 부재 호버 회색 강조. 기본 회색, Phase 13 에서 다음
  - `clear_ops_member_hover()` (L2148) — 부재 호버 강조 제거.
  - `highlight_ops_members()` (L2154) — ops 모델에서 여러 부재를 한 번에 파란색 강조.
  - `highlight_ops_node()` (L2178) — ops 모델에서 노드 nid 를 파란색 점으로 강조.
  - `get_native_widget()` (L2197)


### `modular_3d/ui/__init__.py` (0 줄)

### `modular_3d/ui/alignment_helpers.py` (253 줄)
> alignment_view 의 순수 기하·레이어 헬퍼 분리.

**모듈 상수**
- `LAYER_BOTTOM` (L23) = `0`
- `LAYER_TOP` (L24) = `1`
- `TYPE_COLORS` (L28) = `{_CT.MODULE: (80, 80, 200), _CT.FLOOR_PANEL: (80, 180, 80), _CT.STRUCT_WALL: ...`
- `STATE_IDLE` (L42) = `'idle'`
- `STATE_SELECTED` (L43) = `'selected'`
- `STATE_DIRECTION` (L44) = `'direction'`
- `STATE_REFERENCE` (L45) = `'reference'`
- `STATE_DEPENDENCY_PICK` (L46) = `'dependency_pick'`
- `ALIGN_TOL` (L53) = `100.0`
- `GAP_20` (L54) = `float(_GAP_MM)`
- `SNAP_20` (L55) = `float(_GAP_MM)`
- `SNAP_220` (L56) = `float(_SECTION_W + _GAP_MM)`
- `EPS` (L57) = `0.5`
- `EDGE_PICK_PX` (L58) = `8`

**최상위 함수**
- `slab_xy()` (L67) — SlabData → XY 사각형.
- `column_xy()` (L74) — ColumnData → 200×200 XY 사각형 (기둥 중심 기준).
- `beam_xy()` (L82) — BeamData → 축 정렬 XY 사각형 (단면 폭만큼 수직 확장).
- `allowed_parent_types()` (L101) — 종속 부재 타입에 따라 부모로 가능한 ComponentType 집합.
- `nearest_edge_id()` (L116) — 클릭한 월드 좌표에서 가장 가까운 부모 모서리 ID(0~3) 반환.
- `detect_mid_beam_level()` (L131) — 모듈 내 클릭 y 위치에 따라 'bottom' / 'top' 결정 (DD1).
- `iter_component_rects()` (L140) — 부재의 내부 구성 요소를 (rect, role) 튜플로 순회.
- `component_layers()` (L201) — 부재가 속한 레이어 집합.
- `visible_ids()` (L238) — 현재 레이어에 표시되는 부재 ID 리스트 (페어/sibling 중복 제외).


### `modular_3d/ui/alignment_paint.py` (450 줄)
> AlignmentCanvas 의 paintEvent + 보조 그리기 Mixin.

**클래스**
- `class AlignmentCanvasPaintMixin` (L30) — paintEvent + 직접 그리기 헬퍼.
  - `_draw_vline()` (L33)
  - `_draw_hline()` (L38)
  - `_draw_bbox_edges()` (L43)
  - `paintEvent()` (L54)
  - `_draw_f5_ghost()` (L271) — F5 PREVIEW 시 마우스 위치에 반투명 고스트 부재 + 스냅 마커 그림.
  - `_f5_world_snap()` (L308) — 마우스 월드 좌표에서 가장 가까운 1층 본체 꼭지점에 스냅.
  - `_apply_auto_gap()` (L350)
  - `_apply_dep_snap()` (L376)
  - `_f5_compute_ghost_corners()` (L433) — 현재 PREVIEW 부재의 1층 본체 4꼭지점(월드 XY) 반환. 없으면 None.


### `modular_3d/ui/alignment_pick.py` (102 줄)
> AlignmentCanvas 의 hit-test / edge picking Mixin.

**클래스**
- `class AlignmentCanvasPickMixin` (L17) — 클릭 좌표 ↔ 부재·모서리 매칭.
  - `_hit_test_component()` (L20) — XY-bbox 안에 마우스가 있으면 반환 (가장 작은 면적 우선).
  - `_pick_direction_edge()` (L36) — DIRECTION 상태에서 노란 모서리 클릭 → (axis, coord) 반환 or None.
  - `_pick_target_edge()` (L59) — 대상 부재의 두 모서리 중 마우스 커서에 가까운 쪽 좌표 반환.
  - `_misaligned_set()` (L69) — 선택 부재 대비 오차가 있는 부재 → {oid: (dx_or_None, dy_or_None)}.


### `modular_3d/ui/alignment_view.py` (752 줄)
> 정렬 검증 뷰 (F5) — 2D 탑뷰 평면도 기반.

**클래스**
- `class AlignmentCanvas(QLabel, AlignmentCanvasPaintMixin, AlignmentCanvasPickMixin)` (L61) — 2D 평면뷰 캔버스.
  - **시그널** `move_requested = pyqtSignal(int, int, float)` (L64)
  - **시그널** `selection_changed = pyqtSignal(int)` (L66)
  - `__init__()` (L68)
  - `set_placement_callback()` (L123) — Controller 가 F5 배치/조작 콜백을 주입.
  - `handle_palette_select()` (L160)
  - `enter_dependency_pick()` (L182) — controls.py 가 dim 확정 후 호출 — DEPENDENCY_PICK 상태로 진입.
  - `reset_state()` (L193) — 선택/방향/기준을 모두 초기화.
  - `set_layer()` (L204)
  - `_world_to_screen()` (L209)
  - `_screen_to_world()` (L215)
  - `resizeEvent()` (L221)
  - `_auto_fit()` (L228)
  - `_current_visible_ids()` (L249)
  - `mousePressEvent()` (L256)
  - `mouseMoveEvent()` (L334)
  - `mouseReleaseEvent()` (L364)
  - `wheelEvent()` (L369)
  - `keyPressEvent()` (L393)
  - `_apply_snap()` (L525) — 1/2/3/4/5 키에 해당하는 스냅으로 이동 요청.
- `class AlignmentDockPanel(QWidget)` (L565) — F5 모드 — 2D 탑뷰 배치 + 정렬 도킹 패널.
  - **시그널** `floors_changed = pyqtSignal(int)` (L574)
  - **시그널** `save_requested = pyqtSignal()` (L576)
  - **시그널** `load_requested = pyqtSignal()` (L577)
  - `__init__()` (L579)
  - `canvas()` (L643)
  - `floors()` (L647)
  - `set_floors()` (L650) — 외부에서 층수 변경. emit=True 면 floors_changed 시그널 발화.
  - `_snap_to_three()` (L662) — 가장 가까운 3 의 배수로 스냅 (동률은 내림). 최소 3.
  - `_on_floors_changed()` (L671)
  - `request_focus()` (L685) — 캔버스에 포커스 — 키 라우팅 필요 시 사용.
  - `_apply_move()` (L690) — 선택 부재가 속한 다층 그룹 전체를 (axis, delta)만큼 이동(g5).


### `modular_3d/ui/analysis_panel.py` (1344 줄)
> 메인 윈도우 오른쪽에 도킹되는 구조해석 결과 패널.

**모듈 상수**
- `_ROLE_KO` (L40) = `{'module_column': '기둥', 'module_bottom_beam': '바닥보', 'module_top_beam': '천장보'...`
- `_ROLE_KO_ORDER` (L62) = `['기둥', '벽 기둥', '코어 기둥', '중간기둥', '천장보', '바닥보', '벽 상부보', '벽 하부보', '코어 상부보', '코어...`
- `_COMP_TYPE_ORDER` (L73) = `['모듈', '수직 3층 모듈', '바닥패널', '구조벽', '캔틸레버보', '캔틸레버슬래브', '중간보', '중간기둥', 'RC 코어',...`
- `_CRITICAL_KO` (L106) = `{'axial': '축력', 'bend': '휨', 'shear': '전단', 'combined': '복합', 'defl': '처짐', '...`
- `_KIND_KO` (L116) = `{'beam': '보', 'column': '기둥', 'shell': '쉘'}`
- `_GROUP_KO` (L125) = `{'columns': '기둥', 'beams': '보', 'cantilevers': '캔틸레버', 'all': '전체'}`
- `CASE_DISPLAY_MAP` (L154) = `{'ENVELOPE': '지배조합 (5케이스 envelope)', 'D+L': '1.2D + 1.6L (중력)', 'Ex': '1.2D +...`
- `CASE_DISPLAY_TO_KEY` (L163) = `{v: k for k, v in CASE_DISPLAY_MAP.items()}`
- `CASE_DISABLED_ITEMS` (L166) = `['1.4D (자중 단독) — 추후 구현', '0.9D + 1.0Ex (X 지진 전도 방지) — 추후 구현', '0.9D + 1.0Ey (...`
- `_RATIO_COLOR_BANDS` (L1323) = `[(0.3, (51, 102, 255)), (0.6, (102, 204, 255))]`
- `_RATIO_COLOR_BANDS_LE` (L1329) = `[(0.85, (77, 230, 77)), (1.0, (255, 153, 0))]`
- `_RATIO_COLOR_NG` (L1333) = `(255, 51, 51)`

**최상위 함수**
- `_alpha_label()` (L80) — 0→a, 1→b, … 25→z, 26→aa, 27→ab …  컴포넌트 위치 인덱스 라벨.
- `_role_ko_order_key()` (L91)
- `_comp_type_order_key()` (L98)
- `_group_label_ko()` (L133) — 그룹 키 → 한글 표시명. 'columns_F{a}_{b}' / 'columns_F{a}' 동적 처리.
- `_ratio_to_qcolor()` (L1336) — 응력비 → 5단계 색상 (3D 뷰 `_ratio_to_5band` 와 동일 경계/색).

**클래스**
- `class AnalysisPanel(QWidget)` (L177) — F6 구조해석 결과를 표시하는 도킹 패널.
  - **시그널** `member_selected = pyqtSignal(int)` (L181)
  - **시그널** `members_selected = pyqtSignal(list)` (L183)
  - **시그널** `case_changed = pyqtSignal(str)` (L185)
  - **시그널** `contour_changed = pyqtSignal(str)` (L187)
  - **시그널** `ratio_view_changed = pyqtSignal(str)` (L190)
  - **시그널** `dof_color_toggle = pyqtSignal(bool)` (L193)
  - **시그널** `column_segments_changed = pyqtSignal(int)` (L195)
  - `__init__()` (L197)
  - `select_envelope_case()` (L299) — (2026-05-19 작업 5) 콤보박스를 '지배조합' 으로 자동 전환.
  - `select_dl_case()` (L310) — (2026-05-19 작업 5) 콤보박스를 D+L 로 되돌림.
  - `set_visible_subtabs()` (L320) — 메인 윈도우의 [구조해석]/[물량] 탭에서 서브탭만 선택적으로 노출.
  - `_build_quantity_tab()` (L343) — 물량산출 탭 위젯 구성.
  - `populate_ops()` (L474) — OpenSees 결과로 패널을 채운다.
  - `_populate_case_combo_initial()` (L511) — 콤보박스 초기 항목 — 활성 5 종 + 비활성 추가 조합 (KBC 향후 구현용).
  - `_on_case_index_changed()` (L533) — 콤보박스 변경 → 부재 트리 갱신 + 시그널 emit (좌측 변형 갱신용).
  - `_on_case_changed()` (L557) — 레거시 — currentTextChanged 호환 시그니처. 현재 호출 자리 없음.
  - `_on_contour_changed()` (L568)
  - `_fill_member_tree_ops()` (L571) — ops 모델용 부재 트리 — 가독성 개선 (2026-05-18).
  - `_classify_role_ko()` (L735) — 부재 role 영어 → 한글 표시명. floor_edge_beam 은 길이로 장/단변 분기.
  - `_fill_member_tree_flat()` (L755) — scene 미주입 폴백 — 옛 단일컬럼식 평탄 트리.
  - `get_member_full_label()` (L796) — 부재 mid 의 트리 위계 풀이름. 캐시 미스 시 폴백.
  - `get_current_policy()` (L809) — 현재 물량산출 라디오 정책. 초기 상태 폴백은 '1종'.
  - `get_member_group_name()` (L813) — 현 정책 기준 부재의 단면 그룹명. 없으면 ''.
  - `get_member_section_name()` (L819) — 현 정책 기준 부재의 채택 단면 명칭. 없으면 ''.
  - `get_member_ratio()` (L830) — 현 정책 기준 부재 응력비. 없으면 None.
  - `_fit_panel_to_quantity()` (L837) — 물량산출 탭의 표·트리 컨텐츠 폭에 맞춰 패널 minimumWidth 갱신.
  - `_fit_panel_to_tree()` (L903) — 트리 컬럼 폭 합계에 맞춰 패널의 minimumWidth 를 갱신.
  - `clear()` (L931) — 패널 내용을 비운다.
  - `_on_tree_item_changed()` (L940) — 트리 선택 변경 → 단일 부재 / 그룹 모두 처리.
  - `_collect_leaf_mids()` (L958) — 주어진 트리 아이템의 모든 자손 중 mid 가 달려 있는 leaf 의 mid 리스트.
  - `get_column_segments()` (L977) — (2026-05-19) 사용자가 입력한 기둥 층구간 수. 1=분할 없음.
  - `_on_col_seg_apply()` (L981) — (2026-05-19) '적용' 버튼 → controls 가 받아 물량 재산정.
  - `populate_quantity()` (L985) — 물량산출 자료를 받아 탭을 채운다.
  - `_on_policy_changed()` (L995) — 라디오버튼 변경.
  - `_on_tab_changed()` (L1004) — 탭 전환 — 물량산출 탭이면 ratio:정책 emit, 아니면 off.
  - `_on_ratio_tree_changed()` (L1011) — 응력비 트리 선택 → 잎이면 단일 부재 emit, 그룹이면 하위 mid 묶음 emit.
  - `_refresh_quantity_views()` (L1023) — 현재 정책 기준으로 모든 표를 다시 채운다.
  - `_fill_ratio_table()` (L1102) — 부재별 응력비 트리 — 구조해석탭(_fill_member_tree_ops) 과
  - `_iter_member_to_group()` (L1252) — rep.member_ratios 키 + sections_by_group 의 한정으로 (mid, gname) 생성.
  - `_lookup_member_group()` (L1267)
  - `_safe_group_max_ratio()` (L1271) — rep 에 group max_ratio 보관이 없어 ratio 사전에서 그룹 부재만 추려 max.
  - `_sum_group_steel()` (L1280) — 그룹별 길이·중량 합 — member_to_group 캐시 + members 길이 사용.
  - `populate_quantity_full()` (L1307) — quantity_reports + 정책별 member_to_group 캐시 함께 주입.


### `modular_3d/ui/controls.py` (1483 줄)
> FSM 컨트롤러 — 상태 전환, 이벤트 처리, 레이캐스팅.

**모듈 상수**
- `KEY_TO_TYPE` (L23) = `{'1': ComponentType.MODULE, '2': ComponentType.FLOOR_PANEL, '3': ComponentTyp...`
- `DEFAULT_DIMS` (L42) = `{ComponentType.MODULE: {'width': 3400, 'depth': 6820, 'height': 3400}, Compon...`

**클래스**
- `class Controller(F5Mixin, F6Mixin)` (L77) — 앱 상태 관리 + Qt 이벤트 → 모델/뷰 연결.
  - `__init__()` (L80)
  - `_set_state()` (L165)
  - `_current_defaults()` (L171)
  - `_update_preview()` (L177) — 고스트를 world_pos에 갱신 (타입별 z 시프트 포함).
  - `_preview_z_for_type()` (L189) — 부재 타입별 배치 z 레벨.
  - `on_qt_key_press()` (L208)
  - `on_qt_mouse_move()` (L287)
  - `refresh_after_camera()` (L294) — 카메라 이동/회전 후 고스트 위치 재계산.
  - `_snap_find()` (L299) — 스냅 탐색 헬퍼: vispy 씬 변환으로 find_nearest 호출.
  - `_update_from_mouse()` (L309)
  - `on_qt_mouse_press()` (L415)
  - `_on_dimension_confirmed()` (L459)
  - `_on_dimension_cancelled()` (L630) — 치수 입력 Esc — 정책 (b) 2026-05-12: IDLE 로 완전 취소.
  - `_do_undo()` (L646)
  - `_pick_component()` (L892) — 화면 좌표에서 레이를 쏴서 가장 가까운 부재 ID 반환.
  - `_select_component()` (L905) — 부재 선택 + 하이라이트 표시. 복사본이면 마스터로 리다이렉트.
  - `_deselect()` (L946) — 선택 해제.
  - `_delete_selected()` (L955) — 선택된 부재 삭제. 페어 모듈도 같이 삭제.
  - `_copy_selected()` (L1015) — 선택된 부재와 같은 타입/치수로 배치 모드 진입.
  - `_refresh_all_meshes()` (L1042) — 모든 컴포넌트 메쉬 재생성 (합체/분리 후 시각화 업데이트).
  - `_start_moving()` (L1052) — 선택된 부재의 이동 모드 시작.
  - `_update_moving_preview()` (L1070) — 이동 중 고스트로 미리보기.
  - `_confirm_moving()` (L1083) — 이동 확정.
  - `_move_merge_group()` (L1197) — 합체(merge) 관계로 연결된 컴포넌트를 XY delta만큼 이동.
  - `_cancel_moving()` (L1251) — 이동 취소 — 원래 위치 보존 + IDLE 로 완전 취소 (정책 b 2026-05-12).
  - `_start_axis_move()` (L1261) — MOVING 상태에서 X/Y 축 선택 → 거리 입력 모드.
  - `_confirm_axis_move()` (L1270) — 축 이동 거리 확정.
  - `_calc_mid_beam_placement()` (L1384) — 스냅 지점에서 중간보 자동 배치 (controls_geom 위임).
  - `_calc_mid_column_placement()` (L1388) — 스냅 지점에 중간기둥 자동 배치 (controls_geom 위임).
  - `_get_all_children()` (L1394) — comp_id와 그 페어의 모든 children ID를 반환.
  - `_move_child()` (L1402) — child를 delta만큼 이동 + 비주얼/스냅 갱신.
  - `_delete_children()` (L1415) — comp_id와 그 페어의 모든 children 삭제. {child_id: Component} 반환.
  - `_restore_children()` (L1438) — 삭제된 children 복원. parent_map: {child_id: parent_id}.
  - `_cancel_placement()` (L1476)


### `modular_3d/ui/controls_f5.py` (1300 줄)
> controls.py 의 F5 (도크 배치 모드) 관련 메소드 분리.

**클래스**
- `class F5Mixin` (L26) — F5 배치 모드 메소드 모음 — Controller 가 inherit.
  - `_resolve_move_targets()` (L29) — 선택된 부재 ID 로부터 이동/회전 대상 ID 리스트 결정 (정책 b 단계, 멀티층 의식).
  - `_refresh_2d()` (L119) — 부재 상태가 바뀐 직후 2D 캔버스 즉시 갱신.
  - `set_f5_dock()` (L127) — main_3d 가 만든 F5 도킹 패널을 컨트롤러에 등록.
  - `_f5_save_scene()` (L157) — F5 패널 '저장' 버튼 → Scene + 층수를 JSON 파일로 저장.
  - `_f5_load_scene()` (L173) — F5 패널 '불러오기' 버튼 → JSON 파일 로드 후 Scene 재구성.
  - `regenerate_all_core_slabs()` (L222) — 좌측 팔레트의 '코어 슬래브 생성/재생성' 버튼 핸들러.
  - `_on_floors_changed()` (L298) — 층수 입력창 변경 콜백 (단계 3d + 종속 부재 보존 패치).
  - `_f5_on_type_key()` (L492) — F5 캔버스에서 1~7 키 입력 → 배치 시작.
  - `_f5_on_canvas_click()` (L508) — F5 캔버스에서 PREVIEW 상태일 때 마우스 클릭 → 부재 생성 확정.
  - `_f5_on_escape()` (L837) — F5 캔버스 Esc — 배치/이동/복사 취소.
  - `_f5_update_ghost_at()` (L857) — 현재 pending dims/comp_type/rotation/anchor 로 좌측 3D 고스트 갱신.
  - `_f5_on_canvas_move()` (L876) — F5 캔버스 마우스 이동 (PREVIEW) → 좌측 3D 고스트 갱신.
  - `_f5_on_rotate()` (L880) — R 키 — PREVIEW 고스트 90° 회전. 즉시 2D 화면 갱신.
  - `_f5_on_anchor()` (L894) — V 키 — PREVIEW 고스트 앵커 변경. 즉시 2D 화면 갱신.
  - `_f5_on_move_key()` (L912) — SELECTED + M 키 — 자유 이동 모드 진입.
  - `_f5_on_dependency_pick()` (L970) — DEPENDENCY_PICK 단계에서 부모 부재 클릭 시 호출.
  - `_f5_on_undo()` (L1050) — Z 키 — 직전 사용자 액션 한 번 되돌리기 (그룹 단위).
  - `_f5_on_copy()` (L1056) — C 키 — 선택된 부재의 복사 모드 진입.
  - `_f5_on_delete()` (L1228) — SELECTED + Delete — 다층 그룹 통째로 삭제 + 좌측 3D visual 제거.


### `modular_3d/ui/controls_f6.py` (966 줄)
> controls.py 의 F6 (구조해석 도크) 관련 메소드 분리.

**모듈 상수**
- `_PIN_PALETTE` (L24) = `[(0.0, 0.78, 0.78, 1.0), (0.82, 0.0, 0.75, 1.0), (0.66, 0.82, 0.0, 1.0), (0.1...`
- `_PIN_MAX_SLOTS` (L31) = `5`

**클래스**
- `class F6Mixin` (L34) — F6 해석 모드 메소드 모음 — Controller 가 inherit.
  - `_open_alignment_view()` (L37) — F5 도킹 패널 토글. F6 ↔ F5 배타 관리(2-2).
  - `_run_structural_analysis()` (L104) — F6: OpenSeesPy 기반 구조해석 (Phase 1 = 모델 빌드 + 좌측 OpenSees 뷰).
  - `_populate_ops_panel()` (L239) — 우측 도킹 패널에 ops 모델 요약 + 부재력 표시 + 케이스 토글.
  - `_run_quantity_takeoff()` (L274) — 초기 진입 — D+L 결과로 단면 산정 + 물량 집계 → 패널·뷰어 주입.
  - `_run_quantity_takeoff_for_case()` (L309) — (2026-05-19 작업 5) 선택 케이스에 대해 단면 산정 + 물량 집계 재실행.
  - `_on_column_segments_changed()` (L367) — (2026-05-19) 기둥 층구간 분할 '적용' 버튼 → 현재 케이스 재산정.
  - `attach_deformed_controls()` (L398) — main_3d 가 만든 변형 형상 컨트롤 위젯들을 컨트롤러에 연결 (Phase 4-B).
  - `_active_case_results()` (L407) — 현재 콤보박스에서 선택된 케이스의 OpsResults 반환 (없으면 None).
  - `_on_panel_case_changed()` (L416) — 패널 케이스 콤보박스 변경 → 좌측 변형 + 컨투어 + 물량 재산정.
  - `_refresh_all_pinned_popups()` (L452) — Phase 15 — 떠 있는 클릭 고정 정보창의 본문을 현재 케이스로 갱신.
  - `_on_deformed_toggled()` (L463)
  - `_on_deformed_scale_changed()` (L469)
  - `_refresh_deformed_view()` (L476) — 현재 케이스 + 슬라이더 배율로 좌측 변형 형상 그리기.
  - `_on_panel_contour_changed()` (L501)
  - `_refresh_contour()` (L505)
  - `_update_ops_hover()` (L552) — Phase 2~3 (설계서_부재호버.md) — F6 ops 뷰에서 마우스 호버 부재 강조 + 정보창.
  - `_ensure_hover_popup()` (L586) — 첫 호버 시 정보창 위젯 lazy 생성. 부모는 캔버스 native widget.
  - `_show_hover_popup_for()` (L601) — 호버 정보창 표시 — 마우스 옆 이동 + Phase 4 본문 갱신.
  - `_refresh_popup_content()` (L617) — Phase 4 — 부재 mid 정보로 정보창 본문 갱신.
  - `_hide_hover_popup()` (L675) — 호버 정보창 숨김. 위젯이 없으면 무시.
  - `_pinned_slots()` (L684) — 슬롯 리스트 lazy init. 각 항목: dict(mid, popup, color) 또는 None.
  - `_find_pinned_slot_for_mid()` (L695) — 같은 부재가 이미 고정돼 있으면 그 슬롯 인덱스 반환, 없으면 None.
  - `_allocate_pinned_slot()` (L703) — 빈 슬롯 인덱스 반환. 다 차면 가장 오래된 슬롯(인덱스 0) 자동 해제.
  - `_free_pinned_slot()` (L717) — 슬롯 닫기 — 정보창 destroy + 슬롯 None. 강조선은 호출자가 갱신.
  - `_refresh_pinned_highlight()` (L728) — 모든 슬롯의 부재 강조선을 한 번에 viewer 에 보냄.
  - `pin_member()` (L749) — 부재 클릭 시 호출 — 슬롯 할당 + 정보창 생성 + 강조선 갱신.
  - `close_all_pinned()` (L784) — Phase 14 — ESC 단축키 → 떠 있는 모든 정보창 + 강조선 동시 제거.
  - `_close_pinned_by_mid()` (L791) — Phase 11 — × 버튼 클릭으로 mid 슬롯 해제 + 강조선 갱신.
  - `_next_pin_color_preview()` (L799) — Phase 13 — 다음 클릭 시 배정될 슬롯 색.
  - `_cascade_if_overlapped()` (L811) — Phase 12 — 새 정보창이 기존 슬롯 창과 거의 겹치면 비킴.
  - `_on_panel_members_selected()` (L857) — 그룹 노드(모듈/층/역할) 클릭 → 하위 모든 부재를 한 번에 파란색 강조.
  - `_pin_member_at_canvas_corner()` (L880) — Phase 16 — 트리 잎 클릭 시 정보창을 캔버스 우하단에 등장.
  - `_on_panel_member_selected()` (L902) — 도킹 패널에서 부재 클릭 → 좌측 ops 뷰에 파란색 강조.
  - `_on_ratio_view_changed()` (L928) — analysis_panel.ratio_view_changed 시그널 핸들러.
  - `set_quantity_reports()` (L957) — Phase 6 통합 진입점에서 호출 — 정책별 QuantityReport 주입.


### `modular_3d/ui/controls_geom.py` (161 줄)
> controls.py 의 순수 기하 헬퍼 분리.

**최상위 함수**
- `ray_aabb_intersect()` (L24) — Ray-AABB 교차 판정. 교차 시 t 값 반환, 아니면 None.
- `raycast_z0()` (L47) — Ray 와 평면 z=z_target 의 교점 반환. 평행/뒤쪽이면 None.
- `create_component()` (L60) — Component 인스턴스 생성 — model.core.instantiate 위임 (단계 2 통합).
- `calc_mid_beam_placement()` (L69) — 스냅 지점에서 중간보 자동 배치.
- `calc_mid_column_placement()` (L143) — 스냅 지점에 중간기둥 자동 배치.


### `modular_3d/ui/design_props_panel.py` (211 줄)
> 우측 디자인 속성 패널 — 활성/선택 부재 정보 + AI 최적배치 입력 폼.

**클래스**
- `class DesignPropertiesPanel(QWidget)` (L31) — 디자인 탭 우측 패널.
  - **시그널** `floors_changed = pyqtSignal(int)` (L40)
  - `__init__()` (L42)
  - `_setup_ui()` (L49)
  - `_on_ai_floors_changed()` (L171) — AI 층수 SpinBox 사용자 입력 → 외부에 시그널 발화.
  - `set_floors_from_external()` (L177) — 외부(F5 패널 등)에서 층수 변경 시 호출 — 재귀 발화 방지하며 동기화.
  - `refresh_active()` (L189) — 활성 부재 변경 시 호출 — 라벨 갱신.
  - `refresh_selected()` (L197) — 선택 부재 변경 시 호출 — 라벨 갱신.


### `modular_3d/ui/joint_edit_panel.py` (188 줄)
> 접합부 조정 탭 우측 위젯 — 결합 토글·옵션·범례.

**클래스**
- `class JointEditPanel(QWidget)` (L24) — 접합부 조정 탭 우측 패널.
  - **시그널** `diaphragm_toggle = pyqtSignal(bool)` (L37)
  - **시그널** `rule_visibility_changed = pyqtSignal(str, bool)` (L40)
  - `__init__()` (L42)
  - `_setup_ui()` (L50)
  - `refresh_legend()` (L126) — spec 에서 추출한 rule_id 집합으로 범례를 새로 그림.


### `modular_3d/ui/main_3d.py` (739 줄)
> 진입점 — QMainWindow + QTabWidget(7 탭) + Vispy + Controller 연결.

**모듈 상수**
- `TAB_DESIGN` (L59) = `0`
- `TAB_JOINT_EDIT` (L60) = `1`
- `TAB_ANALYSIS` (L61) = `2`
- `TAB_QUANTITY` (L62) = `3`
- `TAB_TRANSPORT` (L63) = `4`
- `TAB_JOINT_AIR` (L64) = `5`
- `TAB_FINAL` (L65) = `6`

**최상위 함수**
- `_apply_app_font()` (L714) — 전역 폰트를 Pretendard 로 설정.
- `main()` (L730)

**클래스**
- `class MainWindow(QMainWindow)` (L68)
  - `__init__()` (L69)
  - `_build_deformed_widget()` (L195)
  - `_build_design_tab()` (L216) — 디자인 탭: 좌 팔레트 + 중앙(3D | 2D) + 우 속성 + 하단 치수.
  - `_build_joint_edit_tab()` (L263) — 접합부 조정 탭: 중앙(3D 와이어프레임) + 우 접합부 UI placeholder.
  - `_build_analysis_tab()` (L293) — 구조해석 탭: 중앙(3D + 변형슬라이더) + 우 AnalysisPanel(요약/내부력).
  - `_build_quantity_tab()` (L323) — 물량 탭: 중앙(3D + 변형슬라이더) + 우 AnalysisPanel(물량산출).
  - `_build_placeholder_tab()` (L350) — 빈 탭 — 운송 / 공기 및 접합부 / 최종평가 용. 내부 내용은 추후 추가.
  - `_on_tab_changed()` (L371)
  - `_restore_design_view()` (L457) — 구조해석/물량 탭 → 디자인 탭으로 돌아왔을 때 일반 메쉬 뷰 복원.
  - `_rebuild_all_component_visuals()` (L494) — Scene 의 모든 부재 메쉬를 viewer 에서 완전히 제거 후 재빌드.
  - `_run_joint_edit_preview()` (L522) — 접합부 조정 탭 진입 시 spec 만 빌드해 와이어프레임 표시.
  - `_run_analysis_if_needed()` (L577) — 구조해석/물량 탭 진입 시 OpenSeesPy 해석을 실행.
  - `_on_palette_select()` (L607) — 팔레트 버튼 클릭 → 2D 캔버스의 키 1~8 입력과 100% 동일 경로.
  - `_on_scenes_floors_changed()` (L627) — 시나리오 탭의 층수 SpinBox 변경 → F5 패널과 동기화 + 시그널 발화.
  - `_on_scene_save()` (L632) — 시나리오 탭 '저장' 버튼 → F5Mixin._f5_save_scene 재사용.
  - `_on_scene_load()` (L637) — 시나리오 탭 '불러오기' 버튼 → F5Mixin._f5_load_scene 재사용 후
  - `_is_design_tab()` (L646)
  - `eventFilter()` (L651)
  - `keyPressEvent()` (L698) — 메인 윈도우 키 이벤트.


### `modular_3d/ui/member_info_popup.py` (436 줄)
> 부재 단위 호버/클릭 정보창 위젯 (설계서_부재호버.md).

**모듈 상수**
- `_KIND_KO` (L27) = `{'beam': '보', 'column': '기둥', 'truss': '트러스'}`
- `_DEFAULT_W` (L151) = `240`
- `_DEFAULT_H` (L152) = `70`
- `HOVER_OFFSET` (L155) = `(16, 16)`

**최상위 함수**
- `_fmt_kn()` (L17) — N(뉴턴) → kN 문자열, 소수 1 자리.
- `_fmt_knm()` (L22) — N·mm → kN·m 문자열, 소수 1 자리.

**클래스**
- `class ForceDiagramWidget(QWidget)` (L31) — AFD/SFD/BMD 공통 미니 그래프 — QPainter 직접 그림 (경량, matplotlib X).
  - `__init__()` (L42)
  - `sizeHint()` (L60)
  - `set_values()` (L63) — station 값 배열 갱신 후 다시 그림. None 이면 빈 상태.
  - `set_title()` (L71) — 제목 갱신 (예: 'SFD (Vz)' — dominant 성분 표시).
  - `paintEvent()` (L77)
- `class MemberInfoPopup(QWidget)` (L158) — 부재 정보 떠다니는 패널 — 캔버스 위 absolute 위젯.
  - **시그널** `closed = pyqtSignal()` (L169)
  - `__init__()` (L171)
  - `_on_close_clicked()` (L248) — × 버튼 클릭 → closed 시그널 emit. 슬롯 매니저가 해제 처리.
  - `set_pinned_mode()` (L252) — 호버 임시창 / 클릭 고정창 모드 전환 (자체점검 #5).
  - `resizeEvent()` (L263) — × 버튼을 항상 우측 상단 모서리에 위치 유지.
  - `set_border_color()` (L272) — 외곽선 색 갱신. paintEvent 가 즉시 다시 그림.
  - `paintEvent()` (L285) — 배경 + 둥근 모서리 + 외곽선을 직접 그림.
  - `set_info()` (L297) — Phase 4 — 부재 본문 텍스트 갱신.
  - `move_to_mouse()` (L385) — 마우스 Qt 좌표 옆 (offset 적용) 으로 이동. 부모 캔버스 영역 안에
  - `mousePressEvent()` (L401) — 클릭 시작 — 드래그 anchor 기록.
  - `mouseMoveEvent()` (L409) — 드래그 — 부모 영역 안으로 클램프하며 이동.
  - `mouseReleaseEvent()` (L431)


### `modular_3d/ui/palette_panel.py` (123 줄)
> 좌측 부재 팔레트 패널 — 1~9 단축키 버튼화.

**모듈 상수**
- `PALETTE_ITEMS` (L34) = `[('1', ComponentType.MODULE, '모듈', '3400×6820×3400', True), ('2', ComponentTy...`

**클래스**
- `class PalettePanel(QWidget)` (L47) — 좌측 부재 팔레트 — 디자인 탭 전용.
  - `__init__()` (L50)
  - `_setup_ui()` (L61)


### `modular_3d/ui/ui_panel.py` (302 줄)
> Qt UI — 치수 입력 패널, 상태바 관리.

**클래스**
- `class DimensionInputPanel(QWidget)` (L13) — 하단 치수 입력 패널. Tab으로 필드 이동, Enter 확정, Esc 취소.
  - **시그널** `confirmed = pyqtSignal(dict)` (L16)
  - **시그널** `cancelled = pyqtSignal()` (L17)
  - `__init__()` (L19)
  - `_setup_ui()` (L28)
  - `set_mode_text()` (L71) — 하단 dim 패널 좌측에 현재 모드 표시 (c4).
  - `configure_for_type()` (L75) — 부재 타입별 필드 표시/숨김.
  - `set_defaults()` (L153) — 기본값 설정.
  - `get_values()` (L166) — 현재 입력값 반환. 구조벽일 때 'merge' 키 포함 (X1).
  - `activate()` (L194) — 패널 표시 + 첫 필드에 포커스.
  - `activate_move()` (L207) — 이동 거리 입력 모드. axis_name = 'X' or 'Y'.
  - `get_move_distance()` (L223) — 이동/확장 거리 반환.
  - `deactivate()` (L230) — 패널 숨기기.
  - `eventFilter()` (L237) — QLineEdit의 Enter/Esc/Tab을 가로채서 패널 동작으로 처리.
  - `_focus_next()` (L252) — 다음 활성 필드로 포커스 이동.
- `class StatusBarManager` (L271) — 상태바 관리.
  - `__init__()` (L274)
  - `update_mode()` (L288)
  - `update_coords()` (L298)
  - `update_count()` (L301)


### `modular_3d/카탈로그/__init__.py` (29 줄)
> 매직넘버·재료·단면 카탈로그 — 본부 (2026-05-12 신규).


### `modular_3d/카탈로그/geometry.py` (65 줄)
> 부재 치수 상수 — 모듈 높이·단면 폭·갭·슬래브 두께 등.

**모듈 상수**
- `MODULE_HEIGHT_MM` (L14) = `3400`
- `MODULE_JOINT_GAP_MM` (L16) = `20`
- `FLOOR_HEIGHT` (L19) = `MODULE_HEIGHT_MM + MODULE_JOINT_GAP_MM`
- `VERTICAL_MODULE_FLOORS` (L22) = `3`
- `SECTION_W_MM` (L25) = `200.0`
- `SECTION_H_MM` (L26) = `200.0`
- `SECTION_T_MM` (L27) = `8.0`
- `SLAB_THICKNESS_MODEL_MM` (L33) = `150.0`
- `SLAB_THICKNESS_MM` (L34) = `150.0`
- `CORE_SLAB_DEFAULT_THICKNESS_MM` (L36) = `180.0`
- `CORE_WALL_DEFAULT_THICKNESS_MM` (L38) = `300.0`
- `CORE_TRUSS_GRID_MM` (L40) = `800.0`
- `SPLIT_NODE_BASE_OFFSET` (L50) = `10000`
- `RULE_NODE_OFFSET_R03` (L51) = `20000`
- `RULE_NODE_OFFSET_R06` (L52) = `30000`
- `RULE_NODE_OFFSET_R09` (L53) = `40000`


### `modular_3d/카탈로그/loads.py` (22 줄)
> 하중·하중조합·압력 상수.

**모듈 상수**
- `LIVE_LOAD_KN_M2` (L7) = `2.0`
- `LIVE_LOAD_N_MM2` (L8) = `LIVE_LOAD_KN_M2 * 0.001`
- `LOAD_COMBO_DL` (L11) = `1.2`
- `LOAD_COMBO_LL` (L12) = `1.6`
- `SLAB_ONE_WAY_RATIO` (L15) = `2.0`


### `modular_3d/카탈로그/materials.py` (52 줄)
> 재료 상수 — 강재(SS275), 콘크리트(보통 강도).

**모듈 상수**
- `G_M_PER_S2` (L8) = `9.80665`
- `STEEL_E_MPA` (L11) = `205000.0`
- `STEEL_NU` (L12) = `0.3`
- `STEEL_G_MPA` (L13) = `STEEL_E_MPA / (2.0 * (1.0 + STEEL_NU))`
- `STEEL_DENSITY_KG_M3` (L14) = `7850.0`
- `STEEL_UNIT_WEIGHT_N_M3` (L15) = `STEEL_DENSITY_KG_M3 * G_M_PER_S2`
- `STEEL_UNIT_WEIGHT_N_MM3` (L16) = `STEEL_UNIT_WEIGHT_N_M3 * 1e-09`
- `STEEL_FY_MPA` (L19) = `275.0`
- `STEEL_FU_MPA` (L20) = `410.0`
- `PHI_T` (L23) = `0.9`
- `PHI_C` (L24) = `0.9`
- `PHI_B` (L25) = `0.9`
- `PHI_V` (L26) = `0.9`
- `CONCRETE_E_MPA` (L29) = `27000.0`
- `CONCRETE_NU` (L30) = `0.2`
- `CONCRETE_G_MPA` (L31) = `CONCRETE_E_MPA / (2.0 * (1.0 + CONCRETE_NU))`
- `CONCRETE_DENSITY_KG_M3` (L32) = `2400.0`
- `CONCRETE_UNIT_WEIGHT_N_M3` (L33) = `CONCRETE_DENSITY_KG_M3 * G_M_PER_S2`
- `CONCRETE_UNIT_WEIGHT_N_MM3` (L34) = `CONCRETE_UNIT_WEIGHT_N_M3 * 1e-09`
- `RC_WALL_E_MPA` (L37) = `CONCRETE_E_MPA`
- `RC_WALL_G_MPA` (L38) = `CONCRETE_E_MPA / 2.4`


### `modular_3d/카탈로그/rigid.py` (19 줄)
> OpenSees 가상 강체 강성 상수 (rigid arm 의 elasticBeamColumn 단면).

**모듈 상수**
- `RIGID_E_MPA` (L13) = `1000000000.0`
- `RIGID_A_MM2` (L14) = `1000000.0`
- `RIGID_I_MM4` (L15) = `1000000000000.0`
- `RIGID_J_MM4` (L16) = `1000000000000.0`


### `modular_3d/카탈로그/steel_sections.py` (142 줄)
> KS D 3568 각형강관(SHS) 단면 카탈로그.

**모듈 상수**
- `SHS_200x200x8` (L44) = `_shs_section_props(SECTION_W_MM, SECTION_H_MM, SECTION_T_MM)`
- `_SHS_SPECS` (L92) = `[(75, 3.2), (75, 4.5), (100, 3.2), (100, 4.5), (100, 6.0), (125, 4.5), (125, ...`
- `SECTION_PN_N` (L132) = `_FY * SHS_200x200x8['A']`
- `SECTION_MN_N_MM` (L133) = `_FY * (SHS_200x200x8['Iy'] / (SECTION_H_MM / 2.0))`

**최상위 함수**
- `_shs_section_props()` (L18) — 중공 각형강관 단면 특성 계산.
- `build_shs_catalog()` (L106) — SHS 카탈로그를 단면적 오름차순으로 반환.
- `find_section_by_name()` (L117) — 이름으로 카탈로그 검색.

**클래스**
- `class SHSSection` (L48) — 각형강관 단면 (정사각 SHS).
  - `from_dimensions()` (L66) — 치수로부터 모든 단면 특성 계산.
  - `weight_per_m_kg()` (L85) — 단위 길이당 중량 (kg/m).


### `modular_3d/카탈로그/tolerances.py` (51 줄)
> 거리·임계 허용오차 — 노드 병합·스냅·인터페이스 등.

**모듈 상수**
- `NODE_MERGE_TOL_MM` (L12) = `1.0`
- `LINE_COLINEAR_TOL` (L14) = `0.001`
- `OVERLAP_TOL_MM` (L16) = `1.0`
- `JOINT_BRIDGE_TOL_MM` (L21) = `25.0`
- `JOINT_HORIZ_CONNECT_MAX_MM` (L23) = `225.0`
- `FP_SUPPORT_TOL_XY` (L28) = `350.0`
- `FP_SUPPORT_TOL_Z` (L30) = `500.0`
- `SNAP_TRIGGER_MM` (L34) = `100.0`
- `HOVER_SNAP_RADIUS_MM` (L36) = `300.0`
- `GRID_SNAP_MM` (L38) = `100.0`
- `EQUILIBRIUM_TOL` (L41) = `0.001`
- `BENCHMARK_TOL` (L42) = `0.01`


